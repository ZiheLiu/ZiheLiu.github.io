<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Netty 读数据流程</title>
    <url>/2020/netty/netty%E8%AF%BB%E6%95%B0%E6%8D%AE%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="读缓存"><a href="#读缓存" class="headerlink" title="读缓存"></a>读缓存</h1><p>当读事件发生时，WorkerEventLoop 调用 <code>unsafe.read()</code>（<code>NioByteUnsafe#read()</code>）。</p>
<p>根据 <code>continueReading()</code> 最多只会读取 16 次。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// NioByteUnsafe#read()</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">read</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 省略</span></span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">      byteBuf = allocHandle.allocate(allocator);</span><br><span class="line">      allocHandle.lastBytesRead(doReadBytes(byteBuf));</span><br><span class="line">      <span class="comment">// 省略 ...</span></span><br><span class="line">    &#125; <span class="keyword">while</span> (allocHandle.continueReading());</span><br><span class="line">    <span class="comment">// 省略 ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// DefaultMaxMessagesRecvByteBufAllocator#continueReading()</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">continueReading</span><span class="params">(UncheckedBooleanSupplier maybeMoreDataSupplier)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> config.isAutoRead() &amp;&amp;</span><br><span class="line">           (!respectMaybeMoreData || maybeMoreDataSupplier.get()) &amp;&amp;</span><br><span class="line">           totalMessages &lt; maxMessagePerRead &amp;&amp; <span class="comment">// 默认 16</span></span><br><span class="line">           totalBytesRead &gt; <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="Epoll-ET-模式"><a href="#Epoll-ET-模式" class="headerlink" title="Epoll ET 模式"></a>Epoll ET 模式</h2><p>对于 Epoll ET 模式，只有从不可读变为可读才会触发，如果只读取 16 次，剩下的数据将没有办法继续读取。</p>
<p>所以 <code>EpollSocketChannel</code> 会在时 Epoll ET 模式时，如果数据没有读取完，会重新触发一个 EPOLLIN 事件。</p>
<p><img src="/images/EpollRecvByteAllocatorHandle-3767276.png" alt="EpollRecvByteAllocatorHandle"></p>
<p>如下，在 <code>AbstractEpollChannel#epollInFinally()</code> 中，如果是 EPOLLET 模式、且还有数据没有读完，会再触发一次 EPOLLIN 事件。</p>
<p><img src="/images/image-20200309232504893.png" alt="image-20200309232504893"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">executeEpollInReadyRunnable</span><span class="params">(ChannelConfig config)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (epollInReadyRunnablePending || !isActive() || shouldBreakEpollInReady(config)) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    epollInReadyRunnablePending = <span class="keyword">true</span>;</span><br><span class="line">    <span class="comment">// 提交任务。任务中触发 epollInReady()。</span></span><br><span class="line">    eventLoop().execute(epollInReadyRunnable);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Runnable epollInReadyRunnable = <span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        epollInReadyRunnablePending = <span class="keyword">false</span>;</span><br><span class="line">        epollInReady();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
      </tags>
  </entry>
  <entry>
    <title>Netty 构建连接流程</title>
    <url>/2020/netty/netty%E6%9E%84%E5%BB%BA%E8%BF%9E%E6%8E%A5%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://time.geekbang.org/course/detail/237-158156">源码剖析：构建连接</a></li>
</ul>
<h1 id="主线"><a href="#主线" class="headerlink" title="主线"></a>主线</h1><ul>
<li><p>boss thread</p>
<ul>
<li>NioEventLoop 中的 selector 轮询创建连接事件（OP_ACCEPT）。</li>
<li>创建 socket channel。</li>
<li>初始化 socket channel 并从 worker group 中选择一个 NioEventLoop。</li>
</ul>
</li>
<li><p>worker thread</p>
<ul>
<li>将 socket channel 注册到选择的 NioEventLoop 的 selector。</li>
<li>注册读事件（OP_READ）到 selector 上。</li>
</ul>
</li>
</ul>
<h1 id="创建-SocketChannel"><a href="#创建-SocketChannel" class="headerlink" title="创建 SocketChannel"></a>创建 SocketChannel</h1><p>NioEventLoop 的<strong>死循环</strong>中，接收到 <code>OP_ACCEPT</code> 事件后，调用 <code>unsafe.read()</code>，最终调用 <code>serverSocketChannel.accept()</code> 得到 <code>socketChannel</code>。</p>
<ul>
<li><code>unsafe.read()</code> 有两种<ul>
<li><code>NioMessageUnsafe#read()</code> 处理 OP_ACCEPT 事件。这里是调用这个。</li>
<li><code>NioByteUnsafe#read()</code> 处理 OP_READ 事件。</li>
</ul>
</li>
</ul>
<h1 id="注册-SocketChannel"><a href="#注册-SocketChannel" class="headerlink" title="注册 SocketChannel"></a>注册 SocketChannel</h1><h2 id="传播-SocketChannel-到-handler-链中"><a href="#传播-SocketChannel-到-handler-链中" class="headerlink" title="传播 SocketChannel 到 handler 链中"></a>传播 SocketChannel 到 handler 链中</h2><p>把创建的 <code>socketChannel</code> 放到 <code>readBuf</code> 中。通过 <code>pipeline.fireChannelRead(readBuf)</code> 把 <code>socketChannel</code> 传播到 handler 链中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">readBuf.add(<span class="keyword">new</span> NioSocketChannel(<span class="keyword">this</span>, ch));</span><br><span class="line">pipeline.fireChannelRead(readBuf.get(i));</span><br></pre></td></tr></table></figure>
<h2 id="ServerBootstrapAcceptor"><a href="#ServerBootstrapAcceptor" class="headerlink" title="ServerBootstrapAcceptor"></a>ServerBootstrapAcceptor</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ServerBootstrapAcceptor 在 ServerSocketChannel 初始化时，</span></span><br><span class="line"><span class="comment">// 通过 ChannelInitializer 加入到 pipeline 中的。</span></span><br><span class="line">p.addLast(<span class="keyword">new</span> ChannelInitializer&lt;Channel&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initChannel</span><span class="params">(<span class="keyword">final</span> Channel ch)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> ChannelPipeline pipeline = ch.pipeline();</span><br><span class="line">        ChannelHandler handler = config.handler();</span><br><span class="line">        <span class="keyword">if</span> (handler != <span class="keyword">null</span>) &#123;</span><br><span class="line">            pipeline.addLast(handler);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ch.eventLoop().execute(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                pipeline.addLast(<span class="keyword">new</span> ServerBootstrapAcceptor(</span><br><span class="line">                        ch, currentChildGroup, currentChildHandler, currentChildOptions, currentChildAttrs));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>其中 <code>ServerBootstrapAcceptor</code> 完成主要工作。</p>
<p>把刚刚创建的 <code>SocketChannel</code> 注册到 <code>childGroup</code> 中的一个 <code>EventLoop</code> 上。</p>
<p>注册好之后，调用 <code>pipeline.fireChannelActive()</code> 传播事件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Channel child = (Channel) msg;</span><br><span class="line"></span><br><span class="line">    child.pipeline().addLast(childHandler);</span><br><span class="line"></span><br><span class="line">    setChannelOptions(child, childOptions, logger);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (Entry&lt;AttributeKey&lt;?&gt;, Object&gt; e: childAttrs) &#123;</span><br><span class="line">        child.attr((AttributeKey&lt;Object&gt;) e.getKey()).set(e.getValue());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        childGroup.register(child).addListener(<span class="keyword">new</span> ChannelFutureListener() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">operationComplete</span><span class="params">(ChannelFuture future)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">                <span class="keyword">if</span> (!future.isSuccess()) &#123;</span><br><span class="line">                    forceClose(child, future.cause());</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">        forceClose(child, t);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="注册-OP-READ-事件"><a href="#注册-OP-READ-事件" class="headerlink" title="注册 OP_READ 事件"></a>注册 OP_READ 事件</h1><p>和  类似，最终调用 <code>AbstractNioChannel#doBeginRead()</code> 把 <code>readInterestOp</code> 注册到 <code>Selector</code> 上。</p>
<p>而 <code>NioSocketChannel</code> 在构造函数中传递了 <code>readInterestOp</code> 为  <code>OP_READ</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="title">AbstractNioByteChannel</span><span class="params">(Channel parent, SelectableChannel ch)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>(parent, ch, SelectionKey.OP_READ);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="传播的事件"><a href="#传播的事件" class="headerlink" title="传播的事件"></a>传播的事件</h1><p>注意，这里的 <code>pipeline</code> 是 <code>ServerSocketChannel.pipeline</code>。</p>
<ol>
<li>由 <code>unsafe.read()</code> accept 得到 <code>socketChannel</code>。<code>pipeline.fireChannelRead()</code> 传播创建的 <code>socketChannel</code>。</li>
<li>由 <code>ServerBootstrapAcceptor#channelRead()</code> 把 <code>socketChannel</code> 注册到一个 <code>WorkerEventLoop</code> 中。<code>pipeline.fireChannelActive()</code> 传播出去。</li>
<li>由 <code>head.channelActive()</code>。调用 <code>channel.read()</code> 从tail 开始传播。</li>
<li>由 <code>head.read()</code> 注册 <code>OP_READ</code> 。</li>
</ol>
<h1 id="接受连接的本质"><a href="#接受连接的本质" class="headerlink" title="接受连接的本质"></a>接受连接的本质</h1><ul>
<li>selector.select()/selectNow()/select(timeoutMillis) 发现 OP_ACCEPT 事件，处理：<ul>
<li>SocketChannel socketChannel = serverSocketChannel.accept()</li>
<li>selectionKey = javaChannel().register(eventLoop().unwrappedSelector(), 0, this)</li>
<li>selectionKey.interestOps(OP_READ)</li>
</ul>
</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><ul>
<li><p>创建连接的初始化和注册是通过 pipeline.fireChannelRead 在 ServerBootstrapAcceptor 中完成的。</p>
</li>
<li><p>第一次 Register 并不是监听 OP_READ ，而是 0 ：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">selectionKey = javaChannel().register(eventLoop().unwrappedSelector(), <span class="number">0</span>, <span class="keyword">this</span>)</span><br></pre></td></tr></table></figure>
</li>
<li><p>最终监听 OP_READ 是通过注册完成后的 <code>fireChannelActive()</code>，在 pipeline 的 head 中完成注册 OP_READ 事件的。</p>
</li>
<li><p>Worker’s NioEventLoop 是通过 Register 操作执行来启动。</p>
</li>
<li><p>接受连接的读操作，不会尝试读取更多次（16次）。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
      </tags>
  </entry>
  <entry>
    <title>Recycler 对象池</title>
    <url>/2020/netty/Recycler%E5%AF%B9%E8%B1%A1%E6%B1%A0/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://stackoverflow.com/questions/1468007/atomicinteger-lazyset-vs-set">AtomicInteger lazySet vs. set</a></li>
<li><a href="https://github.com/netty/netty/issues/8215">why use lazySet instead set in WeakOrderQueue#add</a></li>
<li><a href="https://blog.csdn.net/alex_xfboy/article/details/90384332">冷知识：netty的Recycler对象池</a></li>
</ul>
<h1 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h1><p>Netty 场景下的轻量级对象池 Recycler。</p>
<h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h2 id="stack"><a href="#stack" class="headerlink" title="stack"></a>stack</h2><p>每个线程使用一个 <code>stack</code> 存储对象池。避免线程同步的问题。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> FastThreadLocal&lt;Stack&lt;T&gt;&gt; threadLocal</span><br></pre></td></tr></table></figure>
<h2 id="WeakOrderQueue"><a href="#WeakOrderQueue" class="headerlink" title="WeakOrderQueue"></a>WeakOrderQueue</h2><p>在回收对象时，要释放对象的线程可能不是对象的 <code>stack</code> 线程。</p>
<p>所以对于<strong>每个</strong> <code>stack</code>，<strong>每个</strong>线程存储了一个对象的队列，供之后把对象放入到 <code>stack</code> 中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> FastThreadLocal&lt;Map&lt;Stack&lt;?&gt;, WeakOrderQueue&gt;&gt; DELAYED_RECYCLED =</span><br><span class="line">        <span class="keyword">new</span> FastThreadLocal&lt;Map&lt;Stack&lt;?&gt;, WeakOrderQueue&gt;&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> Map&lt;Stack&lt;?&gt;, WeakOrderQueue&gt; initialValue() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> WeakHashMap&lt;Stack&lt;?&gt;, WeakOrderQueue&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h3 id="stack-中记录-queue"><a href="#stack-中记录-queue" class="headerlink" title="stack 中记录 queue"></a>stack 中记录 queue</h3><p>且 <code>stack</code> 中，以<strong>链表</strong>的形式，存储了在多个线程中创建的它的 queue。</p>
<p>实现方式是，在创建 queue 时，把它加入到 <code>stack</code> 的头结点中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> WeakOrderQueue <span class="title">newQueue</span><span class="params">(Stack&lt;?&gt; stack, Thread thread)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> WeakOrderQueue queue = <span class="keyword">new</span> WeakOrderQueue(stack, thread);</span><br><span class="line">    stack.setHead(queue);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">final</span> Head head = queue.head;</span><br><span class="line">    ObjectCleaner.register(queue, head);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> queue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="节点内容"><a href="#节点内容" class="headerlink" title="节点内容"></a>节点内容</h3><p>节点 <code>Link</code> 继承自 <code>AtomicInteger</code>，内置了一个对象数组（默认16），在元素满了后，会创建下一个节点。</p>
<p>继承自 <code>AtomicInteger</code> 本身的值作为 <code>writerIndex</code>。通过 <code>AtomicInteger</code> 保证了<strong>写索引</strong>的可见性。</p>
<p>内置一个 <code>int</code> 的 <code>readIndex</code>，由于只有 <code>stack</code> 线程会去读，<strong>不用保证线程安全</strong>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Link</span> <span class="keyword">extends</span> <span class="title">AtomicInteger</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> DefaultHandle&lt;?&gt;[] elements = <span class="keyword">new</span> DefaultHandle[LINK_CAPACITY];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> readIndex;</span><br><span class="line">    Link next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="取对象-allocate"><a href="#取对象-allocate" class="headerlink" title="取对象 allocate()"></a>取对象 allocate()</h1><ul>
<li>从 <code>stack</code> 中取一个对象。</li>
<li>如果取不到，去 queue 的链表中扫描，找到可使用的对象。</li>
<li>如果找不到，创建一个对象。</li>
</ul>
<h1 id="释放对象-deallocate"><a href="#释放对象-deallocate" class="headerlink" title="释放对象 deallocate()"></a>释放对象 deallocate()</h1><ul>
<li>当调用的线程是 <code>stack</code> 的拥有者线程，把对象放入到 <code>stack</code> 里。<ul>
<li>每 <code>RATIO</code> 个才会放进去一个，默认是 8。</li>
<li>如果对象池大小超过阈值，不放进去。</li>
</ul>
</li>
<li>否则，是其他线程在操作。<ul>
<li>把元素放到队列里。</li>
<li>在放入时，使用了 <code>lazySet()</code> 来更新 <code>writerIndex</code>。</li>
</ul>
</li>
</ul>
<h2 id="lazySet"><a href="#lazySet" class="headerlink" title="lazySet()"></a>lazySet()</h2><h3 id="lazySet-功能"><a href="#lazySet-功能" class="headerlink" title="lazySet() 功能"></a>lazySet() 功能</h3><ul>
<li><code>lazySet()</code> 只会在写之前插入 <code>StoreStore</code> 屏障，保证与前面写操作的可见性。</li>
<li>不会像 <code>voltatile</code> 写一样，在写之后插入 <code>StoreLoad</code> 屏障，保证与后面操作的可见性。 <code>StoreLoad</code> 屏障代价比较大。</li>
</ul>
<h3 id="使用的作用"><a href="#使用的作用" class="headerlink" title="使用的作用"></a>使用的作用</h3><ul>
<li><p>保证在看到 <code>tail == writerIndex + 1</code> 之前，看到 <code>handle.stack</code> 被设为 null 了。</p>
</li>
<li><p>这样可能会有一段时间，<code>stack</code> 线程看不到这个新释放的对象，是可以容忍的。</p>
</li>
<li>但是可以不用插入 <code>StoreLoad</code> 屏障。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">add</span><span class="params">(DefaultHandle&lt;?&gt; handle)</span> </span>&#123;</span><br><span class="line">    handle.lastRecycledId = id;</span><br><span class="line"></span><br><span class="line">    Link tail = <span class="keyword">this</span>.tail;</span><br><span class="line">    <span class="keyword">int</span> writeIndex;</span><br><span class="line">    <span class="keyword">if</span> ((writeIndex = tail.get()) == LINK_CAPACITY) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!head.reserveSpace(LINK_CAPACITY)) &#123;</span><br><span class="line">            <span class="comment">// Drop it.</span></span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// We allocate a Link so reserve the space</span></span><br><span class="line">        <span class="keyword">this</span>.tail = tail = tail.next = <span class="keyword">new</span> Link();</span><br><span class="line"></span><br><span class="line">        writeIndex = tail.get();</span><br><span class="line">    &#125;</span><br><span class="line">    tail.elements[writeIndex] = handle;</span><br><span class="line">    handle.stack = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    tail.lazySet(writeIndex + <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
        <tag>record</tag>
      </tags>
  </entry>
  <entry>
    <title>Netty 记录</title>
    <url>/2020/netty/netty%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="为什么不直接用-JDK-NIO"><a href="#为什么不直接用-JDK-NIO" class="headerlink" title="为什么不直接用 JDK NIO"></a>为什么不直接用 JDK NIO</h1><h2 id="Netty-做的更多"><a href="#Netty-做的更多" class="headerlink" title="Netty 做的更多"></a>Netty 做的更多</h2><ul>
<li>支持常用应用层协议。</li>
<li>解决传输问题：粘包、板报现象。</li>
<li>支持流量整形。</li>
<li>完善的断连、Idle 等异常处理。</li>
</ul>
<h2 id="Netty-做的更好"><a href="#Netty-做的更好" class="headerlink" title="Netty 做的更好"></a>Netty 做的更好</h2><ul>
<li>规避 JDK NIO bug。<ul>
<li>epoll bug：异常唤醒空转导致 CPU 100%。</li>
<li>IO_TOS 参数（IP 包的优先级和 QoS 选项）使用时抛出异常。</li>
</ul>
</li>
<li>API 更友好、更强大。<ul>
<li>ByteBuffer 读写切换、无法扩容 -&gt; ByteBuf。</li>
<li>ThreadLocal -&gt; FastThreadLocal。</li>
</ul>
</li>
<li>隔离变化、屏蔽细节。</li>
</ul>
<h1 id="为什么只支持-NIO"><a href="#为什么只支持-NIO" class="headerlink" title="为什么只支持 NIO"></a>为什么只支持 NIO</h1><h2 id="为什么不建议阻塞-I-O"><a href="#为什么不建议阻塞-I-O" class="headerlink" title="为什么不建议阻塞 I/O"></a>为什么不建议阻塞 I/O</h2><ul>
<li><p>连接数少时，并发度低，BIO 性能不差于 NIO。</p>
</li>
<li><p>连接数高时，阻塞-&gt;耗资源、效率低。</p>
</li>
</ul>
<h2 id="为什么不再支持-AIO"><a href="#为什么不再支持-AIO" class="headerlink" title="为什么不再支持 AIO"></a>为什么不再支持 AIO</h2><p>Linux 中 AIO 实现不成熟。相比较 NIO 性能提升不明显。</p>
<h1 id="为什么-Netty-自己实现了-Epoll"><a href="#为什么-Netty-自己实现了-Epoll" class="headerlink" title="为什么 Netty 自己实现了 Epoll"></a>为什么 Netty 自己实现了 Epoll</h1><ul>
<li>Netty 暴露了更多的可控参数。<ul>
<li>JDK 的 NIO 只有水平触发</li>
<li>Netty 默认是边缘触发、可切换为水平触发。</li>
</ul>
</li>
<li>Netty 实现的垃圾回收更少、性能搞好。</li>
</ul>
<h2 id="边缘触发与水平触发"><a href="#边缘触发与水平触发" class="headerlink" title="边缘触发与水平触发"></a>边缘触发与水平触发</h2><blockquote>
<p><a href="https://blog.csdn.net/dongfuye/article/details/50880251">epoll LT/ET 深入剖析</a>（主要参考）</p>
<p><a href="https://www.zhihu.com/question/22524908/answer/69054646">java nio使用的是水平触发还是边缘触发</a></p>
</blockquote>
<h3 id="水平触发-Level-Triggered-LT"><a href="#水平触发-Level-Triggered-LT" class="headerlink" title="水平触发 Level Triggered (LT)"></a>水平触发 Level Triggered (LT)</h3><ul>
<li>socket 接收缓冲区不为空，有数据可读， 读事件一直触发。</li>
<li>.socket发送缓冲区不满，可以继续写入数据， 写事件一直触发。</li>
</ul>
<h3 id="边缘触发-Edge-Triggered-ET"><a href="#边缘触发-Edge-Triggered-ET" class="headerlink" title="边缘触发 Edge Triggered (ET)"></a>边缘触发 Edge Triggered (ET)</h3><p>仅在状态变化时触发事件。</p>
<ul>
<li>socket 的接收缓冲区状态变化时触发读事件，即空的接收缓冲区刚接收到数据时，触发读事件。</li>
<li>socket 的发送缓冲区状态变化时触发写事件，即满的缓冲区刚空出空间时，触发写事件。</li>
</ul>
<h3 id="LT-处理过程"><a href="#LT-处理过程" class="headerlink" title="LT 处理过程"></a>LT 处理过程</h3><ul>
<li>accept 一个连接，添加到 epoll 中监听 <code>EPOLLIN</code> 事件。</li>
<li>当 <code>EPOLLIN</code> 事件到达时，read fd 中的数据并处理。</li>
<li>当需要写出数据时，把数据 write 到 fd 中；如果数据较大，无法一次性写出，那么在 epoll 中监听 <code>EPOLLOUT</code>事件。</li>
<li>当 <code>EPOLLOUT</code> 事件到达时，继续把数据 write 到 fd 中；如果数据写出完毕，那么在 epoll 中关闭 <code>EPOLLOUT</code> 事件。</li>
</ul>
<h3 id="ET-处理过程"><a href="#ET-处理过程" class="headerlink" title="ET 处理过程"></a>ET 处理过程</h3><ul>
<li>accept 一个一个连接，添加到 epoll 中监听 <code>EPOLLIN|EPOLLOUT</code> 事件。</li>
<li>当 <code>EPOLLIN</code> 事件到达时，read fd 中的数据并处理，read 需要一直读，直到返回 <code>EAGAIN</code> 为止。</li>
<li>当需要写出数据时，把数据 write 到 fd 中，直到数据全部写完，或者 write 返回 <code>EAGAIN</code>。</li>
<li>当 <code>EPOLLOUT</code> 事件到达时，继续把数据 write 到 fd 中，直到数据全部写完，或者 write 返回 <code>EAGAIN</code>。</li>
</ul>
<h3 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h3><ul>
<li><p>ET 的要求是需要一直读写，直到返回 <code>EAGAIN</code>，否则就会遗漏事件。</p>
</li>
<li><p>LT 的处理过程中，直到返回 <code>EAGAIN</code> 不是硬性要求，但通常的处理过程都会读写直到返回 <code>EAGAIN</code>。</p>
</li>
<li>LT 比 ET 多了一个开关 <code>EPOLLOUT</code> 事件的系统调用。</li>
</ul>
<h1 id="Netty-与-Reactor-模式"><a href="#Netty-与-Reactor-模式" class="headerlink" title="Netty 与 Reactor 模式"></a>Netty 与 Reactor 模式</h1><h2 id="MainReactor-一般单线程"><a href="#MainReactor-一般单线程" class="headerlink" title="MainReactor 一般单线程"></a>MainReactor 一般单线程</h2><p>Netty 的 MainReactor，每次 <code>bind(address)</code> 会注册一次 <code>ServerSocketChannel</code> 到 <code>EventLoopGroup</code> 上，只能使用一个线程。</p>
<h2 id="分配-EventLoop-规则"><a href="#分配-EventLoop-规则" class="headerlink" title="分配 EventLoop 规则"></a>分配 EventLoop 规则</h2><p>为 Channel 分配 EventLoop 时，循环选择下一个。线程池数为 2 的次幂时，则用位运算优化。</p>
<h2 id="通用模式的-NIO-实现多路复用器跨平台"><a href="#通用模式的-NIO-实现多路复用器跨平台" class="headerlink" title="通用模式的 NIO 实现多路复用器跨平台"></a>通用模式的 NIO 实现多路复用器跨平台</h2><p><code>DefaultSelectorProvider.create()</code> 不同操作系统会创建不同的 <code>SelectorProvider</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> sun.nio.ch;</span><br><span class="line"><span class="comment">// MaxOS</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DefaultSelectorProvider</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SelectorProvider <span class="title">create</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> sun.nio.ch.KQueueSelectorProvider();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Windows</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DefaultSelectorProvider</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SelectorProvider <span class="title">create</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> WindowsSelectorProvider();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="粘包与半包"><a href="#粘包与半包" class="headerlink" title="粘包与半包"></a>粘包与半包</h1><h2 id="粘包原因"><a href="#粘包原因" class="headerlink" title="粘包原因"></a>粘包原因</h2><ul>
<li><p>发送方每次写入数据 &lt; 套接字缓冲区大小。</p>
</li>
<li><p>接收方读取套接字缓冲区数据不够及时。</p>
</li>
</ul>
<h2 id="半包原因"><a href="#半包原因" class="headerlink" title="半包原因"></a>半包原因</h2><ul>
<li>发送方写入数据 &gt; 套接字缓冲距大小。</li>
<li>发送的数据大于协议的 MTU（Maximum Transmission Unit，最大传输单元），必须拆包。</li>
</ul>
<h2 id="根本原因"><a href="#根本原因" class="headerlink" title="根本原因"></a>根本原因</h2><p>TCP 是流式协议，消息无边界。</p>
<h2 id="UDP"><a href="#UDP" class="headerlink" title="UDP"></a>UDP</h2><p>UDP 像邮寄的包裹，虽然一次运输多个，但每个包裹都有“界限”，一个一个签收， 所以无粘包、半包问题。</p>
<h2 id="如何解决"><a href="#如何解决" class="headerlink" title="如何解决"></a>如何解决</h2><p>找出消息的边界：封装成帧。</p>
<ul>
<li>固定长度。浪费空间。<code>FixedLengthFramDecoder</code>。</li>
<li>分隔符。内容本身出现分隔符时需要转移。<code>DelimiterBasedFrameDecoder</code>。</li>
<li>固定字节数size存长度信息。长度受字节数size限制，需要提前预知可能的最大长度。<code>LengthFieldBasedFrameDecoder</code>解码 与 <code>LengthFieldPrepender</code> 编码。</li>
</ul>
<h3 id="ByteToMessageDecoder"><a href="#ByteToMessageDecoder" class="headerlink" title="ByteToMessageDecoder"></a>ByteToMessageDecoder</h3><p>需要积累数据。两种方式：</p>
<ol>
<li><p>拷贝到一个 <code>ByteBuf</code> 里。</p>
</li>
<li><p>使用 <code>CompositeByteBuf</code> 视图组装多个 <code>ByteBuf</code>。</p>
</li>
</ol>
<p>但是第 2 种方式使用了更为复杂的索引方式，根据使用场景、实现解码方式的不同，可能效率比第一种低。</p>
<p>扩容发生在：</p>
<ul>
<li>第 1 种方式，<code>cumulation</code> 放不下下一个 <code>inByteBuf</code> 了、或 <code>cumulation</code> 被调用了 <code>retain()</code> 或者是只读的。</li>
<li>第 2 种方式，<code>cumulation</code> 被调用了 <code>retain()</code>。</li>
</ul>
<h1 id="二次解码器"><a href="#二次解码器" class="headerlink" title="二次解码器"></a>二次解码器</h1><ul>
<li><p>一次解码器：<code>ByteToMessageDecoder</code></p>
<ul>
<li>io.netty.buffer.ByteBuf （原始数据流）-&gt; io.netty.buffer.ByteBuf （用户数据）</li>
</ul>
</li>
<li><p>二次解码器：<code>MessageToMessageDecoder&lt;I&gt;</code></p>
<ul>
<li>io.netty.buffer.ByteBuf （用户数据）-&gt; Java Object</li>
</ul>
</li>
</ul>
<h2 id="对-JDK-Serialization-的支持"><a href="#对-JDK-Serialization-的支持" class="headerlink" title="对 JDK Serialization 的支持"></a>对 JDK Serialization 的支持</h2><p>Netty 进行了优化。</p>
<ul>
<li>解码后，压缩。</li>
<li>JDK 写入了每个变量的类型、所有修饰符。Netty 只写入了名字，运行时通过反射找到它的其他信息。</li>
</ul>
<h2 id="对-protobuf-的支持"><a href="#对-protobuf-的支持" class="headerlink" title="对 protobuf 的支持"></a>对 protobuf 的支持</h2><ul>
<li><p>一次解码时，没有使用<code>LengthFieldBasedFrameDecoder</code>，而是进行了优化。</p>
<ul>
<li>记录长度的字节数目是可变的，<code>VarInt</code>。</li>
</ul>
</li>
<li><p>二次解码时，把一次解码得到的字节数组，根据 protobuf 的规则解析为对象。</p>
</li>
</ul>
<h1 id="KeepAlive"><a href="#KeepAlive" class="headerlink" title="KeepAlive"></a>KeepAlive</h1><h2 id="开启-TCP-keepalive"><a href="#开启-TCP-keepalive" class="headerlink" title="开启 TCP keepalive"></a>开启 TCP keepalive</h2><p>在 Server 端</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 方式一</span></span><br><span class="line">bootstrap.childOption(ChannelOption.SO_KEEPALIVE,<span class="keyword">true</span>) </span><br><span class="line"><span class="comment">// 方式二  </span></span><br><span class="line">bootstrap.childOption(NioChannelOption.of(StandardSocketOptions.SO_KEEPALIVE), <span class="keyword">true</span>)</span><br></pre></td></tr></table></figure>
<h2 id="应用层-KeepAlive"><a href="#应用层-KeepAlive" class="headerlink" title="应用层 KeepAlive"></a>应用层 KeepAlive</h2><h3 id="方法一-定时发送"><a href="#方法一-定时发送" class="headerlink" title="方法一 定时发送"></a>方法一 定时发送</h3><p>每隔一段时间，分别向所有客户端发送一个 keepalive 消息。</p>
<p>耗费资源，如果客户端太多。</p>
<h3 id="方法二-空闲-Idle-检测"><a href="#方法二-空闲-Idle-检测" class="headerlink" title="方法二 空闲 Idle 检测"></a>方法二 空闲 Idle 检测</h3><p>无数据传输超过一段时间，判定为 Idle，再发 keepalive 消息。</p>
<p>快速释放损坏的、恶意的、很久不用的连接。</p>
<h2 id="Idle-检测"><a href="#Idle-检测" class="headerlink" title="Idle 检测"></a>Idle 检测</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ch.pipeline().addLast(<span class="string">"idleCheckHandler"</span>, <span class="keyword">new</span> IdleStateHandler(<span class="number">0</span>, <span class="number">20</span>, <span class="number">0</span>, TimeUnit.SECONDS));</span><br></pre></td></tr></table></figure>
<h3 id="ReaderIdle"><a href="#ReaderIdle" class="headerlink" title="ReaderIdle"></a>ReaderIdle</h3><ol>
<li>在 <code>channelActive()</code> 中注册延时（<code>readerIdleTimeNanos</code> 纳秒） 任务。</li>
<li>在 <code>channelRead()</code> 中把 <code>reading</code> 设为 true。</li>
<li>在 <code>channelReadComplete()</code> 中把 <code>reading</code> 设为 false。</li>
<li>延时时间到了，执行检测任务，如果没有正在发生读事件（<code>reading</code> 为 false）、且距离上次读完成超过了 <code>readerIdleTimeNanos</code> 纳秒，就在pipeline上传递一个 <code>READER_IDLE</code> 事件。</li>
</ol>
<h3 id="WriterIdle"><a href="#WriterIdle" class="headerlink" title="WriterIdle"></a>WriterIdle</h3><ol>
<li>在 <code>channelActive()</code> 中注册延时（<code>writerIdleTimeNanos</code> 纳秒） 任务。</li>
<li>延时时间到了，执行检测任务。<ul>
<li>如果距离上次写完成超过了 <code>writerIdleTimeNanos</code> 纳秒，就在pipeline上传递一个 <code>WRITER_IDLE</code> 事件。</li>
<li>如果开启了 <code>observeOutput</code>，则只要有写意图（还没有写完），就不会认为是 Idle 状态。</li>
</ul>
</li>
</ol>
<h3 id="使用例子"><a href="#使用例子" class="headerlink" title="使用例子"></a>使用例子</h3><h4 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h4><ul>
<li><p>客户端隔一段<strong>空闲</strong>时间向服务端发送 HEART_BEAT 消息。</p>
</li>
<li><p>服务端隔一段<strong>空闲</strong>时间没有收到客户端的 HEAR_BEAT 消息，就关闭这个连接。</p>
</li>
</ul>
<h4 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h4><ul>
<li>在客户端开启一个 WriterIdle 检测，超过给定时间后，发送 HEART_BEAT 消息给服务端。</li>
<li>在服务端开启一个 ReaderIdle 检测，超过给定时间没有收到消息后，关闭连接。</li>
<li>客户端在 <code>channelInactive()</code> 中，尝试重连服务端。</li>
</ul>
<h1 id="Netty-中锁的使用"><a href="#Netty-中锁的使用" class="headerlink" title="Netty 中锁的使用"></a>Netty 中锁的使用</h1><h2 id="注意锁的对象和范围-减小粒度"><a href="#注意锁的对象和范围-减小粒度" class="headerlink" title="注意锁的对象和范围 减小粒度"></a>注意锁的对象和范围 减小粒度</h2><p>Synchronized method -&gt; Synchronized block</p>
<p><img src="/images/netty锁的粒度.png" alt="netty锁的粒度"></p>
<h2 id="注意锁的对象本身大小-减少空间占用"><a href="#注意锁的对象本身大小-减少空间占用" class="headerlink" title="注意锁的对象本身大小 减少空间占用"></a>注意锁的对象本身大小 减少空间占用</h2><ul>
<li><p>volatile long = 8 bytes</p>
</li>
<li><p>AtomicLong = 8 bytes （volatile long）+ 16bytes （对象头）+ 8 bytes (引用) = 32 bytes</p>
</li>
</ul>
<h2 id="注意锁的速度-提高并发性"><a href="#注意锁的速度-提高并发性" class="headerlink" title="注意锁的速度 提高并发性"></a>注意锁的速度 提高并发性</h2><p>结论： 及时衡量、使用 JDK 最新的功能</p>
<p><img src="/images/nettyLongCounter.png" alt="nettyLongCounter"></p>
<h2 id="不同场景选择不同的并发包-因需而变"><a href="#不同场景选择不同的并发包-因需而变" class="headerlink" title="不同场景选择不同的并发包 因需而变"></a>不同场景选择不同的并发包 因需而变</h2><p><code>NioEventloop</code> 中负责存储 task 的 Queue，单线程的，只有一个消费者。</p>
<p>JDK LinkedBlockingQueue (MPMC) -&gt; jctools’ MPSC</p>
<p>单消费者：出队不用加锁。</p>
<h2 id="衡量好锁的价值-尽量不用锁"><a href="#衡量好锁的价值-尽量不用锁" class="headerlink" title="衡量好锁的价值 尽量不用锁"></a>衡量好锁的价值 尽量不用锁</h2><h3 id="EventLoop"><a href="#EventLoop" class="headerlink" title="EventLoop"></a>EventLoop</h3><p>Netty 使用局部串行 + 整体并行，而不是 一个队列 + 多个线程模式。</p>
<ul>
<li><p>一个 Channel 只由一个 EventLoop（一个线程）负责。</p>
</li>
<li><p>一个 EventLoop 负责多个 Channel。</p>
</li>
</ul>
<p>优点</p>
<ul>
<li><p>降低用户开发难度、逻辑简单、提升处理性能。</p>
</li>
<li><p>避免锁带来的上下文切换、并发保护等额外开销。</p>
</li>
</ul>
<h3 id="Recycler"><a href="#Recycler" class="headerlink" title="Recycler"></a>Recycler</h3><p>用 ThreadLocal 来避免资源争用，例如 Netty 轻量级的线程池实现</p>
<h1 id="Netty-内存使用技巧"><a href="#Netty-内存使用技巧" class="headerlink" title="Netty 内存使用技巧"></a>Netty 内存使用技巧</h1><h2 id="减少对像本身大小"><a href="#减少对像本身大小" class="headerlink" title="减少对像本身大小"></a>减少对像本身大小</h2><ol>
<li>用基本类型就不要用包装类。</li>
<li>应该定义成类变量的不要定义为实例变量。</li>
</ol>
<p>volatile long -&gt; AtomicLong</p>
<h2 id="预测分配大小"><a href="#预测分配大小" class="headerlink" title="预测分配大小"></a>预测分配大小</h2><p>根据接受到的数据动态调整（guess）下个要分配的 Buffer 的大小。</p>
<h2 id="零拷贝"><a href="#零拷贝" class="headerlink" title="零拷贝"></a>零拷贝</h2><ul>
<li>使用逻辑组合，代替实际复制。<ul>
<li>CompositeByteBuf</li>
</ul>
</li>
<li>使用包装，代替实际复制。<ul>
<li>ByteBuf byteBuf = Unpooled.wrappedBuffer(bytes)。</li>
</ul>
</li>
<li>调用 JDK 的 Zero-Copy 接口。<ul>
<li>DefaultFileRegion 中包装了 NIO 的 FileChannel.transferTo()</li>
</ul>
</li>
<li>对外内存。</li>
</ul>
<h2 id="内存池"><a href="#内存池" class="headerlink" title="内存池"></a>内存池</h2><h3 id="为什么引入对象池"><a href="#为什么引入对象池" class="headerlink" title="为什么引入对象池"></a>为什么引入对象池</h3><ul>
<li><p>创建对象开销大。</p>
</li>
<li><p>对象高频率创建且可复用。</p>
</li>
<li><p>支持并发、又能保护系统。</p>
</li>
<li><p>维护、共享有限的资源。</p>
</li>
</ul>
<h3 id="Recycler-1"><a href="#Recycler-1" class="headerlink" title="Recycler"></a>Recycler</h3><p>见 <a href="/2020/netty/Recycler对象池/" title="Recycler 对象池">Recycler 对象池</a></p>
]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
        <tag>record</tag>
      </tags>
  </entry>
  <entry>
    <title>Netty 服务端启动流程</title>
    <url>/2020/netty/netty%E6%9C%8D%E5%8A%A1%E7%AB%AF%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://time.geekbang.org/course/detail/237-158209">源码剖析：启动服务</a></li>
</ul>
<h1 id="主线"><a href="#主线" class="headerlink" title="主线"></a>主线</h1><ul>
<li><p>our thread</p>
<ul>
<li>创建 selector。</li>
<li>创建 server socket channel。</li>
<li>初始化 server socket channel。</li>
<li>给 server socket channel 从 boss group 中选择一个 NioEventLoop。</li>
</ul>
</li>
<li><p>boss thread </p>
<ul>
<li>将 server socket channel 注册到选择的 NioEventLoop 的 selector。</li>
<li>绑定地址启动 NioEventLoop 的线程来监听事件。</li>
<li>注册接受连接事件（OP_ACCEPT）到 selector 上。</li>
</ul>
</li>
</ul>
<h1 id="创建-Selector"><a href="#创建-Selector" class="headerlink" title="创建 Selector"></a>创建 Selector</h1><p>创建 <code>NioEventLoopGroup</code> 会创建 <code>nThreads</code> 个 <code>NioEventLoop</code>。</p>
<p>每个 <code>NioEventLoop</code> 会</p>
<ul>
<li>创建、存储一个 <code>Selector</code>。</li>
<li>在调用 <code>EventLoop#execute()</code> 时，如果线程没有启动，就启动线程。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Selector selector = sun.nio.SelectorProviderImpl.opSelector();</span><br></pre></td></tr></table></figure>
<h1 id="创建和初始化-NioServerSocketChannel"><a href="#创建和初始化-NioServerSocketChannel" class="headerlink" title="创建和初始化 NioServerSocketChannel"></a>创建和初始化 NioServerSocketChannel</h1><h2 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h2><p>用泛型反射工厂方法，创建 <code>NioServerSocketChannel</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">channel = channelFactory.newChannel();</span><br></pre></td></tr></table></figure>
<p>构造函数内，会创建 <code>pipeline</code>。<code>pipeline</code> 初始会有首尾节点。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="title">DefaultChannelPipeline</span><span class="params">(Channel channel)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.channel = ObjectUtil.checkNotNull(channel, <span class="string">"channel"</span>);</span><br><span class="line">    succeededFuture = <span class="keyword">new</span> SucceededChannelFuture(channel, <span class="keyword">null</span>);</span><br><span class="line">    voidPromise =  <span class="keyword">new</span> VoidChannelPromise(channel, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">    tail = <span class="keyword">new</span> TailContext(<span class="keyword">this</span>);</span><br><span class="line">    head = <span class="keyword">new</span> HeadContext(<span class="keyword">this</span>);</span><br><span class="line"></span><br><span class="line">    head.next = tail;</span><br><span class="line">    tail.prev = head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><ul>
<li><p>初始化 channel 的属性。</p>
</li>
<li><p>记录 <code>NioSocketChannel</code> 的属性。</p>
</li>
<li><p>为 pipeline 在 <code>initChannel</code> 被调用时，添加 <code>ServerBootstrapAcceptor</code> handler。</p>
<ul>
<li><p>负责接收客户端连接，创建连接后，对连接的初始化工作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">p.addLast(<span class="keyword">new</span> ChannelInitializer&lt;Channel&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initChannel</span><span class="params">(<span class="keyword">final</span> Channel ch)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> ChannelPipeline pipeline = ch.pipeline();</span><br><span class="line">        ChannelHandler handler = config.handler();</span><br><span class="line">        <span class="keyword">if</span> (handler != <span class="keyword">null</span>) &#123;</span><br><span class="line">            pipeline.addLast(handler);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ch.eventLoop().execute(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                pipeline.addLast(<span class="keyword">new</span> ServerBootstrapAcceptor(</span><br><span class="line">                        ch, currentChildGroup, currentChildHandler, currentChildOptions, currentChildAttrs));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h1 id="注册-NioServerSocketChannel"><a href="#注册-NioServerSocketChannel" class="headerlink" title="注册 NioServerSocketChannel"></a>注册 NioServerSocketChannel</h1><p>注册到 <code>NioEventLoop</code> 的 <code>Selector</code> 上。</p>
<ul>
<li>提交注册任务到 <code>NioEventLoop#execute()</code>。</li>
<li>把 <code>NioServerSocketChannel</code> 注册到 <code>Selector</code> 上，并指定  <code>attachment</code> 回调函数是自己（<code>Selector</code> 有事件到达时，执行 <code>attachment</code>）。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// class AbstractNioChannel</span></span><br><span class="line"><span class="comment">// 注意，这里绑定的是 0 事件，只是为了获取到 selectionKey，在之后多态更新。</span></span><br><span class="line">selectionKey = javaChannel().register(eventLoop().unwrappedSelector(), <span class="number">0</span>, <span class="keyword">this</span>);</span><br></pre></td></tr></table></figure>
<p>提交注册任务后，<code>NioEventLoop</code> 的线程会启动起来。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable task)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (task == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException(<span class="string">"task"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">boolean</span> inEventLoop = inEventLoop();</span><br><span class="line">    addTask(task);</span><br><span class="line">    <span class="keyword">if</span> (!inEventLoop) &#123;</span><br><span class="line">      	<span class="comment">// 启动线程！！！</span></span><br><span class="line">        startThread();</span><br><span class="line">        <span class="keyword">if</span> (isShutdown() &amp;&amp; removeTask(task)) &#123;</span><br><span class="line">            reject();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!addTaskWakesUp &amp;&amp; wakesUpForTask(task)) &#123;</span><br><span class="line">        wakeup(inEventLoop);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="绑定地址"><a href="#绑定地址" class="headerlink" title="绑定地址"></a>绑定地址</h1><p><code>channel.bind()</code> 调用 <code>pipeline.bind()</code>调用 <code>tail.bind()</code>。即从 <code>tail</code> 向前传递。</p>
<p>在 <code>head</code> 这个 <code>ChannelHandlerContext#bind()</code> 方法中，执行 <code>unsafe.bind()</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bind</span><span class="params">(ChannelHandlerContext ctx, </span></span></span><br><span class="line"><span class="function"><span class="params">                 SocketAddress localAddress, </span></span></span><br><span class="line"><span class="function"><span class="params">                 ChannelPromise promise)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    unsafe.bind(localAddress, promise);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>unsafe.bind()</code> 最终调用 <code>javaChannel().bind()</code> 来完成地址的绑定。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doBind</span><span class="params">(SocketAddress localAddress)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (PlatformDependent.javaVersion() &gt;= <span class="number">7</span>) &#123;</span><br><span class="line">        javaChannel().bind(localAddress, config.getBacklog());</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        javaChannel().socket().bind(localAddress, config.getBacklog());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="注册-OP-ACCEPT-事件"><a href="#注册-OP-ACCEPT-事件" class="headerlink" title="注册 OP_ACCEPT 事件"></a>注册 OP_ACCEPT 事件</h1><p>绑定地址后，调用 <code>pipeline.fireChannelActive()</code>，从 <code>pipeline.head</code> 开始执行 <code>channelActive()</code>。</p>
<p>其调用 <code>readIfIsAutoRead()</code> 调用 <code>channel.read()</code>，从 <code>pipeline.tail</code> 开始执行 <code>read()</code>。</p>
<p><code>head.read()</code> 方法执行 <code>unsafe.beginRead()</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">read</span><span class="params">(ChannelHandlerContext ctx)</span> </span>&#123;</span><br><span class="line">    unsafe.beginRead();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最终调用 <code>AbstractNioChannel#doBeginRead()</code> 把 <code>readInterestOp</code> 注册到 <code>Selector</code> 上。</p>
<p>而 <code>NioServerSocketChannel</code> 是 <code>AbstractNioChannel</code> 的子类，在构造函数中传递了 <code>readInterestOp</code> 为  <code>OP_ACCEPT</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// AbstractNioChannel#doBeginRead()</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doBeginRead</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="comment">// Channel.read() or ChannelHandlerContext.read() was called</span></span><br><span class="line">    <span class="keyword">final</span> SelectionKey selectionKey = <span class="keyword">this</span>.selectionKey;</span><br><span class="line">    <span class="keyword">if</span> (!selectionKey.isValid()) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    readPending = <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> interestOps = selectionKey.interestOps();</span><br><span class="line">    <span class="keyword">if</span> ((interestOps &amp; readInterestOp) == <span class="number">0</span>) &#123;</span><br><span class="line">        selectionKey.interestOps(interestOps | readInterestOp);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// class NioServerSocketChannel</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">NioServerSocketChannel</span><span class="params">(ServerSocketChannel channel)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>(<span class="keyword">null</span>, channel, SelectionKey.OP_ACCEPT);</span><br><span class="line">    config = <span class="keyword">new</span> NioServerSocketChannelConfig(<span class="keyword">this</span>, javaChannel().socket());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
      </tags>
  </entry>
  <entry>
    <title>Reactor 模式</title>
    <url>/2020/netty/Reactor%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><p><a href="https://www.cnblogs.com/amei0/p/8117660.html">IO多路复用之Reactor</a></p>
</li>
<li><p><a href="https://www.cnblogs.com/duanxz/p/3696849.html">Netty中的三种Reactor</a></p>
</li>
</ul>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>Reactor（反应器）模式常用来实现 I/O 多路复用机制。</p>
<p>Reactor 管理器调用事件分离器等待事件的发生。事件发生后，Reactor 管理器被唤醒，调度事件处理器链进行事件的处理。</p>
<p>核心流程：注册感兴趣的事件 -&gt; 扫描是否有感兴趣的事件发生 -&gt; 事件发生后做出相应的处理。</p>
<h1 id="Reactor模式组成"><a href="#Reactor模式组成" class="headerlink" title="Reactor模式组成"></a>Reactor模式组成</h1><h2 id="描述符-Handles"><a href="#描述符-Handles" class="headerlink" title="描述符 Handles"></a>描述符 Handles</h2><p>由操作系统提供的资源，用于识别每一个事件，如 Socket 描述符、文件描述符、信号的值等。</p>
<p>在Linux中，它用一个整数来表示。</p>
<p>在网络编程中，一般是一个 Socket 描述符。</p>
<h2 id="同步多路事件分离器-Event-Demultiplexer"><a href="#同步多路事件分离器-Event-Demultiplexer" class="headerlink" title="同步多路事件分离器 Event Demultiplexer"></a>同步多路事件分离器 Event Demultiplexer</h2><p>循环等待。一次同时等待多个事件的到来。</p>
<p>在 Linux 系统上使用 select、poll 等系统调用来实现。</p>
<p>调用者会被阻塞，直到分离器的分离的描述符集上有事件发生。</p>
<h2 id="事件处理器-Event-Hanlder"><a href="#事件处理器-Event-Hanlder" class="headerlink" title="事件处理器 Event Hanlder"></a>事件处理器 Event Hanlder</h2><p>定义事件的处理程序，具有统一的若干种接口（出站、入站）。</p>
<h2 id="Reactor-管理器"><a href="#Reactor-管理器" class="headerlink" title="Reactor 管理器"></a>Reactor 管理器</h2><ul>
<li><p>管理描述符</p>
</li>
<li><p>注册/移除事件处理器</p>
</li>
<li><p>调用事件分离器来等待事件</p>
</li>
<li><p>事件到来时，把时间分发给特定的事件处理器进行处理。</p>
</li>
</ul>
<h1 id="模式演进"><a href="#模式演进" class="headerlink" title="模式演进"></a>模式演进</h1><h2 id="单线程模式"><a href="#单线程模式" class="headerlink" title="单线程模式"></a>单线程模式</h2><p>系统只使用一个线程。</p>
<p>Reactor 管理器来分离连接/读写事件、事件处理器来处理事件 都在一个线程。</p>
<h2 id="单-Reactor-多线程模式"><a href="#单-Reactor-多线程模式" class="headerlink" title="单 Reactor 多线程模式"></a>单 Reactor 多线程模式</h2><ul>
<li><p>Reactor 管理器使用<strong>线程池</strong>来分离连接/读写事件。</p>
</li>
<li><p>事件处理器交给另外的<strong>线程池</strong>来执行。</p>
</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>在客户端连接特别多的时候、或客户端与服务端需要进行安全认证时，Reactor 在建立连接上可能耗费大量资源，使得读写事件受到影响。</p>
<p>所以有了下面的主从 Reactor 模式。</p>
<h2 id="主从-Reactor-多线程模式"><a href="#主从-Reactor-多线程模式" class="headerlink" title="主从 Reactor 多线程模式"></a>主从 Reactor 多线程模式</h2><ul>
<li><p>MainReactor 管理器使用<strong>线程池</strong>来分离连接事件。</p>
</li>
<li><p>MainReactor 将建立的连接分派给 SubReactor，SubReactor 使用另外的<strong>线程池</strong>来分离读写事件。</p>
</li>
<li><p>事件处理器交给另外的<strong>线程池</strong>执行。</p>
</li>
</ul>
<p>在 Netty 中推荐主从 Reactor 模式，使用例子如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// MainReactor</span></span><br><span class="line">EventLoopGroup bossGroup = <span class="keyword">new</span> NioEventLoopGroup(<span class="number">2</span>);</span><br><span class="line"><span class="comment">// SubReactor</span></span><br><span class="line"><span class="comment">// 默认值 CPUS * 2</span></span><br><span class="line">EventLoopGroup workerGroup = <span class="keyword">new</span> NioEventLoopGroup();</span><br><span class="line">ServerBootstrap bootstrap = <span class="keyword">new</span> ServerBootstrap();</span><br><span class="line">bootstrap.group(bossGroup, workerGroup);</span><br></pre></td></tr></table></figure>
<h1 id="Preactor-模式"><a href="#Preactor-模式" class="headerlink" title="Preactor 模式"></a>Preactor 模式</h1><p>Reactor 采用同步 I/O 网络模式。</p>
<p>Preactor 采用异步 I/O 网络模式。</p>
]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
      </tags>
  </entry>
  <entry>
    <title>Etcd 基本概念</title>
    <url>/2020/%E5%88%86%E5%B8%83%E5%BC%8F/Etcd%E5%9F%BA%E6%9C%AC%E6%A6%82%E5%BF%B5/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://youyou-tech.com/2019/07/01/%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81%E7%9A%84%E6%9C%80%E4%BD%B3%E5%AE%9E%E8%B7%B5%E4%B9%8B%EF%BC%9A%E5%9F%BA%E4%BA%8EEtcd%E7%9A%84%E5%88%86%E5%B8%83/">分布式锁的最佳实践之：基于 Etcd 的分布式锁</a></li>
<li><a href="https://www.jianshu.com/p/7abfb83cfa08">分布式锁第五篇 基于etcd</a></li>
<li><a href="https://www.codedump.info/post/20181125-etcd-server/#revision概念">Etcd存储的实现</a></li>
</ul>
<h1 id="Lease-机制"><a href="#Lease-机制" class="headerlink" title="Lease 机制"></a>Lease 机制</h1><p>租约机制（TTL，Time To Live）。Etcd 可以为存储的 key-value 对设置租约，当租约到期，key-value 将失效删除。</p>
<p>支持续约，通过客户端可以在<strong>租约到期之前</strong>续约，以避免 key-value 对过期失效。</p>
<p>支持解约，一旦解约，与该租约绑定的 key-value 将失效删除。</p>
<h1 id="Prefix-机制"><a href="#Prefix-机制" class="headerlink" title="Prefix 机制"></a>Prefix 机制</h1><p>前缀机制，也称目录机制。</p>
<p>如两个 key 命名如下：key1=<code>/mykey/key1</code> 、key2=<code>/mykey/key2</code>。</p>
<p>可以通过前缀-<code>/mykey</code> 查询，返回包含两个 key-value 对的列表。</p>
<h1 id="Watch-机制"><a href="#Watch-机制" class="headerlink" title="Watch 机制"></a>Watch 机制</h1><p>监听机制。</p>
<p>Watch 机制支持 Watch 某个固定的key，也支持 Watch 一个范围（前缀机制），当被 Watch 的 key 或范围发生变化，客户端将收到通知。</p>
<h1 id="Revision-机制"><a href="#Revision-机制" class="headerlink" title="Revision 机制"></a>Revision 机制</h1><p>每个 key 带有一个 Revision 属性值，etcd 每进行一次事务对应的全局 Revision 值都会加一，因此每个 key 对应的 Revision 属性值都是全局唯一的。通过比较 Revision 的大小就可以知道进行写操作的顺序。</p>
<h1 id="实现分布式锁"><a href="#实现分布式锁" class="headerlink" title="实现分布式锁"></a>实现分布式锁</h1><p>和 <a href="/2020/分布式/Zookeeper基本概念/" title="Zookeeper基本概念">Zookeeper基本概念</a> 类似。</p>
<p>在目录 <code>/lock</code> 下， <code>put</code> key 为 <code>/lock/UUID</code> 的元素。通过比较 <code>/lock</code> 下元素的 RevsionID 进行操作。</p>
]]></content>
      <categories>
        <category>分布式</category>
      </categories>
      <tags>
        <tag>分布式</tag>
      </tags>
  </entry>
  <entry>
    <title>ZooKeeper 基本概念</title>
    <url>/2020/%E5%88%86%E5%B8%83%E5%BC%8F/Zookeeper%E5%9F%BA%E6%9C%AC%E6%A6%82%E5%BF%B5/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><p><a href="https://www.cnblogs.com/binarylei/p/8721126.html">Zookeeper 系列（一）基本概念</a></p>
</li>
<li><p><a href="http://www.jasongj.com/zookeeper/distributedlock/">深入浅出Zookeeper（二） 基于Zookeeper的分布式锁与领导选举</a></p>
</li>
</ul>
<h1 id="属性结构"><a href="#属性结构" class="headerlink" title="属性结构"></a>属性结构</h1><p>ZooKeeper 使用类似文件系统的方式组织数据。</p>
<p>每个节点用从根节点到它的绝对路径表示 <code>/node1/node12</code>。</p>
<p>其中 <code>/zookeeper</code> 用以保存管理信息，比如关键配额信息。</p>
<h1 id="Znode"><a href="#Znode" class="headerlink" title="Znode"></a>Znode</h1><p>每个节点用 Znode 表示，兼具文件和目录两种特点。</p>
<p>由 3 部分组成:</p>
<ul>
<li><strong>stat</strong> ：状态信息, 描述该 Znode 的版本、权限等信息</li>
<li><strong>data</strong> ：关联的数据。最多为 1M。主要做配置、状态管理，不要用来存储大数据。</li>
<li><strong>children</strong> ：子节点。</li>
</ul>
<h1 id="数据访问"><a href="#数据访问" class="headerlink" title="数据访问"></a>数据访问</h1><p>原子操作。</p>
<p>读操作获取与节点相关的所有数据。</p>
<p>写操作替换掉节点的所有数据。</p>
<p>每一个节点有 ACL(访问控制列表)，这个列表规定了用户的权限，限定了特定用户对目标节点可以执行的操作。</p>
<h1 id="节点类型"><a href="#节点类型" class="headerlink" title="节点类型"></a>节点类型</h1><h2 id="Persist-与-Ephemeral"><a href="#Persist-与-Ephemeral" class="headerlink" title="Persist 与 Ephemeral"></a>Persist 与 Ephemeral</h2><ul>
<li>Persist 节点，一旦被创建，便不会意外丢失，即使服务器全部重启也依然存在。每个 Persist 节点即可包含数据，也可包含子节点</li>
<li>Ephemeral 点，在创建它的客户端与服务器间的 Session 结束时自动被删除。服务器重启会导致 Session 结束，因此 Ephemeral 类型的 znode 此时也会自动删除。</li>
</ul>
<h2 id="Sequence-与-Non-sequence"><a href="#Sequence-与-Non-sequence" class="headerlink" title="Sequence 与 Non-sequence"></a>Sequence 与 Non-sequence</h2><ul>
<li>Non-sequence 节点，多个客户端同时创建同一 Non-sequence 节点时，只有一个可创建成功，其它匀失败。并且创建出的节点名称与创建时指定的节点名完全一样</li>
<li>Sequence 节点，创建出的节点名在指定的名称之后带有10位10进制数的序号。多个客户端创建同一名称的节点时，都能创建成功，只是序号不同</li>
</ul>
<h1 id="Zookeeper语义保证"><a href="#Zookeeper语义保证" class="headerlink" title="Zookeeper语义保证"></a>Zookeeper语义保证</h1><p>Zookeeper 简单高效，同时提供如下语义保证，从而使得我们可以利用这些特性提供复杂的服务。</p>
<ul>
<li>顺序性。客户端发起的更新会按发送顺序被应用到 Zookeeper 上。</li>
<li>原子性。 更新操作要么成功要么失败，不会出现中间状态。</li>
<li>单一系统镜像。一个客户端无论连接到哪一个服务器都能看到完全一样的系统镜像。<ul>
<li>写操作并不保证更新被所有的 Follower 立即确认，因此通过部分 Follower 读取数据并不能保证读到最新的数据，而部分 Follwer 及 Leader 可读到最新数据。如果一定要保证单一系统镜像，可在读操作前使用 sync 方法。</li>
</ul>
</li>
<li>可靠性。一个更新操作一旦被接受即不会意外丢失，除非被其它更新操作覆盖。</li>
<li>最终一致性。写操作最终（而非立即）会对客户端可见。</li>
</ul>
<h1 id="Watch机制"><a href="#Watch机制" class="headerlink" title="Watch机制"></a>Watch机制</h1><p>所有对 Zookeeper 的读操作，都可附带一个 Watch 。一旦相应的数据有变化，该 Watch 即被触发。Watch 有如下特点</p>
<ul>
<li>主动推送。Watch被触发时，由 Zookeeper 服务器主动将更新推送给客户端，而不需要客户端轮询。</li>
<li>一次性。数据变化时，Watch 只会被触发一次。如果客户端想得到后续更新的通知，必须要在 Watch 被触发后重新注册一个 Watch。</li>
<li>可见性。如果一个客户端在读请求中附带 Watch，Watch 被触发的同时再次读取数据，客户端在得到 Watch 消息之前肯定不可能看到更新后的数据。换句话说，更新通知先于更新结果。</li>
<li>顺序性。如果多个更新触发了多个 Watch ，那 Watch 被触发的顺序与更新顺序一致。</li>
</ul>
<h1 id="实现分布式锁"><a href="#实现分布式锁" class="headerlink" title="实现分布式锁"></a>实现分布式锁</h1><h2 id="简单版本"><a href="#简单版本" class="headerlink" title="简单版本"></a>简单版本</h2><p>基于 ZooKeeper 实现分布式锁。</p>
<p>对于一个锁 <code>lock</code>，用一个节点表示。</p>
<p>创建这个节点的获取锁。</p>
<p>释放锁时删除节点。</p>
<p><img src="/images/ZooKeeper锁1.svg" alt="ZooKeeper锁1"></p>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>惊群。在释放锁时，所有等待锁的节点都被唤醒。</p>
<h2 id="改进版本"><a href="#改进版本" class="headerlink" title="改进版本"></a>改进版本</h2><p><code>lock</code> 目录下都是顺序节点，编号最小的节点获取到锁。</p>
<p>每个节点 watch 编号比它小 1 的节点，而不是获取锁的节点。</p>
<p>在前面的节点获取锁、并释放后，节点会被删除。本节点就获取到了锁。</p>
<p><img src="/images/ZooKeeper锁2.svg" alt="ZooKeeper锁2"></p>
]]></content>
      <categories>
        <category>分布式</category>
      </categories>
      <tags>
        <tag>分布式</tag>
      </tags>
  </entry>
  <entry>
    <title>分布式 ID 生成</title>
    <url>/2020/%E5%88%86%E5%B8%83%E5%BC%8F/%E5%88%86%E5%B8%83%E5%BC%8FID%E7%94%9F%E6%88%90/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://tech.meituan.com/2017/04/21/mt-leaf.html">Leaf——美团点评分布式ID生成系统</a></li>
</ul>
<h1 id="UUID"><a href="#UUID" class="headerlink" title="UUID"></a>UUID</h1><h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><ul>
<li>性能非常高：本地生成，没有网络消耗。</li>
</ul>
<h2 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h2><ul>
<li>不易于存储：UUID太长，16字节128位，通常以36长度的字符串表示，很多场景不适用。</li>
<li>信息不安全：基于MAC地址生成UUID的算法可能会造成MAC地址泄露。</li>
<li>ID 作为数据库主键时，UUID 会引起位置变化频繁。</li>
<li>不是递增的。</li>
</ul>
<h1 id="类-snowflake-方案"><a href="#类-snowflake-方案" class="headerlink" title="类 snowflake 方案"></a>类 snowflake 方案</h1><p>时间戳 + workerID + 每毫秒的递增序列号。</p>
<p>需要单独部署服务。</p>
<p>每台服务对应一个 workerID。每台服务是单线程的。</p>
<p><img src="/images/snowflake.png" alt="snowflake"></p>
<h2 id="优点-1"><a href="#优点-1" class="headerlink" title="优点"></a>优点</h2><ul>
<li>序号是递增趋势的。</li>
<li>不依赖数据库。</li>
</ul>
<h2 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h2><ul>
<li>时钟回拨，会出现重复的序列号。</li>
</ul>
<h2 id="优化"><a href="#优化" class="headerlink" title="优化"></a>优化</h2><h3 id="workerID"><a href="#workerID" class="headerlink" title="workerID"></a>workerID</h3><p>很多台 snowflake 服务的话，手工指定 workerID 不现实。</p>
<p>可以使用 Zookeeper。</p>
<p>每次启动服务，到 Zookeeper 查看 <code>/leaf_forever</code> 下是否有自己注册过的节点，有取回，没有生成序列节点。</p>
<p>弱依赖 Zookeeper：只在启动时请求一次。</p>
<h3 id="时钟回拨"><a href="#时钟回拨" class="headerlink" title="时钟回拨"></a>时钟回拨</h3><p>每隔一段时间，向 Zookeeper 上传自己的时钟。并检查自己的时钟是否回拨了。</p>
<p>回拨了则抛出异常，服务关闭。</p>
<h1 id="数据库方案"><a href="#数据库方案" class="headerlink" title="数据库方案"></a>数据库方案</h1><p>使用数据库的 <code>auto_increment_increment</code> 保证 ID 的自增。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">begin</span>;</span><br><span class="line"><span class="keyword">REPLACE</span> <span class="keyword">INTO</span> Tickets64 (stub) <span class="keyword">VALUES</span> (<span class="string">'a'</span>);</span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">LAST_INSERT_ID</span>();</span><br><span class="line"><span class="keyword">commit</span>;</span><br></pre></td></tr></table></figure>
<h2 id="优点-2"><a href="#优点-2" class="headerlink" title="优点"></a>优点</h2><ul>
<li>实现简单。</li>
<li>单调递增。</li>
</ul>
<h2 id="缺点-2"><a href="#缺点-2" class="headerlink" title="缺点"></a>缺点</h2><ul>
<li>每次生成 ID 都要写入数据库，效率差。瓶颈限制在单台MySQL的读写性能。</li>
<li>依赖数据库。</li>
</ul>
<h2 id="优化-1"><a href="#优化-1" class="headerlink" title="优化"></a>优化</h2><h3 id="多台服务-分段"><a href="#多台服务-分段" class="headerlink" title="多台服务 + 分段"></a>多台服务 + 分段</h3><p>多台服务依赖一个数据库，每个服务每次把数据库的值递增 <code>step</code>。</p>
<p>服务处理完 <code>step</code> 个 ID 后，再去到数据库请求。</p>
<p>趋势上是递增的。</p>
<h1 id="微信序列号"><a href="#微信序列号" class="headerlink" title="微信序列号"></a>微信序列号</h1><p>每个 user 对应一个 ID，每次一个 user 取到的 ID 比它之前取到的大（不一定递增 1）。</p>
<p>每个 user 设置一个 <code>maxID</code>，在 ID 增加到这个值后，<code>maxID</code> 增加 <code>step</code>，并写入磁盘。</p>
<p>为了重启的时候，不读取大量的 <code>maxID</code>。多个 user 公用一个 <code>regionID</code>。和 <code>maxId</code> 类似，某个 <code>maxID</code> 达到 <code>regionID</code> 后，<code>regionID</code> 增加一定的步长。</p>
]]></content>
      <categories>
        <category>分布式</category>
      </categories>
      <tags>
        <tag>分布式</tag>
      </tags>
  </entry>
  <entry>
    <title>B 树与 B+ 树</title>
    <url>/2020/%E6%95%B0%E6%8D%AE%E5%BA%93/B%E6%A0%91%E4%B8%8EB+%E6%A0%91/</url>
    <content><![CDATA[<blockquote>
<ul>
<li><a href="https://www.cnblogs.com/nullzx/p/8729425.html">B树和B+树的插入、删除图文详解</a></li>
<li><a href="https://www.cs.usfca.edu/~galles/visualization/BTree.html">B-Tree Visualization</a></li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>record</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>FastThreadLocal</title>
    <url>/2020/netty/FastThreadLocal/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.jianshu.com/p/3fc2fbac4bb7">Netty 高性能之道 FastThreadLocal 源码分析（快且安全）</a></li>
</ul>
<h1 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h1><h2 id="threadLocalMap"><a href="#threadLocalMap" class="headerlink" title="threadLocalMap"></a>threadLocalMap</h2><p>每个线程内部有一个 <code>threadLocalMap</code> 。</p>
<p>其内部使用 <code>Object[] indexedVariables</code> 存储了每一个本地线程变量。</p>
<ul>
<li>如果这个线程时 Netty 的 <code>FastThreadLocalThread</code> 线程，它的内部存有 <code>threadLocalMap</code> 域。</li>
<li>如果是原生的线程，使用 原生的 <code>ThreadLocal</code> 存储每个原生线程的 <code>threadLocalMap</code>。</li>
</ul>
<h2 id="自增-index"><a href="#自增-index" class="headerlink" title="自增 index"></a>自增 index</h2><p>每创建一个 <code>FastThreadLocal</code>，会使用一个全局的 <code>AtomicInteger</code> 自增作为 <code>index</code>。</p>
<p>稍后 value 会存储在 <code>threadLocalMap.indexedVariables[index]</code> 处。</p>
<ul>
<li>注意，多个线程的 <code>index</code> 是自增的，会浪费空间。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// class FastThreadLocal</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">FastThreadLocal</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    index = InternalThreadLocalMap.nextVariableIndex();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// class InternalThreadLocalMap</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> AtomicInteger nextIndex = <span class="keyword">new</span> AtomicInteger();</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">nextVariableIndex</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> index = nextIndex.getAndIncrement();</span><br><span class="line">    <span class="keyword">if</span> (index &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        nextIndex.decrementAndGet();</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">"too many thread-local indexed variables"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> index;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="toRemoveSet"><a href="#toRemoveSet" class="headerlink" title="toRemoveSet"></a>toRemoveSet</h2><p>一个线程占用了一处 <code>threadLocalMap.indexedVariables</code> 来存储 <code>toRemoveSet</code>。</p>
<p><code>toRemoveSet</code> 存储了需要删除的 <code>FastThreadLocal</code> 对象。</p>
<h1 id="set"><a href="#set" class="headerlink" title="set()"></a>set()</h1><p>调用 <code>`fastThreadLocal.set(val)</code> 时，如果是新增而不是更新值：</p>
<ul>
<li>把 <code>fastThreadLocal</code> 对象加入<code>toRemoveSet</code>。</li>
<li>把 <code>fastThreadLocal</code> 对象同时注册一个 <code>ObjectCleaner</code>，在这个线程被 GC 时，会调用 <code>fastThreadLocal.remove()</code>。</li>
</ul>
<h1 id="remove"><a href="#remove" class="headerlink" title="remove()"></a>remove()</h1><p>在调用 <code>fastThreadLocal.remove()</code> 时，</p>
<ul>
<li>把 <code>indexedVariables</code> 对应 <code>index</code> 设为 UNSET。</li>
<li>把该对象从 <code>toRemoveSet</code> 删除。</li>
</ul>
<h1 id="removeAll"><a href="#removeAll" class="headerlink" title="removeAll()"></a>removeAll()</h1><p>调用 <code>FastThreadLocal.removeAll()</code> （类的静态方法）</p>
<ul>
<li>从本线程的 <code>indexedVariables</code> 中取出<code>toRemoveSet</code>。</li>
<li>遍历 <code>toRemoveSet</code> 每一个 <code>fastThreadLocal</code> 对象，调用 <code>remove()</code>。</li>
</ul>
<h1 id="ObjectCleaner"><a href="#ObjectCleaner" class="headerlink" title="ObjectCleaner"></a>ObjectCleaner</h1><h2 id="FastThreadLocal-set"><a href="#FastThreadLocal-set" class="headerlink" title="FastThreadLocal#set()"></a>FastThreadLocal#set()</h2><p>在 <code>FastThreadLocal#set(val)</code> 时，如果是新增的，会注册 GC 时的行为。</p>
<p>如下，把当前线程、要执行的函数传给 <code>ObjectCleaner.register()</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ObjectCleaner.register(current, <span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        remove(threadLocalMap);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h2 id="ObjectCleaner-register"><a href="#ObjectCleaner-register" class="headerlink" title="ObjectCleaner.register()"></a>ObjectCleaner.register()</h2><p>把线程封装为弱引用，那弱引用与 <code>REFERENCE_QUEUE</code> 引用队列关联起来。</p>
<p>把这个弱引用放入 <code>LIVE_SET</code>。</p>
<p>启动 <code>cleanupThread</code> 来运行 <code>CLEANER_TASK</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">AutomaticCleanerReference</span> <span class="keyword">extends</span> <span class="title">WeakReference</span>&lt;<span class="title">Object</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Runnable cleanupTask;</span><br><span class="line">		<span class="comment">// 弱引用 REFERENCE_QUEUE 放入队列。</span></span><br><span class="line">    AutomaticCleanerReference(Object referent, Runnable cleanupTask) &#123;</span><br><span class="line">        <span class="keyword">super</span>(referent, REFERENCE_QUEUE);</span><br><span class="line">        <span class="keyword">this</span>.cleanupTask = cleanupTask;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">cleanup</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        cleanupTask.run();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Thread <span class="title">get</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        LIVE_SET.remove(<span class="keyword">this</span>);</span><br><span class="line">        <span class="keyword">super</span>.clear();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">register</span><span class="params">(Object object, Runnable cleanupTask)</span> </span>&#123;</span><br><span class="line">  	<span class="comment">// 弱引用。</span></span><br><span class="line">  	AutomaticCleanerReference reference = <span class="keyword">new</span> AutomaticCleanerReference(object,</span><br><span class="line">            ObjectUtil.checkNotNull(cleanupTask, <span class="string">"cleanupTask"</span>));</span><br><span class="line">    </span><br><span class="line">    LIVE_SET.add(reference);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Check if there is already a cleaner running.</span></span><br><span class="line">    <span class="keyword">if</span> (CLEANER_RUNNING.compareAndSet(<span class="keyword">false</span>, <span class="keyword">true</span>)) &#123;</span><br><span class="line">      	<span class="comment">// CLEANER_TASK 要执行的。</span></span><br><span class="line">        <span class="keyword">final</span> Thread cleanupThread = <span class="keyword">new</span> FastThreadLocalThread(CLEANER_TASK);</span><br><span class="line">        cleanupThread.setPriority(Thread.MIN_PRIORITY);</span><br><span class="line">        AccessController.doPrivileged(<span class="keyword">new</span> PrivilegedAction&lt;Void&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> Void <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                cleanupThread.setContextClassLoader(<span class="keyword">null</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        cleanupThread.setName(CLEANER_THREAD_NAME);</span><br><span class="line"></span><br><span class="line">        cleanupThread.setDaemon(<span class="keyword">true</span>);</span><br><span class="line">        cleanupThread.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="CLEANER-TASK-run"><a href="#CLEANER-TASK-run" class="headerlink" title="CLEANER_TASK.run()"></a>CLEANER_TASK.run()</h2><p>在 <code>LIVE_SET</code> 非空时，阻塞出队 <code>REFERENCE_QUEUE</code>。</p>
<ul>
<li>JVM 保证对象在被 GC 时，会加入到和它关联的引用队列中。</li>
<li>见  <a href="/2020/java/jvm/2Java垃圾回收/" title="垃圾回收四种引用类别">垃圾回收四种引用类别</a></li>
</ul>
<p>出队成功后，调用 <code>reference.cleanup()</code>，即注册的方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Runnable CLEANER_TASK = <span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">while</span> (!LIVE_SET.isEmpty()) &#123;</span><br><span class="line">                <span class="keyword">final</span> AutomaticCleanerReference reference;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    reference = (AutomaticCleanerReference) REFERENCE_QUEUE.remove(REFERENCE_QUEUE_POLL_TIMEOUT_MS);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException ex) &#123;</span><br><span class="line">                    interrupted = <span class="keyword">true</span>;</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (reference != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        reference.cleanup();</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (Throwable ignored) &#123;</span><br><span class="line">                    &#125;</span><br><span class="line">                    LIVE_SET.remove(reference);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            CLEANER_RUNNING.set(<span class="keyword">false</span>);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (LIVE_SET.isEmpty() || !CLEANER_RUNNING.compareAndSet(<span class="keyword">false</span>, <span class="keyword">true</span>)) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (interrupted) &#123;</span><br><span class="line">            Thread.currentThread().interrupt();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h1 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h1><h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><ul>
<li>时间效率很高。<ul>
<li>没有用 hash、以及线性寻址法。</li>
<li><code>ThreadLocal</code> 还需要在各类操作中花费 log(N) 时间检查被回收的对象。</li>
</ul>
</li>
<li>内存安全的，在线程被 GC 时，必然会被回收掉。<ul>
<li>而 <code>ThreadLocal</code> 有内存泄漏的风险。见 <a href="/2020/java/Java并发编程的艺术笔记/4Java并发编程基础/" title="ThreadLocal">ThreadLocal</a></li>
</ul>
</li>
</ul>
<h2 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h2><ul>
<li>空间效率低。<ul>
<li>所有线程共有自增的 <code>index</code>，每个线程的 <code>threadLocalMap</code> 有很多空洞。</li>
<li><code>remove()</code> 调用后，虽然把位置设为了 <code>UNSET</code>，但是也不能再使用了。每次 <code>set()</code> 都是递增的 index。</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang 记录</title>
    <url>/2020/golang/golang%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="Channel-原理"><a href="#Channel-原理" class="headerlink" title="Channel 原理"></a>Channel 原理</h1><blockquote>
<p><a href="https://draveness.me/golang/docs/part3-runtime/ch06-concurrency/golang-channel/">Go 语言设计与实现 Channel</a></p>
</blockquote>
<p>阻塞队列</p>
<h1 id="Goroutine"><a href="#Goroutine" class="headerlink" title="Goroutine"></a>Goroutine</h1><blockquote>
<p><a href="https://www.zhihu.com/question/20862617">Golang 的 goroutine 是如何实现的？</a></p>
</blockquote>
<p>用户级线程</p>
]]></content>
      <categories>
        <category>golang</category>
      </categories>
      <tags>
        <tag>golang</tag>
        <tag>record</tag>
      </tags>
  </entry>
  <entry>
    <title>Raft 协议</title>
    <url>/2020/%E5%88%86%E5%B8%83%E5%BC%8F/raft%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.jianshu.com/p/ddbe4209be0f">RAFT协议完整笔记</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/27207160">Raft协议详解</a></li>
</ul>
<h1 id="节点状态"><a href="#节点状态" class="headerlink" title="节点状态"></a>节点状态</h1><ol>
<li>跟随者</li>
<li>候选者</li>
<li>领导者</li>
</ol>
<h1 id="跟随者"><a href="#跟随者" class="headerlink" title="跟随者"></a>跟随者</h1><p>节点收到心跳/添加日志请求后：</p>
<ol>
<li>如果当前的term大于请求的term，返回false。</li>
<li>如果当前的term小于请求的term，自己变为跟随者。</li>
<li>如果请求的PrevLogIndex不在自己的log里，返回false。</li>
<li>如果请求的PrevLogTerm不是自己的这个index的term，略过自己所有这个term的log，返回最右边不是这个term的log的index，返回false。</li>
<li>否则可以添加请求中的log entries。</li>
<li>更新commitIndex为请求中的commitIndex（如果更大的话）。</li>
<li>返回true。</li>
</ol>
<h1 id="候选者"><a href="#候选者" class="headerlink" title="候选者"></a>候选者</h1><p>跟随者转变为候选者的时机：</p>
<ul>
<li>如果跟随者 一定时间内 没有收到领导者的心跳消息，则转变为候选者。</li>
</ul>
<p>转变为候选者后，</p>
<ul>
<li>term++，</li>
<li>投票给自己，</li>
<li>给其他所有节点发送投票请求。请求中附带4个信息：<ul>
<li>当前的term</li>
<li>候选者id</li>
<li>上一条log的term</li>
<li>上一条log的index</li>
</ul>
</li>
</ul>
<p>节点在收到投票请求时：</p>
<ol>
<li><p>比较自己的term和请求的term，</p>
<ul>
<li><p>如果自己的term大，直接返回false。</p>
</li>
<li><p>如果请求的term大，更新自己为跟随者（自己可能是领导者、候选者），投票清空。</p>
</li>
</ul>
</li>
<li><p>如果当前已经投票过了，返回false。</p>
</li>
<li><p>如果没有投过票，并且请求的候选者的log更新（args.LastLogTerm &gt; lastLogTerm || (args.LastLogTerm == lastLogTerm &amp;&amp; args.LastLogIndex + 1 &gt;= len(rf.log))，则返回true，更新自己为跟随者。否则返回false。</p>
</li>
</ol>
<p>候选者收到投票请求的回复后：</p>
<ul>
<li>如果收到true的数目多于1半，则成为领导者。</li>
<li>否则等待选举时间超时，进行下一个term的选举。或者收到其他候选者的请求（请求的term更大），进行投票，成为跟随者。或者收到其他人的心跳请求，成为跟随者。<br>这里为了防止不断的有候选者平分选票重试，候选者和跟随者等待的超时时间要加一个随机数。</li>
</ul>
<h1 id="领导者"><a href="#领导者" class="headerlink" title="领导者"></a>领导者</h1><p>领导者在收到添加log请求后，会把log添加到自己的logs里，在下一次心跳时，发送添加log的请求。</p>
<p>领导者在收到添加log请求的回复时：</p>
<ul>
<li>如果返回的是false，把rf.nextIndex[index] = reply.NextIndex，稍后心跳用新的nextIndex重试。</li>
<li>如果返回的是true，如果大多数跟随者都返回了true，把这条记录标记为commit。</li>
</ul>
<h1 id="选举约束"><a href="#选举约束" class="headerlink" title="选举约束"></a>选举约束</h1><p>选举约束：被选举出来的 leader 必须要包含所有已经提交的 entries。</p>
<p>Raft 的简单实现：lastLog的term越大谁越新，如果term相同，谁的lastLog的index越大谁越新。</p>
<p>但是这样的实现，可能会出现日志覆盖的情况（见<a href="https://zhuanlan.zhihu.com/p/27207160">Raft协议详解</a>）。</p>
<p>所以 Raft 添加了约束2：当前term的leader不能“直接”提交之前term的entries，必须要等到<strong>当前</strong> term 有entry过半了，才<strong>顺便</strong>一起将之前term的entries进行提交。</p>
<p>不能覆盖已经提交的请求（因为领导者要半数节点同意才可以，而同意需要领导者的上一条的term和index最大）</p>
<h1 id="网络分区"><a href="#网络分区" class="headerlink" title="网络分区"></a>网络分区</h1><blockquote>
<ul>
<li><a href="https://pingcap.com/blog-cn/linearizability-and-raft/">PingCAP-线性一致性和 Raft</a></li>
<li><a href="https://zhengyinyong.com/post/etcd-linearizable-read-implementation/#什么是线性一致性读">etcd 中线性一致性读的具体实现</a></li>
</ul>
</blockquote>
<p>A - D 五个节点，A 为 Leader。</p>
<p>某一时刻，网络分区为 AB和CDE。CDE由于心跳超时，假设 C 成为新的 Leader。</p>
<p>这样，CDE 认为 C 是 Leader，而 AB 仍然认为 A 是 Leader。</p>
<p>这个时候，就产生了<strong>网络分区</strong>。</p>
<h2 id="写请求"><a href="#写请求" class="headerlink" title="写请求"></a>写请求</h2><p>Client 可以把读写请求发送到集群的任意节点，节点会把请求转发给 Leader。</p>
<p>网络分区对于写请求没有影响。</p>
<ul>
<li><p>因为 Client 如果向 AB 请求写入，没有办法产生过半数的同意，所以没有办法<strong>提交</strong>这个写请求。</p>
</li>
<li><p>只有 Client 向 CDE 请求写入，才能被<strong>提交</strong>。</p>
</li>
</ul>
<h2 id="读请求"><a href="#读请求" class="headerlink" title="读请求"></a>读请求</h2><p>从 AB 读可能读到旧的数据。</p>
<h3 id="ReadIndex"><a href="#ReadIndex" class="headerlink" title="ReadIndex"></a>ReadIndex</h3><p>使用 ReadIndex 解决这个问题，主要是第 2 步。</p>
<ol>
<li>记录当前的 commit index，称为 ReadIndex</li>
<li>向 Follower 发起一次心跳，如果大多数节点回复了，那就能确定现在仍然是 Leader</li>
<li>等待状态机<strong>至少</strong>应用到 ReadIndex 记录的 Log</li>
<li>执行读请求，将结果返回给 Client</li>
</ol>
<p>1 和 3 步是为了保证线性一致性读。</p>
<ul>
<li>线性一致性读：当存储系统已将写操作提交成功，那此时读出的数据应是最新的数据、</li>
</ul>
<h3 id="LeaseRead"><a href="#LeaseRead" class="headerlink" title="LeaseRead"></a>LeaseRead</h3><p>这样每次读请求，都要发起一次心跳，网络消耗很大。使用 LeaseRead 来优化。</p>
<p>基本的思路是 Leader 取一个比 Election Timeout 小的租期，在租期不会发生选举，确保 Leader 不会变，所以可以跳过 ReadIndex 的第二步，也就降低了延时。</p>
]]></content>
      <categories>
        <category>分布式</category>
      </categories>
      <tags>
        <tag>分布式</tag>
      </tags>
  </entry>
  <entry>
    <title>红黑树</title>
    <url>/2020/algorithm/%E7%BA%A2%E9%BB%91%E6%A0%91/</url>
    <content><![CDATA[<h1 id="性质"><a href="#性质" class="headerlink" title="性质"></a>性质</h1><ol>
<li>所有节点都是红色或者黑色。</li>
<li>根节点为黑色。</li>
<li>每个叶子节点下挂两个NULL节点，并且NULL节点为黑色。</li>
<li>如果该节点是红色的，那么该节点的子节点一定都是黑色。</li>
<li>从根节点到叶子节点的路径，每条路径的黑色节点数目是相同的。</li>
</ol>
<h1 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h1><ol>
<li>插入节点X到对应位置，初始为红色。</li>
<li>如果X是根节点，则标记为黑色。</li>
<li>如果X的父节点是根节点，返回。</li>
<li>否则，X和X的父节点都不是根节点：<ol>
<li>如果X的父节点是黑色，返回。</li>
<li>否则，看X的uncle节点是不是红色：<ol>
<li>如果X的父节点和X的uncle都是红色，变色，<strong>向上检查</strong>X的父亲的父亲节点的颜色。</li>
<li>否则，如果X在父节点是X的祖先节点的左子节点<ol>
<li>如果X也是父节点的左子节点（LL），右旋变色。<strong>返回。</strong></li>
<li>如果X是父节点的右子节点（LR），先左旋、再右旋变色。<strong>返回。</strong></li>
</ol>
</li>
<li>如果X在父节点是X的祖先节点的右子节点<ol>
<li>如果X也是父节点的右子节点（RR），左旋变色。<strong>返回。</strong></li>
<li>如果X是父节点的左子节点（RL），先右旋、再左旋变色。<strong>返回。</strong></li>
</ol>
</li>
</ol>
</li>
</ol>
</li>
</ol>
<h1 id="平衡二叉树与红黑树的对比"><a href="#平衡二叉树与红黑树的对比" class="headerlink" title="平衡二叉树与红黑树的对比"></a>平衡二叉树与红黑树的对比</h1><ol>
<li>搜索长度<ul>
<li>红黑树搜索路径的最大长度不超过搜索路径的最短长度的2倍。</li>
<li>平衡二叉树差值不大于1。</li>
</ul>
</li>
<li>插入<ul>
<li>红黑树在插入的时候，只有X的父节点和Uncle都是红色，才要递归到根节点变色，否则只要旋转1-2次就可以。</li>
<li>AVL在插入时，要找到最低的不平衡位置，向上递归到根节点旋转。</li>
</ul>
</li>
<li>删除<ul>
<li>红黑树只有个别情况才会递归平衡父节点，它发生在：兄弟节点是黑色，两个侄儿也是黑色。当兄弟节点是红色的时候，转化为兄弟节点是黑色的情况处理，当两个侄儿有红色节点的时候，则在常数时间内就可以达到平衡。</li>
<li>AVL和插入类似，找到第一个不平衡位置，向上递归到根节点。</li>
</ul>
</li>
</ol>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ol>
<li><a href="https://zhuanlan.zhihu.com/p/34840762">一篇搞懂AVL平衡二叉树</a></li>
<li><a href="[https://pjmike.github.io/2018/08/09/%E5%B9%B3%E8%A1%A1%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E6%8F%92%E5%85%A5%E4%B8%8E%E5%88%A0%E9%99%A4/](https://pjmike.github.io/2018/08/09/平衡二叉树的插入与删除/">平衡二叉树的插入与删除</a>)</li>
<li><a href="https://acm.sdut.edu.cn/onlinejudge2/index.php/Home/Index/problemdetail/pid/3374.html">sdut-数据结构实验之查找二：平衡二叉树</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/79980618">红黑树，超强动静图详解，简单易懂</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/22800206">数据结构：红黑树的删除</a></li>
<li><a href="https://www.zhihu.com/question/19856999">红黑树比 AVL 树具体更高效在哪里？</a></li>
</ol>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>排序</title>
    <url>/2020/algorithm/%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<blockquote>
<p><a href="https://www.zhihu.com/question/51337272/answer/572455307">知乎-10大排序算法的时间复杂度、空间复杂度、稳定性</a></p>
</blockquote>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>平衡二叉树</title>
    <url>/2020/algorithm/%E5%B9%B3%E8%A1%A1%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<p>二叉搜索树 的中序遍历是有序的。</p>
<h1 id="性质"><a href="#性质" class="headerlink" title="性质"></a>性质</h1><ol>
<li>对于一个节点，其左子树的值小于它的值，右子树的值大于它的值。</li>
<li>对于一个节点，其左子树和右子树的高度之差不大于1。</li>
</ol>
<h1 id="旋转操作"><a href="#旋转操作" class="headerlink" title="旋转操作"></a>旋转操作</h1><ol>
<li>左旋<ul>
<li>根节点的右节点成为新根节点。</li>
<li>旧根节点成为新根节点的左节点。</li>
<li>新根节点本来的左节点成为旧根节点的右节点。</li>
</ul>
</li>
<li>右旋<ul>
<li>根节点的左节点成为新根节点。</li>
<li>旧根节点成为新根节点的右节点。</li>
<li>新根节点本来的右节点成为旧根节点的左节点。</li>
</ul>
</li>
</ol>
<h1 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h1><p>对于1个节点，插入值为val的节点后，看它是否已经平衡(balance = 左子树高度-右子树高度)，如果不平衡：</p>
<ul>
<li>balance &gt;= 2，左子树多了节点。<ul>
<li>如果val比左子节点的值小，说明在左子节点的左子树里多了一个节点(LL)。<ul>
<li>对当前节点进行右旋操作。</li>
</ul>
</li>
<li>否则，说明在左子节点的右子树里多了一个节点(LR)。<ul>
<li>对左子节点进行左旋操作。</li>
<li>随后，对当前节点进行右旋操作。</li>
</ul>
</li>
</ul>
</li>
<li>balance &lt;= -2，左子树多了节点。<ul>
<li>如果val比右子节点的值大，说明在右子节点的右子树里多了一个节点(RR)。<ul>
<li>对当前节点进行左旋操作。</li>
</ul>
</li>
<li>否则，说明在右子节点的左子树里多了一个节点(RL)。<ul>
<li>对右子节点进行右旋操作。</li>
<li>随后，对当前节点进行左旋操作。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>注意，</p>
<ul>
<li>旋转之后，要更新旧根节点和新根节点的高度。</li>
<li>平衡之后，要更新当前节点的高度。</li>
</ul>
<p>示意图</p>
<p><img src="/images/avl.png" alt="avl"></p>
<h1 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h1><p>对于1个节点，要删除值为val的节点，首先找到对应的节点：</p>
<ul>
<li>如果当前节点为空，说明树中不存在值为val的节点，返回空。</li>
<li>如果val比当前节点的值小，在左子树递归寻找。</li>
<li>如果val比当前节点的值大，在右子树递归寻找。</li>
<li>否则，找到了值为val的节点。</li>
</ul>
<p>找到值为val的节点root后，要删除root节点：</p>
<ul>
<li>如果root的左右子节点至少有 一个为空，删除root节点，新的root为那个不为空的子节点（或者为空）。</li>
<li>如果左右子节点都不为空，去root的右子树里找最小的元素（比val大的最小的元素），把找到的值赋给root，删除那个节点（递归调用这个函数）。</li>
</ul>
<p>删除root节点后，要进行节点的平衡：</p>
<ul>
<li>如果balance &gt;= 2<ul>
<li>如果root左子节点的balance&gt;=0，说明多的节点在左子节点的左子树里(LL)。<ul>
<li>对root进行右旋。</li>
</ul>
</li>
<li>否则，说明多的节点在左子节点的右子树里(LR)。<ul>
<li>对root的左子节点进行左旋。</li>
<li>随后，对root进行右旋。</li>
</ul>
</li>
</ul>
</li>
<li>如果balance &lt;= -2<ul>
<li>如果root右子节点的balance&lt;0，说明多的节点在右子节点的右子树里(RR)。<ul>
<li>对root进行左旋。</li>
</ul>
</li>
<li>否则，说明多的节点在右子节点的左子树里(RL)。<ul>
<li>对root的右子节点进行右旋。</li>
<li>随后，对root进行左旋。</li>
</ul>
</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>加密与哈希</title>
    <url>/2020/algorithm/%E5%8A%A0%E5%AF%86%E4%B8%8E%E5%93%88%E5%B8%8C/</url>
    <content><![CDATA[<h1 id="非对称加密"><a href="#非对称加密" class="headerlink" title="非对称加密"></a>非对称加密</h1><h2 id="RSA-原理"><a href="#RSA-原理" class="headerlink" title="RSA 原理"></a>RSA 原理</h2><p><a href="https://www.zhihu.com/answer/388573650">https://www.zhihu.com/answer/388573650</a></p>
<h1 id="对称加密"><a href="#对称加密" class="headerlink" title="对称加密"></a>对称加密</h1><ul>
<li>AES <a href="https://mp.weixin.qq.com/s?__biz=MzI1MTIzMzI2MA==&amp;mid=2650561454&amp;idx=1&amp;sn=dd91cdd8f3853fe07e1f2d02f4f126a9&amp;chksm=f1feed2dc689643bc143bc9462cc36315843a96e1babe372216375808148c43258278723749a&amp;scene=21#wechat_redirect">漫画：什么是 AES 算法？</a></li>
</ul>
<p>TODO</p>
<h1 id="HTTPS-加密"><a href="#HTTPS-加密" class="headerlink" title="HTTPS 加密"></a>HTTPS 加密</h1><h2 id="流程"><a href="#流程" class="headerlink" title="流程"></a>流程</h2><p><img src="/images/https-encrypt.jpg" alt="https-encrypt" style="zoom:70%;" /></p>
<h2 id="HTTPS-为什么数据传输用对称加密？"><a href="#HTTPS-为什么数据传输用对称加密？" class="headerlink" title="HTTPS 为什么数据传输用对称加密？"></a>HTTPS 为什么数据传输用对称加密？</h2><ol>
<li><p>非对称加密运算很慢，有很多除法运算。</p>
</li>
<li><p>在 HTTPS 的场景中只有服务端保存了私钥，一对公私钥只能实现单向的加解密</p>
</li>
</ol>
<h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://zhuanlan.zhihu.com/p/97154765">知乎-你知道，HTTPS用的是对称加密还是非对称加密？</a></li>
</ul>
<h1 id="哈希"><a href="#哈希" class="headerlink" title="哈希"></a>哈希</h1><h2 id="信息安全中的-hash"><a href="#信息安全中的-hash" class="headerlink" title="信息安全中的 hash"></a>信息安全中的 hash</h2><h3 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h3><p>hash 是把任意长度的输入，通过散列算法，变换成固定长度的输出，该输出就是散列值。</p>
<p>这种转换是一种压缩映射，也就是，散列值的空间通常远小于输入的空间，不同的输入可能会散列成相同的输出，而不可能从散列值来唯一的确定输入值。</p>
<p>简单的说就是一种将任意长度的消息压缩到某一固定长度的信息摘要的函数。</p>
<h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h3><p>1、输入更改一点，输出完全不同。只要原始信息更改哪怕一个字节，哈希计算的结果都会完全不同，要找到同一哈希值的两个不同源数据，在计算上来说是基本不可能的。</p>
<p>2、不同的输入有可能会得到相同的哈希值。因此不可能通过哈希值来逆向确定唯一的输入值。通俗的讲就是，哈希算法不能从结果推算出输入。</p>
<h3 id="主要作用"><a href="#主要作用" class="headerlink" title="主要作用"></a>主要作用</h3><p>信息一致性和完整性检验。</p>
<h3 id="在信息安全方面的应用"><a href="#在信息安全方面的应用" class="headerlink" title="在信息安全方面的应用"></a>在信息安全方面的应用</h3><ol>
<li><p>文件校验</p>
<p>校验算法 <strong>奇偶校验</strong>和 <strong>CRC 校验</strong>，一定程度上能检测并纠正数据传输中的信道误码。没有抗数据篡改的能力，不能防止对数据的恶意破坏。</p>
<p>使用 Hash 算法作为校验和。对接收的传输数据执行散列运算来检查数据的正确性。计算出的散列值拿来和随数据传输的散列值比较。如果两个值相同，说明传输的数据完整无误、没有被窜改过。</p>
</li>
<li><p>数字签名</p>
<p>因为非对称算法的运算速度较慢，所以在数字签名协议中，单向散列函数扮演了一个重要的角色。</p>
<p>通常我们使用公钥加密，用私钥解密。而在数字签名中，我们使用私钥加密（相当于生成签名），公钥解密（相当于验证签名）。</p>
<p>签名方法：我们可以直接对消息进行签名（即使用私钥加密，此时加密的目的是为了签名，而不是保密），验证者用公钥正确解密消息，如果和原消息一致，则验证签名成功。但通常我们会对消息的散列值签名，因为通常散列值的长度远小于消息原文，使得签名（非对称加密）的效率大大提高。注意，计算消息的散列值不是数字签名的必要步骤。</p>
</li>
<li><p>鉴权协议</p>
<p>有个称为“挑战-认证模式”的鉴权协议：需要鉴权的一方，向将被鉴权的一方发送随机串（“挑战”），被鉴权方将该随机串和自己的鉴权口令字一起进行 Hash 运算后，返还鉴权方，鉴权方将收到的Hash值与在己端用该随机串和对方的鉴权口令字进行 Hash 运算的结果相比较（“认证”），如相同，则可在统计上认为对方拥有该口令字，即通过鉴权。</p>
</li>
<li><p>存储加密后的密码</p>
</li>
<li><p>比特币</p>
</li>
</ol>
<h3 id="安全-hash-算法的2个条件"><a href="#安全-hash-算法的2个条件" class="headerlink" title="安全 hash 算法的2个条件"></a>安全 hash 算法的2个条件</h3><ol>
<li>抗碰撞性。根据一个输入，找到一个其它输入得到相同的输出，在计算上是不可行的；</li>
<li>不可逆性。根据一个输出，找到一个输入其散列值等于输出，在计算上是不可行的，即不可能从结果逆向推导初始值</li>
</ol>
<h3 id="MD5碰撞"><a href="#MD5碰撞" class="headerlink" title="MD5碰撞"></a>MD5碰撞</h3><p>已知MD5(A)，可以快速构造出MD5(B) = MD5(A)。这样使用MD5校验和，就没有办法保证原内容没有被篡改。</p>
<p>所以MD5不能再用于软件完整性检查或代码签名的用途。</p>
<p>SHA0 和 SHA1 (Secure Hash Algorithm 1) 算法也被研究出了碰撞算法。SHA256 还没有被碰撞破解。</p>
<h3 id="引用-1"><a href="#引用-1" class="headerlink" title="引用"></a>引用</h3><ul>
<li><a href="[https://zh.wikipedia.org/wiki/%E6%95%B8%E4%BD%8D%E7%B0%BD%E7%AB%A0](https://zh.wikipedia.org/wiki/數位簽章">维基百科-数字签名</a>)</li>
<li><a href="https://zhuanlan.zhihu.com/p/59210080">知乎-哈希（HASH）简介和取证应用</a></li>
<li><a href="https://blog.csdn.net/liangkwok/article/details/7441867">CSDN-快速MD5碰撞生成器,MD5真的不可靠了</a></li>
<li><a href="https://blog.csdn.net/K346K346/article/details/87938911">CSDN-王小云院士真地破解了 MD5 吗</a></li>
<li><a href="[https://zh.wikipedia.org/wiki/SHA-1#SHA-1%E7%9A%84%E7%A0%B4%E8%A7%A3](https://zh.wikipedia.org/wiki/SHA-1#SHA-1的破解">维基百科-SHA-1的破解</a>)</li>
</ul>
<h2 id="哈希表"><a href="#哈希表" class="headerlink" title="哈希表"></a>哈希表</h2><h3 id="散列函数"><a href="#散列函数" class="headerlink" title="散列函数"></a>散列函数</h3><h4 id="直接定址法"><a href="#直接定址法" class="headerlink" title="直接定址法"></a>直接定址法</h4><p>$f(key) = key$</p>
<h4 id="数字分析法"><a href="#数字分析法" class="headerlink" title="数字分析法"></a>数字分析法</h4><p>具体分析数字特征。</p>
<p>关键字位数比较大的情况，例如存储员工登记表，如果用手机号作为关键字，那么我们发现抽取后面的四位数字作为散列地址是不错的选择。</p>
<p><img src="/images/hash-数字分析法.jpg" alt="hash-数字分析法" style="zoom:70%;" /></p>
<h4 id="平方取中法"><a href="#平方取中法" class="headerlink" title="平方取中法"></a>平方取中法</h4><p>平方取中法是将关键字平方之后取中间若干位数字作为散列地址。这种方法适用于不知道关键字的分布，且数值的位数又不是很大的情况。</p>
<h4 id="折叠法"><a href="#折叠法" class="headerlink" title="折叠法"></a>折叠法</h4><p>折叠法是将关键字从左到右分割成位数相等的几部分，然后将这几部分叠加求和，并按散列表表长取后几位作为散列地址。</p>
<h4 id="除留余数法"><a href="#除留余数法" class="headerlink" title="除留余数法"></a>除留余数法</h4><p>此方法为最常用的构造散列函数方法，对于散列表长为m的散列函数计算公式为： </p>
<p>$f(key) = key  \%  p$  其中$(p&lt;=m)$</p>
<h3 id="处理散列冲突的方法"><a href="#处理散列冲突的方法" class="headerlink" title="处理散列冲突的方法"></a>处理散列冲突的方法</h3><h4 id="开放定址法"><a href="#开放定址法" class="headerlink" title="开放定址法"></a>开放定址法</h4><p>一旦发生了冲突，就去寻找下一个空的散列地址，只要散列表足够大，空的散列地址总能找到，并将记录存入。它的公式是：</p>
<p>$fi(key) = (f(key)+di) MOD m $</p>
<p>$di$ 的选取有3中方法：</p>
<ol>
<li>递增：di=1,2,…,m-1</li>
<li>平方运算来尽量解决堆积问题：di=1²,-1²,2²,-2²…,q²,-q²,q&lt;=m</li>
<li>di随机</li>
</ol>
<h4 id="再散列函数法"><a href="#再散列函数法" class="headerlink" title="再散列函数法"></a>再散列函数法</h4><p>同时准备多个散列函数，当第一个散列函数发生冲突的时候可以用备选的散列函数进行计算。</p>
<h4 id="链地址法"><a href="#链地址法" class="headerlink" title="链地址法"></a>链地址法</h4><p>每个bucket位置存储一个链表。</p>
<h3 id="引用-2"><a href="#引用-2" class="headerlink" title="引用"></a>引用</h3><ul>
<li><a href="https://www.zhihu.com/question/26762707/answer/418939144">知乎-什么是 hash</a></li>
</ul>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>同步/异步IO与阻塞/非阻塞IO</title>
    <url>/2020/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E5%9B%9B%E7%A7%8Dio/</url>
    <content><![CDATA[<p>对于读/写 I/O 操作，流程分为两步（以读事件为例）：</p>
<ol>
<li><p>等待数据准备好。（例如从网络来的数据写入网卡、再复制进内核）。</p>
</li>
<li><p>数据从内核空间复制到用户空间。</p>
</li>
</ol>
<h1 id="阻塞与非阻塞-I-O"><a href="#阻塞与非阻塞-I-O" class="headerlink" title="阻塞与非阻塞 I/O"></a>阻塞与非阻塞 I/O</h1><p>阻  I/O 和非阻塞 I/O 是看在调用 <code>read()</code>接口时，在阶段 1 数据没有准备好时，处理的方法。</p>
<ul>
<li>阻塞 I/O 调用 read()` 接口在数据没有准备好时，会阻塞在这个系统调用出。（这个进程被放入了等待队列）。</li>
<li>非阻塞 I/O 调用 <code>read()</code> 接口在数据没有准备好时，会直接返回失败。</li>
</ul>
<p><img src="/images/阻塞io.jpg" alt="阻塞I/O"><br><img src="/images/非阻塞io.jpg" alt="非阻塞I/O"></p>
<h1 id="同步与异步-I-O"><a href="#同步与异步-I-O" class="headerlink" title="同步与异步 I/O"></a>同步与异步 I/O</h1><p>同步 I/O 和异步 I/O 是看在有数据可读时，是否要等待从内核拷贝到用户空间的过程。</p>
<ul>
<li>同步 I/O 在有数据可读时，会等待从内核拷贝到用户的过程。</li>
<li><p>异步 I/O 在有数据可读时，会继续做其他的，等内核已经拷贝数据到用户空间，再进行数据的处理。</p>
<p><img src="/images/异步io.jpg" alt="异步I/O"></p>
</li>
</ul>
<h1 id="Netty-使用的-NIO，为什么说自己是异步事件驱动的"><a href="#Netty-使用的-NIO，为什么说自己是异步事件驱动的" class="headerlink" title="Netty 使用的 NIO，为什么说自己是异步事件驱动的"></a>Netty 使用的 NIO，为什么说自己是异步事件驱动的</h1><p>Netty 的网络模型是<strong>同步非阻塞</strong>的，并不是 AIO 异步的网络模型。</p>
<p>Netty 说的异步，是伪异步。异步事件驱动框架体现在所有的 I/O 操作是异步的，所有的 I/O 调用会立即返回，并不保证调用成功与否，但是调用会返回 <code>ChannelFuture</code>，Netty 会通过<code>ChannelFuture</code> 通知你调用是成功了还是失败了亦或是取消了。</p>
<h1 id="为什么-Netty-使用-NIO-而不是-AIO"><a href="#为什么-Netty-使用-NIO-而不是-AIO" class="headerlink" title="为什么 Netty 使用 NIO 而不是 AIO"></a>为什么 Netty 使用 NIO 而不是 AIO</h1><blockquote>
<p><a href="https://www.jianshu.com/p/df1d6d8c3f9d">为什么Netty使用NIO而不是AIO？</a></p>
</blockquote>
<ul>
<li>Netty的目标主要是Linux系统，AIO在linux上是使用Epoll实现，没有很好实现AIO，因此在性能上没有明显的优势。</li>
<li>被JDK封装了一层不容易深度优化。</li>
<li>Netty整体架构是reactor模型, 而AIO是proactor模型, 混合在一起会非常混乱。</li>
<li>AIO还有个缺点是接收数据需要预先分配缓存, 而不是NIO那种需要接收时才需要分配缓存, 所以对<strong>连接数量非常大但流量小</strong>的情况, 内存浪费很多。</li>
</ul>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
        <tag>io</tag>
      </tags>
  </entry>
  <entry>
    <title>3. 网络层</title>
    <url>/2020/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/3%E7%BD%91%E7%BB%9C%E5%B1%82/</url>
    <content><![CDATA[<h1 id="IP协议首部的部分字段"><a href="#IP协议首部的部分字段" class="headerlink" title="IP协议首部的部分字段"></a>IP协议首部的部分字段</h1><ol>
<li>源地址</li>
<li>目标地址</li>
<li>标识：在数据报长度过长时，要进行分片，相同数据报的不同分片的标识相同。</li>
<li>片偏移：单位为8个字节，在需要分片时和标识一起使用。</li>
</ol>
<h1 id="IP地址"><a href="#IP地址" class="headerlink" title="IP地址"></a>IP地址</h1><p>IP地址共4个字节，</p>
<p>IP地址 = <code>&lt;网络号&gt; &lt;主机号&gt;</code>，分为：</p>
<ul>
<li>A类地址：0开头，网络号8位，主机号24位</li>
<li>B类地址：10开头，网络号16位，主机号16位</li>
<li>C类地址：110开头，网络号24位，主机号8位。</li>
<li>D类地址：1110开头，多播地址 #TODO。</li>
<li>E类地址：11110开头，保留，为以后使用。</li>
</ul>
<h1 id="子网划分"><a href="#子网划分" class="headerlink" title="子网划分"></a>子网划分</h1><p>​    从主机号中分出一部分用来表示子网号，需要和子网掩码配合使用。子网掩码为1的bit位说明是主机号或子网号。例如B类地址默认子网掩码为255.255.0.0。</p>
<h1 id="NAT"><a href="#NAT" class="headerlink" title="NAT"></a>NAT</h1><p>NAT网络地址转换协议，把内网请求的源IP地址替换为整个公网IP的地址。位置在网关出。</p>
<h1 id="NAPT"><a href="#NAPT" class="headerlink" title="NAPT"></a>NAPT</h1><p>NAPT网络地址端口转换协议，要把源端口也替换为一个没有在使用的端口。修改TCP的数据包？</p>
<p>使用NAPT（网络地址端口转换），内网客户端可以向外网服务发请求、并收到回复。</p>
<p>内网客户端发起请求后，到NAT网关，会把数据包中的IP替换成自己的ip、port替换成一个空闲的port。</p>
<p>NAT网关有一张映射表，存储了内网数据包的IP:PORT和替换后的IP:PORT的映射关系。这样收到回复时，NAT网关可以找到真正的客户端。</p>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
      </tags>
  </entry>
  <entry>
    <title>2. 传输层</title>
    <url>/2020/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/2%E4%BC%A0%E8%BE%93%E5%B1%82/</url>
    <content><![CDATA[<h1 id="UDP报文的最大长度"><a href="#UDP报文的最大长度" class="headerlink" title="UDP报文的最大长度"></a>UDP报文的最大长度</h1><p>根据IP协议的规范，IP包的长度不大于65535 bytes。所以，理论上UDP报文的最大字节数是 - IP头部（20 bytes） - UDP头部（8bytes）。</p>
<p>但是这么长的数据包已经超过了以太网和通信线路传输的上限，需要让IP模块对数据包进行分片后再传输。分片有时候会带来组包的错误，所以UDP的包长度不要超过MTU值（1500字节），一般不建议超过1K。</p>
<h1 id="基于TCP的应用层协议"><a href="#基于TCP的应用层协议" class="headerlink" title="基于TCP的应用层协议"></a>基于TCP的应用层协议</h1><ul>
<li>Telnet</li>
<li>HTTP</li>
<li>SMTP</li>
<li>POP3</li>
<li>FTP</li>
</ul>
<h1 id="基于UDP的应用层协议"><a href="#基于UDP的应用层协议" class="headerlink" title="基于UDP的应用层协议"></a>基于UDP的应用层协议</h1><ul>
<li>DNS</li>
</ul>
<h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><p>MSL是Maximum Segment Lifetime的英文缩写，可译为“最长报文段寿命”，它是任何报文在网络上存在的最长的最长时间，超过这个时间报文将被丢弃</p>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
      </tags>
  </entry>
  <entry>
    <title>1. 应用层</title>
    <url>/2020/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/1%E5%BA%94%E7%94%A8%E5%B1%82/</url>
    <content><![CDATA[<h1 id="5层网络模型"><a href="#5层网络模型" class="headerlink" title="5层网络模型"></a>5层网络模型</h1><ol>
<li>应用层</li>
<li>传输层</li>
<li>网络层</li>
<li>数据链路层</li>
<li>物理层</li>
</ol>
<h1 id="DNS"><a href="#DNS" class="headerlink" title="DNS"></a>DNS</h1><p>全称：Domain Name System。</p>
<p>作用：把主机域名转化为IP地址。</p>
<p>一般最多512字节（因为最开始的MTU比较小）</p>
<h2 id="DNS是"><a href="#DNS是" class="headerlink" title="DNS是"></a>DNS是</h2><ol>
<li><p>一个由分层的DNS服务器实现的分布式数据库。</p>
</li>
<li><p>一个使得主机能够查询分布式数据库的应用层协议。</p>
</li>
</ol>
<h2 id="为什么不是单点的"><a href="#为什么不是单点的" class="headerlink" title="为什么不是单点的"></a>为什么不是单点的</h2><ul>
<li>单点故障。</li>
<li>通信容量限制。</li>
<li>远距离的延迟。</li>
</ul>
<h2 id="三种DNS服务器"><a href="#三种DNS服务器" class="headerlink" title="三种DNS服务器"></a>三种DNS服务器</h2><ul>
<li>根DNS服务器。共13个，每个可能是冗余存储的。</li>
<li>顶级域服务器。org、com、edu等。</li>
<li>权威DNS服务器。在因特网上可访问的主机，要提供公共可访问的DNS记录。</li>
</ul>
<h2 id="递归和迭代"><a href="#递归和迭代" class="headerlink" title="递归和迭代"></a>递归和迭代</h2><ul>
<li><p>递归：DNS客户端把解析名称的请求发给DNS服务器后，DNS服务器会把找到的ip地址返回给客户端。如果DNS服务器在自己这里没有找到，会代替客户端去访问其他的DNS服务器。</p>
</li>
<li><p>迭代：DNS服务器没有找到后，客户端基于DNS服务器的应答，自己去和其他DNS服务器请求。</p>
</li>
</ul>
<h2 id="DNS解析的流程"><a href="#DNS解析的流程" class="headerlink" title="DNS解析的流程"></a>DNS解析的流程</h2><blockquote>
<p><a href="https://www.zhihu.com/question/23042131/answer/66571369">DNS解析的过程是什么</a></p>
</blockquote>
<ol>
<li><p>对于 <code>www.baidu.com</code>。DNS客户端首先检查本地hosts文件是否有对应的映射关系，有则返回对应的IP地址。</p>
</li>
<li><p>把解析请求发给本地DNS服务器（在本地操作系统TCP/IP的参数设置中）。如果要解析的域名包含在本地DNS服务器的配置资源中，则返回对应的IP。</p>
</li>
<li>本地DNS服务器把请求发往13根DNS服务器。根服务器返回对应的顶级DNS服务器.com的IP地址。</li>
<li>本地DNS服务器向顶级DNS服务器.com发请求。如果.com自己无法解析，它会去找管理.com的<strong>下一级域名</strong>地址（.baidu）的DNS服务器的地址，返回。</li>
<li>本地DNS服务器向.baidu DNS服务器发请求，重复步骤4，一直到找到为止。</li>
</ol>
<p>DNS客户端向本地DNS服务器发的请求是递归查询，DNS服务器之间是迭代查询。</p>
<p>如果是转发模式，在步骤2之后，本地DNS服务器会向上一级DNS服务器请求转发，一直向上转发，一直到可以解析为止。？如何知道上一级的DNS服务器的地址？TODO</p>
<h2 id="为什么DNS使用UDP"><a href="#为什么DNS使用UDP" class="headerlink" title="为什么DNS使用UDP"></a>为什么DNS使用UDP</h2><blockquote>
<p><a href="https://www.zhihu.com/question/310145373/answer/583869215">为什么DNS使用UDP而不是TCP</a> </p>
</blockquote>
<p>DNS需要传输的数据量少，使用UDP可以加快传输速度，并且迭代查询可能会查询多次。</p>
<p>现在的DNS为了携带证书验证，数据量变大，会使用TCP。</p>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
      </tags>
  </entry>
  <entry>
    <title>正向代理、反向代理、透明代理</title>
    <url>/2020/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://zhuanlan.zhihu.com/p/68560128">彻底理解正向代理、反向代理、透明代理</a></li>
<li><a href="https://foofish.net/proxy-and-reverse-proxy.html">正向代理与反向代理</a></li>
<li><a href="http://peachey.blog/2017/12/28/py-shadowsocks/#2-Shadowsocks-Socks-5工作原理">Shadowsocks原理详解</a></li>
<li><a href="https://www.cnblogs.com/yinzhengjie/p/7357860.html">HTTP协议和SOCKS5协议</a></li>
</ul>
<h1 id="正向代理"><a href="#正向代理" class="headerlink" title="正向代理"></a>正向代理</h1><p>正向代理代理的是客户端，客户端把<strong>所有</strong>的请求都交给代理服务器，由代理服务器代替客户端去请求。服务端并不知道真正的客户端是谁，<strong>对server透明</strong>。例如shadowsocks使用socks5协议。</p>
<h1 id="反向代理"><a href="#反向代理" class="headerlink" title="反向代理"></a>反向代理</h1><p>反向代理代理的是服务端，对于<strong>某一种</strong>服务，客户端向代理服务器发送请求，由代理服务器代替客户端去请求。客户端并不知道后边真正是谁服务的，<strong>对client透明</strong>。例如nginx来负载均衡和代理转发。</p>
<h1 id="透明代理"><a href="#透明代理" class="headerlink" title="透明代理"></a>透明代理</h1><p>透明代理的意思是客户端根本不需要知道有代理服务器的存在，它改编你的request fields（报文），并会传送真实IP。注意，加密的透明代理则是属于匿名代理，意思是不用设置使用代理了。</p>
<p>透明代理实践的例子就是时下很多公司使用的行为管理软件</p>
<h1 id="Socks5-协议"><a href="#Socks5-协议" class="headerlink" title="Socks5 协议"></a>Socks5 协议</h1><p>Shadowsocks的处理方式是将Socks 5客户端与Socks5服务器的连接提前，Socks5协议的交互完全是在本地进行的，在网络中传输的完全是利用加密算法加密后的密文，这就很好的进行了去特征化，使得传输的数据不是很容易的被特征识别，本人认为这是Shadowsocks的最大的创新了，具体的流程如下：</p>
<pre class="mermaid">graph LR
    subgraph client 
        Socks5客户端 --> Socks5
        Socks5 --> sslocal
    end
    subgraph server
        sslocal -->|密文| ssserver
        ssserver -->|正常请求| 目标主机
    end</pre>

<p>socks5本身只是个代理提供转发，不提供加密的功能，所以不提供安全性，安全性只能由客户端自己提供，比如如果客户端使用SSL, 那么该网络连接就有了安全性。？</p>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP 协议</title>
    <url>/2020/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/http/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://zhuanlan.zhihu.com/p/61423830">你猜一个 TCP 连接上面能发多少个 HTTP 请求</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/26559480">一文读懂 HTTP/2 特性</a></li>
</ul>
<h1 id="HTTP是无状态的-V-S-Connection-keep-alive"><a href="#HTTP是无状态的-V-S-Connection-keep-alive" class="headerlink" title="HTTP是无状态的 V.S. Connection: keep-alive"></a>HTTP是无状态的 V.S. Connection: keep-alive</h1><ul>
<li>无状态<ul>
<li>指协议对于事务的处理没有记忆的能力，服务器不知道客户端的状态。具体来说，这一次向服务端发送的请求，与同一个客户端上一次发送的请求没有任何联系。</li>
</ul>
</li>
<li>Connection: keep-alive<ul>
<li>HTTP的传输层协议是TCP。当一个HTTP请求处理结束后，服务端和客户端之间的TCP连接并不会马上关闭，当一段时间内客户端再次发起HTTP请求时，会使用同一个TCP连接进行通信。</li>
<li>keep-alive并不是代表TCP连接会永远存在，而是一定时间范围内的。 #TODO</li>
<li>HTTP/1.1出现的，默认是开启的。</li>
</ul>
</li>
</ul>
<h1 id="对同一个服务端的多个HTTP的发送情况"><a href="#对同一个服务端的多个HTTP的发送情况" class="headerlink" title="对同一个服务端的多个HTTP的发送情况"></a>对同一个服务端的多个HTTP的发送情况</h1><h2 id="使用一个TCP连接，可以同时发送多个HTTP请求吗？"><a href="#使用一个TCP连接，可以同时发送多个HTTP请求吗？" class="headerlink" title="使用一个TCP连接，可以同时发送多个HTTP请求吗？"></a>使用一个TCP连接，可以同时发送多个HTTP请求吗？</h2><h3 id="http-1-1-无法发送多个，使用多个TCP连接来解决"><a href="#http-1-1-无法发送多个，使用多个TCP连接来解决" class="headerlink" title="http/1.1 无法发送多个，使用多个TCP连接来解决"></a>http/1.1 无法发送多个，使用多个TCP连接来解决</h3><p>默认情况下，一个TCP连接，同一时刻只能处理一个HTTP请求，两个请求不能有重叠。</p>
<p>HTTP/1.1中规定了Pipelining，但是只存在于理想情况，实际比较复杂。</p>
<blockquote>
<p>一个支持持久连接的客户端可以在一个连接中发送多个请求（不需要等待任意请求的响应）。收到请求的服务器必须按照请求收到的顺序发送响应。</p>
</blockquote>
<p>流水线的缺点：</p>
<ol>
<li>一些代理服务器不能正确的处理 HTTP Pipelining。</li>
<li>“队头堵塞”（Head-of-line blocking）：如果第一个请求阻塞了，即使后边的请求处理好了，也会被阻塞住。</li>
</ol>
<p>因此，浏览器默认是关闭的，为了提高并发性能，对于一个服务端会建立多个TCP连接。不过，这个连接数目是有上限的，一般不超过6-8个。</p>
<h3 id="http-2-Multiplexing"><a href="#http-2-Multiplexing" class="headerlink" title="http/2 Multiplexing"></a>http/2 Multiplexing</h3><ul>
<li><p><strong>帧：</strong>HTTP/2 数据通信的最小单位消息：指 HTTP/2 中逻辑上的 HTTP 消息。例如请求和响应等，消息由一个或多个帧组成。</p>
</li>
<li><p><strong>流：</strong>存在于连接中的一个虚拟通道。流可以承载双向消息，每个流都有一个唯一的整数ID。</p>
</li>
</ul>
<p>HTTP/2 采用二进制格式传输数据，而非 HTTP 1.x 的文本格式，二进制协议解析起来更高效。 HTTP / 1 的请求和响应报文，都是由起始行，首部和实体正文（可选）组成，各部分之间以文本换行符分隔。HTTP/2 将请求和响应数据分割为更小的帧，并且它们采用二进制编码。</p>
<p><strong>HTTP/2 中，同域名下所有通信都在单个连接上完成，该连接可以承载任意数量的双向数据流。</strong>每个数据流都以消息的形式发送，而消息又由一个或多个帧组成。多个帧之间可以乱序发送，根据帧首部的<strong>流标识</strong>可以重新组装。</p>
<h1 id="HTTP-重定向"><a href="#HTTP-重定向" class="headerlink" title="HTTP 重定向"></a>HTTP 重定向</h1><blockquote>
<p><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Redirections">HTTP 的重定向</a></p>
</blockquote>
<h2 id="永久重定向"><a href="#永久重定向" class="headerlink" title="永久重定向"></a>永久重定向</h2><p>这种重定向操作是永久性的。它表示原 URL 不应再被使用，而应该优先选用新的 URL。搜索引擎机器人会在遇到该状态码时触发更新操作，在其索引库中修改与该资源相关的 URL 。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th><strong>编码</strong></th>
<th><strong>含义</strong></th>
<th><strong>处理方法</strong></th>
<th><strong>典型应用场景</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td><code>301</code></td>
<td>Moved Permanently</td>
<td><code>GET</code> 方法不会发生变更，其他方法有可能会变更为 <code>GET</code> 方法。[1]</td>
<td>网站重构。</td>
</tr>
<tr>
<td><code>308</code></td>
<td>Permanent Redirect</td>
<td>方法和消息主体都不发生变化。</td>
<td>网站重构，用于非GET方法。(with non-GET links/operations)</td>
</tr>
</tbody>
</table>
</div>
<p>[1] 该规范无意使方法发生改变，但在实际应用中用户代理会这么做。 308 状态码被创建用来消除在使用非 GET 方法时的歧义行为。</p>
<h2 id="临时重定向"><a href="#临时重定向" class="headerlink" title="临时重定向"></a>临时重定向</h2><p>有时候请求的资源无法从其标准地址访问，但是却可以从另外的地方访问。在这种情况下可以使用临时重定向。搜索引擎不会记录该新的、临时的链接。在创建、更新或者删除资源的时候，临时重定向也可以用于显示临时性的进度页面。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th><strong>编码</strong></th>
<th><strong>含义</strong></th>
<th><strong>处理方法</strong></th>
<th><strong>典型应用场景</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td><code>302</code></td>
<td><code>Found</code></td>
<td><code>GET</code> 方法不会发生变更，其他方法有可能会变更为 GET 方法。[2]</td>
<td>由于不可预见的原因该页面暂不可用。在这种情况下，搜索引擎不会更新它们的链接。</td>
</tr>
<tr>
<td><code>303</code></td>
<td><code>See Other</code></td>
<td><code>GET</code> 方法不会发生变更，其他方法会<strong>变更</strong>为 GET 方法（消息主体会丢失）。</td>
<td>用于 <code>PUT</code> 或 <code>POST</code> 请求完成之后进行页面跳转来防止由于页面刷新导致的操作的重复触发。</td>
</tr>
<tr>
<td><code>307</code></td>
<td><code>Temporary Redirect</code></td>
<td>方法和消息主体都不发生变化。</td>
<td>由于不可预见的原因该页面暂不可用。在这种情况下，搜索引擎不会更新它们的链接。当站点支持非 GET 方法的链接或操作的时候，该状态码优于 302 状态码。</td>
</tr>
</tbody>
</table>
</div>
<p>[2] 该规范无意使方法发生改变，但在实际应用中用户代理会这么做。 307 状态码被创建用来消除在使用非 GET 方法时的歧义行为。</p>
<h1 id="页面跳转方式"><a href="#页面跳转方式" class="headerlink" title="页面跳转方式"></a>页面跳转方式</h1><blockquote>
<p><a href="https://blog.csdn.net/Radic_Feng/article/details/6798378">实现页面重定向（跳转）的4种方式</a></p>
</blockquote>
<ol>
<li>后端跳转。返回30X状态码，Response的Header中的Location为要跳转的路径。因为body是空的，页面不会闪烁。</li>
<li>前端JavaScript跳转。在JavaScript中使用 window.location 跳转。Body不是空的，页面会闪烁。</li>
<li>前端HTML跳转。在Header中使用 <code>&lt;meta http-equiv=&quot;refresh&quot; content=&quot;0;url=&lt;c:url value=&quot;/authenticate.html&quot;/&gt;&quot; /&gt;</code> 跳转。Body不是空的，页面会闪烁。</li>
</ol>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
      </tags>
  </entry>
  <entry>
    <title>TCP 协议</title>
    <url>/2020/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/tcp/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://zhuanlan.zhihu.com/p/47074758">TCP短连接和长连接的区别</a></li>
<li><a href="http://hengyunabc.github.io/why-we-need-heartbeat/">为什么基于TCP的应用需要心跳包（TCP keep-alive原理分析）</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/28894266">聊聊 TCP 中的 KeepAlive 机制</a></li>
<li><a href="https://blog.csdn.net/bjrxyz/article/details/71076442">TCP新手误区—心跳的意义</a></li>
</ul>
<h1 id="长连接、短连接"><a href="#长连接、短连接" class="headerlink" title="长连接、短连接"></a>长连接、短连接</h1><p>在TCP连接建立后，发送 一次数据后，就关闭连接。一般是由client关闭的。</p>
<p>建立连接后，双方不主动关闭连接。要使用keepalive来保活，活着使用应用层的心跳机制。</p>
<h2 id="长连接的优点"><a href="#长连接的优点" class="headerlink" title="长连接的优点"></a>长连接的优点</h2><ol>
<li><p>长连接可以省去较多的TCP建立和关闭的操作，减少网络阻塞的影响，</p>
</li>
<li><p>当发生错误时，可以在不关闭连接的情况下进行提示，</p>
</li>
<li><p>减少CPU及内存的使用，因为不需要经常的建立及关闭连接。</p>
</li>
</ol>
<h2 id="长连接的缺点"><a href="#长连接的缺点" class="headerlink" title="长连接的缺点"></a>长连接的缺点</h2><ol>
<li>连接数过多时，影响服务端的性能和并发数量。</li>
<li>这时候server端需要采取一些策略，<ul>
<li>关闭一些长时间没有读写事件发生的连接，这样可以避免一些恶意连接导致server端服务受损。</li>
<li>以客户端机器为颗粒度，限制每个客户端的最大长连接数。</li>
</ul>
</li>
</ol>
<h1 id="keepalive"><a href="#keepalive" class="headerlink" title="keepalive"></a>keepalive</h1><p>KeepAlive并不是TCP协议规范的一部分，但在几乎所有的TCP/IP协议栈（不管是Linux还是Windows）中，都实现了KeepAlive功能。</p>
<p>KeepAlive机制默认是关闭的，需要将setsockopt将SOL_SOCKET.SO_KEEPALIVE设置为1才是打开KeepAlive状态。</p>
<p>流程：</p>
<ol>
<li><p>一端在一定时间（<code>tcp_keepalive_time</code>）内没有收到另外一端的请求后，会发出数据长度为0的心跳包。</p>
</li>
<li><p>如果没有收到回复ack，那么隔一段时间（<code>tcp_keepalive_intvl</code>）后重试。</p>
</li>
<li><p>如果重试<code>tcp_keepalive_probes</code>次后，还是没有收到ack。则主动断开连接。</p>
</li>
</ol>
<p>默认值：</p>
<ul>
<li><code>tcp_keepalive_time</code>：2h</li>
<li><code>tcp_keepalive_intvl</code>：75s</li>
<li><code>tcp_keepalive_probes</code>：9次</li>
</ul>
<p>设置keepalive的值</p>
<ul>
<li>注意，这三个值是在操作系统里进行设置的，无法对某一个应用指定。</li>
</ul>
<p>双向keepalive</p>
<ul>
<li>哪一端想要探测对方端是否挂了，就需要开启keepalive。</li>
</ul>
<p>和应用层自己实现的心跳的对比：</p>
<p>缺点</p>
<ol>
<li>它不是 TCP 的标准协议, 并且是默认关闭的，且经过路由等中转设备 keepalive 包可能会被丢弃。</li>
<li>协议分层，各层关注点不同。<ul>
<li>传输层关注是否“通”。</li>
<li>应用层关注是否可服务。</li>
</ul>
</li>
<li>TCP的keepalive的数据长度为0，无法自定义；上层的心跳包可以自定义，携带更多的数据。</li>
<li>TCP 层的 keepalive 时间太长。<ul>
<li>默认 &gt; 2 小时，虽然可改，但属于系统参数，改动影响所有应用。</li>
</ul>
</li>
</ol>
<p>缺点</p>
<ul>
<li>TCP的keepalive的数据包更小（数据长度为0）。</li>
</ul>
<p>与HTTP中Connection: keep-alive的对比：</p>
<ol>
<li>HTTP中Connection: keep-alive是说要复用这个TCP连接。</li>
<li>TCP中的keepalive是用探测机制看对方是否还活着。</li>
</ol>
<h1 id="三次握手与四次挥手"><a href="#三次握手与四次挥手" class="headerlink" title="三次握手与四次挥手"></a>三次握手与四次挥手</h1><blockquote>
<p><a href="https://cyc2018.github.io/CS-Notes/#/notes/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%20-%20%E4%BC%A0%E8%BE%93%E5%B1%82?id=tcp-%e7%9a%84%e4%b8%89%e6%ac%a1%e6%8f%a1%e6%89%8b">计算机网络 - 传输层</a></p>
</blockquote>
<p>四次挥手中，第三次通信 B 向 A 发送关闭确认后，为什么 A 要 向 B 进行第四次通信？</p>
<ul>
<li>B 要确保 A 关闭了才可以。B 如果收不到 A 的第四次通信，会进行重试。</li>
</ul>
<h1 id="滑动窗口"><a href="#滑动窗口" class="headerlink" title="滑动窗口"></a>滑动窗口</h1><blockquote>
<ul>
<li><p><a href="http://mrpeak.cn/blog/tcp-flow-control00/">TCP/IP 系列之 TCP 流控与拥塞控制</a></p>
</li>
<li><p><a href="https://cyc2018.github.io/CS-Notes/#/notes/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%20-%20%E4%BC%A0%E8%BE%93%E5%B1%82?id=tcp-%e6%bb%91%e5%8a%a8%e7%aa%97%e5%8f%a3">计算机网络 - 传输层</a></p>
</li>
</ul>
</blockquote>
<h1 id="流量控制"><a href="#流量控制" class="headerlink" title="流量控制"></a>流量控制</h1><p>流量控制是为了控制发送方发送速率，保证接收方来得及接收</p>
<p>发送窗口大小。</p>
<h1 id="拥塞避免"><a href="#拥塞避免" class="headerlink" title="拥塞避免"></a>拥塞避免</h1><ul>
<li><p>拥塞窗口</p>
</li>
<li><p>慢开始与拥塞避免</p>
</li>
<li><p>快重传与快恢复</p>
</li>
</ul>
<h1 id="UDP最大长度"><a href="#UDP最大长度" class="headerlink" title="UDP最大长度"></a>UDP最大长度</h1><p>最好不超过链路层的MTU(最大传输单元) 1500字节</p>
]]></content>
      <categories>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
      </tags>
  </entry>
  <entry>
    <title>最大堆</title>
    <url>/2020/algorithm/%E5%A0%86/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://blog.csdn.net/LeoSha/article/details/46116959">为什么建堆的时间复杂度是O(n)？</a></li>
<li><a href="https://www.jianshu.com/p/21bef3fc3030">最大堆（创建、删除、插入和堆排序）</a></li>
</ul>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>最大堆为完全二叉树。对于每一个子树，它的根节点的值比两个孩子都大。</p>
<p>用数组 <code>nums</code> 实现，<code>nums[1]</code> ~ <code>nums[length]</code> 存储值。</p>
<h1 id="插入节点"><a href="#插入节点" class="headerlink" title="插入节点"></a>插入节点</h1><ol>
<li><p>插入<strong>最后一个位置</strong> $length + 1$。</p>
<p>因为插入后，最后一个节点所在的子树不符合最大堆的性质，所以进行如下的上浮调整。</p>
</li>
<li><p><strong>上浮调整</strong>。把当前节点的值和父节点比较。</p>
<ul>
<li><p>如果大于父节点的值，则插入完成，退出。</p>
</li>
<li><p>如果小于父节点的值，则把当前节点和父节点进行调换，调换后的父节点作为当前节点，重复步骤 2。</p>
</li>
</ul>
</li>
</ol>
<h1 id="删除节点"><a href="#删除节点" class="headerlink" title="删除节点"></a>删除节点</h1><ol>
<li><p>首先把<strong>根节点</strong>  <code>nums[1]</code> 与最后一个节点 <code>nums[length]</code> 交换位置。</p>
<p>相当于删除了根节点，为了删除后还是一个完全二叉树，所以只能把左后一个节点移动到头部。</p>
</li>
<li><p>进行<strong>下沉调整</strong>。比较当前节点和两个子节点中值更大的节点，</p>
<ul>
<li>如果当前节点大，则删除完成，退出。</li>
<li>如果当前节点小，则把当前节点和这个子节点调换，调换完成后的子节点作为当前节点，重复步骤(2)。</li>
</ul>
</li>
</ol>
<h1 id="创建最大堆"><a href="#创建最大堆" class="headerlink" title="创建最大堆"></a>创建最大堆</h1><ol>
<li><p>将所有数字按顺序直接填到 nums 中。</p>
</li>
<li><p>从<strong>最后一个非叶子</strong>节点开始调整，每次<strong>向下</strong>调整节点对应的子树。</p>
<p>因为最后叶子节点下标为 n、它的父节点是最后一个非叶子节点，所以最后一个非叶子节点的下标为n / 2。</p>
</li>
</ol>
<p>时间复杂度：$O(n)$。</p>
<h2 id="证明过程"><a href="#证明过程" class="headerlink" title="证明过程"></a>证明过程</h2><ol>
<li>具有$n$个元素的平衡二叉树，树高为$log(n)$，我们设这个变量为$h$。</li>
<li>最下层非叶节点的元素，只需做一次线性运算便可以确定大根，而这一层具有$2^{h-1}$个元素，我们假定$O(1)=1$，那么这一层元素所需时间为$2^{h-1}*1$。</li>
<li>由于是bottom-top建立堆，因此在调整上层元素的时候，并不需要同下层所有元素做比较，只<strong>需要同其中之一分支作比较</strong>，而作比较次数则是树的高度减去当前节点的高度。因此，第$x$层元素的计算量为$2^x*(h-x)$。</li>
<li>又以上通项公式可得知，构造树高为h的二叉堆的精确时间复杂度为：<br><script type="math/tex">S = 2^{h-1}*1 + 2^{h-2}*2 + …… +1* (h-1)</script> ①</li>
</ol>
<p>通过观察第四步得出的公式可知，该求和公式为等差数列和等比数列的乘积，因此用错位想减发求解，给公式左右两侧同时乘以2，可知： </p>
<p><script type="math/tex">2S = 2^h*1 + 2^{h-1}*2+ …… +2 × (h-1)</script> ②</p>
<p>用②减去①可知： $S =2^h * 1 - h +1$ ③</p>
<p>将$h = log(n)$带入③，得出如下结论：</p>
<p>$S = n - log(n) +1 = O(n)$</p>
<p>结论：构造二叉堆的时间复杂度为线性得证。</p>
]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>进程简介</title>
    <url>/2020/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%BF%9B%E7%A8%8B%E7%AE%80%E4%BB%8B/</url>
    <content><![CDATA[<h1 id="进程间通信"><a href="#进程间通信" class="headerlink" title="进程间通信"></a>进程间通信</h1><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://zhuanlan.zhihu.com/p/82840452">进程间通信的7种方式你都了解吗-知乎</a></li>
</ul>
<h2 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h2><p>每个进程各自有不同的用户地址空间，任何一个进程的全局变量在另一个进程中都看不到。</p>
<p>所以进程之间要交换数据必须通过内核，在内核中开辟一块缓冲区，进程 A 把数据从用户空间拷到内核缓冲区，进程 B 再从内核缓冲区把数据读走，内核提供的这种机制称为<strong>进程间通信</strong>。</p>
<h2 id="七种方式"><a href="#七种方式" class="headerlink" title="七种方式"></a>七种方式</h2><ol>
<li>PIPE：匿名管道。只能在父子进程、或兄弟进程间。实现原理，在内核中开辟了一个区域，作为文件。只能一个方向写，一个方向读。</li>
<li>FIFO：命名管道。开辟的内存区域所表示的文件加了路径名，所以所有进程都可以访问。管道是特殊类型的文件，在满足先入先出的原则条件下可以进行读写，但不能进行定位读写</li>
<li>共享内存。</li>
<li>信号。一个进程可以在任何时候发送一个信号给其他进程。</li>
<li>消息(Message)队列。消息队列可以实现消息的随机查询,消息不一定要以先进先出的次序读取,也可以按消息的类型读取.比FIFO更有优势。</li>
<li>信号量(semaphore)。信号量是一个计数器，用于多进程对共享数据的访问，信号量的意图在于进程间同步</li>
<li>socket。</li>
</ol>
<h2 id="信号"><a href="#信号" class="headerlink" title="信号"></a>信号</h2><p>进程可以不管对方进程的状态而发送信号。</p>
<h3 id="信号的状态"><a href="#信号的状态" class="headerlink" title="信号的状态"></a>信号的状态</h3><ul>
<li>产生</li>
<li>被传递</li>
</ul>
<h3 id="对信号的操作"><a href="#对信号的操作" class="headerlink" title="对信号的操作"></a>对信号的操作</h3><h4 id="阻塞信号"><a href="#阻塞信号" class="headerlink" title="阻塞信号"></a>阻塞信号</h4><p>某一种信号可以被对方进程阻塞（发送但是未传递），一直到对方接触这个信号的阻塞才能传递。</p>
<p>同一时刻，一个信号只能阻塞一个，其他同种信号会被丢弃。</p>
<h4 id="屏蔽信号"><a href="#屏蔽信号" class="headerlink" title="屏蔽信号"></a>屏蔽信号</h4><p>被屏蔽的某一种信号，会被看做传递了，即消失了。</p>
<h4 id="取消信号"><a href="#取消信号" class="headerlink" title="取消信号"></a>取消信号</h4><p>如果对方进程停止运行了，则取消信号。</p>
<h4 id="SIGKILL-和-SIGSTOP-信号"><a href="#SIGKILL-和-SIGSTOP-信号" class="headerlink" title="SIGKILL 和 SIGSTOP 信号"></a>SIGKILL 和 SIGSTOP 信号</h4><p>SIGKILL 和 SIGSTOP 信号不可被屏蔽、阻塞、忽略，必须执行缺省操作。特权用户使用他们来终止进程/暂停进程（可以用信号SIGCONT恢复）。</p>
<h3 id="内核与信号"><a href="#内核与信号" class="headerlink" title="内核与信号"></a>内核与信号</h3><ul>
<li>信号处理程序是在<strong>用户态</strong>的，可以重入。</li>
<li>信号产生后，如果对方进程没有在运行状态，这个信号要由<strong>内核保存</strong>，直到进程恢复执行。因此，每个进程在从<strong>内核态进入用户态</strong>时，都要<strong>检查</strong>是否有信号要处理。</li>
</ul>
<h1 id="进程的内存分布"><a href="#进程的内存分布" class="headerlink" title="进程的内存分布"></a>进程的内存分布</h1><h2 id="堆栈段"><a href="#堆栈段" class="headerlink" title="堆栈段"></a>堆栈段</h2><ul>
<li>堆<ul>
<li>存放进程运行中被动态分配的内存段。</li>
<li>大小并不固定，可动态扩张或缩减。</li>
<li>需要手动申请、释放内存。</li>
</ul>
</li>
<li>栈 <ul>
<li>存放程序的局部变量、函数参数、函数返回值。</li>
<li>大小固定。</li>
<li>由于栈的先进先出特点，所以栈特别方便用来保存/恢复调用现场。从这个意义上讲，我们可以把堆栈看成一个寄存、交换临时数据的内存区。</li>
</ul>
</li>
</ul>
<h2 id="数据段"><a href="#数据段" class="headerlink" title="数据段"></a>数据段</h2><p>数据段（Data Segment）存放程序中<strong>已初始化</strong>的<strong>全局变量</strong>的一块内存区域。</p>
<p>数据段属于静态内存分配。</p>
<h2 id="BSS段"><a href="#BSS段" class="headerlink" title="BSS段"></a>BSS段</h2><p>BSS段（Block Started by Symbol Segment）存放程序中未初始化的全局变量的一块内存区域。</p>
<p>BSS段属于静态内存分配。</p>
<h2 id="代码段"><a href="#代码段" class="headerlink" title="代码段"></a>代码段</h2><p>代码段（Code Segment/Text Segment）。程序段为程序代码在内存中的映射。</p>
<p>一个程序可以在内存中多有个副本。</p>
<p>特点：</p>
<ul>
<li><p>存放程序执行代码 的一块内存区域。</p>
</li>
<li><p>也有可能包含一些只读的常数变量，例如字符串常量等。</p>
</li>
<li><p>只读</p>
<ul>
<li>区域的大小在程序运行前就已经确定，并且内存区域通常属于只读 。</li>
<li>某些架构也允许代码段为可写，即允许修改程序。</li>
</ul>
</li>
</ul>
<h1 id="进程和线程"><a href="#进程和线程" class="headerlink" title="进程和线程"></a>进程和线程</h1><h2 id="引用-1"><a href="#引用-1" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://zhuanlan.zhihu.com/p/60248566">进程、线程、以及它们之间的区别与联系</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/46410285">谈谈进程和线程的区别</a></li>
</ul>
<h2 id="根本区别"><a href="#根本区别" class="headerlink" title="根本区别"></a>根本区别</h2><ul>
<li>进程是操作系统资源分配的基本单位。</li>
<li>线程是任务调度和执行的基本单位。</li>
</ul>
<h2 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h2><ul>
<li>进程都有独立的代码和数据空间（程序上下文）。</li>
<li>进程之间的切换会有较大的开销</li>
<li>线程可以看做轻量级的进程，同一类线程<strong>共享代码和数据空间</strong>，每个线程都有自己独立的运行栈和程序计数器（PC）。</li>
<li>线程之间切换的开销小。</li>
</ul>
<h1 id="进程的状态"><a href="#进程的状态" class="headerlink" title="进程的状态"></a>进程的状态</h1><h2 id="引用-2"><a href="#引用-2" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://zhuanlan.zhihu.com/p/96098130">进程状态与僵尸进程、孤儿进程</a> </li>
</ul>
<h2 id="状态种类"><a href="#状态种类" class="headerlink" title="状态种类"></a>状态种类</h2><h3 id="概念上进程的状态"><a href="#概念上进程的状态" class="headerlink" title="概念上进程的状态"></a>概念上进程的状态</h3><ul>
<li>创建</li>
<li>就绪</li>
<li>运行</li>
<li>阻塞</li>
<li>终止</li>
</ul>
<h3 id="Linux-中进程的状态"><a href="#Linux-中进程的状态" class="headerlink" title="Linux 中进程的状态"></a>Linux 中进程的状态</h3><p>Linux中，进程的状态：</p>
<ul>
<li>运行</li>
<li>睡眠（可中断睡眠）</li>
<li>磁盘休眠（不可中断睡眠，等待IO技术）</li>
<li>停止状态，SIGSTOP</li>
<li>死亡状态</li>
<li>僵死状态</li>
</ul>
<h2 id="僵死进程"><a href="#僵死进程" class="headerlink" title="僵死进程"></a>僵死进程</h2><ul>
<li><p>在父进程调用 <code>fork</code> 后，产生子进程。</p>
</li>
<li><p>子进程在结束运行后，子进程的退出状态（返回值）会回报给操作系统，系统以 <code>SIGCHLD</code> 信号将子进程被结束的<strong>事件告知</strong>父进程，此时子进程的进程控制块 PCB 仍驻留在内存中。</p>
<ul>
<li>收到 <code>SIGCHLD</code> 后，父进程会使用 <code>wait()</code>系统调用以获取子进程的退出状态，内核就可以从内存中释放已结束的子进程的 PCB。</li>
<li>如果父进程没有这么做，子进程的 PCB 就会一直驻留在内存中，也即成为僵尸进程。</li>
</ul>
</li>
</ul>
<p>简单来说，当进程退出但是父进程并没有调用 <code>wait()</code> 或 <code>waitpid()</code> 获取子进程的状态信息时就会产生僵尸进程。</p>
<h2 id="孤儿进程"><a href="#孤儿进程" class="headerlink" title="孤儿进程"></a>孤儿进程</h2><p>当一个父进程退出，而它的一个或多个子进程还在运行，那么那些子进程将成为孤儿进程。</p>
<p>孤儿进程将被 init 进程（进程号为1）所收养，并由 init 进程对它们完成状态收集工作。</p>
<h1 id="软中断"><a href="#软中断" class="headerlink" title="软中断"></a>软中断</h1><blockquote>
<ul>
<li><a href="https://zhuanlan.zhihu.com/p/80371745">Linux的中断处理机制(四) </a></li>
</ul>
</blockquote>
<p>中断的上半段不能被延后执行，而下半段可以。</p>
<p>下半段被称为软中断。</p>
<h1 id="fork-vfork-clone"><a href="#fork-vfork-clone" class="headerlink" title="fork/vfork/clone"></a>fork/vfork/clone</h1><ul>
<li><code>fork</code> 使用 <code>clone</code> 实现。<ul>
<li>传入的 <code>flags</code> 是 SIGCHILD 信号、所有清0的 clone标志位。</li>
<li>写时复制机制。父子进程暂时共享一个用户态堆栈，只要父子进程有一个试图去改变栈，立即得到用户态堆栈的一份拷贝。</li>
</ul>
</li>
<li><code>vfork</code> 使用 <code>clone</code> 实现。<ul>
<li>传入 SIGCHILD 信号、CLONE_VM、CLONE_VFORK。</li>
<li>创建的进程能共享父进程的内存地址空间，因此需要阻塞父进程的执行，直到子进程调用 <code>exec()</code> 或 <code>exit()</code> 。</li>
</ul>
</li>
</ul>
<h2 id="exec"><a href="#exec" class="headerlink" title="exec()"></a>exec()</h2><p> exec函数族提供了一种在进程中<strong>启动另一个程序</strong>执行的方法。</p>
<p>可以根据指定的文件名、或目录名找到可执行文件，并用它来取代原调用进程的数据段、代码段、堆栈段。</p>
<p>在执行完之后，原调用进程的内容除了 PID 外，其他全部都被替换了。</p>
<h1 id="会话与进程组"><a href="#会话与进程组" class="headerlink" title="会话与进程组"></a>会话与进程组</h1><blockquote>
<ul>
<li><a href="https://yq.aliyun.com/articles/8874">linux会话浅析</a></li>
<li><a href="https://www.cnblogs.com/klb561/p/12051027.html"><a href="https://www.cnblogs.com/klb561/p/12051027.html">linux下的SIGHUP系统信号</a></a></li>
</ul>
</blockquote>
<h2 id="进程组"><a href="#进程组" class="headerlink" title="进程组"></a>进程组</h2><p>线程组是一组进程的集合，可以一次性送信号给进程组的所有进程。</p>
<p>一个进程组的进程一般都是由一个进程创建出来的，它是 leader 进程。进程组的 PGID 是这个 leader 进程的 PID。</p>
<p>可以使用 <code>setpgid(pid, pgid)</code> 来设置进程的进程组。</p>
<h2 id="会话-session"><a href="#会话-session" class="headerlink" title="会话 session"></a>会话 session</h2><p>会话是一组线程组的集合。</p>
<p>一个会话最多有一个控制终端（可以没有）。</p>
<p>用来建立起控制终端连接的进程是会话的 leader 进程，会话的 SID 是这个 leader 进程的 PID。</p>
<p>前台和后台进程组</p>
<ul>
<li>一个会话只有一个<strong>前台</strong>进程组，可以和终端交互。</li>
<li>一个会话可以有多少<strong>后台</strong>进程组，是通过加入 <code>&amp;</code> 参数来实现的。<ul>
<li><code>&amp;</code> 会忽略 SIGINT 信号（来自键盘的中断）。</li>
</ul>
</li>
</ul>
<p>可以使用 <code>setsid(pid, sid)</code> 来设置进程的会话，但是不能是会话的 leader 继承。</p>
<h2 id="终端关闭"><a href="#终端关闭" class="headerlink" title="终端关闭"></a>终端关闭</h2><ol>
<li>终端关闭时，<code>SIGHUP</code> 信号被发送到会话的 leader 首进程、以及后台进程。</li>
<li>会话 leader 进程退出时，<code>SIGHUP</code> 该信号被发送到前台进程组中的每一个进程。</li>
</ol>
<h2 id="nohup"><a href="#nohup" class="headerlink" title="nohup"></a>nohup</h2><p>使用 <code>nohup</code></p>
<ul>
<li><p>可以使进程忽略 SIGHUP 信号。</p>
</li>
<li><p>还会将进程的标准输入重定向为 <code>/dev/null</code>、输出重定向到 <code>nohup.out</code> 文件。</p>
</li>
</ul>
<p>使用 <code>nohup &amp;</code>。</p>
<ul>
<li>可以使进程在后台运行, 忽略 <code>SIGINT</code>、<code>SIGHUP</code> 信号，输出到 nohup.out，关闭会话进程不结束。</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>进程</tag>
      </tags>
  </entry>
  <entry>
    <title>Netty 中的零拷贝</title>
    <url>/2020/netty/Netty%E4%B8%AD%E7%9A%84%E9%9B%B6%E6%8B%B7%E8%B4%9D/</url>
    <content><![CDATA[<p>零拷贝指在整个数据传输过程中，消除了一部分操作的拷贝代价。</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.cnblogs.com/binarylei/p/10117437.html">Netty 零拷贝（三）Netty 对零拷贝的改进</a></li>
<li><a href="https://cloud.tencent.com/developer/article/1402669">Netty 之 FileRegion 文件传输</a></li>
</ul>
<h1 id="零拷贝的操作"><a href="#零拷贝的操作" class="headerlink" title="零拷贝的操作"></a>零拷贝的操作</h1><ol>
<li><p>堆外内存。</p>
<p>Netty 接收和发送 <code>ByteBuffer</code> 使用 <code>DirectByteBuffer</code>。消除了从堆外内存拷贝到堆内内存。</p>
</li>
<li><p>使用 JDK 零拷贝。</p>
<p>通过 <code>FileRegion</code> 包装的 <code>FileChannel#transferTo()</code> 方法实现文件传输。消除了内核与用户空间的拷贝。</p>
</li>
<li><p>使用逻辑逻辑组合。</p>
<p><code>CompositeByteBuf</code> 类，将多个 <code>ByteBuf</code> 在逻辑上合并为一个 <code>ByteBuf</code>。消除了合并时的拷贝。</p>
</li>
<li><p>使用包装。</p>
<ul>
<li><p>通过 <code>wrap</code> 操作，将 <code>byte[]</code>、<code>ByteBuf</code>、<code>ByteBuffer</code> 等包装成一个 <code>ByteBuf</code>，避免了拷贝操作。</p>
</li>
<li><p>通过 <code>slice</code> 操作，获得 <code>ByteBuf</code> 逻辑上的切片。</p>
</li>
</ul>
</li>
</ol>
<h1 id="FileRegion"><a href="#FileRegion" class="headerlink" title="FileRegion"></a>FileRegion</h1><p>在将文件写入 <code>channel</code> 时，可以使用 <code>FileRegion</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileServerHandler</span> <span class="keyword">extends</span> <span class="title">SimpleChannelInboundHandler</span>&lt;<span class="title">String</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead0</span><span class="params">(ChannelHandlerContext ctx, String msg)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        RandomAccessFile raf = <span class="keyword">new</span> RandomAccessFile(msg, <span class="string">"r"</span>);</span><br><span class="line">     		ctx.write(<span class="keyword">new</span> DefaultFileRegion(raf.getChannel(), <span class="number">0</span>, length));</span><br><span class="line">        ctx.writeAndFlush(<span class="string">"\n"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>查看 <code>DefaultFileRegion</code> 源码，可以看到写入操作是使用 <code>FileChannel#transferTo()</code> （见 <a href="/2020/java/io/内存映射文件/" title="FileChannel 与内存映射文件">FileChannel 与内存映射文件</a>）实现的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DefaultFileRegion</span> <span class="keyword">extends</span> <span class="title">AbstractReferenceCounted</span> <span class="keyword">implements</span> <span class="title">FileRegion</span> </span>&#123;</span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">long</span> <span class="title">transferTo</span><span class="params">(WritableByteChannel target, <span class="keyword">long</span> position)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="comment">// 省略了好多代码。</span></span><br><span class="line">    <span class="comment">// Call open to make sure fc is initialized. This is a no-oop if we called it before.</span></span><br><span class="line">    open();</span><br><span class="line">    <span class="comment">// call FileChannel#transferTo。</span></span><br><span class="line">    <span class="keyword">long</span> written = file.transferTo(<span class="keyword">this</span>.position + position, count, target);</span><br><span class="line">    <span class="keyword">if</span> (written &gt; <span class="number">0</span>) &#123;</span><br><span class="line">      transferred += written;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> written;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>netty</category>
      </categories>
      <tags>
        <tag>netty</tag>
      </tags>
  </entry>
  <entry>
    <title>3. 类加载机制</title>
    <url>/2020/java/jvm/3%E7%B1%BB%E5%8A%A0%E8%BD%BD%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li>《深入理解 Java 虚拟机-第三版》</li>
</ul>
<h1 id="类加载时机"><a href="#类加载时机" class="headerlink" title="类加载时机"></a>类加载时机</h1><h2 id="类的生命周期"><a href="#类的生命周期" class="headerlink" title="类的生命周期"></a>类的生命周期</h2><p>类的生命周期如下图。</p>
<p><img src="/images/类的生命周期.png" alt="类的生命周期"></p>
<p>加载、验证、准备、初始化、卸载 这五个阶段的顺序是确定的。</p>
<p>解析阶段，在某些情况下可以在初始化阶段之后再开始，来支持 Java 运行时绑定特性。</p>
<h2 id="何时进行"><a href="#何时进行" class="headerlink" title="何时进行"></a>何时进行</h2><h3 id="类初始化时机"><a href="#类初始化时机" class="headerlink" title="类初始化时机"></a>类初始化时机</h3><p>对于第一步“加载”，《Java 虚拟机规范》没有强制约束。</p>
<p>对于“初始化阶段”，《Java 虚拟机规范》规定有且只有如下六种情况必须立即进行类的“初始化”：</p>
<ol>
<li>当虚拟机启动时，用户需要指定一个要执行的主类（包含 <code>main()</code> 方法的那个类），虚拟机会先初始化这个主类。</li>
<li>遇到 <code>new</code>、<code>getstatic</code>、<code>putstatic</code>、<code>invokestatic</code> 字节码指令时，如果类型没有进行过初始化，则需要先触发其初始化阶段。<br>能够生成这四条指令的典型Java代码场景有：<ul>
<li>使用 <code>new</code> 关键字实例化对象。</li>
<li>读取或设置一个类型的静态字段。<ul>
<li>被 <code>final</code> 修饰、已在编译期把结果放入常量池的静态字段除外。</li>
</ul>
</li>
<li>调用一个类型的静态方法。</li>
</ul>
</li>
<li>使用 <code>java.lang.reflect</code> 包的方法对类型进行反射调用时，如果类型没有进行过初始化，则需要先触发其初始化。</li>
<li>当初始化类时，如果发现其父类还没有进行过初始化，则需要先触发其父类的初始化。</li>
<li>一个接口中定义了 JDK 8 新加入的默认方法（被 <code>default</code> 修饰的接口方法），如果有这个接口的实现类发生了初始化，那该接口要在其之前被初始化。</li>
<li>当使用 JDK 7 新加入的动态语言支持时，如果一个java.lang.invoke.MethodHandle实例最后的解析结果为REF_getStatic、REF_putStatic、REF_invokeStatic、REF_newInvokeSpecial四种类型的方法句柄，并且这个方法句柄对应的类没有进行过初始化，则需要先触发其初始化。</li>
</ol>
<h3 id="需要注意的四点"><a href="#需要注意的四点" class="headerlink" title="需要注意的四点"></a>需要注意的四点</h3><h4 id="静态字段"><a href="#静态字段" class="headerlink" title="静态字段"></a>静态字段</h4><p>对于静态字段，只有直接定义这个字段的类才会被初始化。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SuperClass</span> </span>&#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> value = <span class="number">123</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SubClass</span> <span class="keyword">extends</span> <span class="title">SuperClass</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 只有 SuperClass 被初始化，SubClass 不会初始化。</span></span><br><span class="line">    System.out.println(SuperClass.value);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="常量池中的常量"><a href="#常量池中的常量" class="headerlink" title="常量池中的常量"></a>常量池中的常量</h4><p>使用常量池中的常量，不会发生类的初始化。</p>
<p>由于在<strong>编译阶段</strong>通过常量传播优化，把字符串的字面值 “hello” 存储在了 <code>Test</code> 类的常量池中。对 <code>ConstClass.HELLO</code> 的引用，都会转为对自身常量池的引用。</p>
<p>也就是说，这两个类在编译成 Class 文件后，不存在任何联系。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConstClass</span> </span>&#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String HELLO = <span class="string">"hello"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// ConstClass 不会初始化。</span></span><br><span class="line">    System.out.println(ConstClass.HELLO);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="数组对象的实例"><a href="#数组对象的实例" class="headerlink" title="数组对象的实例"></a>数组对象的实例</h4><p>在调用 <code>new SuperClass[10]</code> 时，是实例化了一个内部的数组类型 <code>[Lorg.fenxisoft.classloading.SuperClass]</code>，并不是实例化 <code>SuperClass</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// SuperClass 不会初始化。</span></span><br><span class="line">    SuperClass[] items = <span class="keyword">new</span> SuperClass[<span class="number">10</span>];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="接口的初始化"><a href="#接口的初始化" class="headerlink" title="接口的初始化"></a>接口的初始化</h4><p>初始化接口或类时，不要求初始化父接口和实现的接口</p>
<p>只有在真正使用对应接口的时候，才会初始化。（例如，引用了接口中定义的常量）。</p>
<h1 id="加载"><a href="#加载" class="headerlink" title="加载"></a>加载</h1><p>主要完成三件事情：</p>
<ol>
<li>通过一个类的全限定名来获取定义此类的二进制字节流。</li>
<li>将这个字节流所代表的<strong>静态存储结构</strong>转化为<strong>方法区</strong>的<strong>运行时数据结构</strong>。</li>
<li>在内存中生成一个代表这个类的 <code>java.lang.Class</code> 对象，作为方法区这个类的各种数据的访问入口。</li>
</ol>
<p>具体请看 <a href="/2020/java/jvm/4类加载器/" title="4. 类加载器">4. 类加载器</a>。</p>
<h1 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h1><p>连接阶段的第一步。</p>
<h2 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h2><p>确保 Class 文件的字节流的信息符合《Java 虚拟机规范》的约束。</p>
<p>保证它在运行时，不会危害虚拟机的安全。</p>
<h2 id="四个阶段检验动作"><a href="#四个阶段检验动作" class="headerlink" title="四个阶段检验动作"></a>四个阶段检验动作</h2><h3 id="文件格式验证"><a href="#文件格式验证" class="headerlink" title="文件格式验证"></a>文件格式验证</h3><p>验证字节流是否符合 Class 文件格式的规范，并且能被当前版本的虚拟机处理。</p>
<p>目的</p>
<ul>
<li>保证输入的字节流能正确地解析并存储于方法区内。</li>
</ul>
<h3 id="元数据验证"><a href="#元数据验证" class="headerlink" title="元数据验证"></a>元数据验证</h3><p>对字节码的信息进行语义分析，保证信息符合《Java 语言规范》。</p>
<p>例如</p>
<ul>
<li>这个类是否有父类（除了java.lang.Object之外，所有的类都应当有父类）。</li>
<li>这个类的父类是否继承了不允许被继承的类（被final修饰的类）。</li>
<li>如果这个类不是抽象类，是否实现了其父类或接口之中要求实现的所有方法。</li>
</ul>
<p>目的</p>
<ul>
<li>对类的元数据信息进行语义校验，保证不存在与《Java 语言》规范相悖的元数据信息。</li>
</ul>
<h3 id="字节码验证"><a href="#字节码验证" class="headerlink" title="字节码验证"></a>字节码验证</h3><p>目的</p>
<ul>
<li>通过数据流分析和控制流分析，确定程序语义是合法、符合逻辑的。</li>
</ul>
<h3 id="符号引用验证"><a href="#符号引用验证" class="headerlink" title="符号引用验证"></a>符号引用验证</h3><p>这个验证行为发生在 JVM 把符号引用转化为直接引用时，即“<strong>解析</strong>”阶段。</p>
<p>对类自身以外的信息进行匹配校验。该类是否缺少、或被禁止访问它依赖的外部类、方法、字段资源。</p>
<p>目的</p>
<ul>
<li>确保解析行为能正常执行。</li>
</ul>
<h1 id="准备"><a href="#准备" class="headerlink" title="准备"></a>准备</h1><ul>
<li>为类中定义的变量（静态变量）分配内存。</li>
<li>初始化为零值。</li>
<li>如果类变量是 <code>final</code> 的，且数据类型是基本类型、或 <code>String</code>，会被初始化为指定的初始值，而不是零值。</li>
</ul>
<h1 id="解析"><a href="#解析" class="headerlink" title="解析"></a>解析</h1><p>JVM 将常量池内的符号引用替换为直接引用的过程。</p>
<ul>
<li>符号引用<ul>
<li>符号引用的字面量形式定义在 Class 文件格式中。</li>
</ul>
</li>
<li>直接引用<ul>
<li>直接指向目标的指针、或间接指向目标的句柄。</li>
<li>有了直接引用，引用的目标必定已经在 JVM 的内存中存在。</li>
</ul>
</li>
</ul>
<p>解析的时机《Java 虚拟机规范》没有规定。</p>
<ul>
<li>是在类加载器加载类的时候进行解析。</li>
<li>还是在符号引用要被使用前采取解析它。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>SPI 机制</title>
    <url>/2020/java/jvm/SPI/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://juejin.im/post/5d2db85d6fb9a07ea7134408">Java SPI机制：ServiceLoader实现原理及应用剖析</a></li>
<li><a href="https://juejin.im/post/5af952fdf265da0b9e652de3">Java SPI机制详解</a></li>
<li><a href="https://juejin.im/post/5c959901e51d456d8054c635#heading-2">从Dubbo内核-SPI聊聊双亲委派机制</a></li>
</ul>
<h1 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h1><p>Service Provider Interfaces (SPI)，即服务提供接口。是 JDK 内置的<strong>服务发现和管理</strong>的机制。</p>
<h2 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h2><ul>
<li><p>它可以<strong>动态</strong>的为某个接口<strong>寻找服务实现</strong>，有点类似 IOC(Inversion of Control) 的思想，将装配的控制权移到<strong>程序之外</strong>，在模块化设计中这个机制尤其重要。</p>
</li>
<li><p>解耦 服务具体实现 和服务使用，使程序的<strong>可扩展性</strong>大大增强，甚至可插拔。</p>
</li>
</ul>
<h2 id="服务、服务提供方、服务使用方"><a href="#服务、服务提供方、服务使用方" class="headerlink" title="服务、服务提供方、服务使用方"></a>服务、服务提供方、服务使用方</h2><ul>
<li><strong>服务</strong> 一般是系统定义好的接口，</li>
<li><strong>服务提供方</strong>提供具体的服务实现、并向系统注册服务，</li>
<li><strong>服务使用方</strong>通过查找发现服务、而不是直接引用导入具体的服务，达到与服务提供方的分离。<ul>
<li>这样可以把想使用的服务提供方写在配置文件中，不需要修改代码就可以完成服务提供方的更改。</li>
</ul>
</li>
</ul>
<h1 id="实现难点"><a href="#实现难点" class="headerlink" title="实现难点"></a>实现难点</h1><p>由于服务<strong>提供者</strong>没有被服务<strong>使用者</strong>引用，因此对服务<strong>提供者</strong>进行类加载的任务需要服务<strong>定义者</strong>来完成。但是服务定义者的类加载器一般是服务提供者类加载器的祖先，默认的<strong>双亲委派</strong>类加载机制使得服务定义者无法完成对服务提供者的类加载。</p>
<p>例如 JDK 中定义了 <code>JDBC</code> 服务，而 MySQL、PostgreSQL 提供了具体的实现。<code>JDBC</code> 的服务接口定义在 <code>java.sql</code> 中，由引导类加载器进行的加载。而具体的服务实现类在 Classpath 下，应该由系统类加载器进行加载。显然，使用 JDBC 的引导类加载器是无法对这些具体服务进行加载的。</p>
<p>因此，在 JDBC 4.0 以前，在连接数据库的时候，需要首先使用 <code>Class.forName(&quot;com.mysql.jdbc.Driver&quot;)</code> 来在服务使用者这里显式的加载服务实现者。</p>
<h1 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h1><p>为了解决这个问题，需要使<strong>父类</strong>加载器去<strong>请求子类</strong>加载器去执行类加载任务，这样就要破坏双亲委派类加载机制。</p>
<p>因此，引入了<strong>线程上下文类加载器</strong>，线程的上下文类加载器默认都是系统类加载器。这样，服务定义者就可以使用这个线程上下文类加载器完成服务提供者的类加载。</p>
<h1 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h1><p><code>java.util.ServiceLoader</code> 实现了上述的方案。</p>
<p>在 <code>Classpath/resources/META-INF/services/MyService的接口全名</code> 文件中写入多个服务提供者的类全名（以行为单位），使用 <code>ServiceLoader</code> 就可以去加载这些服务提供者。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ServiceLoader&lt;MyService&gt; loader = ServiceLoader.load(MyService.class);</span><br><span class="line"><span class="keyword">for</span> (MyService serviceImpl : loader) &#123;</span><br><span class="line">  serviceImpl.func();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="JDBC-例子"><a href="#JDBC-例子" class="headerlink" title="JDBC 例子"></a>JDBC 例子</h2><h3 id="DriverManager"><a href="#DriverManager" class="headerlink" title="DriverManager"></a>DriverManager</h3><p>在 JDBC 中就使用了 <code>ServiceLoader</code> 去加载具体的驱动。</p>
<p><code>DriverManager</code> 在类初始化时，使用 <code>ServiceLoader</code> 去加载了 <code>Driver.class</code> 的实现类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DriverManager</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        loadInitialDrivers();</span><br><span class="line">        println(<span class="string">"JDBC DriverManager initialized"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">loadInitialDrivers</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        String drivers;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            drivers = AccessController.doPrivileged(<span class="keyword">new</span> PrivilegedAction&lt;String&gt;() &#123;</span><br><span class="line">                <span class="function"><span class="keyword">public</span> String <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                    <span class="keyword">return</span> System.getProperty(<span class="string">"jdbc.drivers"</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">            drivers = <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// If the driver is packaged as a Service Provider, load it.</span></span><br><span class="line">        <span class="comment">// Get all the drivers through the classloader</span></span><br><span class="line">        <span class="comment">// exposed as a java.sql.Driver.class service.</span></span><br><span class="line">        <span class="comment">// ServiceLoader.load() replaces the sun.misc.Providers()</span></span><br><span class="line"></span><br><span class="line">        AccessController.doPrivileged(<span class="keyword">new</span> PrivilegedAction&lt;Void&gt;() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> Void <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">                ServiceLoader&lt;Driver&gt; loadedDrivers = ServiceLoader.load(Driver.class);</span><br><span class="line">                Iterator&lt;Driver&gt; driversIterator = loadedDrivers.iterator();</span><br><span class="line"></span><br><span class="line">                <span class="keyword">try</span>&#123;</span><br><span class="line">                    <span class="keyword">while</span>(driversIterator.hasNext()) &#123;</span><br><span class="line">                        driversIterator.next();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">catch</span>(Throwable t) &#123;</span><br><span class="line">                <span class="comment">// Do nothing</span></span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="服务提供者"><a href="#服务提供者" class="headerlink" title="服务提供者"></a>服务提供者</h3><p>在 <code>mysql-connector-java-5.1.45.jar</code> 中，META-INF/services目录下会有一个名字为java.sql.Driver的文件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">com.mysql.jdbc.Driver</span><br><span class="line">com.mysql.fabric.jdbc.FabricMySQLDriver</span><br></pre></td></tr></table></figure>
<p>在 <code>postgresql-42.2.2.jar</code> 中，META-INF/services目录下会有一个名字为java.sql.Driver的文件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">org.postgresql.Driver</span><br></pre></td></tr></table></figure>
<p>这样在使用 JDBC 的时候，直接获取连接就可以了，不需要额外加载对应的驱动类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String url = <span class="string">"jdbc:mysql://localhost:3306/test"</span>;</span><br><span class="line">Connection conn = DriverManager.getConnection(url,username,password);</span><br></pre></td></tr></table></figure>
<h1 id="具体实现"><a href="#具体实现" class="headerlink" title="具体实现"></a>具体实现</h1><ul>
<li><p>使用 <code>ServiceLoader.load(MyService.class)</code> 方法，会把当前线程的上下文类加载器存储在 <code>loader</code> 中。</p>
</li>
<li><p><code>ServiceLoader#iterator()</code> 返回一个内置的迭代器。</p>
</li>
<li><code>iterator#hasNext()</code> 会去读取所有 resources 文件夹中 META-INF/services/serviceName 文件。</li>
<li><code>iterator#next()</code> 用 <code>loader</code> 去加载每一个 <code>hadNext()</code> 读取出来的类。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 默认使用上下文类加载器。</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;S&gt; <span class="function">ServiceLoader&lt;S&gt; <span class="title">load</span><span class="params">(Class&lt;S&gt; service)</span> </span>&#123;</span><br><span class="line">    ClassLoader cl = Thread.currentThread().getContextClassLoader();</span><br><span class="line">    <span class="keyword">return</span> ServiceLoader.load(service, cl);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;S&gt; <span class="function">ServiceLoader&lt;S&gt; <span class="title">load</span><span class="params">(Class&lt;S&gt; service, ClassLoader loader)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> ServiceLoader&lt;&gt;(service, loader);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="title">ServiceLoader</span><span class="params">(Class&lt;S&gt; svc, ClassLoader cl)</span> </span>&#123;</span><br><span class="line">    service = Objects.requireNonNull(svc, <span class="string">"Service interface cannot be null"</span>);</span><br><span class="line">    loader = (cl == <span class="keyword">null</span>) ? ClassLoader.getSystemClassLoader() : cl;</span><br><span class="line">    acc = (System.getSecurityManager() != <span class="keyword">null</span>) ? AccessController.getContext() : <span class="keyword">null</span>;</span><br><span class="line">    reload();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> Iterator&lt;S&gt; <span class="title">iterator</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String PREFIX = <span class="string">"META-INF/services/"</span>;</span><br><span class="line"><span class="comment">// hasNext() 最终调用 hasNextService()</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">hasNextService</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (nextName != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (configs == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            String fullName = PREFIX + service.getName();</span><br><span class="line">            <span class="keyword">if</span> (loader == <span class="keyword">null</span>)</span><br><span class="line">              	<span class="comment">// 找到所有 resources 文件夹中 META-INF/services/serviceName 文件。</span></span><br><span class="line">                configs = ClassLoader.getSystemResources(fullName);</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                configs = loader.getResources(fullName);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException x) &#123;</span><br><span class="line">            fail(service, <span class="string">"Error locating configuration files"</span>, x);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// 遍历每一个 config 文件。</span></span><br><span class="line">    <span class="keyword">while</span> ((pending == <span class="keyword">null</span>) || !pending.hasNext()) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!configs.hasMoreElements()) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        pending = parse(service, configs.nextElement());</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// 存储下一个要加载的类名。</span></span><br><span class="line">    nextName = pending.next();</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// next() 最终调用 nextService()</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> S <span class="title">nextService</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!hasNextService())</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NoSuchElementException();</span><br><span class="line">    String cn = nextName;</span><br><span class="line">    nextName = <span class="keyword">null</span>;</span><br><span class="line">    Class&lt;?&gt; c = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 使用构造函数传入的 loader，加载 nextName。</span></span><br><span class="line">        c = Class.forName(cn, <span class="keyword">false</span>, loader);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ClassNotFoundException x) &#123;</span><br><span class="line">        fail(service,</span><br><span class="line">             <span class="string">"Provider "</span> + cn + <span class="string">" not found"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!service.isAssignableFrom(c)) &#123;</span><br><span class="line">        fail(service,</span><br><span class="line">             <span class="string">"Provider "</span> + cn  + <span class="string">" not a subtype"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        S p = service.cast(c.newInstance());</span><br><span class="line">        providers.put(cn, p);</span><br><span class="line">        <span class="keyword">return</span> p;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Throwable x) &#123;</span><br><span class="line">        fail(service,</span><br><span class="line">             <span class="string">"Provider "</span> + cn + <span class="string">" could not be instantiated"</span>,</span><br><span class="line">             x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> Error();          <span class="comment">// This cannot happen</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>2. Java垃圾收集器与内存分配分配策略</title>
    <url>/2020/java/jvm/2Java%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li>《深入理解 Java 虚拟机-第三版》</li>
<li><a href="https://blog.csdn.net/aitangyong/article/details/41358427">java中虚引用PhantomReference与弱引用WeakReference（软引用SoftReference）的差别</a></li>
</ul>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><h2 id="堆和方法区的垃圾回收"><a href="#堆和方法区的垃圾回收" class="headerlink" title="堆和方法区的垃圾回收"></a>堆和方法区的垃圾回收</h2><ul>
<li>程序计数器、虚拟机栈、本地方法栈不需要垃圾回收。<ul>
<li>所需要分配的内存在编译器可以确定下来，内存分配和回收具有确定性。</li>
</ul>
</li>
<li>堆和方法区，内存的分配和回收是动态的。<ul>
<li>一个接口的多个实现类需要的内存可能不同。</li>
<li>一个方法不同的条件分支所需要的内存可能不同。</li>
</ul>
</li>
</ul>
<h2 id="垃圾回收需要完成的三件事"><a href="#垃圾回收需要完成的三件事" class="headerlink" title="垃圾回收需要完成的三件事"></a>垃圾回收需要完成的三件事</h2><ol>
<li>哪些内存需要回收？</li>
<li>什么时候回收？</li>
<li>如何回收？</li>
</ol>
<h1 id="哪些对象需要回收"><a href="#哪些对象需要回收" class="headerlink" title="哪些对象需要回收"></a>哪些对象需要回收</h1><h2 id="没有使用引用计数的原因"><a href="#没有使用引用计数的原因" class="headerlink" title="没有使用引用计数的原因"></a>没有使用引用计数的原因</h2><p>有很多例外情况要考虑，要配合大量额外处理才能保证正确地工作。例如循环引用。</p>
<h2 id="核心算法：可达性分析"><a href="#核心算法：可达性分析" class="headerlink" title="核心算法：可达性分析"></a>核心算法：可达性分析</h2><ul>
<li><p>GC Roots：通过一系列 “GC Roots” 的根对象作为起始节点集。</p>
</li>
<li><p>引用链：从 GC Roots 开始，根据引用关系向下搜索，搜索过程所走过的路径。</p>
</li>
</ul>
<p>如果某个对象到 GC Roots 没有引用链相连，即从 GC Roots 到这个对象不可达时，说明对象是不可能再被使用的。</p>
<h3 id="对象不可达与-finalize"><a href="#对象不可达与-finalize" class="headerlink" title="对象不可达与 finalize()"></a>对象不可达与 <code>finalize()</code></h3><p>对象不可达并<strong>不一定</strong>会被回收。</p>
<p>如果对象重写了 <code>finalize()</code> 函数、并且还没有被 JVM 调用过 。会<strong>进行两次标记</strong>。</p>
<ol>
<li>第一次标记。在对象不可达时进行第一次标记。随后，把对象加入到 <code>F-Queue</code> 队列中，在虚拟机的优先级的 Finalizer 线程去执行他们的 <code>finalize()</code> 方法。</li>
<li>第二次标记。收集器对 <code>F-Queue</code> 中的对象进行第二次标记。如果对象“拯救”了自己（把自己赋值给其他变量），就从“即将回收”的集合中移除。</li>
</ol>
<p><code>finalize()</code> 方法需要注意的点：</p>
<ul>
<li>并<strong>不保证</strong>会等待它运行完毕，否则某些 <code>finalize()</code> 执行很缓慢、死循环，会导致其他<code>finalize()</code> 无法被执行。</li>
<li>每个对象的 <code>finalize()</code> 只会执行一次。也就是每个对象只能“拯救”自己一次。</li>
</ul>
<h2 id="作为-GC-Roots-的对象"><a href="#作为-GC-Roots-的对象" class="headerlink" title="作为 GC Roots 的对象"></a>作为 GC Roots 的对象</h2><p>固定的。</p>
<ol>
<li>虚拟机栈的本地变量表中引用的对象。</li>
<li>本地方法栈中引用的对象。</li>
<li>方法区中类静态属性引用的对象。</li>
<li>方法区中常量引用的对象。例如字符串常量池里的引用。</li>
<li>被监视器锁 <code>synchronized</code> 持有的对象。</li>
<li>JVM 内部的引用<ul>
<li>常驻的异常对象（NullPointException、OutOfMemoryError等）。</li>
<li>系统类加载器。</li>
<li>基本数据类型对应的 Class 对象。</li>
</ul>
</li>
<li>反 JVM 内部情况的 JMXBean、JVMTI 中注册的回调、本地代码缓存等。</li>
</ol>
<p>临时的。</p>
<ul>
<li>分代回收、局部回收会把堆内存划分区域，相关联区域的对象也要加入到 GC Roots 中。</li>
</ul>
<h2 id="四种引用类别"><a href="#四种引用类别" class="headerlink" title="四种引用类别"></a>四种引用类别</h2><ol>
<li>强引用。<ul>
<li>强引用关系存在，垃圾收集器就不会回收被引用的对象。</li>
</ul>
</li>
<li>软引用。<ul>
<li>描述还有用、非必须的对象。</li>
<li><strong>只</strong>被软引用的对象，在系统要发生内存溢出异常时，把这些对象纳入回收范围，进行第二次回收。</li>
<li><code>SoftReference</code> 类。<ul>
<li><code>SoftReference#get()</code> 方法返回引用的对象。</li>
<li>在被 GC 时，会把引用的对象置为 null，再加入到 <code>ReferenceQueue</code> 中。</li>
</ul>
</li>
</ul>
</li>
<li>弱引用。<ul>
<li>描述非必须的对象。</li>
<li><strong>只</strong>被弱引用的对象，下次垃圾收集就会回收掉。</li>
<li><code>WeakReference</code> 类。<ul>
<li><code>WeakReference#get()</code> 方法返回引用的对象。</li>
<li>在被 GC 时，会把引用的对象置为 null，再加入到 <code>ReferenceQueue</code> 中。</li>
</ul>
</li>
</ul>
</li>
<li>虚引用。<ul>
<li>用来在这个对象被垃圾回收时收到系统通知。</li>
<li><code>PhantomReference</code> 类。<ul>
<li><code>PhantomReference#get()</code> 方法返回 null，即无法通过虚引用得到被引用的对象。</li>
<li>在被 GC 时，会把引用的对象不会置为 null，直接加入到 <code>ReferenceQueue</code> 中。</li>
<li>需要手动调用 <code>PhantomReference#clear()</code> 才会清除弱引用的对象。如果不清楚，可能会<strong>内存泄漏</strong>！！</li>
</ul>
</li>
</ul>
</li>
</ol>
<h2 id="回收方法区"><a href="#回收方法区" class="headerlink" title="回收方法区"></a>回收方法区</h2><p>方法区主要回收两部分内容：</p>
<ul>
<li><p>废弃的常量。没有任何字符串对象引用、且虚拟机中也没有其他地方引用这个字面量。</p>
</li>
<li><p>无用的类型。需要满足 3 个条件：</p>
<ol>
<li><p>该类所有的实例都已被回收。</p>
</li>
<li><p>该类的类加载器已被回收。</p>
</li>
<li><p>该类对应的 <code>java.lang.Class</code> 对象没有在任何地方被引用。</p>
</li>
</ol>
</li>
</ul>
<p>在满足上述条件的时候，如果发生了 GC，废弃的常量、无用的类型可以被回收掉。</p>
<p>但是并不是一定会回收，HotSpot 虚拟机提供 <code>-Xnoclassgc</code> 参数对类型的写在进行控制。</p>
<h1 id="垃圾收集算法"><a href="#垃圾收集算法" class="headerlink" title="垃圾收集算法"></a>垃圾收集算法</h1><h2 id="理论基础"><a href="#理论基础" class="headerlink" title="理论基础"></a>理论基础</h2><h3 id="三个分代假说"><a href="#三个分代假说" class="headerlink" title="三个分代假说"></a>三个分代假说</h3><ol>
<li><p>弱分代假说：绝大多数对象都是朝生夕灭的。</p>
</li>
<li><p>强分代假说：熬过越多次垃圾收集过程的对象就越难以消亡。</p>
</li>
<li>跨代引用假说：跨代引用相对于同代引用来说仅占极少数。</li>
</ol>
<h3 id="分代收集"><a href="#分代收集" class="headerlink" title="分代收集"></a>分代收集</h3><p>因此可以根据对象的年龄，把堆内存划分成不同的区域。</p>
<ul>
<li>新生代。区域内大部分都是朝生夕灭的对象，只需要关注如何保留少量存活的对象。</li>
<li>老年代。区域内都是难以消亡的对象，可以使用较低的频率来回收这个区域。</li>
</ul>
<h3 id="记忆集"><a href="#记忆集" class="headerlink" title="记忆集"></a>记忆集</h3><p>对象不是孤立的，对象之间会存在跨代引用。因此，在 GC 时，需要知道老年代的对象引用了哪些新生代的对象。</p>
<p>根据假说 3，只存在极少量的跨代引用。不必要专门记录每个对象存在哪些跨代引用，只需在新生代上建立一个全局的数据结构“记忆集” <code>RememberedSet</code>。</p>
<ul>
<li>老年代分块。标识出老年代的哪一块内存会存在跨代引用。</li>
<li>部分块加入 GC Roots。发生 MinorGC 时，只有包含了跨代引用的小块内存里的对象才会被加入到 GC Roots 进行扫描。</li>
<li>需要在对象改变引用关系时维护记录数据的正确性，但是不必要 GC 时，扫描整个老年代。</li>
</ul>
<h3 id="收集的分类"><a href="#收集的分类" class="headerlink" title="收集的分类"></a>收集的分类</h3><ul>
<li>部分收集 PartialGC。指目标不是完整收集整个 Java 堆的垃圾收集。分为：<ul>
<li>新生代收集 MinorGC/YoungGC。指目标只是新生代的垃圾收集。</li>
<li>老年代收集 MajorGC/OldGC。指目标只是老年代的垃圾收集。目前只有CMS收集器会有单独收集老年代的行为。</li>
<li>混合收集 MixedGC。指目标是收集整个新生代以及部分老年代的垃圾收集。目前只有G1收集器会有这种行为。</li>
</ul>
</li>
<li>整堆收集 FullGC。收集整个Java堆和<strong>方法区</strong>的垃圾收集。</li>
</ul>
<h2 id="标记-清除算法"><a href="#标记-清除算法" class="headerlink" title="标记-清除算法"></a>标记-清除算法</h2><p>两个阶段：</p>
<ol>
<li>标记出所有需要回收的对象。</li>
<li>统一回收掉所有被标记的对象。</li>
</ol>
<p>两个缺点：</p>
<ol>
<li>执行效率不稳定。如果堆中包含大量对象、且大部分需要回收。需要进行大量的标记和清除操作。</li>
<li>内存空间的碎片化。会产生大量不连续的内存碎片，在之后分配大对象时无法找到足够大的连续区域，就需要提前触发一次 GC。（见CMS-TODO）</li>
</ol>
<h2 id="标记-复制算法"><a href="#标记-复制算法" class="headerlink" title="标记-复制算法"></a>标记-复制算法</h2><p>HotSpot 的做法：</p>
<ul>
<li>把新生代分为较大的 Eden 区、较小的 FromSurvivor 区、ToSurvivor 区。默认是 <code>8:1:1</code>。</li>
<li>分配内存时，只使用 Eden 区、FromSurvivor 区。</li>
<li>GC 时，将 Eden 区、FromSurvivor 区存活的对象复制到 ToSurvivor 区。清理掉 Eden 区、FromSurvivor 区。</li>
<li>当一块 Survivor 空间不足以容纳一次 MinorGC 之后存活的对象时，需要依赖老年代进行分配担保。</li>
</ul>
<p>缺点：</p>
<ul>
<li>在对象存活率较高时，要进行较多的复制操作。</li>
</ul>
<h2 id="标记-整理算法"><a href="#标记-整理算法" class="headerlink" title="标记-整理算法"></a>标记-整理算法</h2><h3 id="两个阶段"><a href="#两个阶段" class="headerlink" title="两个阶段"></a>两个阶段</h3><ol>
<li>标记出所有需要回收的对象。</li>
<li>让所有存活的对象都向内存空间一端移动，然后直接清理掉边界以外的内存。</li>
</ol>
<h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ul>
<li>不会像<strong>标记-清除</strong>算法那样产生内存空间碎片。</li>
</ul>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>在对象存活率较高时，操作的负担很重。</li>
<li>需要暂停用户程序。</li>
</ul>
<h3 id="与标记-清除算法的对比。"><a href="#与标记-清除算法的对比。" class="headerlink" title="与标记-清除算法的对比。"></a>与标记-清除算法的对比。</h3><ul>
<li>标记-清除算法的延时较低。GC 时，直接清除、不移动对象，停顿时间更短。</li>
<li>标记-整理算法的吞吐量较高。指用户程序执行时间/(用户程序执行时间+GC 时间)。<ul>
<li>虽然 GC 时移动对象效率低；</li>
<li>但整理内存后，内存分配、访问效率很高。</li>
<li>而且内存分配和访问相比 GC 频率要高得多。因此吞吐量是更高的。</li>
</ul>
</li>
</ul>
<h3 id="与标记-清除算法结合使用"><a href="#与标记-清除算法结合使用" class="headerlink" title="与标记-清除算法结合使用"></a>与标记-清除算法结合使用</h3><p>CMS 收集器平时使用标记-清除算法，在内存空间很大，无法分配对象时，再使用标记-整理算法收集一次。（见CMS-TODO）</p>
<h1 id="HotSpot-的算法细节实现"><a href="#HotSpot-的算法细节实现" class="headerlink" title="HotSpot 的算法细节实现"></a>HotSpot 的算法细节实现</h1><h2 id="根节点枚举"><a href="#根节点枚举" class="headerlink" title="根节点枚举"></a>根节点枚举</h2><p>方法区大小数百兆，类、常量的数量很多。栈中也类似。</p>
<p>虚拟机需要有办法直接得到哪些地方存放着对象引用。</p>
<p>HotSpot 使用 <code>OopMap</code> (Ordinary Object Pointer) 来实现这个功能，会在<strong>特定位置</strong>记录下栈、寄存器里哪些位置是引用。</p>
<h2 id="安全点"><a href="#安全点" class="headerlink" title="安全点"></a>安全点</h2><p>不能每一条指令都记录到 <code>OopMap</code>，空间成本太高。只能在<strong>特定位置</strong>记录。这些位置就是<strong>安全点</strong>。</p>
<p>用户程序只有执行到安全点后才能暂停。</p>
<ul>
<li>如何选择安全点？<ul>
<li>标准是“是否具有让程序长时间执行的特征”，指令序列的复用，例如方法调用、循环跳转、异常跳转。</li>
<li>只有具有这些功能的指令才会产生安全点。</li>
</ul>
</li>
<li>如何让所有线程都执行到最近的安全点？<ul>
<li>主动式中断。设置一个标志位。线程执行时不断主动去轮询这个标志，如果发现中断标志位真，就在最近的安全点主动挂起。</li>
<li>轮询的位置：安全点、以及分配堆内存时（避免没有足够内存分配新对象）。</li>
<li>实现方式：内存保护陷阱。<ul>
<li>JVM 把这个指令要读的内存页被设置为不可读，</li>
<li>用户线程执行到这条指令时，就会产生自陷异常信号，在异常处理器中挂起线程，实现等待。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="安全区域"><a href="#安全区域" class="headerlink" title="安全区域"></a>安全区域</h2><p>如果程序在阻塞，就无法响应 JVM 的中断请求，无法走到安全点挂起自己。使用安全区域来解决。</p>
<p>安全区域，指能够确保在某一段代码片段中，引用关系不会发生变化。在安全区域进行垃圾收集都是安全的。</p>
<ul>
<li>用户线程执行到安全区域的代码时，标识自己进入了安全区。这段时间 JVM 要发起垃圾收集，就不需要管在安全区域的线程。</li>
<li>用户线程离开安全区域时，需要等待 JVM 完成了根节点的枚举。</li>
</ul>
<h2 id="记忆表与卡表"><a href="#记忆表与卡表" class="headerlink" title="记忆表与卡表"></a>记忆表与卡表</h2><p>HotSpot 使用卡表 CARD_TABLE，一个字节数组来实现记忆表。</p>
<p>每个元素表示一个内存块（卡页）是否存在跨代指针，如果存在，则标为 0 （变脏）。</p>
<h2 id="写屏障"><a href="#写屏障" class="headerlink" title="写屏障"></a>写屏障</h2><p>需要提供机制来使<strong>维护卡表</strong>的动作放到赋值操作中。</p>
<p><strong>写屏障</strong>可以在<strong>引用类型字段赋值</strong>指令前后产生环形通知，供程序执行额外的动作。</p>
<h3 id="伪共享"><a href="#伪共享" class="headerlink" title="伪共享"></a>伪共享</h3><p>如果不同线程更新的对象所对应的卡表项处于同一个缓存行，就会导致缓存不断写回、失效。</p>
<p>不采用无条件的写屏障，先检查卡表项是否为 0，如果是 0，才进行写入。</p>
<h2 id="并发的可达性分析"><a href="#并发的可达性分析" class="headerlink" title="并发的可达性分析"></a>并发的可达性分析</h2><p>在确定 GC Roots 后，可以使 JVM 对引用链的可达性分析、与用户线程并发执行。</p>
<h3 id="三色标记"><a href="#三色标记" class="headerlink" title="三色标记"></a>三色标记</h3><ul>
<li>白色：还没有被垃圾收集器访问过。</li>
<li>黑色：对象被垃圾收集器访问过，其所有引用都被扫描过了。（GC Roots 要设为黑色的）。</li>
<li>灰色：对象被垃圾收集器访问过，至少存在一个引用没有被扫描过。</li>
</ul>
<h3 id="对象消失问题"><a href="#对象消失问题" class="headerlink" title="对象消失问题"></a>对象消失问题</h3><p>并发分析引用链时，用户线程（赋值器）可能改变了引用关系。</p>
<p>在同时满足如下两个关系时，会把还有引用的对象错误的标记为白色：</p>
<ul>
<li>赋值器插入了从黑色节点到白色节点的新引用。</li>
<li>赋值器删除了从灰色节点到这个白色节点的所有直接、间接引用。</li>
</ul>
<h3 id="解决办法"><a href="#解决办法" class="headerlink" title="解决办法"></a>解决办法</h3><p>只要破坏上述其中一个条件就可以。通过写屏障实现。</p>
<ul>
<li>增量更新。<ul>
<li>在黑色节点插入到白色节点的新引用时，记录下来这个引用。</li>
<li>在并发扫描结束后，从这些记录的黑节点为根，再扫描一次。</li>
</ul>
</li>
<li>原始快照。<ul>
<li>在灰色对象要删除指向白色节点的引用时，记录下来要删除的引用。</li>
<li>在并发扫描结束后，从这些记录的灰色节点为根，再扫描一次。</li>
</ul>
</li>
</ul>
<h1 id="经典垃圾收集器"><a href="#经典垃圾收集器" class="headerlink" title="经典垃圾收集器"></a>经典垃圾收集器</h1><h2 id="简介-1"><a href="#简介-1" class="headerlink" title="简介"></a>简介</h2><h3 id="HotSpot垃圾收集器"><a href="#HotSpot垃圾收集器" class="headerlink" title="HotSpot垃圾收集器"></a>HotSpot垃圾收集器</h3><p>图中连线的说明可以配合使用。</p>
<p><img src="/images/HotSpot垃圾收集器.png" alt="HotSpot垃圾收集器"></p>
<h3 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h3><div class="table-container">
<table>
<thead>
<tr>
<th>名称</th>
<th>针对区域</th>
<th>线程数</th>
<th>是否暂停</th>
<th>算法</th>
<th>GC 操作</th>
</tr>
</thead>
<tbody>
<tr>
<td>Serial</td>
<td>新生代</td>
<td>单线程</td>
<td>暂停</td>
<td>标记-复制</td>
<td>Minor GC</td>
</tr>
<tr>
<td>Serial Old</td>
<td>老年代</td>
<td>单线程</td>
<td>暂停</td>
<td>标记-整理</td>
<td>Full GC</td>
</tr>
<tr>
<td>ParNew</td>
<td>新生代</td>
<td>多线程</td>
<td>暂停</td>
<td>标记-复制</td>
<td>Minor GC</td>
</tr>
<tr>
<td>Parallel Scavenge</td>
<td>新生代</td>
<td>多线程</td>
<td>暂停</td>
<td>标记-复制</td>
<td>Minor GC</td>
</tr>
<tr>
<td>Parallel Old</td>
<td>老年代</td>
<td>多线程</td>
<td>暂停</td>
<td>标记-整理</td>
<td>Full GC</td>
</tr>
<tr>
<td>CMS</td>
<td>老年代</td>
<td>初始标记单线程<br />其他多线程</td>
<td>部分暂停</td>
<td>标记-清理</td>
<td>Major GC</td>
</tr>
<tr>
<td>G1</td>
<td>新生代<br />老年代</td>
<td>初始标记单线程<br />其他多线程</td>
<td>部分暂停</td>
<td>从Region复制到其他Region</td>
</tr>
</tbody>
</table>
</div>
<ul>
<li>只有 CMS 有Marjor GC。其他收集器没有专门在老年代进行 GC的。要进行老年代 GC，他们都是进行 Full GC。</li>
</ul>
<h2 id="Serial-收集器"><a href="#Serial-收集器" class="headerlink" title="Serial 收集器"></a>Serial 收集器</h2><ul>
<li>新生代收集器。</li>
<li>单线程工作。</li>
<li>垃圾收集时，需要暂停工作线程。</li>
<li>采用<strong>标记-复制</strong>算法。</li>
<li>客户端模式下默认的新生代收集器。</li>
</ul>
<h2 id="Serial-Old-收集器"><a href="#Serial-Old-收集器" class="headerlink" title="Serial Old 收集器"></a>Serial Old 收集器</h2><ul>
<li>老年代收集器。Serial 的老年代版本。</li>
<li>单线程工作。</li>
<li>使用<strong>标记-整理</strong>算法。对整堆进行 Full GC，不是只针对老年代。</li>
</ul>
<h2 id="ParNew-收集器"><a href="#ParNew-收集器" class="headerlink" title="ParNew 收集器"></a>ParNew 收集器</h2><ul>
<li><p>新生代收集器。</p>
</li>
<li><p>ParNew 只是在新生代时，多线程工作。其他和 Serial 一样。</p>
</li>
</ul>
<h2 id="Parallel-Scavenge-收集器"><a href="#Parallel-Scavenge-收集器" class="headerlink" title="Parallel Scavenge 收集器"></a>Parallel Scavenge 收集器</h2><ul>
<li>新生代收集器。</li>
<li>多线程工作。</li>
<li>垃圾收集时，需要暂停工作线程。</li>
<li>采用<strong>标记-复制</strong>算法。</li>
<li>侧重吞吐量。更有效的利用处理器资源，适合后台计算任务。<ul>
<li><code>-XX:MaxGCPauseMills</code> 收集器尽力保证垃圾回收的时间不超过这个值。</li>
<li><code>-XX:GCTimeRatio</code> 垃圾收集时间占总时间的比率，吞吐量的导数。</li>
<li><code>-XX+UseAdpativeSizePolicy</code> 自调整新生代大小、Eden与Survivor遍历、晋升老年代的年龄等参数，以提供最合适的停顿时间、或最大的吞吐量。</li>
</ul>
</li>
</ul>
<h2 id="Parallel-Old-收集器"><a href="#Parallel-Old-收集器" class="headerlink" title="Parallel Old 收集器"></a>Parallel Old 收集器</h2><ul>
<li>老年代收集器。Parallel Scavenge 的老年代版本。</li>
<li>多线程工作。</li>
<li>垃圾收集时，需要暂停工作线程。</li>
<li>采用<strong>标记-整理</strong>算法。对整堆进行 Full GC，不是只针对老年代。</li>
</ul>
<h2 id="CMS-收集器"><a href="#CMS-收集器" class="headerlink" title="CMS 收集器"></a>CMS 收集器</h2><p>CMS 指 Concurrent Mark Sweep。</p>
<ul>
<li><p>老年代收集器。</p>
</li>
<li><p>多线程工作。</p>
</li>
<li><p>部分步骤与用户线程并发。</p>
<ol>
<li><p>初始标记。</p>
<ul>
<li><p>单线程。</p>
</li>
<li><p>需要暂停。</p>
</li>
<li><p>标记 GC Roots 能直接关联的对象。</p>
</li>
</ul>
</li>
<li><p>并发标记。</p>
<ul>
<li><p>多线程。</p>
</li>
<li><p>不需要暂停。</p>
</li>
<li><p>从 GC Roots 直接关联的对象遍历整个对象图。</p>
</li>
</ul>
</li>
<li><p>重新标记。</p>
<ul>
<li>多线程。</li>
<li>需要暂停。</li>
<li>修正并发标记时，用户程序继续运作产生变动的对象（见三色收集）。</li>
</ul>
</li>
<li><p>并发清除。</p>
<ul>
<li>多线程。</li>
<li><p>不需要暂停，因为不需要移动对象。</p>
</li>
<li><p>清除掉标记阶段判断死亡的对象。</p>
</li>
</ul>
</li>
</ol>
</li>
<li><p>基于<strong>标记-清除</strong>算法。只针对老年代。</p>
</li>
<li><p>由于 GC 期间，用户线程在运行，老年代要预留区域供用户线程使用。</p>
<ul>
<li>不能像其他收集器那样，等老年代几乎满了才 GC。</li>
<li>JDK 5，默认 68% 就会触发 GC，JDK 6，默认 92%。</li>
<li>如果 Major GC 期间，预留的内存无法满足新对象的分配。则并发失败。<ul>
<li>暂停用户线程的执行。启用 Serial Old 收集器来进行老年代的垃圾收集 。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h3 id="缺点-1"><a href="#缺点-1" class="headerlink" title="缺点"></a>缺点</h3><ol>
<li>对处理器资源非常敏感。<ul>
<li>默认回收线程数时  (CPUS + 3) / 4。</li>
<li>如果有4个以上CPU，只会占用1个CPU，25%资源。</li>
<li>如果少于4个CPU，占用1个CPU，用户线程需要分出一半的运算能力。</li>
</ul>
</li>
<li>无法处理浮动垃圾。<ul>
<li>浮动垃圾。在 GC <strong>期间</strong>一些对象成为垃圾，这些对象无法在<strong>本次</strong> GC 回收。</li>
</ul>
</li>
<li>空间碎片。<ul>
<li>没有足够大的连续空间分配新对象时，发生 Full GC。</li>
</ul>
</li>
</ol>
<h2 id="Garbage-First-收集器"><a href="#Garbage-First-收集器" class="headerlink" title="Garbage First 收集器"></a>Garbage First 收集器</h2><ul>
<li>把堆划分成多个大小相等的独立区域。<ul>
<li>每个 Region 根据需要，扮演新生代的Eden和Survivor、老年代空间。</li>
</ul>
</li>
<li>一类 Humongous 区域。存储大对象。<ul>
<li>超过 Region 容量一半的对象可判定为大对象。</li>
<li>G1大多数行为把它作为老年代的一部分看待。</li>
</ul>
</li>
<li><p>建立可预测的停顿时间模型。</p>
<ul>
<li>将Region作为单次回收的最小单元。</li>
<li>统计计算每个Region回收需要的时间和可以获得空间大小的经验值。</li>
<li>选取回收率高的Region去回收。</li>
</ul>
</li>
<li><p>跨 Region引用对象。</p>
<ul>
<li>每个Region维护自己的记忆集，记录其他Region指向自己的指针。</li>
<li>耗费约堆容量10%-20%的额外内存。</li>
</ul>
</li>
<li><p>四个步骤。</p>
<ol>
<li><p>初始标记。</p>
<ul>
<li>单线程。</li>
<li>需要暂停。</li>
</ul>
</li>
<li><p>并发标记。</p>
<ul>
<li>多线程。</li>
<li><p>不需暂停。</p>
</li>
<li><p>需要预留空间，供新对象分配。</p>
</li>
</ul>
</li>
<li><p>最终标记。</p>
<ul>
<li><p>多线程。</p>
</li>
<li><p>需要暂停。</p>
</li>
<li>处理并发标记期间变动的对象（原始快照法）。</li>
</ul>
</li>
<li><p>筛选回收。</p>
<ul>
<li><p>多线程。</p>
</li>
<li><p>需要暂停。</p>
</li>
<li>根据用户参数，指定回收计划。</li>
<li>把需要回收的 Region 的存活对象复制到新的Region。</li>
</ul>
</li>
</ol>
</li>
<li><p>如果无法分配新的对象，触发 Full GC。</p>
</li>
<li>停顿时间不能设置太低。<ul>
<li>太低，每次只能回收很少的 Region。</li>
<li>收集的速度跟不上分配的速度，不断触发 Full GC。</li>
</ul>
</li>
</ul>
<h1 id="内存分配策略"><a href="#内存分配策略" class="headerlink" title="内存分配策略"></a>内存分配策略</h1><ul>
<li>大多数情况下，对象在新生代 Eden 区中分配。当 Eden 区没有足够空间进行分配时，虚拟机发起一次 Minor GC。</li>
<li>需要大量连续内存的对象（长字符串、数组），直接在老年代分配。</li>
<li>在GC 时，年龄超过阈值的对象进入老年代。<ul>
<li>对象年龄计数器，存储在对象头。</li>
<li>每次从Survivor存活，年龄加1。</li>
</ul>
</li>
<li>如果Survivor空间中，相同年龄的对象大小综合超过Survivor空间的一般。年龄大于等于这个年龄的对象进入老年代。</li>
<li>空间分配担保。<ul>
<li>如果 Minor GC 期间，ToSurvivor 区无法放下存活的对象。要把存活的对象提前转移到老年代。</li>
<li>因此，要保证老年代放得下。在 Minor GC 之前，JVM检查老年代最大可用的连续空间是否大于新生代所有对象总空间。<ul>
<li>如果成立，本次 Minor GC 是安全。</li>
<li>否则，如果 <code>-XX:HandlePromotionFailure</code>允许担保失败。检查是否大于历次晋升到老年代对象的平均大小。如果大于，将尝试进行一次 Minor GC。<ul>
<li>JDK 6 Update 24 后，这个参数无效。总是允许担保失败。</li>
</ul>
</li>
<li>否则，进行 Full GC。</li>
</ul>
</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>4. 类加载器</title>
    <url>/2020/java/jvm/4%E7%B1%BB%E5%8A%A0%E8%BD%BD%E5%99%A8/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://zhuanlan.zhihu.com/p/51374915">知乎-老大难的 Java ClassLoader 再不理解就老了</a></li>
<li>《Java 核心技术-卷2-第九章》</li>
<li>[<a href="https://www.hollischuang.com/archives/199">深度分析Java的ClassLoader机制（源码级别）</a>]</li>
<li>[<a href="https://www.hollischuang.com/archives/201">Java类的加载、链接和初始化</a>]</li>
</ul>
<h1 id="ClassLoader-的用途"><a href="#ClassLoader-的用途" class="headerlink" title="ClassLoader 的用途"></a>ClassLoader 的用途</h1><p>加载 .class 字节码的二进制文件，转换成内存形式的<code>Class</code> 对象。</p>
<p>.class 文件可以来自于磁盘、jar包中、远程服务器的字节流。</p>
<h1 id="延迟加载"><a href="#延迟加载" class="headerlink" title="延迟加载"></a>延迟加载</h1><p>虚拟机值加载程序执行是所需要的类文件。</p>
<p>假设从 MyProgram.class 开始执行，虚拟机的执行步骤：</p>
<ol>
<li>加载 MyProgram.class 类文件的内容。</li>
<li>类 MyProgram 中另静态域（非基本类型）、超类也会被加载。</li>
<li>执行静态 main 方法。</li>
<li>加载 main 方法、main 调用的方法中使用的类。</li>
</ol>
<h1 id="四种类加载器"><a href="#四种类加载器" class="headerlink" title="四种类加载器"></a>四种类加载器</h1><p>每个 Java 程序 至少有 3 种类加载器。</p>
<h2 id="引导类加载器-BootstrapClassLoader"><a href="#引导类加载器-BootstrapClassLoader" class="headerlink" title="引导类加载器 BootstrapClassLoader"></a>引导类加载器 BootstrapClassLoader</h2><p>负责加载核心系统类，位于 JAVA_HOME/lib/rt.jar 文件中，<code>java.util.*</code>、 <code>java.io.*</code>、 <code>java.nio.*</code>、 <code>java.lang.*</code> 等等。</p>
<p>用 C 语言编写，没有对应的 ClassLoader 对象。例如 <code>String.class.getClassLoader() == null</code>。</p>
<h2 id="扩展类加载器-ExtClassLoader"><a href="#扩展类加载器-ExtClassLoader" class="headerlink" title="扩展类加载器 ExtClassLoader"></a>扩展类加载器 ExtClassLoader</h2><p>负责加载 JVM 扩展类，比如 swing 系列、内置的 js 引擎、xml 解析器 等等，这些库名通常以 javax 开头，它们的 jar 包位于 <code>JAVA_HOME/lib/ext/*.jar</code> 中，有很多 jar 包。</p>
<p>注意，扩展类加载器不使用类路径。</p>
<h2 id="系统类加载器-ApClassLoader"><a href="#系统类加载器-ApClassLoader" class="headerlink" title="系统类加载器 ApClassLoader"></a>系统类加载器 ApClassLoader</h2><p>加载应用类，在 <code>CLASSPATH</code> 环境变量、或 <code>-classpath</code> 命令行参数的目录中查找。</p>
<h2 id="自定义类加载器"><a href="#自定义类加载器" class="headerlink" title="自定义类加载器"></a>自定义类加载器</h2><p> <code>ClassLoader</code> 暴露有两种构造函数，即可以指定父加载器。</p>
<p>可以通过 <code>ClassLoader.getSystemClassLoader()</code> 得到 <code>ApClassLoader</code>，再通过 <code>classLoader.getParent</code> 得到 <code>ExtClassLoader</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="title">ClassLoader</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>(checkCreateClassLoader(), getSystemClassLoader());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="title">ClassLoader</span><span class="params">(ClassLoader parent)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>(checkCreateClassLoader(), parent);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="类加载器的层次结构"><a href="#类加载器的层次结构" class="headerlink" title="类加载器的层次结构"></a>类加载器的层次结构</h1><p>除了引导类加载器，每个类加载器有一个父加载器（如果 parent 是null，则父加载器是引导类加载器）。</p>
<p>每个类加载器在加载类时，会首先委托给父加载器加载，父加载器找不到了，才会自己去寻找。父加载器也递归次过程。</p>
<p><img src="/images/类加载层次结构.svg" alt="类加载层次结构"></p>
<p>所以，系统类加载器只负责加载 CLASSPATH 中的类，系统类库会委派给父亲 扩展类加载器和引导类加载。这就是<strong>双亲委派</strong>。</p>
<h1 id="ClassLoader-源码分析"><a href="#ClassLoader-源码分析" class="headerlink" title="ClassLoader 源码分析"></a>ClassLoader 源码分析</h1><p>继承 <code>ClassLoader</code> 类，覆盖 <code>findClass()</code> 方法，注意在构造函数中调用 <code>super()</code> 来指定 <code>parent</code>。</p>
<p><code>ClassLoader</code> 类主要有 3 个方法：</p>
<ul>
<li><p>loadClass<br>加载入口，实现了双亲委派规则。<br>有加锁机制，具体看 5.1。</p>
</li>
<li><p>findClass</p>
<p>由子类实现。<br>需要根据类名，从磁盘或远程加载 .class 的 bytes，再调用 defineClass 得到 Class 对象。</p>
</li>
<li>defineClass<br>把 bytes 转化成 Class 对象。由 native 方法实现。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ClassLoader</span> </span>&#123;</span><br><span class="line">  Classloader parent;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">protected</span> <span class="title">ClassLoader</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 默认的构造函数，会把 parent 设为当前的系统类加载器</span></span><br><span class="line">  	<span class="keyword">this</span>(checkCreateClassLoader(), getSystemClassLoader());</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">protected</span> <span class="title">ClassLoader</span><span class="params">(ClassLoader parent)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(checkCreateClassLoader(), parent);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 加载入口，实现了双亲委派规则</span></span><br><span class="line">  <span class="keyword">protected</span> Class&lt;?&gt; loadClass(String name, <span class="keyword">boolean</span> resolve)</span><br><span class="line">        <span class="keyword">throws</span> ClassNotFoundException</span><br><span class="line">    &#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (getClassLoadingLock(name)) &#123;</span><br><span class="line">      <span class="comment">// First, check if the class has already been loaded</span></span><br><span class="line">      Class&lt;?&gt; c = findLoadedClass(name);</span><br><span class="line">      <span class="keyword">if</span> (c == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">long</span> t0 = System.nanoTime();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          <span class="keyword">if</span> (parent != <span class="keyword">null</span>) &#123;</span><br><span class="line">            c = parent.loadClass(name, <span class="keyword">false</span>);</span><br><span class="line">          &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            c = findBootstrapClassOrNull(name);</span><br><span class="line">          &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">          <span class="comment">// ClassNotFoundException thrown if class not found</span></span><br><span class="line">          <span class="comment">// from the non-null parent class loader</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (c == <span class="keyword">null</span>) &#123;</span><br><span class="line">          <span class="comment">// If still not found, then invoke findClass in order</span></span><br><span class="line">          <span class="comment">// to find the class.</span></span><br><span class="line">          <span class="keyword">long</span> t1 = System.nanoTime();</span><br><span class="line">          c = findClass(name);</span><br><span class="line"></span><br><span class="line">          <span class="comment">// this is the defining class loader; record the stats</span></span><br><span class="line">          sun.misc.PerfCounter.getParentDelegationTime().addTime(t1 - t0);</span><br><span class="line">          sun.misc.PerfCounter.getFindClassTime().addElapsedTimeFrom(t1);</span><br><span class="line">          sun.misc.PerfCounter.getFindClasses().increment();</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (resolve) &#123;</span><br><span class="line">        resolveClass(c);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> c;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 交给子类自己去实现</span></span><br><span class="line">  <span class="comment">// 根据类名，从磁盘或远程加载类。</span></span><br><span class="line">  <span class="keyword">protected</span> Class&lt;?&gt; findClass(String name) &#123;</span><br><span class="line">    <span class="keyword">throw</span> ClassNotFoundException();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 把 bytes 转化成 Class 对象。</span></span><br><span class="line">  <span class="function"><span class="keyword">protected</span> Class <span class="title">defineClass</span><span class="params">(<span class="keyword">byte</span>[] code, String name)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 调用 native 方法。</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CustomClassLoader</span> <span class="keyword">extends</span> <span class="title">ClassLoader</span> </span>&#123;</span><br><span class="line">  <span class="keyword">protected</span> Class&lt;?&gt; findClass(String name) &#123;</span><br><span class="line">    <span class="comment">// 寻找字节码</span></span><br><span class="line">    <span class="keyword">byte</span>[] code = findCodeFromSomewhere(name);</span><br><span class="line">    <span class="comment">// 组装Class对象</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.defineClass(code, name);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="loadClass-分析"><a href="#loadClass-分析" class="headerlink" title="loadClass 分析"></a>loadClass 分析</h2><p><code>loadClass</code> 的逻辑主要分为几个部分：</p>
<ol>
<li><code>synchronized (getClassLoadingLock(name))</code> 锁住整个处理逻辑。</li>
<li>查看是否已经加载了类（调用 native 方法）。</li>
<li>没有加载的话，先到父类加载器加载。</li>
<li>父类加载器没有加载到，调用 <code>findClass()</code> 加载类。</li>
<li>如果 <code>resolve</code> 为 <code>true</code>，调用 <code>resolveClass(c)</code> 对加载进来的类进行链接（link，native 方法）。</li>
</ol>
<p>可以看到，对于类的加载和链接都加了锁，而在链接阶段，会加载类的 静态域（具体看#TODO）。<br>所以，类的 静态域的初始化 是线程安全的。</p>
<h3 id="加锁逻辑"><a href="#加锁逻辑" class="headerlink" title="加锁逻辑"></a>加锁逻辑</h3><p>对于注册了并行处理的类加载器，每个类会对应一把锁。</p>
<p>否则，所有类对应一把锁（锁是 <code>this</code> 对象）。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> Object <span class="title">getClassLoadingLock</span><span class="params">(String className)</span> </span>&#123;</span><br><span class="line">  Object lock = <span class="keyword">this</span>;</span><br><span class="line">  <span class="keyword">if</span> (parallelLockMap != <span class="keyword">null</span>) &#123;</span><br><span class="line">    Object newLock = <span class="keyword">new</span> Object();</span><br><span class="line">    lock = parallelLockMap.putIfAbsent(className, newLock);</span><br><span class="line">    <span class="keyword">if</span> (lock == <span class="keyword">null</span>) &#123;</span><br><span class="line">      lock = newLock;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> lock;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注册并行锁，需要调用如下函数。注意，必须父加载器也注册了并行锁才可以。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">registerAsParallelCapable</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  Class&lt;? extends ClassLoader&gt; callerClass =</span><br><span class="line">    Reflection.getCallerClass().asSubclass(ClassLoader.class);</span><br><span class="line">  <span class="keyword">return</span> ParallelLoaders.register(callerClass);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">ParallelLoaders</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">register</span><span class="params">(Class&lt;? extends ClassLoader&gt; c)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (loaderTypes) &#123;</span><br><span class="line">      <span class="keyword">if</span> (loaderTypes.contains(c.getSuperclass())) &#123;</span><br><span class="line">        <span class="comment">// register the class loader as parallel capable</span></span><br><span class="line">        <span class="comment">// if and only if all of its super classes are.</span></span><br><span class="line">        <span class="comment">// Note: given current classloading sequence, if</span></span><br><span class="line">        <span class="comment">// the immediate super class is parallel capable,</span></span><br><span class="line">        <span class="comment">// all the super classes higher up must be too.</span></span><br><span class="line">        loaderTypes.add(c);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>类 <code>SecureClassLoader</code>、 <code>URLClassLoader</code> 都在 <code>static</code>块中调用了 <code>registerAsParallelCapable</code>，由于 <code>URLClassLoader</code> 是 <code>ExtClassLoader</code>、<code>ApClassLoader</code> 的父类，说明他们都是注册了并行锁的。</p>
<h1 id="类加载器作为命名空间"><a href="#类加载器作为命名空间" class="headerlink" title="类加载器作为命名空间"></a>类加载器作为命名空间</h1><p>类是由类的全名（包+类名） + 类加载来确定的。</p>
<p>所以，可以在两个类加载器中加载相同的类的不同版本。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        String v1dir = <span class="string">"file:///Users/qianwp/source/jcl/v1/"</span>;</span><br><span class="line">        String v2dir = <span class="string">"file:///Users/qianwp/source/jcl/v2/"</span>;</span><br><span class="line">        URLClassLoader v1 = <span class="keyword">new</span> URLClassLoader(<span class="keyword">new</span> URL[]&#123;<span class="keyword">new</span> URL(v1dir)&#125;);</span><br><span class="line">        URLClassLoader v2 = <span class="keyword">new</span> URLClassLoader(<span class="keyword">new</span> URL[]&#123;<span class="keyword">new</span> URL(v2dir)&#125;);</span><br><span class="line"></span><br><span class="line">        Class&lt;?&gt; depv1Class = v1.loadClass(<span class="string">"Dep"</span>);</span><br><span class="line">        Object depv1 = depv1Class.getConstructor().newInstance();</span><br><span class="line">        depv1Class.getMethod(<span class="string">"print"</span>).invoke(depv1);</span><br><span class="line"></span><br><span class="line">        Class&lt;?&gt; depv2Class = v2.loadClass(<span class="string">"Dep"</span>);</span><br><span class="line">        Object depv2 = depv2Class.getConstructor().newInstance();</span><br><span class="line">        depv2Class.getMethod(<span class="string">"print"</span>).invoke(depv2);</span><br><span class="line"></span><br><span class="line">        System.out.println(depv1Class.equals(depv2Class));</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="线程上下文类加载器"><a href="#线程上下文类加载器" class="headerlink" title="线程上下文类加载器"></a>线程上下文类加载器</h1><p>每个线程 <code>Thread</code> 类有一个域 <code>ClassLoader contextClassLoader</code>。可以通过 <code>getContextClassLoader</code> 和 <code>setContextClassLoader</code> 使用。</p>
<p>当创建新线程时，<code>contextClassLoader</code> 被设置为创建该线程的线程的上下文加载器。</p>
<p>主线程的上下文加载器是系统类加载器。</p>
<p>所以默认情况下（没有用 <code>setContextClassLoader</code>），所有线程的上下文加载器都是系统类加载器。</p>
<h1 id="内存泄漏"><a href="#内存泄漏" class="headerlink" title="内存泄漏"></a>内存泄漏</h1><blockquote>
<p>参考：<a href="http://www.throwable.club/2020/01/19/java-thread-context-class-loader-memory-leak-risk/">线程上下文类加载器ContextClassLoader内存泄漏隐患</a></p>
</blockquote>
<h2 id="Class-回收条件"><a href="#Class-回收条件" class="headerlink" title="Class 回收条件"></a>Class 回收条件</h2><p><code>Class</code> 被 GC 需要满足的条件：</p>
<ol>
<li>它的实例没有被强引用。</li>
<li><p>它的 <code>ClassLoader</code> 没有被强引用。</p>
</li>
<li><p>这个 <code>java.lang.Class</code> 对象没有被强引用。</p>
</li>
</ol>
<h2 id="内存泄漏场景"><a href="#内存泄漏场景" class="headerlink" title="内存泄漏场景"></a>内存泄漏场景</h2><p>对于动态加载类的场景，一般会使用自定义的类加载器。</p>
<p>正常情况下，类加载器会被 GC，它加载出的类也会就被 GC。</p>
<p>但是如果在多线程中，把自定义的类加载器设置为了 <code>ContextClassLoader</code>，这样子线程也都会使用这个自定义类加载作为 <code>ContextClassLoader</code>，虽然这些子线程都没有使用这个类加载器，却持有了它的强引用，导致动态加载的类都没有释放掉。</p>
<h2 id="具体例子"><a href="#具体例子" class="headerlink" title="具体例子"></a>具体例子</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    BlockingQueue&lt;String&gt; eventQueue = <span class="keyword">new</span> LinkedBlockingQueue&lt;&gt;();</span><br><span class="line">    BlockingQueue&lt;String&gt; classQueue = <span class="keyword">new</span> LinkedBlockingQueue&lt;&gt;();</span><br><span class="line">    Thread wrapperThread = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        ClassLoader classLoader = <span class="keyword">new</span> URLClassLoader(<span class="keyword">new</span> URL[] &#123;<span class="keyword">new</span> URL(<span class="string">"file:///Users/liuzihe/IntellijProjects/test3/"</span>)&#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// wrapperThread.loadClassThread</span></span><br><span class="line">        Thread loadClassThread = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line"></span><br><span class="line">          <span class="comment">// wrapperThread.loadClassThread.eventThread</span></span><br><span class="line">          Thread eventThread = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">              <span class="keyword">try</span> &#123;</span><br><span class="line">                String event = eventQueue.take();</span><br><span class="line">                System.out.println(<span class="string">"Receive event: "</span> + event);</span><br><span class="line">              &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">              &#125;</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;, <span class="string">"eventThread"</span>);</span><br><span class="line">          eventThread.setDaemon(<span class="keyword">true</span>);</span><br><span class="line">          eventThread.start();</span><br><span class="line"></span><br><span class="line">          <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">              String className = classQueue.take();</span><br><span class="line">              <span class="keyword">if</span> (className.equals(<span class="string">"bye"</span>)) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">              &#125;</span><br><span class="line">              Class&lt;?&gt; klass = classLoader.loadClass(className);</span><br><span class="line">              eventQueue.put(<span class="string">"Load class "</span> + className + <span class="string">" successfully!"</span>);</span><br><span class="line">              HelloService service = (HelloService) klass.newInstance();</span><br><span class="line">              service.sayHello();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">              e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line">          System.out.println(<span class="string">"loadClassThread exits"</span>);</span><br><span class="line">        &#125;, <span class="string">"loadClassThread"</span>);</span><br><span class="line"></span><br><span class="line">        loadClassThread.setContextClassLoader(classLoader); <span class="comment">// A</span></span><br><span class="line">        <span class="comment">// loadClassThread.setContextClassLoader(null); // C</span></span><br><span class="line">        loadClassThread.setDaemon(<span class="keyword">true</span>);</span><br><span class="line">        loadClassThread.start();</span><br><span class="line">        System.out.println(<span class="string">"wrapperThread exits"</span>);</span><br><span class="line">      &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;, <span class="string">"wrapperThread"</span>);</span><br><span class="line">    wrapperThread.start();</span><br><span class="line"></span><br><span class="line">    classQueue.put(<span class="string">"ziheliu.com.HelloServiceImpl2"</span>);</span><br><span class="line">    classQueue.put(<span class="string">"ziheliu.com.HelloServiceImpl3"</span>);</span><br><span class="line">    Thread.sleep(<span class="number">5000</span>);</span><br><span class="line">    classQueue.put(<span class="string">"bye"</span>); <span class="comment">// B</span></span><br><span class="line">    Thread.sleep(Integer.MAX_VALUE);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>wrapperThread 中，loadClassThread 来不断加载动态类，eventThread 来接收通知（并没有用到类加载器）。</p>
<p>如果有 A 处的代码，把loadClassThread 和 eventThread的类加载器设为自定义类加载器。</p>
<p>在 B 处关闭loadClassThread后，loadClassThread 动态加载的类依然无法释放掉。</p>
<p>使用 VIsualVM，可以看到两个类 <code>HelloServiceImpl</code> 都没有被释放掉。</p>
<p><img src="/images/ClassLoader内存泄漏例子1.png" alt="ClassLoader内存泄漏例子1"></p>
<p>而把 A 处删除，或改为 C 处的代码，就可以正常释放掉。</p>
<p><img src="/images/ClassLoader内存泄漏例子2.png" alt="ClassLoader内存泄漏例子2"></p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>在子线程不使用上下文类加载器时，</p>
<ul>
<li>把其上下文类加载器设为 null，</li>
<li>或在使用自定义类加载器的线程之前，初始化线程，这样上下文加载器是系统类加载器。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>1. Java内存区域</title>
    <url>/2020/java/jvm/1Java%E5%86%85%E5%AD%98%E5%8C%BA%E5%9F%9F/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li>《深入理解 Java 虚拟机 - 第三版》</li>
</ul>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>JVM 在执行 Java 程序过程中，所管理的内存区域如下图，共有两类：</p>
<ol>
<li><p>由所有线程共享的数据区。随虚拟机进程的启动而一直存在。</p>
</li>
<li><p>线程隔离的数据区。依赖用户线程的启动和结束而建立和销毁。</p>
</li>
</ol>
<p><img src="/images/JVM内存运行时数据区.svg" alt="JVM内存运行时数据区"></p>
<h1 id="程序计数器"><a href="#程序计数器" class="headerlink" title="程序计数器"></a>程序计数器</h1><p>当前线程所执行的字节码指令的行号指示器。</p>
<p>较小的内存空间。</p>
<ul>
<li>如果线程正在执行的是 Java 方法，它记录正在执行的虚拟机字节码指令的地址。</li>
<li>如果线程正在执行的是本地 native 方法，它的值为空（undefined）。</li>
</ul>
<h2 id="Java-虚拟机栈"><a href="#Java-虚拟机栈" class="headerlink" title="Java 虚拟机栈"></a>Java 虚拟机栈</h2><p>每个 Java 方法被执行时，JVM 会创建一个栈帧，存储局部变量表、操作数栈、动态连结、方法出口等信息。</p>
<p>方法从被调用到执行完毕的过程，对应栈帧在虚拟机栈中从入栈到出栈的过程。</p>
<h2 id="局部变量表"><a href="#局部变量表" class="headerlink" title="局部变量表"></a>局部变量表</h2><p>局部变量表存放了</p>
<ul>
<li>基本数据类型</li>
<li>对象引用</li>
<li>returnAddress 类型（指向一条字节码指令的地址）</li>
</ul>
<p>它的存储空间使用局部变量槽（Slot）来表示，64 位的 <code>long</code> <code>double</code> 占用两个变量槽。</p>
<p>局部变量表所需的内存空间，在编译期间就完成了分配。</p>
<h2 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h2><p>分配的栈过多、或栈的内存使用太多，会导致新的栈帧内存无法分配时，HotSpot 虚拟机都会抛出 <code>StackOverflowError</code>。</p>
<h2 id="JVM-参数"><a href="#JVM-参数" class="headerlink" title="JVM 参数"></a>JVM 参数</h2><ul>
<li>-Xss：栈容量。</li>
<li>-Xoss：本地方法栈容量。HotSpot 虚拟机不区分两个栈，所以这个参数没有用。</li>
</ul>
<h1 id="本地方法栈"><a href="#本地方法栈" class="headerlink" title="本地方法栈"></a>本地方法栈</h1><p>与 Java 虚拟机栈类似，为虚拟机使用到的本地 native 方法服务。</p>
<p>《Java 虚拟机规范》没有强制规定如何实现本地方法栈。HotSpot 虚拟机中，直接把 Java 虚拟机栈 和 本地方法栈合二为一。</p>
<h1 id="Java-堆"><a href="#Java-堆" class="headerlink" title="Java 堆"></a>Java 堆</h1><p>唯一目的：存放对象实例。是垃圾收集器管理的内存区域。</p>
<h2 id="对象的创建"><a href="#对象的创建" class="headerlink" title="对象的创建"></a>对象的创建</h2><ol>
<li>执行字节码 <code>new</code> 指令。<ol>
<li>类加载检查。<ol>
<li>检查指令的参数能否在常量池中定位到类的符号引用。</li>
<li>进行类的加载、解析、初始化。（如果没有进行的话）。</li>
</ol>
</li>
<li>为对象分配内存。<ul>
<li>大小在类加载完成后可以完全确定。</li>
<li>如果保证多个对象分配的线程安全？<ul>
<li>CAS + 重试保证原子性更新。</li>
<li>本地线程分配缓冲（Thread local Allocation Buffer，TLAB）。每个线程在 Java 堆中预先分配一块内存。分配内存现在线程的 TLAB 上进行，只有用完了，才去锁定。</li>
</ul>
</li>
</ul>
</li>
<li>将分配来的内存空间（除了对象头）初始化为零。</li>
<li>在对象头中设置必要的信息。</li>
</ol>
</li>
<li>执行 <code>&lt;init&gt;()</code> 方法，即构造函数。</li>
</ol>
<h2 id="对象的内存布局"><a href="#对象的内存布局" class="headerlink" title="对象的内存布局"></a>对象的内存布局</h2><ol>
<li>对象头部信息。<ol>
<li>Mark Word。对象自身运行时信息。对象哈希码、对象分代年龄、锁的信息。</li>
<li>类型指针。指向它的元数据的指针。</li>
<li>如果是 Java 数组，还需记录数组长度。</li>
</ol>
</li>
<li>实例数据部分。</li>
<li>对齐填充。<ol>
<li>HotSpot 虚拟机要求对象起始地址必须是 8 字节的整数倍。</li>
</ol>
</li>
</ol>
<h2 id="对象的访问定位"><a href="#对象的访问定位" class="headerlink" title="对象的访问定位"></a>对象的访问定位</h2><p>两种主流方式。</p>
<ul>
<li>句柄。Java 堆中分出一块内存作为句柄池。reference 存储的是对象的句柄地址。句柄包含了对象实例数据的地址、类型数据的地址。<ul>
<li>这种方式，不需要对象头存储类型指针。</li>
</ul>
</li>
<li>直接指针。reference 直接指向对象实例数据的地址，类型数据的地址存储在对象头的类型指针中。HotSpot 使用这种方式。</li>
</ul>
<h2 id="异常-1"><a href="#异常-1" class="headerlink" title="异常"></a>异常</h2><p><code>OutOfMemoryError</code> 。</p>
<h2 id="JVM-参数-1"><a href="#JVM-参数-1" class="headerlink" title="JVM 参数"></a>JVM 参数</h2><ul>
<li>堆的最小值：-Xms</li>
<li>堆的最大值：-Xmx</li>
</ul>
<h1 id="方法区"><a href="#方法区" class="headerlink" title="方法区"></a>方法区</h1><p>存储被虚拟机加载的类型信息、常量、静态变量、即时编译器编译后的代码缓存等。</p>
<h2 id="运行时常量池"><a href="#运行时常量池" class="headerlink" title="运行时常量池"></a>运行时常量池</h2><p>包含</p>
<ul>
<li>.class 文件中包含 编译时生成的字面量、符号引用。</li>
<li>运行期间放入的新的常量，例如 <code>String.intern()</code>。</li>
</ul>
<h2 id="从永久代到元空间"><a href="#从永久代到元空间" class="headerlink" title="从永久代到元空间"></a>从永久代到元空间</h2><p>在 JDK 7 以前，HotSpot 虚拟机使用永久代来实现方法区。把收集器的分代设计扩展到了方法区。</p>
<p>在 JDK 7 把字符串常量池、静态变量移到堆中。</p>
<p>在 JDK 8 把剩余内容（主要是类型信息）移到本地 native 内存的元空间中。</p>
<h2 id="异常-2"><a href="#异常-2" class="headerlink" title="异常"></a>异常</h2><p><code>OutOfMemoryError</code>。</p>
<h2 id="JVM-参数-2"><a href="#JVM-参数-2" class="headerlink" title="JVM 参数"></a>JVM 参数</h2><p>永久代大小（只对JDK 6、JDK 7中除字符串常量池、静态变量部分有效）</p>
<ul>
<li>-XX:PermSize 和 -XX:MaxPermSize。限制永久代的大小。</li>
</ul>
<p>元空间大小（只对JDK 8中类型信息）</p>
<ul>
<li>-XX:MaxMetaspaceSize。最大值。</li>
<li>-XX:MinMetaspaceSize。最小值。</li>
<li>-XX:MetaspaceSize。初始大小。</li>
</ul>
<h1 id="直接内存"><a href="#直接内存" class="headerlink" title="直接内存"></a>直接内存</h1><p>不是 《Java 虚拟机规范》 定义的内存区域。</p>
<p>使用 native 函数 <code>unsafe.allocateMemory()</code> 分配的内存，直接在堆外分配。</p>
<p>通过 Java 堆里的 <code>DirectByeteBuffer</code> 作为这块内存的引用进行操作。</p>
<h2 id="异常-3"><a href="#异常-3" class="headerlink" title="异常"></a>异常</h2><ul>
<li><p>使用  <code>ByeteBuffer.allocateDirect()</code> ，会抛出 <code>OutOfMemoryError:Direct buffer memory</code>。是在 Java 代码中，在向操作系统申请内存前，手动检查的。</p>
</li>
<li><p>使用 <code>unsafe.allocateMemory()</code>，会抛出 <code>java.lang.OutOfMemoryError\ n t sun.misc.Unsafe.allocateMemory(Native Method)</code>。是在操作系统申请内存时，抛出的。</p>
</li>
</ul>
<h2 id="JVM-参数-3"><a href="#JVM-参数-3" class="headerlink" title="JVM 参数"></a>JVM 参数</h2><ul>
<li>-XX:MaxDirectMemorySize。只在 <code>ByeteBuffer.allocateDirect()</code> 手动检查时使用。<code>unsafe</code> 分配不受它的限制。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>jvm</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>jvm</tag>
      </tags>
  </entry>
  <entry>
    <title>I/O流相关类</title>
    <url>/2020/java/io/io%E6%B5%81/</url>
    <content><![CDATA[<h1 id="I-O-流相关类"><a href="#I-O-流相关类" class="headerlink" title="I/O 流相关类"></a>I/O 流相关类</h1><h2 id="0-引用"><a href="#0-引用" class="headerlink" title="0. 引用"></a>0. 引用</h2><ul>
<li><a href="https://zhuanlan.zhihu.com/p/28286559">Java IO，硬骨头也能变软</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/31352578">PipedInputStream和PipedOutStream记录</a></li>
<li>《Java 核心技术-卷2-第二章》</li>
</ul>
<h2 id="1-三种分类方式"><a href="#1-三种分类方式" class="headerlink" title="1. 三种分类方式"></a>1. 三种分类方式</h2><h3 id="字节流和字符流"><a href="#字节流和字符流" class="headerlink" title="字节流和字符流"></a>字节流和字符流</h3><ul>
<li>字节流<ul>
<li>以字节为单位进行读写。</li>
<li>每次读写 1 byte 数据。</li>
<li>可以操作任何类型的数据。</li>
</ul>
</li>
<li>字符流<ul>
<li>以字符码元为单位进行读写。</li>
<li>每次读写 2 bytes 数据。</li>
<li>只能操作字符类型的数据。</li>
</ul>
</li>
</ul>
<h3 id="输入流和输出流"><a href="#输入流和输出流" class="headerlink" title="输入流和输出流"></a>输入流和输出流</h3><ul>
<li>输入流<ul>
<li>可以进行输入相关的操作。</li>
</ul>
</li>
<li>输出流<ul>
<li>可以进行输出相关的操作。</li>
</ul>
</li>
</ul>
<h3 id="节点流和操作流"><a href="#节点流和操作流" class="headerlink" title="节点流和操作流"></a>节点流和操作流</h3><ul>
<li>节点流<ul>
<li>直接与数据相连，数据来源或目的地是要操作的最终数据。</li>
</ul>
</li>
<li>操作流<ul>
<li>利用装饰器模式，对节点流进行包装，提供多种方便的做操方式。</li>
</ul>
</li>
</ul>
<h2 id="2-具体分类"><a href="#2-具体分类" class="headerlink" title="2. 具体分类"></a>2. 具体分类</h2><p>所有读取/写入操作都要加锁。</p>
<h3 id="2-1-输入-字节流"><a href="#2-1-输入-字节流" class="headerlink" title="2.1 输入/字节流"></a>2.1 输入/字节流</h3><h4 id="1-节点流"><a href="#1-节点流" class="headerlink" title="1) 节点流"></a>1) 节点流</h4><h5 id="FileInputStream"><a href="#FileInputStream" class="headerlink" title="FileInputStream"></a>FileInputStream</h5><ul>
<li>用于从文件读取数据。</li>
<li>只能读取字节或字节数组。</li>
</ul>
<h5 id="ByteArrayInputStream"><a href="#ByteArrayInputStream" class="headerlink" title="ByteArrayInputStream"></a>ByteArrayInputStream</h5><ul>
<li>用于从数组读取数据。</li>
<li>只能读取字节或字节数组。</li>
</ul>
<h5 id="PipedInputStream"><a href="#PipedInputStream" class="headerlink" title="PipedInputStream"></a>PipedInputStream</h5><ul>
<li>用于线程间通信，需要和 <code>PipedOutputStream</code> 一起使用。</li>
<li>使用固定长度的字节数组 buffer，在没有可读字节时阻塞。</li>
<li>只能读取字节或字节数组。</li>
</ul>
<h4 id="2-操作流"><a href="#2-操作流" class="headerlink" title="2) 操作流"></a>2) 操作流</h4><h5 id="FilterInputStream"><a href="#FilterInputStream" class="headerlink" title="FilterInputStream"></a>FilterInputStream</h5><ul>
<li>内置一个输入/字节流 inner 用来操作。</li>
<li>所有操作只是简单的调用 inner 来完成。</li>
<li>所有输入/字节/操作流的超类，除了 SequenceInputStream。</li>
</ul>
<h5 id="BufferedInputStream"><a href="#BufferedInputStream" class="headerlink" title="BufferedInputStream"></a>BufferedInputStream</h5><ul>
<li>使用字节数组 buffer 缓存数据。每次读取，先把 buffer 填满，再从 buffer 中读取。</li>
<li>只能读取字节或字节数组。</li>
</ul>
<h5 id="DataInputStream"><a href="#DataInputStream" class="headerlink" title="DataInputStream"></a>DataInputStream</h5><ul>
<li>实现了 <code>DataInput</code> 接口。<ul>
<li><code>readInt/Char/Boolean/等基本类型</code> 读取基本类型</li>
<li><code>readUTF</code> 读取 修订过的 UTF 格式的字符串。<ul>
<li>使用 <code>readUTF</code> 读取字符串。</li>
<li>2个字节的 uint 表示长度 length。</li>
<li>length 个字节组成 修订过的 UTF 编码。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h5 id="ObjectInputStream"><a href="#ObjectInputStream" class="headerlink" title="ObjectInputStream"></a>ObjectInputStream</h5><ul>
<li>用于序列化对象。</li>
<li>实现了 <code>DataInput</code> 接口。</li>
<li><code>readObject</code> 读取对象。</li>
</ul>
<h5 id="SequenceInputStream"><a href="#SequenceInputStream" class="headerlink" title="SequenceInputStream"></a>SequenceInputStream</h5><ul>
<li>从多个流依次读取数据。</li>
<li>构造函数传入任意个 InputStream。</li>
</ul>
<h3 id="2-2-输出-字节流"><a href="#2-2-输出-字节流" class="headerlink" title="2.2 输出/字节流"></a>2.2 输出/字节流</h3><h4 id="1-字节流"><a href="#1-字节流" class="headerlink" title="1) 字节流"></a>1) 字节流</h4><h5 id="FileOutputStream"><a href="#FileOutputStream" class="headerlink" title="FileOutputStream"></a>FileOutputStream</h5><ul>
<li>用于向文件写入数据。</li>
<li>只能写入字节或字节数组。</li>
</ul>
<h5 id="ByteArrayOutputStream"><a href="#ByteArrayOutputStream" class="headerlink" title="ByteArrayOutputStream"></a>ByteArrayOutputStream</h5><ul>
<li>用于向数组写入数据。</li>
<li>只能写入字节或字节数组。</li>
</ul>
<h5 id="PipedOutputStream"><a href="#PipedOutputStream" class="headerlink" title="PipedOutputStream"></a>PipedOutputStream</h5><ul>
<li>用于线程间通信，需要和 <code>PipedInputputStream</code> 一起使用。</li>
<li>在没有空间写入时阻塞。</li>
<li>只能写入字节或字节数组。</li>
</ul>
<h4 id="2-操作流-1"><a href="#2-操作流-1" class="headerlink" title="2) 操作流"></a>2) 操作流</h4><h5 id="FilterOutputStream"><a href="#FilterOutputStream" class="headerlink" title="FilterOutputStream"></a>FilterOutputStream</h5><ul>
<li>内置一个输出/字节流 inner 用来操作。</li>
<li>所有操作只是简单的调用 inner 来完成。</li>
<li>所有输出/字节/操作流的超类。</li>
</ul>
<h5 id="BufferedOutputStream"><a href="#BufferedOutputStream" class="headerlink" title="BufferedOutputStream"></a>BufferedOutputStream</h5><ul>
<li>使用字节数组 buffer （默认 1024 字节）缓存数据。每次写入，先写入 buffer，buffer 满了再写入流。</li>
<li>只能写入字节或字节数组。</li>
</ul>
<h5 id="DataOutputStream"><a href="#DataOutputStream" class="headerlink" title="DataOutputStream"></a>DataOutputStream</h5><ul>
<li>实现了 <code>DataOutput</code> 接口。<ul>
<li><code>writeInt/Char/Boolean/等基本类型</code> 写入基本类型</li>
<li><code>writeUTF</code> 写入 修订过的 UTF 格式的字符串。</li>
<li><code>writeChars</code> 写入 UTF16 格式的字符串。注意没有写入长度。</li>
</ul>
</li>
</ul>
<h5 id="ObjectOutputStream"><a href="#ObjectOutputStream" class="headerlink" title="ObjectOutputStream"></a>ObjectOutputStream</h5><ul>
<li>序列化对象。</li>
<li><code>writeObject</code> 读取对象。</li>
</ul>
<h5 id="PrintStream"><a href="#PrintStream" class="headerlink" title="PrintStream"></a>PrintStream</h5><ul>
<li>用于打印基本类型/字符串到流。<ul>
<li>都转化为字符串。</li>
</ul>
</li>
<li>性能不好。<ul>
<li>内部使用 <code>BufferedWriter</code>。</li>
<li>每个 write 操作后，会 flushBuffer，把 buffer 的内容写入到 inner 流中（没有 flush inner）。</li>
<li><code>prinln</code> 分成两次 write 操作：writer 内容 + writer 换行符。要 flushBuffer 两次。</li>
<li>开启 autoflush 后，每次写入换行符，都会调用 inner 的 flush 方法。</li>
</ul>
</li>
</ul>
<h3 id="2-3-输入-字符流"><a href="#2-3-输入-字符流" class="headerlink" title="2.3 输入/字符流"></a>2.3 输入/字符流</h3><p>除了 <code>BufferedReader.readLine()</code>，都只能读取字符/字符数组。</p>
<h4 id="1-节点流-1"><a href="#1-节点流-1" class="headerlink" title="1) 节点流"></a>1) 节点流</h4><h5 id="FileReader"><a href="#FileReader" class="headerlink" title="FileReader"></a>FileReader</h5><ul>
<li>用于从文件读取数据。</li>
<li>继承了 InputStreamReader。创建一个 fileInputStream，传递给自己。</li>
<li>只能读取字符/字符数组。</li>
</ul>
<h5 id="PipedReader"><a href="#PipedReader" class="headerlink" title="PipedReader"></a>PipedReader</h5><ul>
<li>用于线程间通信。</li>
<li>使用字符数组缓存数据。没有字符可读时，阻塞。</li>
<li>只能读取字符/字符数组。</li>
</ul>
<h5 id="CharArrayReader"><a href="#CharArrayReader" class="headerlink" title="CharArrayReader"></a>CharArrayReader</h5><ul>
<li>用于从数组读取数据。</li>
<li>只能读取字符/字符数组。</li>
</ul>
<h4 id="2-操作流-2"><a href="#2-操作流-2" class="headerlink" title="2) 操作流"></a>2) 操作流</h4><h5 id="BufferedReader"><a href="#BufferedReader" class="headerlink" title="BufferedReader"></a>BufferedReader</h5><ul>
<li>只能读取字符/字符数组。</li>
<li>使用字符数组 buffer （默认 8192 字节）缓存数据。</li>
<li>读取一行字符串。</li>
</ul>
<h5 id="InputStreamWriter"><a href="#InputStreamWriter" class="headerlink" title="InputStreamWriter"></a>InputStreamWriter</h5><ul>
<li>把 输入/字节流 转化为 输入/字符流。</li>
<li>只能读取字符/字符数组。</li>
<li>需要给定编码格式。如果不给定，则为 JVM 的默认字符集，和操作系统有关。</li>
</ul>
<h3 id="2-4-输出-字符流"><a href="#2-4-输出-字符流" class="headerlink" title="2.4 输出/字符流"></a>2.4 输出/字符流</h3><h4 id="1-节点流-2"><a href="#1-节点流-2" class="headerlink" title="1) 节点流"></a>1) 节点流</h4><h5 id="FileWriter"><a href="#FileWriter" class="headerlink" title="FileWriter"></a>FileWriter</h5><ul>
<li>用于向文件写入数据。</li>
<li>继承了 OutputStreamReader。创建一个 fileOutputStream，传递给自己。</li>
<li>只能写入字符/字符串/字符数组。</li>
</ul>
<h5 id="PipedWriter"><a href="#PipedWriter" class="headerlink" title="PipedWriter"></a>PipedWriter</h5><ul>
<li>用于线程间通信。</li>
<li>没有空间写入时，阻塞。</li>
<li>只能写入字符/字符串/字符数组。</li>
</ul>
<h5 id="CharArrayWriter"><a href="#CharArrayWriter" class="headerlink" title="CharArrayWriter"></a>CharArrayWriter</h5><ul>
<li>用于向数组写入数据。</li>
<li>只能写入字符/字符串/字符数组。</li>
</ul>
<h4 id="2-操作流-3"><a href="#2-操作流-3" class="headerlink" title="2) 操作流"></a>2) 操作流</h4><h5 id="BufferedWriter"><a href="#BufferedWriter" class="headerlink" title="BufferedWriter"></a>BufferedWriter</h5><ul>
<li>只能写入字符/字符串/字符数组。</li>
<li>使用字符数组 buffer （默认 8192 字节）缓存数据。每次写入，先写入 buffer，buffer 满了再写入流。</li>
</ul>
<h5 id="OutputStreamWriter"><a href="#OutputStreamWriter" class="headerlink" title="OutputStreamWriter"></a>OutputStreamWriter</h5><ul>
<li>把 输出/字节流 转化为 输出/字符流。</li>
<li>只能写入字符/字符串/字符数组。</li>
<li>需要给定编码格式。如果不给定，则为 JVM 的默认字符集，和操作系统有关。</li>
</ul>
<h5 id="PrintWriter"><a href="#PrintWriter" class="headerlink" title="PrintWriter"></a>PrintWriter</h5><ul>
<li>用于打印基本类型/字符串到流。</li>
<li>性能不好。<ul>
<li>每个 writer 操作，都会写入 inner 流中。</li>
<li><code>println</code> 分开两次 write 操作。</li>
<li>开启 autoflush 后，每次写入后都会调用 inner 的 flush 方法。</li>
</ul>
</li>
</ul>
<h2 id="3-例子"><a href="#3-例子" class="headerlink" title="3. 例子"></a>3. 例子</h2><p>使用 <code>connect</code>把 <code>PipedInputStream</code> 与 <code>PipedOutputStream</code>连接起来。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Sender</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">  PipedOutputStream out = <span class="keyword">new</span> PipedOutputStream();</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> PipedOutputStream <span class="title">getOut</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> out;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      out.write(<span class="string">"hello, world"</span>.getBytes(StandardCharsets.UTF_8));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">      e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Receiver</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">  PipedInputStream in = <span class="keyword">new</span> PipedInputStream();</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> PipedInputStream <span class="title">getIn</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> in;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">byte</span>[] buffer = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">100</span>];</span><br><span class="line">      in.read(buffer);</span><br><span class="line">      System.out.println(<span class="keyword">new</span> String(buffer, StandardCharsets.UTF_8));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">      e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    Sender sender = <span class="keyword">new</span> Sender();</span><br><span class="line">    Receiver receiver = <span class="keyword">new</span> Receiver();</span><br><span class="line"></span><br><span class="line">    sender.getOut().connect(receiver.getIn());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">new</span> Thread(sender).start();</span><br><span class="line">    <span class="keyword">new</span> Thread(receiver).start();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>io</category>
      </categories>
      <tags>
        <tag>io</tag>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>FileChannel与内存映射文件</title>
    <url>/2020/java/io/%E5%86%85%E5%AD%98%E6%98%A0%E5%B0%84%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="http://hg.openjdk.java.net/jdk7/jdk7/jdk/file/9b8c96f96a0f/src/share/native/java/io/io_util.c#l76">jdk/src/share/native/java/io/io_util.c</a></li>
<li><a href="http://jikesrvm.sourceforge.net/apidocs/latest/org/jikesrvm/jni/JNIFunctions.html">JNIFunctions</a></li>
<li><a href="https://www.jianshu.com/p/a23ca155f949">Java文件NIO读取的本质——FileInputStream与FileChannel对比</a></li>
<li><a href="https://www.zhihu.com/question/48161206/answer/110863886">Linux 中 mmap() 函数的内存映射问题理解？</a></li>
<li><a href="https://blog.csdn.net/zhxdick/article/details/81130102">JDK核心JAVA源码解析（5） - JAVA File MMAP原理解析</a></li>
<li><a href="https://blog.csdn.net/yangguosb/article/details/77891275">Linux零拷贝之mmap</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/91507035">Java网络编程与NIO详解8：浅析mmap和Direct Buffer</a></li>
<li><a href="https://www.cnkirito.moe/file-io-best-practise/">文件 IO 操作的一些最佳实践</a></li>
<li><a href="https://www.ibm.com/developerworks/cn/java/j-zerocopy/">通过零拷贝实现有效数据传输</a></li>
</ul>
<h1 id="FileChannel-基本用法"><a href="#FileChannel-基本用法" class="headerlink" title="FileChannel 基本用法"></a>FileChannel 基本用法</h1><h2 id="创建-FileChannel"><a href="#创建-FileChannel" class="headerlink" title="创建 FileChannel"></a>创建 FileChannel</h2><p>使用 <code>FileChannel.open</code> 创建。</p>
<p>其中 <code>OpenOption</code> 可以有：</p>
<ul>
<li>READ</li>
<li>WRITE</li>
<li>APPEND</li>
<li>CREATE：如果文件不存在，则创建一个。</li>
<li>CREATE_NEW：创建文件，如果存在抛出异常。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">FileChannel ch = FileChannel.open(Path path, OpenOption... options);</span><br></pre></td></tr></table></figure>
<p>也可以从 <code>FileInputStream</code> 或 <code>RandomAccessFile</code> 中通过 <code>getChannel()</code> 方法得到。<br>这种 channel 的文件描述符和  <code>FileInputStream</code> 或 <code>RandomAccessFile</code> 使用的是同一个，文件指针位置也是共享的。</p>
<h2 id="读写数据"><a href="#读写数据" class="headerlink" title="读写数据"></a>读写数据</h2><p>主要通过 <code>read()</code> 和 <code>write</code> 方法操作数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">read</span><span class="params">(ByteBuffer dst)</span> <span class="keyword">throws</span> IOException</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">write</span><span class="params">(ByteBuffer src)</span> <span class="keyword">throws</span> IOException</span>;</span><br></pre></td></tr></table></figure>
<h2 id="mmap-读写数据"><a href="#mmap-读写数据" class="headerlink" title="mmap 读写数据"></a>mmap 读写数据</h2><p>可以通过 <code>ch.map()</code> 方法建立一个内存映射文件的 <code>DirectByteBuffer</code>。</p>
<p>通过 <code>buffer.get()</code> 和 <code>buffer.put()</code> 方法操作 <code>ByteBuffer</code> 的方式来读写文件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ByteBuffer buffer = ch.map(FileChannel.MapMode.READ_WRITE, <span class="number">0</span>, Integer.MAX_VALUE);</span><br><span class="line">buffer.put(<span class="number">1</span>);</span><br><span class="line">buffer.put(<span class="string">'h'</span>);</span><br><span class="line">buffer.put(<span class="string">"hello"</span>.getBytes(StandardCharset.UTF_8));</span><br></pre></td></tr></table></figure>
<h2 id="Transform-数据"><a href="#Transform-数据" class="headerlink" title="Transform 数据"></a>Transform 数据</h2><p>通过 <code>transformFrom()</code> 和 <code>transformTo()</code> 把数据 从其他通道写入到该通道/从该通道写入到其他通道。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">long</span> <span class="title">transferFrom</span><span class="params">(ReadableByteChannel src,</span></span></span><br><span class="line"><span class="function"><span class="params">                                  <span class="keyword">long</span> position, <span class="keyword">long</span> count)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">long</span> <span class="title">transferTo</span><span class="params">(<span class="keyword">long</span> position, <span class="keyword">long</span> count,</span></span></span><br><span class="line"><span class="function"><span class="params">                                WritableByteChannel target)</span></span></span><br></pre></td></tr></table></figure>
<h1 id="读取文件的拷贝次数"><a href="#读取文件的拷贝次数" class="headerlink" title="读取文件的拷贝次数"></a>读取文件的拷贝次数</h1><h2 id="内核的页缓冲"><a href="#内核的页缓冲" class="headerlink" title="内核的页缓冲"></a>内核的页缓冲</h2><p>正常的文件读写，需要使用系统调用 <code>read()</code> 和 <code>write()</code>。</p>
<p>在内核有数据的页缓冲区。在读写数据时（以读数据为例），<code>read()</code> 系统调用首先到页缓冲区中看是否有足够的数据。</p>
<ul>
<li>如果没有，则去文件中，把数据拷贝到页缓冲区。</li>
<li>如果有，则直接从页缓冲区拷贝用户空间。</li>
</ul>
<h2 id="堆外内存到堆内内存"><a href="#堆外内存到堆内内存" class="headerlink" title="堆外内存到堆内内存"></a>堆外内存到堆内内存</h2><p>在 Java 中读写数据时，因为系统调用 <code>read()</code> 和 <code>write</code>，需要提供要操作的内存地址。而 Java 的堆内内存的变量的内存地址是不固定的，在一次 GC 中，可能进行重排/复制等操作。所以读取数据，需要先创建一个临时的内存区域来加载数据，再从临时区域拷贝到堆内内存。</p>
<p><code>FileInputStream.read()</code>  调用 native 的 <a href="http://hg.openjdk.java.net/jdk7/jdk7/jdk/file/9b8c96f96a0f/src/share/native/java/io/io_util.c#l76">readBytes 方法</a>。可以看到，在 JNI 方法中，申请了一块 <code>buf</code> 字符数组，读取数据后，使用 <a href="http://jikesrvm.sourceforge.net/apidocs/latest/org/jikesrvm/jni/JNIFunctions.html">env.SetByteArrayRegion</a> 把 <code>buf</code> 内容拷贝 Java 的 byteArray中。</p>
<p>经历的拷贝过程：磁盘 —&gt; 内核页缓冲 —&gt; JNI 临时内存 —&gt; Java 堆内内存。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">jint</span><br><span class="line">readBytes(JNIEnv *env, jobject <span class="keyword">this</span>, jbyteArray bytes,</span><br><span class="line">          jint off, jint len, jfieldID fid)</span><br><span class="line">&#123;</span><br><span class="line">    jint nread;</span><br><span class="line">    <span class="keyword">char</span> stackBuf[BUF_SIZE];</span><br><span class="line">    <span class="keyword">char</span> *buf = <span class="literal">NULL</span>;</span><br><span class="line">    FD fd;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (len == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (len &gt; BUF_SIZE) &#123;</span><br><span class="line">        buf = <span class="built_in">malloc</span>(len);</span><br><span class="line">        <span class="keyword">if</span> (buf == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            JNU_ThrowOutOfMemoryError(env, <span class="literal">NULL</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        buf = stackBuf;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fd = GET_FD(<span class="keyword">this</span>, fid);</span><br><span class="line">    <span class="keyword">if</span> (fd == <span class="number">-1</span>) &#123;</span><br><span class="line">        JNU_ThrowIOException(env, <span class="string">"Stream Closed"</span>);</span><br><span class="line">        nread = <span class="number">-1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        nread = (jint)IO_Read(fd, buf, len);</span><br><span class="line">        <span class="keyword">if</span> (nread &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            (*env)-&gt;SetByteArrayRegion(env, bytes, off, nread, (jbyte *)buf);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (nread == JVM_IO_ERR) &#123;</span><br><span class="line">            JNU_ThrowIOExceptionWithLastError(env, <span class="string">"Read error"</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (nread == JVM_IO_INTR) &#123;</span><br><span class="line">            JNU_ThrowByName(env, <span class="string">"java/io/InterruptedIOException"</span>, <span class="literal">NULL</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123; <span class="comment">/* EOF */</span></span><br><span class="line">            nread = <span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (buf != stackBuf) &#123;</span><br><span class="line">        <span class="built_in">free</span>(buf);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nread;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>FileChannel.read()</code> 方法，会有两种情况。</p>
<ul>
<li><code>read</code> 传入的是 <code>DirectByteBuffer</code>，因为堆外内存一般不会 GC（什么时候GC？TODO），所以有固定的内存地址，不需要先复制到临时内存中。<br>经历的拷贝过程：磁盘 —&gt; 内核页缓冲 —&gt; Java 堆外内存。</li>
<li><code>read</code> 传入的是 <code>ByteBuffer</code>，需要先存储到临时的堆外内存，再拷贝到堆内内存。<br>经历的拷贝过程：磁盘 —&gt; 内核页缓冲 —&gt; Java 堆外内存 —&gt; Java 堆外内存。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">read</span><span class="params">(FileDescriptor var0, ByteBuffer var1, <span class="keyword">long</span> var2, NativeDispatcher var4)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (var1.isReadOnly()) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Read-only buffer"</span>);</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (var1 <span class="keyword">instanceof</span> DirectBuffer) &#123; <span class="comment">// 如果是堆外内存，不需要申请暂时的内存区域</span></span><br><span class="line">    <span class="keyword">return</span> readIntoNativeBuffer(var0, var1, var2, var4);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    ByteBuffer var5 = Util.getTemporaryDirectBuffer(var1.remaining());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> var7;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">int</span> var6 = readIntoNativeBuffer(var0, var5, var2, var4);</span><br><span class="line">      var5.flip();</span><br><span class="line">      <span class="keyword">if</span> (var6 &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        var1.put(var5);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      var7 = var6;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      Util.offerFirstTemporaryDirectBuffer(var5);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> var7;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="mmap-原理"><a href="#mmap-原理" class="headerlink" title="mmap 原理"></a>mmap 原理</h1><h2 id="创建和读写过程"><a href="#创建和读写过程" class="headerlink" title="创建和读写过程"></a>创建和读写过程</h2><p><code>mmap</code> 系统调用，会建立一个 <code>vm_area_struct</code> 的数据结构，这个数据结构表示了进程虚拟地址空间中的一段，它把磁盘文件的区域和虚拟地址空间中的一段连接了起来。但是这时候物理内存中还是没有内容的，而 mmu 是在虚拟地址空间和物理内存地址之间建立映射的。</p>
<p>在访问到这段虚拟地址空间时，由于只是建立了映射，而实际的页面还没有放置到页框中，所以会产生一个缺页中断陷入内核，然后会首先在硬盘的swap页面中寻找这个页面是否被交换出去，如果没有找到这个页面，那么会依照mmap建立的vm_area_struct这个数据结构把磁盘中的文件读到物理内存中，这样就可以访问了。</p>
<p>所以，使用 <code>mmap</code> 经历的拷贝过程：磁盘 —&gt; Java 堆外内存。</p>
<h2 id="限制"><a href="#限制" class="headerlink" title="限制"></a>限制</h2><p><code>mmap</code> 创建的堆外内存，不会受 <code>-XX:MaxDirectMemorySize</code> 限制。</p>
<p>这个参数只有在使用 <code>ByteBuffer.allocateDirect</code> 的时候，才会检查，而在使用 <code>allocateMemory</code> JNI 方法和 <code>mmap</code> 的时候，不会检查。</p>
<p>但是 <code>mmap</code> 一次的大小不能超过 <code>Integer.MAX_INT</code> （约 2 G）。因为 <code>mmap</code> 最后会调用如下的 JNI 方法，其中的地址会被转为 int。猜测是因为 Java 为了跨平台，并不知道平台是 32 G 还是 64 G的。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span>* <span class="title">addr_from_java</span><span class="params">(jlong addr)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// This assert fails in a variety of ways on 32-bit systems.</span></span><br><span class="line">  <span class="comment">// It is impossible to predict whether native code that converts</span></span><br><span class="line">  <span class="comment">// pointers to longs will sign-extend or zero-extend the addresses.</span></span><br><span class="line">  <span class="comment">//assert(addr == (uintptr_t)addr, "must not be odd high bits");</span></span><br><span class="line">  <span class="comment">//转换为int</span></span><br><span class="line">  <span class="keyword">return</span> (<span class="keyword">void</span>*)(<span class="keyword">uintptr_t</span>)addr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="文件拷贝与-FileChannel-transformTo"><a href="#文件拷贝与-FileChannel-transformTo" class="headerlink" title="文件拷贝与 FileChannel.transformTo"></a>文件拷贝与 FileChannel.transformTo</h1><ul>
<li><a href="https://www.ibm.com/developerworks/cn/java/j-zerocopy/">通过零拷贝实现有效数据传输</a></li>
</ul>
<p>把数据从一个文件拷贝到另外一个文件。</p>
<p>传统的数据传输，需要先调用 <code>in.read()</code>，在调用 <code>out.write()</code>。需要 4 次拷贝、4 次内核/用户态切换。磁盘 —&gt; 读内核页缓冲区 —&gt; 用户空间 —&gt; 写内核页缓冲区 —&gt; 写入磁盘。</p>
<p>传统方法的数据拷贝：</p>
<p><img src="/images/传统-拷贝.gif" alt="传统-拷贝"></p>
<p>传统方法的上下文切换：</p>
<p><img src="/images/传统-上下文切换.gif" alt="传统-上下文切换"></p>
<p>而 <code>FileChannel.transformTo</code> 可以无需 用户空间的接入，之即完成数据的传输。需要 3 次拷贝、2次切换：磁盘 —&gt; 读内核页缓冲区  —&gt; 写内核页缓冲区 —&gt; 写入磁盘。</p>
<p>这个操作依赖底层操作系统对零拷贝的支持；在 UNIX 和各种 Linux 系统中，此调用被传递到 <code>sendfile()</code> 系统调用中。</p>
<p>transformTo 的数据拷贝：</p>
<p><img src="/images/transferTo-拷贝.gif" alt="transferTo-拷贝"></p>
<p>transformTo 的上下文切换：</p>
<p><img src="/images/transferTo-上下文切换.gif" alt="transferTo-上下文切换"></p>
<p>如果底层网络接口卡支持<em>收集操作</em> 的话，那么我们就可以进一步减少内核的数据复制。在 Linux 内核 2.4 及后期版本中，套接字缓冲区描述符就做了相应调整，以满足该需求。需要2次拷贝、2次切换。</p>
<p>transformTo + 收集 的数据拷贝：</p>
<p><img src="/images/transferTo-拷贝2.gif" alt="transferTo-拷贝2"></p>
]]></content>
      <categories>
        <category>java</category>
        <category>io</category>
      </categories>
      <tags>
        <tag>io</tag>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 基础知识汇总</title>
    <url>/2020/java/basic/java%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E6%B1%87%E6%80%BB/</url>
    <content><![CDATA[<p>Java 区分大小写。</p>
<p>源代码的文件名必须与公共类的名字相同。</p>
<p>main 方法必须声明为 public。</p>
<p>允许在运行时确定数组的长度。</p>
<p>javabeans TODO</p>
<h1 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h1><p>“is-a” 关系是继承的一个明显特征。</p>
<p>继承使用 <code>extends</code> 来表示。</p>
<p>Java 只存在公有继承。</p>
<p>超类不能使用子类定义的方法和域。</p>
<p>子类方法不能访问超类的 <code>private</code> 域，只能通过公有的接口访问。</p>
<h2 id="super-关键字"><a href="#super-关键字" class="headerlink" title="super 关键字"></a><code>super</code> 关键字</h2><h3 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h3><ul>
<li>调用超类的方法。</li>
<li>调用超类的构造函数。只能出现在子类函数的第一条语句。</li>
</ul>
<h3 id="与-this-的不同"><a href="#与-this-的不同" class="headerlink" title="与 this 的不同"></a>与 <code>this</code> 的不同</h3><ul>
<li>不是对象的引用。</li>
<li>用来指示编译器调用超类方法的关键字。</li>
</ul>
<h2 id="多态与动态绑定"><a href="#多态与动态绑定" class="headerlink" title="多态与动态绑定"></a>多态与动态绑定</h2><h3 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h3><h4 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h4><p>一个对象变量可以指示多种实际类型的现象（一个 <code>ClassA</code> 变量可以引用 <code>ClassA</code> 类对象、它的子类对象）。</p>
<h4 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h4><ul>
<li>虚拟机知道变量实际引用的对象类型，可以正确地调用相应的方法。</li>
<li>TODO<ul>
<li>静态类型（外观类型），实际类型</li>
<li>重载。静态分派。<ul>
<li>编译器阶段。取决于传入参数数量、类型（包括第一个静态类型参数）。</li>
</ul>
</li>
<li>重写。动态分派。<ul>
<li>运行阶段。<code>invokevirtual</code> 指令先找第一个参数的动态类型里的方法，再向上找。<ul>
<li>会用虚表优化。</li>
</ul>
</li>
<li>因为使用域时，没有<code>invokevirtual</code>指令。所以只能使用静态类型的域。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="子类数组"><a href="#子类数组" class="headerlink" title="子类数组"></a>子类数组</h4><p><strong>子类</strong>数组的引用可以隐式转化成<strong>父类</strong>数组的引用。</p>
<p>但是数组会记住创建它们的真实元素类型，只允许类型兼容的引用存储到数组中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Child[] children = <span class="keyword">new</span> Child[<span class="number">10</span>];</span><br><span class="line">Parent[] parents = children; <span class="comment">// 合法</span></span><br><span class="line">parents[<span class="number">0</span>] = <span class="keyword">new</span> Parent(); <span class="comment">// throw ArrayStoreException</span></span><br></pre></td></tr></table></figure>
<h4 id="强制类型转换"><a href="#强制类型转换" class="headerlink" title="强制类型转换"></a>强制类型转换</h4><p>超类转化为子类，需要进行强制类型转换。</p>
<p>如果转换失败，throw ClassCastException。</p>
<p>在转换之前，最好用 <code>item instanceof ClassType</code> 来检测。</p>
<h3 id="3-2-2-动态绑定"><a href="#3-2-2-动态绑定" class="headerlink" title="3.2.2 动态绑定"></a>3.2.2 动态绑定</h3><ul>
<li>在运行时能够自动选择调用哪个方法的现象。</li>
<li>动态绑定是默认的处理方式。</li>
</ul>
<h3 id="3-2-3-方法调用的过程"><a href="#3-2-3-方法调用的过程" class="headerlink" title="3.2.3 方法调用的过程"></a>3.2.3 方法调用的过程</h3><p>调用 <code>item.func(param)</code>：</p>
<ol>
<li>编译器获得所有可能的候选方法。根据对象的声明类型（<code>item</code> 的实际类型）、方法名，找到类的所有 <code>func</code> 方法、超类的所有 <code>public</code> 修饰的 <code>func</code> 方法。</li>
<li>匹配提供的参数类型（除了一个隐式参数）。TODO</li>
<li>如果是 <code>private</code>、<code>static</code>、<code>final</code> 方法，是<strong>静态绑定</strong>，无法被继承，编译器知道应用调用的确切方法。否则，编译器采用动态绑定的方式生成一条调用 <code>func</code> 的指令。</li>
<li>在程序运行时，如果 <code>item.func</code> 是动态绑定方式调用的，虚拟机找到与 <code>item</code> 所引用对象的实际类型 <code>D</code> 最合适的类的 <code>func</code>方法。首先看类 <code>D</code> 是否有对应的方法，再看超类，以此类推。<br>为了节约开销，虚拟机会为每一个类生成方法表，不需要每次都进行动态搜索。</li>
</ol>
<h2 id="重载-overload-和-覆写-override"><a href="#重载-overload-和-覆写-override" class="headerlink" title="重载 overload 和 覆写 override"></a>重载 overload 和 覆写 override</h2><p>函数的签名：方法名  + 参数列表。注意，<strong>返回值</strong>不属于签名！</p>
<p>因此，</p>
<ul>
<li>重载时，不可以声明两个函数名、参数列表都相同，只有返回值不同的函数。</li>
<li>覆写时，子类的函数返回值可以与父类不同，但是必须是兼容的。即子类的返回值必须是父类的返回值类型、或其子类型。否则，编译错误。</li>
</ul>
<p>对于覆写的方法，加上 <code>@override</code> 修饰，如果这个方法没有真的覆写父类的方法，编译错误。</p>
<p>覆写方法的访问权限要大于等于原方法的权限。（本来是public，子类的方法也必须是public）。</p>
<p>覆写方法可以指定更加严格的返回类型。</p>
<h3 id="3-4-final-类和方法，内联"><a href="#3-4-final-类和方法，内联" class="headerlink" title="3.4 final 类和方法，内联"></a>3.4 final 类和方法，内联</h3><h4 id="final-关键字"><a href="#final-关键字" class="headerlink" title="final 关键字"></a>final 关键字</h4><p><code>final</code> 修饰的方法，子类不可以覆盖它。</p>
<p><code>final</code> 修饰的类，不能有子类继承它。</p>
<ul>
<li>其中方法自动被 <code>final</code> 修饰，但是域不会。</li>
</ul>
<h4 id="内联"><a href="#内联" class="headerlink" title="内联"></a>内联</h4><ul>
<li><p>如果一个方法没有被覆盖并且很短，编译器会优化它，进行内联。</p>
</li>
<li><p>虚拟机的即时编译器，检测方法没有真正的覆盖、并且被频繁调用，会对它进行内联处理。</p>
</li>
<li>TODO 即时编译器？</li>
</ul>
<p>优点：</p>
<ul>
<li>使用分支转移（函数调用），会扰乱 CPU 预取指令的策略。</li>
</ul>
<h2 id="abstract-关键字"><a href="#abstract-关键字" class="headerlink" title="abstract 关键字"></a>abstract 关键字</h2><p><code>abstract</code> 修饰的方法，不需要有函数体。</p>
<p>有抽象方法的类，必须用 <code>abstract</code> 修饰。但是 <code>abstract</code> 修饰的类，不一样要有抽象方法。</p>
<p>子类如果没有实现全部的抽象方法，必须也用 <code>abstract</code> 修饰类。</p>
<p>抽象类可以包含具有函数体的方法。</p>
<h2 id="Object-类"><a href="#Object-类" class="headerlink" title="Object 类"></a>Object 类</h2><ul>
<li><p>是所有类的超类。</p>
</li>
<li><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object obj = <span class="keyword">new</span> Employee(<span class="string">"name"</span>, <span class="number">100</span>);</span><br><span class="line">((Employee) obj).func();</span><br></pre></td></tr></table></figure>
</li>
<li><p>数组也扩展了 Object 类。</p>
</li>
</ul>
<h3 id="equals-函数"><a href="#equals-函数" class="headerlink" title="equals 函数"></a>equals 函数</h3><p>Java语言规范要求 <code>equals</code> 方法具有 5 个特性：</p>
<ol>
<li>自反性</li>
<li>对称性</li>
<li>传递性</li>
<li>一致性</li>
<li>对于非空引用 <code>item</code>，<code>item.equals(null)</code> 应该返回false。</li>
</ol>
<p>编写 <code>equals</code> 的步骤：</p>
<ol>
<li>参数命名为 <code>otherOject</code>，之后转化后的命名为 <code>other</code>。</li>
<li>检测 <code>this</code> 与 <code>otherObject</code> 是否引用了相同的对象。</li>
<li>检测 <code>otherObject</code> 是否为 null。</li>
<li>比较 <code>this</code> 与 <code>otherObject</code> 是否属于同一个类，用 <code>instanceof</code> 或 <code>getClass()</code>。</li>
<li>将 <code>otherObject</code> 转化为具体的类型对象 <code>other</code>。</li>
<li>进行域的比较。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object otherObject)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span> == otherObject) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (otherObject == <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 或者 不同子类可以进行比较</span></span><br><span class="line">  <span class="comment">// if (!(otherOBject instance of ParentClass)) &#123;</span></span><br><span class="line">  <span class="comment">// 	return false;</span></span><br><span class="line">  <span class="comment">// &#125;</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span>.getClass() != otherObject.getClass()) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  Item other = (Item) otherObject;</span><br><span class="line">  <span class="keyword">return</span> num == other.num;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="hashCode-函数"><a href="#hashCode-函数" class="headerlink" title="hashCode 函数"></a>hashCode 函数</h3><p><code>equals</code> 函数 与 <code>hashCode</code> 函数的定义必须一致。</p>
<ul>
<li>如果 <code>x.equals(y)== true</code> ，那么 <code>x.hashCode()== y.hashCode()</code>。</li>
</ul>
<p>静态方法 <code>Object.hashCode(item)</code>，如果 item 是 null，会返回0，否则调用 item 的 hashCode 方法。</p>
<p>静态方法 <code>Object.hash(item1, item2, ...)</code> 可以提供多个参数。</p>
<p>类的 <code>hashCode()</code> 方法，默认返回引用的地址。</p>
<h3 id="toString-函数"><a href="#toString-函数" class="headerlink" title="toString 函数"></a>toString 函数</h3><p>对象与一个字符串通过 <code>+</code> 连接起来，会自动调用对象的 <code>toString()</code> 函数。</p>
<p><code>&quot;&quot; + x</code> 与 <code>x.toString()</code> 等同，但是 <code>x</code> 是基本类型时，不能用 <code>x.toString()</code>。</p>
<p>默认方法，输出 类型 + 散列码。</p>
<h2 id="包装类、自动装箱和拆箱"><a href="#包装类、自动装箱和拆箱" class="headerlink" title="包装类、自动装箱和拆箱"></a>包装类、自动装箱和拆箱</h2><h3 id="包装类"><a href="#包装类" class="headerlink" title="包装类"></a>包装类</h3><p>8 个类：Long、Integer、Short、Byte，Double、Float，Boolean，Void。</p>
<p>这些类的值是 <code>final</code> 的，不可变。</p>
<p>类也是 <code>final</code> 的，不可被继承。</p>
<p>因为是类，所以进行算数运算时，可能 throw NullPointerException。</p>
<h3 id="自动拆箱和装箱"><a href="#自动拆箱和装箱" class="headerlink" title="自动拆箱和装箱"></a>自动拆箱和装箱</h3><p>发生在编译期间，编译器补全拆箱和装箱的代码。</p>
<h3 id="性能差"><a href="#性能差" class="headerlink" title="性能差"></a>性能差</h3><ul>
<li><p><code>ArrayList&lt;Integer&gt;</code> 的每个值包在一个对象里，性能远低于 <code>int[]</code>。</p>
</li>
<li><p>算数运算可能进行频繁的拆箱、装箱。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer n = <span class="number">1</span>;</span><br><span class="line">Double x = <span class="number">2.0</span>;</span><br><span class="line">System.out.println(ok ? n : x); <span class="comment">// n 先拆箱，再转为 double，再装箱为 Double</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="相等判断"><a href="#相等判断" class="headerlink" title="相等判断"></a>相等判断</h3><p>包装类在比较时，最好使用 <code>equals</code>。</p>
<p>因为 <code>==</code> 可能无法得到预期的结果。</p>
<p>boolean/byte/char &lt;= 127/-128-127的short和int，会被包装到固定的对象中。此时，值相等，用 <code>==</code> 判断也会相等。</p>
<h2 id="参数数量可变方法"><a href="#参数数量可变方法" class="headerlink" title="参数数量可变方法"></a>参数数量可变方法</h2><p>函数最后一个参数可以是 <code>ClassName... args</code>，编译器会替换为 <code>ClassName[] args</code>。<code>func(item1, item2)</code> 会替换为 <code>func(ClassName[] {item1, item2})</code>。</p>
<h2 id="枚举类"><a href="#枚举类" class="headerlink" title="枚举类"></a>枚举类</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Size &#123;</span><br><span class="line">  SMALL,</span><br><span class="line">  MEDIUM,</span><br><span class="line">  LARGE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实际上，<code>Size</code> 是定义了一个类。<code>SMALL</code>、<code>MEDIUM</code>、<code>LARGE</code> 是类 <code>Size</code> 的三个实例。比较枚举的值，使用 <code>==</code> 即可。</p>
<p>因为它是类，所以可以添加 <code>private</code> 构造函数。<br><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Size &#123;</span><br><span class="line">  SMALL(<span class="number">0</span>),</span><br><span class="line">  MEDIUM(<span class="number">1</span>),</span><br><span class="line">  LARGE(<span class="number">2</span>);</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> value;</span><br><span class="line">  <span class="function"><span class="keyword">private</span> <span class="title">Size</span><span class="params">(<span class="keyword">int</span> value)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.value = value;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getValue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> value;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h1><h2 id="相关函数"><a href="#相关函数" class="headerlink" title="相关函数"></a>相关函数</h2><h3 id="Arrays-sort"><a href="#Arrays-sort" class="headerlink" title="Arrays.sort"></a>Arrays.sort</h3><p>调用 <code>Arrays.sort(items)</code>，数组中的元素必须实现了 <code>Comparable&lt;T&gt;</code>。</p>
<p>调用 <code>Arrays.sort(items, comparator)</code>，提供一个实现了 <code>Comparator&lt;T&gt;</code> 接口的对象即可。</p>
<h3 id="instanceof"><a href="#instanceof" class="headerlink" title="instanceof"></a>instanceof</h3><p><code>instanceof</code> 可以检查一个对象是否实现了一个接口。</p>
<h3 id="Cloneable"><a href="#Cloneable" class="headerlink" title="Cloneable"></a>Cloneable</h3><p><code>Object.clone</code> 方法</p>
<ul>
<li>是浅拷贝。</li>
<li>如果要深拷贝，需要再克隆每个非基本变量域。</li>
<li>是 <code>protected</code> 的，所以如果要克隆对象，需要 <code>override</code> 这个方法，调用 <code>super.clone()</code>。<ul>
<li>并且需要实现 <code>Cloneable</code> 接口。否则会在运行时抛出 <code>CloneNotSupportedException</code>。</li>
</ul>
</li>
</ul>
<p><code>Cloneable</code> 是一个空的接口，<strong>标记接口</strong>，作用是允许在类型查询中使用 <code>instanceof</code>。</p>
<h2 id="接口的特性"><a href="#接口的特性" class="headerlink" title="接口的特性"></a>接口的特性</h2><h3 id="接口的方法和域"><a href="#接口的方法和域" class="headerlink" title="接口的方法和域"></a>接口的方法和域</h3><p>接口的所有<strong>方法</strong>自动属于 <code>public</code>。</p>
<p>接口的所有<strong>域</strong>都会自动属于 <code>public static final</code>。所以接口只能有常量，不能有实例域。</p>
<p>Java SE 8 中，接口可以有<strong>静态方法</strong>。避免使用伴随类（例如，Collection 接口 + Collections 抽象类）。</p>
<p>使用 <code>defualt</code> 修饰方法，可以为接口方法提供默认实现。实现类可以不实现这些方法。</p>
<h3 id="默认方法冲突规则"><a href="#默认方法冲突规则" class="headerlink" title="默认方法冲突规则"></a>默认方法冲突规则</h3><ol>
<li>类实现的<strong>两个接口</strong>有相同签名的方法<ul>
<li>只要其中一个方法有默认实现，就会编译错误。</li>
</ul>
</li>
<li>类实现的<strong>接口</strong>和继承的<strong>超类</strong>有相同签名的方法<ul>
<li>超类的方法优先，接口方法的默认实现被忽略。</li>
</ul>
</li>
</ol>
<h3 id="继承和实现相关"><a href="#继承和实现相关" class="headerlink" title="继承和实现相关"></a>继承和实现相关</h3><p>接口可以被其他接口继承 <code>extends</code>。</p>
<h4 id="为什么有了抽象类，还需要接口？"><a href="#为什么有了抽象类，还需要接口？" class="headerlink" title="为什么有了抽象类，还需要接口？"></a>为什么有了抽象类，还需要接口？</h4><p>一个类可以实现多个接口，但是只能继承一个类。</p>
<h1 id="lambda-表达式"><a href="#lambda-表达式" class="headerlink" title="lambda 表达式"></a>lambda 表达式</h1><h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><p>代码块 + 必须传入代码块的变量的规范。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(String first, String second) -&gt; &#123;</span><br><span class="line">  <span class="keyword">return</span> first.length() - second.length();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果可以推导出参数的类型，可以省略类型。</p>
<p>如果参数只有一个，并且可以推导类型，可以省略 <code>()</code>。</p>
<p>不用指定返回类型，从上下文推导出的。</p>
<p>如果代码块只有一个语句，可以省略<code>{}</code>、<code>return</code> 关键字。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(first, second) -&gt; first.length() - second.length();</span><br></pre></td></tr></table></figure>
<h2 id="函数式接口"><a href="#函数式接口" class="headerlink" title="函数式接口"></a>函数式接口</h2><h3 id="定义-1"><a href="#定义-1" class="headerlink" title="定义"></a>定义</h3><p>对于只有一个<strong>抽象方法</strong>的接口，需要接口的对象时，可以提供一个 lambda 表达式。</p>
<p>在这个对象上调用这个方法时，会执行 lambda 表达式的体。</p>
<p>可以在这个接口定义时，增加 <code>@FunctionalInterface</code> 注解。好处：</p>
<ul>
<li>如果有多于一个的抽象方法，会编译错误。</li>
<li>javadoc 会指出这个借口是函数式接口。</li>
</ul>
<h3 id="Object-相关"><a href="#Object-相关" class="headerlink" title="Object 相关"></a>Object 相关</h3><p>注意，lambda 表达式只能赋值给一个接口变量，不能复制给 <code>Object</code>（因为 <code>Object</code> 不是一个接口）。</p>
<h3 id="BiFunction"><a href="#BiFunction" class="headerlink" title="BiFunction"></a>BiFunction</h3><p><code>java.util.function</code> 提供了很多通用函数接口，例如 <code>BiFunction&lt;T, U, R&gt;</code> 描述参数类型为 <code>T</code>和 <code>U</code> 而且返回类型为 <code>R</code> 的函数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">BiFunction&lt;T, U, R&gt; func = (first, second) -&gt; first.length() - second.length();</span><br></pre></td></tr></table></figure>
<p>但是使用接口和函数式接口的地方不能传递一个 <code>BiFunction</code>。</p>
<h2 id="方法引用"><a href="#方法引用" class="headerlink" title="方法引用"></a>方法引用</h2><p>形如 <code>System.out::println</code> 是一个方法引用，等价于 lambda 表达式 <code>x -&gt; System.out.println(x)</code>。</p>
<p>共 3 类：</p>
<ul>
<li><code>object::instanceFunc</code>：<code>x -&gt; object.instanceFunc(x)</code>。</li>
<li><code>ClassName::staticFunc</code>：<code>x -&gt; ClassName.staticFunc(x)</code>。</li>
<li><code>ClassName::instanceFunc</code>：<code>(x, y) -&gt; x.instanceFunc(y)</code>。</li>
</ul>
<p>关于 <code>this</code> 和 <code>super</code>：</p>
<ul>
<li><code>this::instanceFunc</code>：<code>x -&gt; this.instanceFunc(x)</code>。</li>
<li><code>super::instanceFunc</code>：<code>x -&gt; super.instanceFunc(x)</code>。</li>
</ul>
<p>多个同名的重载函数时，根据上下文找到最合适的函数。</p>
<h2 id="构造器引用"><a href="#构造器引用" class="headerlink" title="构造器引用"></a>构造器引用</h2><p>与方法引用类似，方法名为 <code>new</code>。例如 <code>Person::new</code>。</p>
<p>可以有数组的构造器引用，<code>int[]::new</code>。</p>
<p>Java 无法构造泛型 <code>T</code> 的数组，即不可以使用 <code>new T[10]</code>。<code>stream.toArray(Person[]::new)</code> 可以用类似泛型的方式构造数组。</p>
<h2 id="变量作用域"><a href="#变量作用域" class="headerlink" title="变量作用域"></a>变量作用域</h2><p>引用的外部变量，必须是不可变的，相当于必须是 <code>final</code> 的，但是不用使用 <code>final</code> 修饰符。</p>
<p>lambda 表达式的体与嵌套块有相同的作用域。</p>
<ul>
<li><p>因此不能声明和嵌套块中的同名变量。</p>
</li>
<li><p>体中的 <code>this</code> 指的是嵌套块所在对象，不是函数式接口这个对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Application</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ActionListener linstener = event -&gt; &#123;</span><br><span class="line">      <span class="comment">// this 是 Application，而不是 ActionListener</span></span><br><span class="line">      Systme.out.println(<span class="keyword">this</span>.toString());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="Comparator"><a href="#Comparator" class="headerlink" title="Comparator"></a>Comparator</h2><p><code>Comparator.comparing(Person::getName)</code> 提供“键提取函数”。</p>
<p><code>Comparator.comparing(Person::getFirstName).thenComparing(Person::getSecondName)</code> 可以和thenComparing 串起来。</p>
<p>comparing 和 thenComparing 可以提供第二个参数，是一个 Comparator 对象。<code>Comparator.comparing(Person::getName, (x, y) -&gt; x &gt; y)</code>。</p>
<p><code>Comparator</code> 的 <code>nullFirst</code> 和 <code>nullSecond</code> 方式使装饰器，接收一个 Comparator 对象，遇到 null 时，标记为小于正常值 或 大于正常值。 </p>
<h1 id="内部类"><a href="#内部类" class="headerlink" title="内部类"></a>内部类</h1><p>内部类是定义在另一个类中的类。</p>
<p>3 个优点：</p>
<ul>
<li>内部类可以访问该类定义所在作用域的数据，包括 <code>private</code> 的数据。</li>
<li>内部类可以对同一个包中的其他类隐藏起来。</li>
<li>匿名内部类实现回调函数方便。</li>
</ul>
<h2 id="内部类访问外部对象"><a href="#内部类访问外部对象" class="headerlink" title="内部类访问外部对象"></a>内部类访问外部对象</h2><h3 id="编译器转化形式"><a href="#编译器转化形式" class="headerlink" title="编译器转化形式"></a>编译器转化形式</h3><p>内部类之所以可以访问创建它的外部类对象的数据域，是因为内部类的对象有一个隐式的外部类对象的引用。是编译器加上去的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TalkingClock</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">boolean</span> keep;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TimePrinter</span> <span class="keyword">implements</span> <span class="title">ActionListener</span> </span>&#123;</span><br><span class="line">    <span class="meta">@override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">actionPerformed</span><span class="params">(ActionEvent event)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (keep) &#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">start</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    TimePrinter t = <span class="keyword">new</span> TimePrinter();</span><br><span class="line">    <span class="keyword">new</span> Timer(<span class="number">1000</span>, t).start();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>会被编译器添加内部类的关于 <code>outer</code> 的构造函数、生成内部类对象时添加 <code>this</code> 参数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TalkingClock</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">boolean</span> keep;</span><br><span class="line">  </span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">TimePrinter</span> <span class="keyword">implements</span> <span class="title">ActionListener</span> </span>&#123;</span><br><span class="line">    TalkingClock outer;</span><br><span class="line">    TimePrinter(TalkingClock outer) &#123;</span><br><span class="line">      <span class="keyword">this</span>.outer = outer;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">actionPerformed</span><span class="params">(ActionEvent event)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (keep) &#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">start</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    TimePrinter t = <span class="keyword">new</span> TimePrinter(<span class="keyword">this</span>);</span><br><span class="line">    <span class="keyword">new</span> Timer(<span class="number">1000</span>, t).start();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>实际上，内部类会被翻译成 <code>TalkingClock$TimePrinter</code> 的类形式。</p>
<ul>
<li>自己无法编写类似内部类的形式，因为内部类可以访问外部类的 <code>private</code> 域，但是自己定义的不可以。</li>
<li>如何实现的访问私有域的：<code>TalkingClock.access$0(outer)</code>。</li>
</ul>
<h3 id="内外访问方式"><a href="#内外访问方式" class="headerlink" title="内外访问方式"></a>内外访问方式</h3><p>在内部类中使用外部类的 <code>this</code> 对象，可以用 <code>OuterClass.this</code> 的形式。</p>
<p>在外部类作用域之外，使用 <code>OuterClass.InnerClass</code> 来访问这个内部类，如果它是 <code>public</code> 的。</p>
<h3 id="规则限制"><a href="#规则限制" class="headerlink" title="规则限制"></a>规则限制</h3><p>内部类的静态域必须是 <code>final</code> 的。因为每一个外部类的对象，都有一个单独的内部类实例，为了保证“静态域只有一个实例”。</p>
<p>内部类不能有 <code>static</code> 方法。</p>
<h2 id="局部内部类"><a href="#局部内部类" class="headerlink" title="局部内部类"></a>局部内部类</h2><h3 id="规则限制-1"><a href="#规则限制-1" class="headerlink" title="规则限制"></a>规则限制</h3><p>局部内部类不能用 <code>public</code> 或 <code>private</code> 修饰，因为作用域被限定在声明这个局部内部类的块中。</p>
<p>可以访问外部函数中的变量，但是必须是类 <code>final</code> 的（Java SE 8 之前必须显式声明为 <code>final</code> 的）。</p>
<h3 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h3><ul>
<li>会把访问的外部函数的变量，进行备份。其实是，添加到了构造函数中。</li>
</ul>
<h3 id="绕过外部变量的-final-限制"><a href="#绕过外部变量的-final-限制" class="headerlink" title="绕过外部变量的 final 限制"></a>绕过外部变量的 final 限制</h3><p>要改变外部变量 <code>int num = 1</code>，定义为 <code>int[] num = {1}</code> 长度为 1 的数组即可。</p>
<h2 id="匿名内部类"><a href="#匿名内部类" class="headerlink" title="匿名内部类"></a>匿名内部类</h2><p>构造函数调用的右小括号后面跟一个开大括号，正在定义的就是匿名内部类。</p>
<h3 id="双括号初始化"><a href="#双括号初始化" class="headerlink" title="双括号初始化"></a>双括号初始化</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList&lt;String&gt; friends = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">friends.add(<span class="string">"a"</span>);</span><br><span class="line">friends.add(<span class="string">"b"</span>);</span><br><span class="line">invite(friends);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可以简写为</span></span><br><span class="line">invite(<span class="keyword">new</span> ArrayList&lt;&gt;() &#123;</span><br><span class="line">  &#123; add(<span class="string">"a"</span>), add(<span class="string">"b"</span>)&#125; <span class="comment">// 对象构造块</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h3 id="在静态方法中获取-class"><a href="#在静态方法中获取-class" class="headerlink" title="在静态方法中获取 class"></a>在静态方法中获取 class</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Object()&#123;&#125;.getClass().getEnclosingClass();</span><br></pre></td></tr></table></figure>
<h2 id="静态内部类"><a href="#静态内部类" class="headerlink" title="静态内部类"></a>静态内部类</h2><h3 id="定义-2"><a href="#定义-2" class="headerlink" title="定义"></a>定义</h3><p>用 <code>static</code> 修饰内部类，则内部类内部没有外部类的隐式引用，不能使用外部类的域。</p>
<p>只是为了把类隐藏在另一个类内部。</p>
<p>注意，只有内部类可以使用 <code>static</code> 修饰。</p>
<h3 id="规则限制-2"><a href="#规则限制-2" class="headerlink" title="规则限制"></a>规则限制</h3><p>静态内部类可以有静态域和静态方法。</p>
<h3 id="接口中的内部类"><a href="#接口中的内部类" class="headerlink" title="接口中的内部类"></a>接口中的内部类</h3><p>声明在接口中的内部类自动为 <code>public</code> 和 <code>static</code>。</p>
<h1 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h1><h2 id="异常层次结构"><a href="#异常层次结构" class="headerlink" title="异常层次结构"></a>异常层次结构</h2><ul>
<li>Throwable 类<ul>
<li>Error 类：Java 运行时系统的内部错误和资源好紧错误。</li>
<li>Exception 类<ul>
<li>RuntimeException 类：程序错误导致的异常。</li>
<li>其他类：程序本身没有问题。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p><code>Error</code> 和 <code>RuntimeException</code>是 uncheckd 异常，其他异常是 checked 异常。</p>
<h2 id="声明-unchecked-异常"><a href="#声明-unchecked-异常" class="headerlink" title="声明 unchecked 异常"></a>声明 unchecked 异常</h2><p>在方法左花括号前面声明可能抛出的异常</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">FileInputStream</span><span class="params">(String name)</span> <span class="keyword">throws</span> FileNotFoundException</span>;</span><br></pre></td></tr></table></figure>
<p>不需要声明 <code>Error</code> 和 <code>RuntimeException</code>。</p>
<p>函数中调用方法声明的异常，必须被 catch 或声明。</p>
<h3 id="override-中的异常声明"><a href="#override-中的异常声明" class="headerlink" title="override 中的异常声明"></a>override 中的异常声明</h3><ul>
<li><p>子类 <code>override</code> 父类的方法，声明的异常不能比父类方法的声明更通用，只能更具体。</p>
</li>
<li><p>如果父类方法没有声明异常，子类方法也不可以。</p>
</li>
</ul>
<h2 id="抛出异常"><a href="#抛出异常" class="headerlink" title="抛出异常"></a>抛出异常</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">throw</span> <span class="keyword">new</span> MyException(); <span class="comment">// 必须是 Throwable 的子类</span></span><br></pre></td></tr></table></figure>
<h2 id="创建异常类"><a href="#创建异常类" class="headerlink" title="创建异常类"></a>创建异常类</h2><p>定义派生于 <code>Exception</code> 类或其子类的类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyException</span> <span class="keyword">extends</span> <span class="title">Exception</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">MyException</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">MyException</span><span class="params">(String msg)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 存储在 throwable.detailMessage 域中</span></span><br><span class="line">    <span class="comment">// throwable.getMessage() 方法会返回它</span></span><br><span class="line">    <span class="keyword">super</span>(msg); </span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="捕获异常"><a href="#捕获异常" class="headerlink" title="捕获异常"></a>捕获异常</h2><h3 id="执行流程"><a href="#执行流程" class="headerlink" title="执行流程"></a>执行流程</h3><ul>
<li>执行 <code>try</code> 中代码，<code>try</code> 代码块是否抛出了某个 <code>catch</code> 中的异常。<ul>
<li>抛出了，则不再执行try 中代码，执行 <code>catch</code> 中代码。</li>
<li>没有抛出，跳过 <code>catch</code> 代码。</li>
</ul>
</li>
<li>执行 <code>finally</code> 中代码。</li>
</ul>
<h3 id="捕获语法"><a href="#捕获语法" class="headerlink" title="捕获语法"></a>捕获语法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  </span><br><span class="line">&#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125; <span class="keyword">catch</span> (MyException e) &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Java Se 7 中，可以一个 catch 语句捕获多个异常类型</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">  </span><br><span class="line">&#125; <span class="keyword">catch</span> (FileNotFoundException | MyException e) &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>捕获的异常变量隐含为 <code>final</code>。</p>
<p>可以在 <code>catch</code> 中再次抛出捕获的异常或者新的异常。</p>
<h3 id="finally-中的-return"><a href="#finally-中的-return" class="headerlink" title="finally 中的 return"></a>finally 中的 return</h3><p><code>finally</code> 中的 return 会覆盖掉 <code>try</code> 和 <code>catch</code> 中的 return。</p>
<h3 id="带资源的-try-语句"><a href="#带资源的-try-语句" class="headerlink" title="带资源的 try 语句"></a>带资源的 try 语句</h3><p>Java SE 7 中新增。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> (Resource1 res1 = ...; Resource2 res2 = ...; Resource3 res3 = ...;) &#123;</span><br><span class="line">	<span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>Resource</code> 必须是实现了 <code>AutoCloseable</code> 接口的类。</p>
<p>编译器会自动补全相关的调用 <code>res.close()</code> 的方法。</p>
<p>并且，如果 <code>try</code> 块抛出了异常 <code>e1</code>，<code>close</code> 的时候也抛出了异常 <code>e2</code>，会 <code>e1.addSuppressed(e1)</code>，抛出 <code>e1</code>。</p>
<h1 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h1><h2 id="定义-3"><a href="#定义-3" class="headerlink" title="定义"></a>定义</h2><h3 id="泛型类"><a href="#泛型类" class="headerlink" title="泛型类"></a>泛型类</h3><p>诸如 <code>T</code> 的类型变量，用尖括号 <code>&lt;&gt;</code> 括起来，放在类名的后面。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Pair</span>&lt;<span class="title">T</span>, <span class="title">U</span>&gt; </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> T first;</span><br><span class="line">  <span class="keyword">private</span> U second;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Pair</span><span class="params">(T first, U second)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.first = first;</span><br><span class="line">    <span class="keyword">this</span>.second = second;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> T <span class="title">getFirst</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> first;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setFirst</span><span class="params">(T first)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.first = first;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="泛型方法"><a href="#泛型方法" class="headerlink" title="泛型方法"></a>泛型方法</h3><p>泛型方法可以定在普通类中。</p>
<p>定义时，类型变量放在修饰符后面，返回类型的前面。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ArrayUtils</span> </span>&#123;</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">T <span class="title">getMiddle</span><span class="params">(T... items)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> items[item.length / <span class="number">2</span>];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用时，在方法名前面加上 <code>&lt;具体类型&gt;</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String middle = ArrayUtils.&lt;String&gt;getMiddle(<span class="string">"A"</span>, <span class="string">"B"</span>, <span class="string">"C"</span>);</span><br></pre></td></tr></table></figure>
<p>大多数情况，可以省略 <code>&lt;具体类型&gt;</code>，由编译器推断出。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String middle = ArrayUtils.getMiddle(<span class="string">"A"</span>, <span class="string">"B"</span>, <span class="string">"C"</span>);</span><br></pre></td></tr></table></figure>
<h3 id="类型变量的限定"><a href="#类型变量的限定" class="headerlink" title="类型变量的限定"></a>类型变量的限定</h3><p>使用 <code>extends</code> 关键字，<code>&lt;T extends BoundingType&gt;</code> 来限定类型变量 <code>T</code> 是 <code>BoundingType</code> 的子类型。</p>
<h4 id="多个类型限定"><a href="#多个类型限定" class="headerlink" title="多个类型限定"></a>多个类型限定</h4><p>一个类型变量可以有多个限定，用 <code>&amp;</code> 分隔开，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;T extends Comparable &amp; Serializable&gt;</span><br></pre></td></tr></table></figure>
<p>限定中，最多只能有一个类，必须是限定列表的第一个。（原因见泛型的类型擦除）。</p>
<h2 id="类型擦除和虚拟机"><a href="#类型擦除和虚拟机" class="headerlink" title="类型擦除和虚拟机"></a>类型擦除和虚拟机</h2><p>Java 要求旧版本的代码可以运行在新版本的 JVM 上，所以选择了在编译时擦除泛型的类型。</p>
<h3 id="擦除规则"><a href="#擦除规则" class="headerlink" title="擦除规则"></a>擦除规则</h3><p>定义时</p>
<ul>
<li>编译后的字节码，只保留<strong>原始类型</strong>，即删除类型参数后的类名和方法名。</li>
<li><strong>擦除</strong>类型变量，替换为限定类型。<ul>
<li>没有限定，用 <code>Object</code> 替换。</li>
<li>有多个限定，用第一个替换类型变量。</li>
</ul>
</li>
</ul>
<p>调用时</p>
<ul>
<li>编译器在必要的位置插入强制类型转换。</li>
</ul>
<h3 id="桥方法"><a href="#桥方法" class="headerlink" title="桥方法"></a>桥方法</h3><p>方法的类型参数在擦除时，关于 <code>overload</code> 和 <code>override</code> 会出现冲突。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Food</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Fruit</span> <span class="keyword">extends</span> <span class="title">Food</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pair</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">  T first;</span><br><span class="line">  T second;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> T <span class="title">getFirst</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> first;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setFirst</span><span class="params">(T first)</span> </span>&#123;</span><br><span class="line">    System.out.println(first.getClass());</span><br><span class="line">    <span class="keyword">this</span>.first = first;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> T <span class="title">getSecond</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> second;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setSecond</span><span class="params">(T second)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.second = second;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FruitPair</span> <span class="keyword">extends</span> <span class="title">Pair</span>&lt;<span class="title">Fruit</span>&gt; </span>&#123;</span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setSecond</span><span class="params">(Fruit second)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">super</span>.setSecond(second);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> Fruit <span class="title">getSecond</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">super</span>.getSecond();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// example</span></span><br><span class="line">FruitPair fruitPair = <span class="keyword">new</span> FruitPair();</span><br><span class="line">Pair&lt;Fruit&gt; pair = fruitPair;</span><br><span class="line">pair.setSecond(<span class="keyword">new</span> Fruit());</span><br></pre></td></tr></table></figure>
<h4 id="写方法"><a href="#写方法" class="headerlink" title="写方法"></a>写方法</h4><p>对于写方法 <code>setSecond</code>，存在自己定义的 <code>setSecond(Fruit second)</code>，父类 <code>Pair</code> 的方法是 <code>setSecond(Object second)</code>。这两个方法的签名是不同的，无法进行 <code>override</code> 以及多态。</p>
<p>例如，在上述 example 中，调用 <code>pair.setSecond</code> 时。因为 <code>pair</code> 的声明是 <code>Pair</code>，不是 <code>FruitPair</code>，所以无法看到 <code>setSecond(Fruit second)</code> 方法，只能看到 <code>setSecond(Object second)</code>。这样会调用父类的 setSecond 方法，违反了程序的本意。</p>
<p>因此，编译器会在类 <code>FruitPair</code> 中生成一个 <code>setSecond(Object second)</code> <strong>桥方法</strong>来完成覆写和多态：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setSecond</span><span class="params">(Object second)</span> </span>&#123;</span><br><span class="line">  setSecond((Fruit) second);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="读方法"><a href="#读方法" class="headerlink" title="读方法"></a>读方法</h4><p>对于读方法 <code>getSecond</code>，存在自己定义的 <code>Fruit getSecond()</code> 和 父类的方法 <code>Object getSecond()</code>。可以看到这两个方法的签名是相同的。</p>
<p>虚拟机中用参数类型和返回类型确定函数。虽然编写代码时，签名不包括返回值，但是编译器可能产生两个仅返回值不同的函数，虚拟机可以正确处理。</p>
<p>为了完成覆盖和多态，子类 <code>FruitPair</code> 会生成一个 <code>Object getSecond()</code> 方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">getSecond</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> getSecond();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="泛型场景之外的桥方法"><a href="#泛型场景之外的桥方法" class="headerlink" title="泛型场景之外的桥方法"></a>泛型场景之外的桥方法</h4><p>子类的方法可以返回更严格的返回类型，也是用桥方法实现的。</p>
<h2 id="约束与局限"><a href="#约束与局限" class="headerlink" title="约束与局限"></a>约束与局限</h2><h3 id="1-泛型的类型变量不能是基本类型-List-lt-int-gt"><a href="#1-泛型的类型变量不能是基本类型-List-lt-int-gt" class="headerlink" title="1). 泛型的类型变量不能是基本类型 List&lt;int&gt;"></a>1). 泛型的类型变量不能是基本类型 <code>List&lt;int&gt;</code></h3><p>因为擦除类型后，会用 <code>Object</code> 替换， <code>Object</code> 不能存储 <code>int</code>。</p>
<h3 id="2-运行时类型查询只能找到原始类型"><a href="#2-运行时类型查询只能找到原始类型" class="headerlink" title="2). 运行时类型查询只能找到原始类型"></a>2). 运行时类型查询只能找到原始类型</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pair&lt;Fruit&gt; fruitPair;</span><br><span class="line">Pair&lt;String&gt; strPair;</span><br><span class="line"><span class="keyword">assert</span>(fruitPair.getClass() == strPair.getClass()); <span class="comment">// 因为都是 Pair 类的对象</span></span><br><span class="line"></span><br><span class="line">fruitPair <span class="keyword">instanceof</span> Pair&lt;Fruit&gt;; <span class="comment">// 编译错误</span></span><br><span class="line">(Pair&lt;Fruit&gt;) fruitPair; <span class="comment">// 可以，编译警告</span></span><br></pre></td></tr></table></figure>
<h3 id="3-不能-new-参数化类型的数组-new-Pair-lt-Fruit-gt-10"><a href="#3-不能-new-参数化类型的数组-new-Pair-lt-Fruit-gt-10" class="headerlink" title="3). 不能 new 参数化类型的数组 new Pair&lt;Fruit&gt;[10]"></a>3). 不能 new 参数化类型的数组 <code>new Pair&lt;Fruit&gt;[10]</code></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pair&lt;Fruit&gt;[] pairs = <span class="keyword">new</span> Pair&lt;Fruit&gt;[<span class="number">10</span>]; <span class="comment">// 编译错误</span></span><br><span class="line"></span><br><span class="line">Pair&lt;Fruit&gt;[] pairs = (Pair&lt;Fruit&gt;[]) <span class="keyword">new</span> Pair&lt;?&gt;[<span class="number">10</span>]; <span class="comment">// 可以，编译警告</span></span><br></pre></td></tr></table></figure>
<h3 id="4-在函数参数中-new-参数化类型的数组"><a href="#4-在函数参数中-new-参数化类型的数组" class="headerlink" title="4). 在函数参数中 new 参数化类型的数组"></a>4). 在函数参数中 new 参数化类型的数组</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">addAll</span><span class="params">(T... items)</span> </span>&#123;&#125;</span><br><span class="line">addAll(<span class="keyword">new</span> Pair&lt;Fruit&gt;(), <span class="keyword">new</span> Pair&lt;Fruit&gt;());</span><br></pre></td></tr></table></figure>
<p>相比规则 3)，放松了规则，只会得到警告。</p>
<h3 id="5-不能实例化类型变量-new-T"><a href="#5-不能实例化类型变量-new-T" class="headerlink" title="5). 不能实例化类型变量 new T()"></a>5). 不能实例化类型变量 <code>new T()</code></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> T(); <span class="comment">// 编译错误</span></span><br><span class="line"><span class="keyword">new</span> T[<span class="number">10</span>]; <span class="comment">// 编译错误</span></span><br><span class="line">T.class; <span class="comment">// 编译错误</span></span><br></pre></td></tr></table></figure>
<p>因为如果运行，类型擦除后，都会变为 <code>new Object()</code>，和程序本意不符。</p>
<p><code>Class</code> 类是泛型类，<code>String.class</code> 返回了 <code>Class&lt;String&gt;</code> 的唯一实例。</p>
<h3 id="6-不能实例化类型变量的数组-new-T-10"><a href="#6-不能实例化类型变量的数组-new-T-10" class="headerlink" title="6). 不能实例化类型变量的数组 new T[10]"></a>6). 不能实例化类型变量的数组 <code>new T[10]</code></h3><p>因为如果允许，这样 new 出来的是 <code>Object[]</code>，无法转化成其他类的对象。</p>
<p>如果只是内部使用数组，可以如下，强制类型转化 <code>E[]</code> 是假象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ArrayList</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> E[] elements;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    elements = (E[]) <span class="keyword">new</span> Object[<span class="number">10</span>];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但是如果用这种方法，返回了<code>E[]</code>，是不可以的，因为它是用 <code>new Object[]</code>构建的。所以编译不会报错，在执行到 AAA 处时，报出 ClassCastException。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ArrayList</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> E[] elements;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    elements = (E[]) <span class="keyword">new</span> Object[<span class="number">10</span>];</span><br><span class="line">  &#125;</span><br><span class="line">  E[] getList() &#123;</span><br><span class="line">    <span class="keyword">return</span> elements;</span><br><span class="line">  &#125;</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">ArrayList&lt;Integer&gt; nums;</span><br><span class="line">Integer[] nums2 = nums.getList(); <span class="comment">// AAA</span></span><br></pre></td></tr></table></figure>
<h3 id="7-泛型类不能有静态的类型变量"><a href="#7-泛型类不能有静态的类型变量" class="headerlink" title="7). 泛型类不能有静态的类型变量"></a>7). 泛型类不能有静态的类型变量</h3><p>如果允许，<code>Pair&lt;String&gt;</code> 和 <code>Pair&lt;Integer&gt;</code> 会生成两个静态域，不符合语义。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pair</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">  <span class="keyword">static</span> T item;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="8-擦除后的冲突"><a href="#8-擦除后的冲突" class="headerlink" title="8). 擦除后的冲突"></a>8). 擦除后的冲突</h3><p>擦除类型后，如果有冲突是不可以的。</p>
<p>如下，这样会生成 <code>equals(Object)</code> 与超类 <code>Object</code> 的方法冲突。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Pair</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(T value)</span> </span>&#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>一个类不能同时成为两个接口类型的子类，这两个接口类型是同一个接口的不同参数化。</p>
<p>如下，<code>Fruit</code> 实现了 <code>Comparable&lt;Food&gt;</code> 和 <code>Comparable&lt;Fruit&gt;</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Food</span> <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">Food</span>&gt; </span>&#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Fruit</span> <span class="keyword">extends</span> <span class="title">Food</span> <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">Fruit</span>&gt; </span>&#123;&#125;</span><br></pre></td></tr></table></figure>
<h2 id="通配符"><a href="#通配符" class="headerlink" title="通配符"></a>通配符</h2><p><a href="https://www.zhihu.com/question/20400700/answer/117464182">知乎-通配符</a></p>
<h3 id="8-4-1-继承关系"><a href="#8-4-1-继承关系" class="headerlink" title="8.4.1 继承关系"></a>8.4.1 继承关系</h3><p><code>Pair&lt;Fruit&gt;</code> 与 <code>Pair&lt;Food&gt;</code> 没有什么关系，他们都是 <code>Pair</code> 的子类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pair&lt;Food&gt; pair = <span class="keyword">new</span> Pair&lt;Fruit&gt;(); <span class="comment">// 编译错误</span></span><br></pre></td></tr></table></figure>
<h3 id="8-4-2-上界通配符-extends"><a href="#8-4-2-上界通配符-extends" class="headerlink" title="8.4.2 上界通配符 extends"></a>8.4.2 上界通配符 extends</h3><p><code>Pair&lt;? exnteds Food&gt;</code> 表示这里最通用可以是 <code>Food</code>，即 <code>Food</code> 和 <code>Food</code> 的子类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pair&lt;? extends Food&gt; pair = <span class="keyword">new</span> Pair&lt;Fruit&gt;();</span><br></pre></td></tr></table></figure>
<p>不能存，只能取。</p>
<p>因为编译器只知道这里是 Food 和 Food 的子类，不知道具体是什么类的对象。编译器实际上会变为 <code>Pair&lt;#CAP1&gt;</code>，用 #CAP1 来占位。</p>
<h3 id="8-4-3-下界通配符"><a href="#8-4-3-下界通配符" class="headerlink" title="8.4.3 下界通配符"></a>8.4.3 下界通配符</h3><p><code>Pair&lt;? super Fruit&gt;</code> 表示这里最具体可以使 <code>Fruit</code>，即 <code>Fruit</code> 和 <code>Fruit</code> 的超类。</p>
<p>可以存，取的时候只能赋值给 <code>Object</code>。</p>
<p>因为这里规定了下界，既然元素是 Fruit 的基类，那么存入 Fruit 的子类都可以。</p>
<p>但是因为模糊了上界，所以只能赋给 Object。</p>
]]></content>
      <categories>
        <category>java</category>
        <category>basic</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>HashMap源码分析</title>
    <url>/2020/java/basic/HashMap%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://segmentfault.com/a/1190000015798586">深入理解HashMap: 关键源码逐行分析之hash算法</a></li>
</ul>
<h1 id="组成"><a href="#组成" class="headerlink" title="组成"></a>组成</h1><h2 id="节点"><a href="#节点" class="headerlink" title="节点"></a>节点</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> hash;</span><br><span class="line">    <span class="keyword">final</span> K key;</span><br><span class="line">    V value;</span><br><span class="line">    Node&lt;K,V&gt; next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>节点 <code>Node</code> 存储：</p>
<ul>
<li>基本：hash 值、key值、value值。</li>
<li>多个值存在一个位置：链表 <code>next</code>。</li>
</ul>
<h2 id="table"><a href="#table" class="headerlink" title="table"></a>table</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Node&lt;K,V&gt;[] table;</span><br></pre></td></tr></table></figure>
<p>节点存储在 <code>Node</code> 数组中。</p>
<h2 id="节点的-key-如何映射到-table-的下标"><a href="#节点的-key-如何映射到-table-的下标" class="headerlink" title="节点的 key 如何映射到 table 的下标"></a>节点的 key 如何映射到 table 的下标</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">hash</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> h;</span><br><span class="line">    <span class="keyword">return</span> (key == <span class="keyword">null</span>) ? <span class="number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="number">16</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">int</span> index = hash(key) &amp; (table.length() - <span class="number">1</span>);</span><br></pre></td></tr></table></figure>
<ol>
<li>使用 <code>hash()</code> 函数把 key 的hashCode 再进行hash。<ul>
<li>将 hashCode 的高16位和低16位进行异或，充分利用高半位和低半位的信息, 对低位进行了扰动。使该hashCode 映射成数组下标时可以更均匀。</li>
</ul>
</li>
<li>table.length() 为 2 的 m 次幂，把第一步的 hash 值取低 m 位，来作为数组的下标。</li>
<li>key 可以是 null，这时 hash 值是 0。</li>
</ol>
<h1 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">float</span> DEFAULT_LOAD_FACTOR = <span class="number">0.75f</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAXIMUM_CAPACITY = <span class="number">1</span> &lt;&lt; <span class="number">30</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity, <span class="keyword">float</span> loadFactor)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (initialCapacity &lt; <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Illegal initial capacity: "</span> + initialCapacity);</span><br><span class="line">  <span class="keyword">if</span> (initialCapacity &gt; MAXIMUM_CAPACITY)</span><br><span class="line">    initialCapacity = MAXIMUM_CAPACITY;</span><br><span class="line">  <span class="keyword">if</span> (loadFactor &lt;= <span class="number">0</span> || Float.isNaN(loadFactor))</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Illegal load factor: "</span> + loadFactor);</span><br><span class="line">  <span class="keyword">this</span>.loadFactor = loadFactor;</span><br><span class="line">  <span class="keyword">this</span>.threshold = tableSizeFor(initialCapacity);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>(initialCapacity, DEFAULT_LOAD_FACTOR);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.loadFactor = DEFAULT_LOAD_FACTOR; <span class="comment">// all other fields defaulted</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>构造函数中只指定了 <code>loadFactor</code> 和 初始容量。</p>
</li>
<li><p><code>threshold</code> 有两个作用。</p>
<ul>
<li>在创建存储 Node 的数组 <code>table</code> 没有创建时，存储<strong>初始</strong>容量，即随后 <code>table</code> 的长度。</li>
<li>在 <code>table</code> 创建后，如果负载超过 <code>threshold</code>，进行 <code>resize</code> 扩容。阈值的初始值和变化请看 resize() 源码分析处。</li>
</ul>
</li>
<li><code>table</code> 的长度为 2 的次幂。</li>
</ul>
<h2 id="tableSizeFor"><a href="#tableSizeFor" class="headerlink" title="tableSizeFor()"></a>tableSizeFor()</h2><p>如果构造函数指定了 <code>initialCapacity</code>，调用 <code>tableSizeFor</code> 转化为大于等于 <code>initialCapacity</code> 的最小的 2 的次幂。</p>
<p><code>tableSizeFor</code> 思想是，把 cap 从最右边的 1 开始到最低位，都变为 1。再加 1，即为 2 的 次幂。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">tableSizeFor</span><span class="params">(<span class="keyword">int</span> cap)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> n = cap - <span class="number">1</span>; <span class="comment">// 如果 cap 本来就是 2 的次幂，不减 1，经过下面操作后，会变为 2 * cap。</span></span><br><span class="line">  </span><br><span class="line">  <span class="comment">// n 的最右边至少有一个 1，例如 1XXXX。</span></span><br><span class="line">  n |= n &gt;&gt;&gt; <span class="number">1</span>;	<span class="comment">// 把 n 的前 2 位都变为 1，例如 11XXX。</span></span><br><span class="line">  n |= n &gt;&gt;&gt; <span class="number">2</span>; <span class="comment">// 把 n 的前 4 位都变为 1，例如 1111X。</span></span><br><span class="line">  n |= n &gt;&gt;&gt; <span class="number">4</span>; <span class="comment">// 类似</span></span><br><span class="line">  n |= n &gt;&gt;&gt; <span class="number">8</span>;	<span class="comment">// 类似</span></span><br><span class="line">  n |= n &gt;&gt;&gt; <span class="number">16</span>; <span class="comment">// 最高位 32 个 1。</span></span><br><span class="line">  <span class="comment">// 以上操作，把 n 转化为每位都是 1。</span></span><br><span class="line">  </span><br><span class="line">  <span class="comment">// n + 1 后只有最高位为1，为 2 的次幂</span></span><br><span class="line">  <span class="keyword">return</span> (n &lt; <span class="number">0</span>) ? <span class="number">1</span> : (n &gt;= MAXIMUM_CAPACITY) ? MAXIMUM_CAPACITY : n + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="putMapEntries"><a href="#putMapEntries" class="headerlink" title="putMapEntries()"></a>putMapEntries()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(Map&lt;? extends K, ? extends V&gt; m)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.loadFactor = DEFAULT_LOAD_FACTOR;</span><br><span class="line">  putMapEntries(m, <span class="keyword">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">putMapEntries</span><span class="params">(Map&lt;? extends K, ? extends V&gt; m, <span class="keyword">boolean</span> evict)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> s = m.size();</span><br><span class="line">    <span class="keyword">if</span> (s &gt; <span class="number">0</span>) &#123; </span><br><span class="line">        <span class="keyword">if</span> (table == <span class="keyword">null</span>) &#123; <span class="comment">// pre-size // 构造函数调用 putMapEntries，一定会进入这个分支。</span></span><br><span class="line">            <span class="keyword">float</span> ft = ((<span class="keyword">float</span>)s / loadFactor) + <span class="number">1.0F</span>;</span><br><span class="line">            <span class="keyword">int</span> t = ((ft &lt; (<span class="keyword">float</span>)MAXIMUM_CAPACITY) ?</span><br><span class="line">                     (<span class="keyword">int</span>)ft : MAXIMUM_CAPACITY);</span><br><span class="line">            <span class="keyword">if</span> (t &gt; threshold)</span><br><span class="line">                threshold = tableSizeFor(t);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (s &gt; threshold) <span class="comment">// putAll 调用时，可能会进入这个分支。</span></span><br><span class="line">            resize();</span><br><span class="line">        <span class="keyword">for</span> (Map.Entry&lt;? extends K, ? extends V&gt; e : m.entrySet()) &#123;</span><br><span class="line">            K key = e.getKey();</span><br><span class="line">            V value = e.getValue();</span><br><span class="line">            putVal(hash(key), key, value, <span class="keyword">false</span>, evict);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>除此之外，还有第 4 个构造函数，把给定 Map 的每个元素使用 <code>putVal()</code> 加入本 HashMap 中。</p>
<p>构造函数调用 <code>putMapEntries</code>，只会进入第一个分支。把初始容量变为 <code>要加入元素个数 / loadFactor</code>。</p>
<h1 id="resize"><a href="#resize" class="headerlink" title="resize()"></a>resize()</h1><h2 id="触发时机"><a href="#触发时机" class="headerlink" title="触发时机"></a>触发时机</h2><ul>
<li>初始化<code>table</code> 时。</li>
<li>元素个数超过 <code>threshold</code> 时进行扩容。</li>
</ul>
<h2 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt;[] resize() &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] oldTab = table;</span><br><span class="line">    <span class="keyword">int</span> oldCap = (oldTab == <span class="keyword">null</span>) ? <span class="number">0</span> : oldTab.length;</span><br><span class="line">    <span class="keyword">int</span> oldThr = threshold;</span><br><span class="line">    <span class="keyword">int</span> newCap, newThr = <span class="number">0</span>;</span><br><span class="line">  </span><br><span class="line">  	<span class="comment">// 已存在 table，是触发扩容进入 resize() 函数的。</span></span><br><span class="line">    <span class="keyword">if</span> (oldCap &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (oldCap &gt;= MAXIMUM_CAPACITY) &#123;</span><br><span class="line">            threshold = Integer.MAX_VALUE;</span><br><span class="line">            <span class="keyword">return</span> oldTab;</span><br><span class="line">        &#125;</span><br><span class="line">      	<span class="comment">// table.length 加倍</span></span><br><span class="line">      	<span class="comment">// threshold 加倍</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((newCap = oldCap &lt;&lt; <span class="number">1</span>) &lt; MAXIMUM_CAPACITY &amp;&amp;</span><br><span class="line">                 oldCap &gt;= DEFAULT_INITIAL_CAPACITY)</span><br><span class="line">            newThr = oldThr &lt;&lt; <span class="number">1</span>; <span class="comment">// double threshold</span></span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// 构造函数指定了 initialCapacity，存储在 threshold中，所以 &gt; 0。</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (oldThr &gt; <span class="number">0</span>) </span><br><span class="line">        newCap = oldThr;</span><br><span class="line">    <span class="comment">// 构造函数没有指定 initialCapacity。</span></span><br><span class="line">  	<span class="keyword">else</span> &#123;              </span><br><span class="line">        newCap = DEFAULT_INITIAL_CAPACITY; <span class="comment">// 1 &lt;&lt; 4</span></span><br><span class="line">        newThr = (<span class="keyword">int</span>)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">  	<span class="comment">// 从此，threshold 存储的是扩容的阈值。</span></span><br><span class="line">  	<span class="comment">// 初始大小为 table.length * loadFactor</span></span><br><span class="line">    <span class="keyword">if</span> (newThr == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">float</span> ft = (<span class="keyword">float</span>)newCap * loadFactor;</span><br><span class="line">        newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (<span class="keyword">float</span>)MAXIMUM_CAPACITY ?</span><br><span class="line">                  (<span class="keyword">int</span>)ft : Integer.MAX_VALUE);</span><br><span class="line">    &#125;</span><br><span class="line">    threshold = newThr;</span><br><span class="line">  </span><br><span class="line">  </span><br><span class="line">    <span class="meta">@SuppressWarnings</span>(&#123;<span class="string">"rawtypes"</span>,<span class="string">"unchecked"</span>&#125;)</span><br><span class="line">    Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])<span class="keyword">new</span> Node[newCap];</span><br><span class="line">    table = newTab;</span><br><span class="line">    <span class="keyword">if</span> (oldTab != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; oldCap; ++j) &#123; <span class="comment">// 把每个元素插入新的 table 中。</span></span><br><span class="line">            Node&lt;K,V&gt; e;</span><br><span class="line">            <span class="keyword">if</span> ((e = oldTab[j]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                oldTab[j] = <span class="keyword">null</span>;</span><br><span class="line">                <span class="keyword">if</span> (e.next == <span class="keyword">null</span>)	<span class="comment">// 如果存储桶里只有一个元素，直接插入对应位置。</span></span><br><span class="line">                    newTab[e.hash &amp; (newCap - <span class="number">1</span>)] = e;</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span> (e <span class="keyword">instanceof</span> TreeNode) <span class="comment">// 如果存储桶里是红黑树，拆分树。TODO</span></span><br><span class="line">                    ((TreeNode&lt;K,V&gt;)e).split(<span class="keyword">this</span>, newTab, j, oldCap);</span><br><span class="line">                <span class="keyword">else</span> &#123; <span class="comment">// 如果存储桶里是链表，拆分链表</span></span><br><span class="line">                    Node&lt;K,V&gt; loHead = <span class="keyword">null</span>, loTail = <span class="keyword">null</span>;</span><br><span class="line">                    Node&lt;K,V&gt; hiHead = <span class="keyword">null</span>, hiTail = <span class="keyword">null</span>;</span><br><span class="line">                    Node&lt;K,V&gt; next;</span><br><span class="line">                    <span class="keyword">do</span> &#123;</span><br><span class="line">                        next = e.next;</span><br><span class="line">                        <span class="keyword">if</span> ((e.hash &amp; oldCap) == <span class="number">0</span>) &#123;</span><br><span class="line">                            <span class="keyword">if</span> (loTail == <span class="keyword">null</span>)</span><br><span class="line">                                loHead = e;</span><br><span class="line">                            <span class="keyword">else</span></span><br><span class="line">                                loTail.next = e;</span><br><span class="line">                            loTail = e;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">else</span> &#123;</span><br><span class="line">                            <span class="keyword">if</span> (hiTail == <span class="keyword">null</span>)</span><br><span class="line">                                hiHead = e;</span><br><span class="line">                            <span class="keyword">else</span></span><br><span class="line">                                hiTail.next = e;</span><br><span class="line">                            hiTail = e;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; <span class="keyword">while</span> ((e = next) != <span class="keyword">null</span>);</span><br><span class="line">                    <span class="keyword">if</span> (loTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">                        loTail.next = <span class="keyword">null</span>;</span><br><span class="line">                        newTab[j] = loHead;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> (hiTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">                        hiTail.next = <span class="keyword">null</span>;</span><br><span class="line">                        newTab[j + oldCap] = hiHead;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> newTab;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="第一部分-容量和阈值的变化"><a href="#第一部分-容量和阈值的变化" class="headerlink" title="第一部分 容量和阈值的变化"></a>第一部分 容量和阈值的变化</h3><p>容量指 <code>table</code> 数组的长度。</p>
<p>阈值指 <code>threshold</code>。</p>
<p>两种情况</p>
<ul>
<li>在 <code>table</code> 还没有创建时：<ul>
<li>容量为构造函数传入的初始容量或默认值（16）。</li>
<li>阈值为容量 * loadFactor。</li>
</ul>
</li>
<li>在 <code>table</code> 还没有创建时：<ul>
<li>容量为原容量的 2 倍。</li>
<li>阈值为原阈值的 2 倍。</li>
</ul>
</li>
</ul>
<h3 id="第二部分-红黑树拆分"><a href="#第二部分-红黑树拆分" class="headerlink" title="第二部分 红黑树拆分"></a>第二部分 红黑树拆分</h3><p>拆成两棵红黑树。</p>
<p>并检查每一棵红黑树的节点数，如果节点数少于阈值，转成树。</p>
<h3 id="第三部分-链表拆分"><a href="#第三部分-链表拆分" class="headerlink" title="第三部分 链表拆分"></a>第三部分 链表拆分</h3><ul>
<li><code>e.hash &amp; oldCap == 0</code> 的节点组成 low 链表，存入 <code>newTable[j]</code> 位置。</li>
<li><code>e.hash &amp; oldCap != 0</code> 的节点组成 high 链表，存入 <code>newTable[j + oldCap]</code> 位置。</li>
</ul>
<p>原因如下。</p>
<p>假设 <code>oldCap = 16 = 0b1_0000</code>，存储在 <code>j</code> 处的元素是 <code>j = e.hash &amp; 0b0_1111</code>。</p>
<p>扩容后 <code>newCap = 16 * 2 = 0b10_0000</code>，节点的下标是 <code>e.hash &amp; 0b1_1111</code>。</p>
<ul>
<li>如果 <code>e.hash</code> 的第 5 位是 0，则 <code>e.hash &amp; 0b1_1111</code> 与 <code>e.hash &amp; 0b0_1111</code> 相等，所以还存储在 <code>j</code> 处。</li>
<li>如果 <code>e.hash</code> 的第 5 位是1，则 <code>e.hash &amp; 0b1_1111</code> 与 <code>(e.hash &amp; 0b0_1111) + 0b1_0000</code>相等，所以存储在 <code>j + oldCap</code> 处。</li>
</ul>
<h1 id="put"><a href="#put" class="headerlink" title="put()"></a>put()</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> putVal(hash(key), key, value, <span class="keyword">false</span>, <span class="keyword">true</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, <span class="keyword">boolean</span> onlyIfAbsent,</span></span></span><br><span class="line"><span class="function"><span class="params">               <span class="keyword">boolean</span> evict)</span> </span>&#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="keyword">int</span> n, i;</span><br><span class="line">  	<span class="comment">// 构造函数没有初始化 table。</span></span><br><span class="line">  	<span class="comment">// 因此，第一次 put，会在这里初始化 table。</span></span><br><span class="line">    <span class="keyword">if</span> ((tab = table) == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">        n = (tab = resize()).length;</span><br><span class="line">  	<span class="comment">// 如果对应的桶里没有元素，直接加进去。</span></span><br><span class="line">    <span class="keyword">if</span> ((p = tab[i = (n - <span class="number">1</span>) &amp; hash]) == <span class="keyword">null</span>)</span><br><span class="line">        tab[i] = newNode(hash, key, value, <span class="keyword">null</span>);</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        Node&lt;K,V&gt; e; K k;</span><br><span class="line">      </span><br><span class="line">      	<span class="comment">// 找这个桶里，与 key 相同的元素，赋值给 e。</span></span><br><span class="line">      	<span class="comment">// 没有找到，就创建一个节点，插入到桶的链表/红黑树里，e 为 null。</span></span><br><span class="line">      </span><br><span class="line">        <span class="keyword">if</span> (p.hash == hash &amp;&amp; <span class="comment">// 如果桶里只有一个元素，且 key 相同，找到 e 了。</span></span><br><span class="line">            ((k = p.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">            e = p;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (p <span class="keyword">instanceof</span> TreeNode) <span class="comment">// 在红黑树里找。</span></span><br><span class="line">            e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="keyword">this</span>, tab, hash, key, value);</span><br><span class="line">        <span class="keyword">else</span> &#123; <span class="comment">// 在链表里找。</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> binCount = <span class="number">0</span>; ; ++binCount) &#123;</span><br><span class="line">                <span class="keyword">if</span> ((e = p.next) == <span class="keyword">null</span>) &#123; <span class="comment">// 整个链表都没有找到，创建一个节点加入链表。</span></span><br><span class="line">                    p.next = newNode(hash, key, value, <span class="keyword">null</span>);</span><br><span class="line">                    <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="number">1</span>) <span class="comment">// 节点数目大于等于 8 个，扩容或变为红黑树。</span></span><br><span class="line">                        treeifyBin(tab, hash);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (e.hash == hash &amp;&amp;	<span class="comment">// 在链表中找到 e 了。</span></span><br><span class="line">                    ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                p = e;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      </span><br><span class="line">        <span class="keyword">if</span> (e != <span class="keyword">null</span>) &#123; <span class="comment">// 如果找到了之前存在的节点，更新值。</span></span><br><span class="line">            V oldValue = e.value;</span><br><span class="line">            <span class="keyword">if</span> (!onlyIfAbsent || oldValue == <span class="keyword">null</span>) <span class="comment">// onlyIfAbsent 只有putIfAbsent调用时是true</span></span><br><span class="line">                e.value = value;</span><br><span class="line">            afterNodeAccess(e); <span class="comment">// 只有 LinkedHashMap 时有用，HashMap 中 afterNodeAccess 是空的。</span></span><br><span class="line">            <span class="keyword">return</span> oldValue;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">  	<span class="comment">// 只有新建节点会到这里</span></span><br><span class="line">    ++modCount;</span><br><span class="line">    <span class="keyword">if</span> (++size &gt; threshold) <span class="comment">// 如果元素数目多于阈值，扩容。</span></span><br><span class="line">        resize();</span><br><span class="line">    afterNodeInsertion(evict); <span class="comment">// 只有 LinkedHashMap 时有用</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">treeifyBin</span><span class="params">(Node&lt;K,V&gt;[] tab, <span class="keyword">int</span> hash)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> n, index; Node&lt;K,V&gt; e;</span><br><span class="line">  <span class="keyword">if</span> (tab == <span class="keyword">null</span> || (n = tab.length) &lt; MIN_TREEIFY_CAPACITY) <span class="comment">// 如果 table 长度小于64，则扩容。</span></span><br><span class="line">    resize();</span><br><span class="line">  <span class="keyword">else</span> <span class="keyword">if</span> ((e = tab[index = (n - <span class="number">1</span>) &amp; hash]) != <span class="keyword">null</span>) &#123; <span class="comment">// 否则桶中链表转化为树。</span></span><br><span class="line">    TreeNode&lt;K,V&gt; hd = <span class="keyword">null</span>, tl = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">      TreeNode&lt;K,V&gt; p = replacementTreeNode(e, <span class="keyword">null</span>);</span><br><span class="line">      <span class="keyword">if</span> (tl == <span class="keyword">null</span>)</span><br><span class="line">        hd = p;</span><br><span class="line">      <span class="keyword">else</span> &#123;</span><br><span class="line">        p.prev = tl;</span><br><span class="line">        tl.next = p;</span><br><span class="line">      &#125;</span><br><span class="line">      tl = p;</span><br><span class="line">    &#125; <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>);</span><br><span class="line">    <span class="keyword">if</span> ((tab[index] = hd) != <span class="keyword">null</span>)</span><br><span class="line">      hd.treeify(tab);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MIN_TREEIFY_CAPACITY = <span class="number">64</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>第一次 put，会创建 table 数组。</li>
<li>在桶的链表/红黑树里找节点，没找到，就创建一个，插入链表/红黑树中。</li>
<li>如果链表插入节点后，链表长度多于 8 个，进行扩容或转化为红黑树。</li>
<li>如果找到了节点，并且不是 <code>putIfAbsent()</code> 调用的，更新节点的值。</li>
<li>如果是新增了节点，查看节点数目是否多于阈值，进行扩容。</li>
</ul>
<h1 id="get"><a href="#get" class="headerlink" title="get()"></a>get()</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">    Node&lt;K,V&gt; e;</span><br><span class="line">    <span class="keyword">return</span> (e = getNode(hash(key), key)) == <span class="keyword">null</span> ? <span class="keyword">null</span> : e.value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title">getNode</span><span class="params">(<span class="keyword">int</span> hash, Object key)</span> </span>&#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; <span class="keyword">int</span> n; K k;</span><br><span class="line">    <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp; <span class="comment">// table 没有初始化，或对应桶是空的，返回。</span></span><br><span class="line">        (first = tab[(n - <span class="number">1</span>) &amp; hash]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (first.hash == hash &amp;&amp; <span class="comment">// 桶中第一个节点是否是对应的key。</span></span><br><span class="line">            ((k = first.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">            <span class="keyword">return</span> first;</span><br><span class="line">        <span class="keyword">if</span> ((e = first.next) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (first <span class="keyword">instanceof</span> TreeNode) <span class="comment">// 如果是红黑树，在红黑树里找。第一个节点是红黑树的根节点。</span></span><br><span class="line">                <span class="keyword">return</span> ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);</span><br><span class="line">            <span class="keyword">do</span> &#123;	<span class="comment">// 在链表中找。</span></span><br><span class="line">                <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                    ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                    <span class="keyword">return</span> e;</span><br><span class="line">            &#125; <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>table 没有初始化、或对应的桶是空的，返回。</li>
<li>查看桶中第一个节点的key是否匹配。</li>
<li>没有匹配，去桶的红黑树或链表中去找。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>basic</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>Enum</title>
    <url>/2020/java/basic/enum/</url>
    <content><![CDATA[<h1 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h1><h2 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h2><p>使用 <code>enum</code>  关键字，类似于定义类的方式。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Fruit &#123;</span><br><span class="line">    ORANGE,</span><br><span class="line">    BANANA;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="自定义构造函数"><a href="#自定义构造函数" class="headerlink" title="自定义构造函数"></a>自定义构造函数</h2><p>可以自定义构造函数，传递额外的信息。注意 <code>Fruit</code> 类的第一个语句必须是枚举变量的定义。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Fruit &#123;</span><br><span class="line">    ORANGE(<span class="number">10</span>),</span><br><span class="line">    BANANA(<span class="number">3</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> price;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">enum</span> <span class="title">Fruit</span><span class="params">(<span class="keyword">int</span> price)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.price = price;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h1><p>编译器会把 <code>enum</code> 类转化为 <code>Enum&lt;E extends Enum&lt;E&gt;&gt;</code> 的子类。</p>
<p>例如，上述的 <code>Fruit</code> 枚举类，反编译后如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Fruit</span> <span class="keyword">extends</span> <span class="title">Enum</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Fruit[] values()</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> (Fruit[])$VALUES.clone();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Fruit <span class="title">valueOf</span><span class="params">(String name)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (Fruit)Enum.valueOf(Fruit, name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Fruit</span><span class="params">(String name, <span class="keyword">int</span> ordinal, <span class="keyword">int</span> price)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(name, ordinal);</span><br><span class="line">        <span class="keyword">this</span>.price = price;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getPrice</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> price;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Fruit ORANGE;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Fruit BANANA;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> price;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Fruit $VALUES[];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> </span><br><span class="line">    &#123;</span><br><span class="line">        ORANGE = <span class="keyword">new</span> Fruit(<span class="string">"ORANGE"</span>, <span class="number">0</span>, <span class="number">10</span>);</span><br><span class="line">        BANANA = <span class="keyword">new</span> Fruit(<span class="string">"BANANA"</span>, <span class="number">1</span>, <span class="number">3</span>);</span><br><span class="line">        $VALUES = (<span class="keyword">new</span> Fruit[] &#123;</span><br><span class="line">            ORANGE, BANANA</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>主要进行的工作：</p>
<ol>
<li>在构造函数中添加 <code>name</code> 、<code>ordinal</code> 。</li>
<li>每个枚举量是静态常量，会调用 1. 中的构造函数。其中 <code>name</code> 是枚举变量名、 <code>ordinal</code> 从0开始编号。</li>
<li>枚举量列表存储在静态常量 <code>$VALUES</code> 中。</li>
<li>为 <code>Fruit</code> 类添加 <code>final</code> 修饰符。</li>
</ol>
<h1 id="常用函数"><a href="#常用函数" class="headerlink" title="常用函数"></a>常用函数</h1><p>根据反编译后的类、超类 <code>Enum</code> ，可以发现如下的常用函数：</p>
<ol>
<li><code>static Fruit valueOf(String name)</code>  返回和传入 <code>name</code> 匹配的枚举量。如果不能匹配，抛出 <code>IllegalArgumentException</code> 。</li>
<li><code>String name()</code> 、<code>String toString()</code>  返回 <code>name</code> 。</li>
<li><code>int ordinal()</code>  返回 <code>ordinal</code> 。</li>
<li><code>static Fruit[] values()</code>  返回 枚举量数组。</li>
<li><code>int compareTo(Fruit other)</code>  返回 <code>ordinal - other.ordinal</code> 。</li>
</ol>
<p>比较两个枚举量是否相等，用 <code>==</code> 即可，即比较两个枚举常量的地址是否相同。<br>当然，用 <code>equals</code> 也可以，因为 <code>Enum</code> 的 <code>equals</code> 函数就是直接 <code>return this == other</code> 。</p>
<h1 id="switch-中的枚举量"><a href="#switch-中的枚举量" class="headerlink" title="switch 中的枚举量"></a>switch 中的枚举量</h1><h2 id="定义-1"><a href="#定义-1" class="headerlink" title="定义"></a>定义</h2><p>可以在 <code>switch</code> 中使用枚举量。注意 <code>case</code> 中不需要写 <code>Fruit.XXXX</code> ，直接 <code>XXXX</code> 即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Fruit fruit = Fruit.BANANA;</span><br><span class="line"><span class="keyword">switch</span> (fruit) &#123;</span><br><span class="line">  <span class="keyword">case</span> BANANA:</span><br><span class="line">    System.out.println(<span class="string">"banana"</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> ORANGE:</span><br><span class="line">    System.out.println(<span class="string">"orange"</span>);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h2><p>编译器会把代码进行转换，创建一个 SwitchMap 类 <code>Main$1</code> ，使用 <code>ordinal</code> 进行比较。</p>
<p>反编译后如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Main$1.class</span></span><br><span class="line"><span class="comment">// $FF: synthetic class</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Main</span>$1 </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// $FF: synthetic field</span></span><br><span class="line">   <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span>[] $SwitchMap$Fruit = <span class="keyword">new</span> <span class="keyword">int</span>[Fruit.values().length];</span><br><span class="line"></span><br><span class="line">   <span class="keyword">static</span> &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         $SwitchMap$Fruit[Fruit.BANANA.ordinal()] = <span class="number">1</span>;</span><br><span class="line">      &#125; <span class="keyword">catch</span> (NoSuchFieldError var2) &#123;</span><br><span class="line">         ;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         $SwitchMap$Fruit[Fruit.ORANGE.ordinal()] = <span class="number">2</span>;</span><br><span class="line">      &#125; <span class="keyword">catch</span> (NoSuchFieldError var1) &#123;</span><br><span class="line">         ;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Main.class</span></span><br><span class="line"><span class="keyword">import</span> Main.1;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">      Fruit fruit = Fruit.BANANA;</span><br><span class="line">      <span class="keyword">switch</span>(<span class="number">1</span>.$SwitchMap$Fruit[fruit.ordinal()]) &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">         System.out.println(<span class="string">"banana"</span>);</span><br><span class="line">         <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">         System.out.println(<span class="string">"orange"</span>);</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="枚举类的序列化"><a href="#枚举类的序列化" class="headerlink" title="枚举类的序列化"></a>枚举类的序列化</h1><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://github.com/hollischuang/toBeTopJavaer/blob/master/basics/java-basic/enum-serializable.md">枚举的序列化如何实现</a></li>
</ul>
<h2 id="类似枚举类的序列化问题"><a href="#类似枚举类的序列化问题" class="headerlink" title="类似枚举类的序列化问题"></a>类似枚举类的序列化问题</h2><p>在 Java 没有枚举类的时候，一般用如下的方法实现枚举类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Fruit</span> </span>&#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> ORANGE = <span class="keyword">new</span> Fruit(<span class="number">0</span>);</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> BANANA = <span class="keyword">new</span> Fruit(<span class="number">1</span>);</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> value;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">private</span> <span class="title">Fruit</span><span class="params">(<span class="keyword">int</span> value)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.value = value;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>例子如下。在反序列化的时候，虽然构造函数是私有的，但是反序列化仍然会创造一个新的对象。<br>这个对象的 <code>value</code> 也是 0，但是没有办法使用 <code>==</code> 与 <code>Fruit.ORANGE</code> 进行比较了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">objectOutputStream.write(Fruit.ORANGE);</span><br><span class="line">objectOutputStream.close();</span><br><span class="line">Fruit orange = (Fruit) objectInputStream.read();</span><br><span class="line">aseertt(orange == Fruit.ORANGE); <span class="comment">// Error</span></span><br></pre></td></tr></table></figure>
<h2 id="枚举类的序列化-1"><a href="#枚举类的序列化-1" class="headerlink" title="枚举类的序列化"></a>枚举类的序列化</h2><p>枚举类在序列化的时候，对于 <code>Enum</code> 子类的对象，只会把 <code>name</code> 属性输出。</p>
<p>在反序列化的时候，会调用 <code>Enum valueof(String name)</code> 方法对读取的 <code>name</code> 进行查找，而不会创建新的 枚举对象。</p>
<p>并且类 <code>Enum</code> 把 方法 <code>readObject</code> 和 <code>readObjectNoData</code> 都设为了私有，这样 <code>enum</code> 类无法自定义序列化过程。</p>
]]></content>
      <categories>
        <category>java</category>
        <category>basic</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>LinkedHashMap 源码分析</title>
    <url>/2020/java/basic/LinkedHashMap%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://juejin.im/post/5ace2bde6fb9a028e25deca8">搞懂 Java LinkedHashMap 源码</a></li>
</ul>
<h1 id="与-HashMap-的关系"><a href="#与-HashMap-的关系" class="headerlink" title="与 HashMap 的关系"></a>与 HashMap 的关系</h1><ul>
<li><p><code>LinkedHashMap</code> 继承自 <code>HashMap</code>，复用了 <code>HashMap</code> 的所有结构、操作和机制。</p>
</li>
<li><p>使用双向链表，记录节点的访问顺序。</p>
<ul>
<li><p>在 <code>accessOrder</code> 为 false 时，只有插入元素时，会把访问的节点放在链表结尾。</p>
</li>
<li><p>在 <code>accessOrder</code> 为 true 时，只要访问/更新元素，就会把访问的节点挪到链表结尾。</p>
</li>
</ul>
</li>
</ul>
<h1 id="实现方式"><a href="#实现方式" class="headerlink" title="实现方式"></a>实现方式</h1><h2 id="扩展节点"><a href="#扩展节点" class="headerlink" title="扩展节点"></a>扩展节点</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">HashMap</span>.<span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">   Entry&lt;K,V&gt; before, after;</span><br><span class="line">   Entry(<span class="keyword">int</span> hash, K key, V value, Node&lt;K,V&gt; next) &#123;</span><br><span class="line">       <span class="keyword">super</span>(hash, key, value, next);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在节点中，添加 before 和 after，来实现双向链表。</p>
<h2 id="创建节点"><a href="#创建节点" class="headerlink" title="创建节点"></a>创建节点</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// LinkedHashMap</span></span><br><span class="line"><span class="function">Node&lt;K,V&gt; <span class="title">newNode</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, Node&lt;K,V&gt; e)</span> </span>&#123;</span><br><span class="line">    LinkedHashMap.Entry&lt;K,V&gt; p =</span><br><span class="line">        <span class="keyword">new</span> LinkedHashMap.Entry&lt;K,V&gt;(hash, key, value, e);</span><br><span class="line">    linkNodeLast(p);</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// HashMap</span></span><br><span class="line"><span class="function">Node&lt;K,V&gt; <span class="title">newNode</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, Node&lt;K,V&gt; next)</span> </span>&#123;</span><br><span class="line">  	<span class="keyword">return</span> <span class="keyword">new</span> Node&lt;&gt;(hash, key, value, next);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>相比 <code>HashMap</code>，在创建节点后，会把节点加入到链表尾部。</p>
<h2 id="双向链表操作"><a href="#双向链表操作" class="headerlink" title="双向链表操作"></a>双向链表操作</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">afterNodeRemoval</span><span class="params">(Node&lt;K,V&gt; e)</span> </span>&#123; <span class="comment">// unlink</span></span><br><span class="line">    LinkedHashMap.Entry&lt;K,V&gt; p =</span><br><span class="line">        (LinkedHashMap.Entry&lt;K,V&gt;)e, b = p.before, a = p.after;</span><br><span class="line">    p.before = p.after = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">if</span> (b == <span class="keyword">null</span>)</span><br><span class="line">        head = a;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        b.after = a;</span><br><span class="line">    <span class="keyword">if</span> (a == <span class="keyword">null</span>)</span><br><span class="line">        tail = b;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        a.before = b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">afterNodeInsertion</span><span class="params">(<span class="keyword">boolean</span> evict)</span> </span>&#123; <span class="comment">// possibly remove eldest</span></span><br><span class="line">    LinkedHashMap.Entry&lt;K,V&gt; first;</span><br><span class="line">    <span class="keyword">if</span> (evict &amp;&amp; (first = head) != <span class="keyword">null</span> &amp;&amp; removeEldestEntry(first)) &#123;</span><br><span class="line">        K key = first.key;</span><br><span class="line">        removeNode(hash(key), key, <span class="keyword">null</span>, <span class="keyword">false</span>, <span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">afterNodeAccess</span><span class="params">(Node&lt;K,V&gt; e)</span> </span>&#123; <span class="comment">// move node to last</span></span><br><span class="line">    LinkedHashMap.Entry&lt;K,V&gt; last;</span><br><span class="line">    <span class="keyword">if</span> (accessOrder &amp;&amp; (last = tail) != e) &#123;</span><br><span class="line">        LinkedHashMap.Entry&lt;K,V&gt; p =</span><br><span class="line">            (LinkedHashMap.Entry&lt;K,V&gt;)e, b = p.before, a = p.after;</span><br><span class="line">        p.after = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">if</span> (b == <span class="keyword">null</span>)</span><br><span class="line">            head = a;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            b.after = a;</span><br><span class="line">        <span class="keyword">if</span> (a != <span class="keyword">null</span>)</span><br><span class="line">            a.before = b;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            last = b;</span><br><span class="line">        <span class="keyword">if</span> (last == <span class="keyword">null</span>)</span><br><span class="line">            head = p;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            p.before = last;</span><br><span class="line">            last.after = p;</span><br><span class="line">        &#125;</span><br><span class="line">        tail = p;</span><br><span class="line">        ++modCount;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>在调用 <code>remove()</code> 时，使用 <code>afterNodeRemoval()</code> 把节点从链表删除。</li>
<li>在调用 <code>get()</code>、<code>put</code> 更新已经存在的节点，使用 <code>afterNodeAccess()</code> 把节点挪到链表尾部。</li>
<li>在插入新的节点后，调用 <code>afterNodeInsertion()</code>，如果 <code>removeEldestEntry()</code> 为 true，则删除头结点（最旧的节点）。<ul>
<li>LinkedHashMap 中 <code>removeEldestEntry()</code> 会返回 false，这个功能是给子类用的。</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>basic</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>16. Executors 工具类</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/16Executors%E5%B7%A5%E5%85%B7%E7%B1%BB/</url>
    <content><![CDATA[<h1 id="16-Executors-工具类"><a href="#16-Executors-工具类" class="headerlink" title="16. Executors 工具类"></a>16. Executors 工具类</h1><p><code>Executors</code> 工具类提供了创建各类线程池的静态方法，这些方法都是基于 <code>ThreadPoolExecutor</code> 和 <code>ScheduledThreadPoolExecutor</code> 实现的。</p>
<h2 id="1-基于-ThreadPoolExecutor"><a href="#1-基于-ThreadPoolExecutor" class="headerlink" title="1. 基于 ThreadPoolExecutor"></a>1. 基于 ThreadPoolExecutor</h2><h3 id="1-1-newSingleThreadExecutor"><a href="#1-1-newSingleThreadExecutor" class="headerlink" title="1.1 newSingleThreadExecutor()"></a>1.1 newSingleThreadExecutor()</h3><ul>
<li><p>唯一一个核心工作线程不断从任务队列中取任务去执行。</p>
<ul>
<li>只有一个核心工作线程。</li>
<li>使用 <code>LinkedBlockingQueue</code>  <strong>无界</strong>模式 作为任务队列，所以任务总是可以放到队列里。</li>
</ul>
</li>
<li><p>使用 <code>FinalizableDelegatedExecutorService</code> 代理了 <code>ThreadPoolExecutor</code>。</p>
<ul>
<li>在被 GC 的时候，会调用 <code>shutdown()</code>。</li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadPoolExecutor</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">ThreadPoolExecutor</span><span class="params">(<span class="keyword">int</span> corePoolSize,</span></span></span><br><span class="line"><span class="function"><span class="params">                            <span class="keyword">int</span> maximumPoolSize,</span></span></span><br><span class="line"><span class="function"><span class="params">                            <span class="keyword">long</span> keepAliveTime,</span></span></span><br><span class="line"><span class="function"><span class="params">                            TimeUnit unit,</span></span></span><br><span class="line"><span class="function"><span class="params">                            BlockingQueue&lt;Runnable&gt; workQueue)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title">newSingleThreadExecutor</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> FinalizableDelegatedExecutorService</span><br><span class="line">        (<span class="keyword">new</span> ThreadPoolExecutor(<span class="number">1</span>, <span class="number">1</span>,</span><br><span class="line">                                <span class="number">0L</span>, TimeUnit.MILLISECONDS, <span class="comment">// poll(0) 等同于 poll()</span></span><br><span class="line">                                <span class="keyword">new</span> LinkedBlockingQueue&lt;Runnable&gt;()));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">FinalizableDelegatedExecutorService</span></span></span><br><span class="line"><span class="class">    <span class="keyword">extends</span> <span class="title">DelegatedExecutorService</span> </span>&#123;</span><br><span class="line">    FinalizableDelegatedExecutorService(ExecutorService executor) &#123;</span><br><span class="line">        <span class="keyword">super</span>(executor);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">finalize</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="1-2-newFixedThreadPool"><a href="#1-2-newFixedThreadPool" class="headerlink" title="1.2 newFixedThreadPool()"></a>1.2 newFixedThreadPool()</h3><ul>
<li><code>nThreads</code> 个核心工作线程不断从任务队列中取任务去执行。<ul>
<li>有 <code>nThreads</code> 个核心工作线程。</li>
<li>使用 <code>LinkedBlockingQueue</code> <strong>无界</strong>模式 作为任务队列，所以任务总是可以放到队列里。</li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title">newFixedThreadPool</span><span class="params">(<span class="keyword">int</span> nThreads)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> ThreadPoolExecutor(nThreads, nThreads,</span><br><span class="line">                                  <span class="number">0L</span>, TimeUnit.MILLISECONDS,</span><br><span class="line">                                  <span class="keyword">new</span> LinkedBlockingQueue&lt;Runnable&gt;());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="1-3-newCachedThreadPool"><a href="#1-3-newCachedThreadPool" class="headerlink" title="1.3 newCachedThreadPool()"></a>1.3 newCachedThreadPool()</h3><ul>
<li>没有核心工作线程。</li>
<li>可以有 <code>Integer.MAX_VALUE</code> 个工作线程。</li>
<li>每个工作线程如果 60s 没有领到任务，就结束。</li>
<li>使用 <code>SynchronousQueue</code>。<ul>
<li><code>poll(e)</code> 时，必须存在出队阻塞的操作，才会插入成功；否则返回false。</li>
<li>可以实现<strong>没有空闲的工作线程，就创建一个新的工作线程去执行任务</strong>。</li>
<li>因为没有空闲的工作线程 说明没有线程阻塞在 <code>SynchronousQueue</code> 的出队操作上，在 <code>poll(task)</code> 的时候会返回 false，继而会创建一个新的非工作线程。</li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title">newCachedThreadPool</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> ThreadPoolExecutor(<span class="number">0</span>, Integer.MAX_VALUE,</span><br><span class="line">                                  <span class="number">60L</span>, TimeUnit.SECONDS,</span><br><span class="line">                                  <span class="keyword">new</span> SynchronousQueue&lt;Runnable&gt;());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-基于-ScheduledThreadPoolExecutor"><a href="#2-基于-ScheduledThreadPoolExecutor" class="headerlink" title="2. 基于 ScheduledThreadPoolExecutor"></a>2. 基于 ScheduledThreadPoolExecutor</h2><h3 id="2-1-newSingleThreadScheduledExecutor"><a href="#2-1-newSingleThreadScheduledExecutor" class="headerlink" title="2.1 newSingleThreadScheduledExecutor()"></a>2.1 newSingleThreadScheduledExecutor()</h3><ul>
<li>唯一工作线程不断从延时队列中取任务去执行。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 任务总是放到延时队列中。因为 DelayedWorkQueue 是无上界的阻塞队列。</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ScheduledThreadPoolExecutor</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ScheduledThreadPoolExecutor</span><span class="params">(<span class="keyword">int</span> corePoolSize)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(corePoolSize, Integer.MAX_VALUE, <span class="number">0</span>, NANOSECONDS,</span><br><span class="line">              <span class="keyword">new</span> DelayedWorkQueue());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// corePoolSize = 1</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> ScheduledExecutorService <span class="title">newSingleThreadScheduledExecutor</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> DelegatedScheduledExecutorService</span><br><span class="line">        (<span class="keyword">new</span> ScheduledThreadPoolExecutor(<span class="number">1</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="2-2-newScheduledThreadPool"><a href="#2-2-newScheduledThreadPool" class="headerlink" title="2.2 newScheduledThreadPool()"></a>2.2 newScheduledThreadPool()</h3><ul>
<li><code>corePoolSize</code> 个工作线程不断从延时队列中取任务去执行。</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static ScheduledExecutorService newScheduledThreadPool(int corePoolSize) &#123;</span><br><span class="line">    return new ScheduledThreadPoolExecutor(corePoolSize);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>15. Timer</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/15Timer/</url>
    <content><![CDATA[<h1 id="15-Timer"><a href="#15-Timer" class="headerlink" title="15. Timer"></a>15. Timer</h1><p><code>Timer</code> 是 <code>java.util</code> 包的定时任务。</p>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><ol>
<li>使用一个数组实现的最小堆 <code>TaskQueue queue</code>。</li>
<li>在操作 <code>queue</code> 时，使用 <code>synchronized</code> 对加锁。</li>
<li>启动一个线程 TimerThread，循环去查看 <code>queue</code> 的首节点的时间是否到了，<ul>
<li>没有到则在 <code>queue</code> 上沉睡对应的时间。</li>
<li>如果首节点可以执行了，在执行前检查是否需要重复执行。<ul>
<li>如果需要，更新节点的 <code>nextExecutionTime</code>，重新放入堆中。</li>
<li>否则，从堆中删掉。</li>
</ul>
</li>
</ul>
</li>
<li>在有新的任务加到 <code>timer</code> 中后，如果新加的任务成为了首节点，因为需要睡眠的时间变小了，所以唤醒 TimerThread。</li>
</ol>
<h2 id="缺陷"><a href="#缺陷" class="headerlink" title="缺陷"></a>缺陷</h2><p>由于是只用一个线程去循环执行多个定时任务，会有两个缺陷：</p>
<ol>
<li>当前正在执行的任务耗时太久，导致后边的任务即使时间已经到了，也无法执行。</li>
<li>当前正在执行的任务抛出异常（除了 <code>InterruptedException</code>)，会使这个线程结束，后边的任务都不执行了。</li>
</ol>
<ul>
<li>此外，<code>Timer</code> 不能像 <code>ScheduleThreadPoolExecutor</code> 那样 <code>cancel(taks)</code> 掉一个任务。<br>只能用 <code>cancel()</code> 取消掉所有任务。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>14. ScheduledThreadPoolExecutor</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/14ScheduledThreadPoolExecutor/</url>
    <content><![CDATA[<h1 id="14-ScheduledThreadPoolExecutor"><a href="#14-ScheduledThreadPoolExecutor" class="headerlink" title="14. ScheduledThreadPoolExecutor"></a>14. ScheduledThreadPoolExecutor</h1><h2 id="0-引用"><a href="#0-引用" class="headerlink" title="0. 引用"></a>0. 引用</h2><ul>
<li><a href="https://www.cnblogs.com/binarylei/p/10963067.html">JUC源码分析-线程池篇（三）ScheduledThreadPoolExecutor</a></li>
</ul>
<h2 id="1-基本用法"><a href="#1-基本用法" class="headerlink" title="1. 基本用法"></a>1. 基本用法</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 delay 时间后执行 command 或 callable。</span></span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; schedule(Runnable command, <span class="keyword">long</span> delay, TimeUnit unit);</span><br><span class="line"><span class="keyword">public</span> &lt;V&gt; <span class="function">ScheduledFuture&lt;V&gt; <span class="title">schedule</span><span class="params">(Callable&lt;V&gt; callable, <span class="keyword">long</span> delay, TimeUnit unit)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 直接执行任务。</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable command)</span>；</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="comment">// 以固定频率执行 command。</span></span></span><br><span class="line"><span class="function"><span class="comment">// initialDelay 后执行第一次，initialDelay + period 后执行第二次，</span></span></span><br><span class="line"><span class="function"><span class="comment">// initialDelay + 2*period 后执行第三次。</span></span></span><br><span class="line"><span class="function"><span class="comment">// 如果一次执行时间超过了 period，后面的任务需要等待这次执行完才能执行，不会并行执行。</span></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; <span class="title">scheduleAtFixedRate</span><span class="params">(Runnable command, </span></span></span><br><span class="line"><span class="function"><span class="params">                                              <span class="keyword">long</span> initialDelay, </span></span></span><br><span class="line"><span class="function"><span class="params">                                              <span class="keyword">long</span> period, </span></span></span><br><span class="line"><span class="function"><span class="params">                                              TimeUnit unit)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 每次执行任务结束，过 delay 时间后，再执行下一次。</span></span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; scheduleWithFixedDelay(Runnable command,</span><br><span class="line">                                                 <span class="keyword">long</span> initialDelay,</span><br><span class="line">                                                 <span class="keyword">long</span> delay,</span><br><span class="line">                                                 TimeUnit unit);</span><br></pre></td></tr></table></figure>
<h2 id="2-基本思想"><a href="#2-基本思想" class="headerlink" title="2. 基本思想"></a>2. 基本思想</h2><p><code>ScheduledThreadPoolExecutor</code> 继承自 <code>ThreadPoolExecutor</code>，使用 <code>DelayedWorkQueue</code> + <code>ThreadPoolExecutor</code> + <code>ScheduledFutureTask</code> 实现。</p>
<p><img src="/images/ScheduledThreadPoolExecutor执行流程.svg" alt="ScheduledThreadPoolExecutor执行流程"></p>
<h2 id="3-DelayedWorkQueue"><a href="#3-DelayedWorkQueue" class="headerlink" title="3. DelayedWorkQueue"></a>3. DelayedWorkQueue</h2><p><code>DelayedWorkQueue</code> 是延时阻塞队列，类似于 <code>DelayQueue</code>。</p>
<p>与 <code>DelayQueue</code> 的算法都相同。</p>
<ul>
<li>使用 <code>ReentrantLock</code> 保证线程安全。</li>
<li>使用数组作为最小堆的存储结构。</li>
</ul>
<p>唯一的不同：</p>
<ul>
<li><code>remove(task)</code> 的时间复杂度从 $O(n)$ 降低到 $O(logn)$。</li>
<li><code>ScheduledFutureTask</code> 中记录了在数组中存储的下标，在 <code>remove()</code> 的时候，直接删除就可以了。</li>
<li>在 <code>task.cancel()</code> 任务时，会调用 <code>task.OuterClass.remove(task)</code> （<code>ScheduledFutureTask</code> 是 <code>DelayedWorkQueue</code> 的内部类）。</li>
</ul>
<p>容量是无限的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">remove</span><span class="params">(Object x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">this</span>.lock;</span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> i = indexOf(x);</span><br><span class="line">        <span class="keyword">if</span> (i &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">        setIndex(queue[i], -<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">int</span> s = --size;</span><br><span class="line">        RunnableScheduledFuture&lt;?&gt; replacement = queue[s];</span><br><span class="line">        queue[s] = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">if</span> (s != i) &#123;</span><br><span class="line">            siftDown(i, replacement);</span><br><span class="line">            <span class="keyword">if</span> (queue[i] == replacement)</span><br><span class="line">                siftUp(i, replacement);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// O(1) 找到节点的下标。</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">indexOf</span><span class="params">(Object x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (x != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (x <span class="keyword">instanceof</span> ScheduledFutureTask) &#123;</span><br><span class="line">            <span class="keyword">int</span> i = ((ScheduledFutureTask) x).heapIndex;</span><br><span class="line">            <span class="comment">// Sanity check; x could conceivably be a</span></span><br><span class="line">            <span class="comment">// ScheduledFutureTask from some other pool.</span></span><br><span class="line">            <span class="keyword">if</span> (i &gt;= <span class="number">0</span> &amp;&amp; i &lt; size &amp;&amp; queue[i] == x)</span><br><span class="line">                <span class="keyword">return</span> i;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; size; i++)</span><br><span class="line">                <span class="keyword">if</span> (x.equals(queue[i]))</span><br><span class="line">                    <span class="keyword">return</span> i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-ScheduledFutureTask"><a href="#4-ScheduledFutureTask" class="headerlink" title="4. ScheduledFutureTask"></a>4. ScheduledFutureTask</h2><p><code>ScheduledFutureTask</code> 存储了</p>
<ul>
<li>下一次执行的时间 <code>time</code></li>
<li>时间间隔 <code>period</code></li>
<li>最小堆数组中的下标 <code>helperIndex</code></li>
<li>下一次执行的任务（一般为自己）<code>outerTask</code>。</li>
</ul>
<p>每次执行 <code>ScheduledFutureTask</code> 结束后，更新下一次的执行时间 <code>time</code>，把任务再放到延时队列里。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 用于ScheduledFutureTask的序列号。</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> AtomicLong sequencer = <span class="keyword">new</span> AtomicLong();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">ScheduledFutureTask</span>&lt;<span class="title">V</span>&gt;</span></span><br><span class="line"><span class="class">        <span class="keyword">extends</span> <span class="title">FutureTask</span>&lt;<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">RunnableScheduledFuture</span>&lt;<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">long</span> sequenceNumber;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 下一次执行的时间戳（nanoTime单位）。</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">long</span> time;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 正数 fixed-rate execution。</span></span><br><span class="line">  	<span class="comment">// 负数 fixed-delay execution。</span></span><br><span class="line">  	<span class="comment">// 0 立即执行一次的任务。</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">long</span> period;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** The actual task to be re-enqueued by reExecutePeriodic */</span></span><br><span class="line">    RunnableScheduledFuture&lt;V&gt; outerTask = <span class="keyword">this</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在最小堆数组中的下标，用来 cancel 时从最小堆中删除节点。</span></span><br><span class="line">    <span class="keyword">int</span> heapIndex;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 不重复执行的任务。</span></span><br><span class="line">    ScheduledFutureTask(Runnable r, V result, <span class="keyword">long</span> ns) &#123;</span><br><span class="line">        <span class="keyword">super</span>(r, result);</span><br><span class="line">        <span class="keyword">this</span>.time = ns;</span><br><span class="line">        <span class="keyword">this</span>.period = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">this</span>.sequenceNumber = sequencer.getAndIncrement();</span><br><span class="line">    &#125;</span><br><span class="line">    ScheduledFutureTask(Callable&lt;V&gt; callable, <span class="keyword">long</span> ns) &#123;</span><br><span class="line">        <span class="keyword">super</span>(callable);</span><br><span class="line">        <span class="keyword">this</span>.time = ns;</span><br><span class="line">        <span class="keyword">this</span>.period = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">this</span>.sequenceNumber = sequencer.getAndIncrement();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// fixed-rate or fixed-delay execution 。</span></span><br><span class="line">    ScheduledFutureTask(Runnable r, V result, <span class="keyword">long</span> ns, <span class="keyword">long</span> period) &#123;</span><br><span class="line">        <span class="keyword">super</span>(r, result);</span><br><span class="line">        <span class="keyword">this</span>.time = ns;</span><br><span class="line">        <span class="keyword">this</span>.period = period;</span><br><span class="line">        <span class="keyword">this</span>.sequenceNumber = sequencer.getAndIncrement();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 还要多少纳秒可以执行。</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">long</span> <span class="title">getDelay</span><span class="params">(TimeUnit unit)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> unit.convert(time - now(), NANOSECONDS);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 如果两个元素 time 相同，使用 sequenceNumber 比较。</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(Delayed other)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (other == <span class="keyword">this</span>) <span class="comment">// compare zero if same object</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span> (other <span class="keyword">instanceof</span> ScheduledFutureTask) &#123;</span><br><span class="line">            ScheduledFutureTask&lt;?&gt; x = (ScheduledFutureTask&lt;?&gt;)other;</span><br><span class="line">            <span class="keyword">long</span> diff = time - x.time;</span><br><span class="line">            <span class="keyword">if</span> (diff &lt; <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (diff &gt; <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (sequenceNumber &lt; x.sequenceNumber)</span><br><span class="line">                <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">long</span> diff = getDelay(NANOSECONDS) - other.getDelay(NANOSECONDS);</span><br><span class="line">        <span class="keyword">return</span> (diff &lt; <span class="number">0</span>) ? -<span class="number">1</span> : (diff &gt; <span class="number">0</span>) ? <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">		<span class="comment">// 是否重复执行。</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isPeriodic</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> period != <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// time 记录了上次运行的时间。</span></span><br><span class="line">  	<span class="comment">// 1. fixed-rate 模式，把 time + period。即从上次运行后果 period 时间运行。</span></span><br><span class="line">  	<span class="comment">// 2. fixed-delay 模式，把 当前时间 + period。即从现在开始过 period 时间运行。</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">setNextRunTime</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> p = period;</span><br><span class="line">        <span class="keyword">if</span> (p &gt; <span class="number">0</span>)</span><br><span class="line">            time += p;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            time = triggerTime(-p);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 取消任务，并从队列中删除节点。</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">cancel</span><span class="params">(<span class="keyword">boolean</span> mayInterruptIfRunning)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">boolean</span> cancelled = <span class="keyword">super</span>.cancel(mayInterruptIfRunning);</span><br><span class="line">        <span class="keyword">if</span> (cancelled &amp;&amp; removeOnCancel &amp;&amp; heapIndex &gt;= <span class="number">0</span>)</span><br><span class="line">            remove(<span class="keyword">this</span>);</span><br><span class="line">        <span class="keyword">return</span> cancelled;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">boolean</span> periodic = isPeriodic();</span><br><span class="line">        <span class="keyword">if</span> (!canRunInCurrentRunState(periodic))</span><br><span class="line">            cancel(<span class="keyword">false</span>);</span><br><span class="line">      	<span class="comment">// 1. 如果时立即执行一次的任务，则直接执行任务。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!periodic)</span><br><span class="line">            ScheduledFutureTask.<span class="keyword">super</span>.run();</span><br><span class="line">      	<span class="comment">// 2. 是重复执行的任务，</span></span><br><span class="line">      	<span class="comment">//		a. 使用 runAndReset执行任务。</span></span><br><span class="line">      	<span class="comment">//		b. 任务执行结束后，设置 task.time，把 outerTask 再次加入到延时队列中。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (ScheduledFutureTask.<span class="keyword">super</span>.runAndReset()) &#123;</span><br><span class="line">            setNextRunTime();</span><br><span class="line">            reExecutePeriodic(outerTask);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">reExecutePeriodic</span><span class="params">(RunnableScheduledFuture&lt;?&gt; task)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (canRunInCurrentRunState(<span class="keyword">true</span>)) &#123;</span><br><span class="line">      	<span class="comment">// 把 task 加入到延时队列中。</span></span><br><span class="line">        <span class="keyword">super</span>.getQueue().add(task);</span><br><span class="line">        <span class="keyword">if</span> (!canRunInCurrentRunState(<span class="keyword">true</span>) &amp;&amp; remove(task))</span><br><span class="line">            task.cancel(<span class="keyword">false</span>);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            ensurePrestart();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="5-schedule"><a href="#5-schedule" class="headerlink" title="5. schedule()"></a>5. schedule()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 延时执行一次。</span></span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; schedule(Runnable command,</span><br><span class="line">                                   <span class="keyword">long</span> delay,</span><br><span class="line">                                   TimeUnit unit) &#123;</span><br><span class="line">    <span class="keyword">if</span> (command == <span class="keyword">null</span> || unit == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">  	<span class="comment">// 使用ScheduledFutureTask包装command、time。</span></span><br><span class="line">    RunnableScheduledFuture&lt;?&gt; t = decorateTask(command,</span><br><span class="line">        <span class="keyword">new</span> ScheduledFutureTask&lt;Void&gt;(command, <span class="keyword">null</span>,</span><br><span class="line">                                      triggerTime(delay, unit)));</span><br><span class="line">    <span class="comment">// 放入延时队列，创建工作线程。</span></span><br><span class="line">  	delayedExecute(t);</span><br><span class="line">    <span class="keyword">return</span> t;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 直接调用。</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable command)</span> </span>&#123;</span><br><span class="line">    schedule(command, <span class="number">0</span>, NANOSECONDS);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; scheduleAtFixedRate(Runnable command,</span><br><span class="line">                                              <span class="keyword">long</span> initialDelay,</span><br><span class="line">                                              <span class="keyword">long</span> period,</span><br><span class="line">                                              TimeUnit unit) &#123;</span><br><span class="line">    <span class="keyword">if</span> (command == <span class="keyword">null</span> || unit == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">if</span> (period &lt;= <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">    ScheduledFutureTask&lt;Void&gt; sft =</span><br><span class="line">        <span class="keyword">new</span> ScheduledFutureTask&lt;Void&gt;(command,</span><br><span class="line">                                      <span class="keyword">null</span>,</span><br><span class="line">                                      triggerTime(initialDelay, unit),</span><br><span class="line">                                      unit.toNanos(period));</span><br><span class="line">    RunnableScheduledFuture&lt;Void&gt; t = decorateTask(command, sft);</span><br><span class="line">    <span class="comment">// 把 outerTask 设为自己。</span></span><br><span class="line">  	sft.outerTask = t;</span><br><span class="line">    delayedExecute(t);</span><br><span class="line">    <span class="keyword">return</span> t;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> ScheduledFuture&lt;?&gt; scheduleWithFixedDelay(Runnable command,</span><br><span class="line">                                                 <span class="keyword">long</span> initialDelay,</span><br><span class="line">                                                 <span class="keyword">long</span> delay,</span><br><span class="line">                                                 TimeUnit unit) &#123;</span><br><span class="line">    <span class="keyword">if</span> (command == <span class="keyword">null</span> || unit == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">if</span> (delay &lt;= <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">    ScheduledFutureTask&lt;Void&gt; sft =</span><br><span class="line">        <span class="keyword">new</span> ScheduledFutureTask&lt;Void&gt;(command,</span><br><span class="line">                                      <span class="keyword">null</span>,</span><br><span class="line">                                      triggerTime(initialDelay, unit),</span><br><span class="line">                                      unit.toNanos(-delay)); <span class="comment">// 注意是 -delay。</span></span><br><span class="line">    RunnableScheduledFuture&lt;Void&gt; t = decorateTask(command, sft);</span><br><span class="line">    <span class="comment">// 把 outerTask 设为自己。</span></span><br><span class="line">    sft.outerTask = t;</span><br><span class="line">    delayedExecute(t);</span><br><span class="line">    <span class="keyword">return</span> t;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="delayedExecute"><a href="#delayedExecute" class="headerlink" title="delayedExecute()"></a>delayedExecute()</h3><p>因为 <code>ScheduledThreadPoolExecutor</code> 的 <code>schedule()</code> 和 <code>submit()</code> 方法都是把任务放入延时队列，没有调用过子类的 <code>ThreadPoolExecutor.execute()</code> 方法。所以必须手动 <code>addWorker()</code>。</p>
<p>会不断补充核心线程，知道达到核心线程数目。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">delayedExecute</span><span class="params">(RunnableScheduledFuture&lt;?&gt; task)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (isShutdown())</span><br><span class="line">        reject(task);</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.getQueue().add(task);</span><br><span class="line">        <span class="keyword">if</span> (isShutdown() &amp;&amp;</span><br><span class="line">            !canRunInCurrentRunState(task.isPeriodic()) &amp;&amp;</span><br><span class="line">            remove(task))</span><br><span class="line">            task.cancel(<span class="keyword">false</span>);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            ensurePrestart();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 保证执行前至少有1个工作线程。</span></span><br><span class="line"><span class="comment">// 工作线程数目小于corePoolSize，就创建一个核心线程。</span></span><br><span class="line"><span class="comment">// 如果corePoolSize是0，也创建1个非核心线程，来保证至少有1个工作线程。</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ensurePrestart</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> wc = workerCountOf(ctl.get());</span><br><span class="line">  <span class="keyword">if</span> (wc &lt; corePoolSize)</span><br><span class="line">    addWorker(<span class="keyword">null</span>, <span class="keyword">true</span>);</span><br><span class="line">  <span class="keyword">else</span> <span class="keyword">if</span> (wc == <span class="number">0</span>)</span><br><span class="line">    addWorker(<span class="keyword">null</span>, <span class="keyword">false</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>13. ExecutorService 源码分析</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/13ExecutorService%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="http://www.throwable.club/2019/07/27/java-concurrency-executor-service/"><a href="http://www.throwable.club/2019/07/27/java-concurrency-executor-service/">JUC线程池服务ExecutorService接口实现源码分析</a></a></li>
</ul>
<h1 id="FutureTask"><a href="#FutureTask" class="headerlink" title="FutureTask"></a>FutureTask</h1><h2 id="主要接口"><a href="#主要接口" class="headerlink" title="主要接口"></a>主要接口</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">RunnableFuture</span>&lt;<span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">Runnable</span>, <span class="title">Future</span>&lt;<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">   </span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>;</span><br><span class="line">&#125;    </span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Future</span>&lt;<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 取消任务。</span></span><br><span class="line">  	<span class="comment">//		1. mayInterruptIfRunning 用于控制是否要中断任务。</span></span><br><span class="line">  	<span class="comment">//				注意，任务可能不响应中断。</span></span><br><span class="line">  	<span class="comment">//		2. 会唤醒所有调用 get() 的线程。</span></span><br><span class="line">  	<span class="comment">//		3. 调用 get() 的线程抛出 CancellationException。</span></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">cancel</span><span class="params">(<span class="keyword">boolean</span> mayInterruptIfRunning)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 任务是否被取消了。</span></span><br><span class="line">  	<span class="comment">//	状态是否为 CANCELLED、INTERRUPTING、INTERRUPTED。</span></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">isCancelled</span><span class="params">()</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 任务是否结束了.</span></span><br><span class="line">  	<span class="comment">//	包括正常和异常的情况。</span></span><br><span class="line">  	<span class="comment">//	状态是否不等于 NEW。</span></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">isDone</span><span class="params">()</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 永久阻塞获取结果，响应中断</span></span><br><span class="line">    <span class="function">V <span class="title">get</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException, ExecutionException</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 带超时的阻塞获取结果，响应中断</span></span><br><span class="line">    <span class="function">V <span class="title">get</span><span class="params">(<span class="keyword">long</span> timeout, TimeUnit unit)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> InterruptedException, ExecutionException, TimeoutException</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h2><p><code>FutureTask</code> 一般是 <code>AbstractExecutorService</code> 在 <code>submit()</code> 时候创建的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FutureTask</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 适配使用Callable类型任务的场景</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">FutureTask</span><span class="params">(Callable&lt;V&gt; callable)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (callable == <span class="keyword">null</span>)</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">      <span class="keyword">this</span>.callable = callable;</span><br><span class="line">      <span class="keyword">this</span>.state = NEW;       </span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 适配使用Runnable类型任务和已经提供了最终计算结果的场景</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">FutureTask</span><span class="params">(Runnable runnable, V result)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">this</span>.callable = Executors.callable(runnable, result);</span><br><span class="line">      <span class="keyword">this</span>.state = NEW;      </span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Callable</span>&lt;<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function">V <span class="title">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Executors</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 将 Runnable 包装为 Callable，返回值是传入的 result。</span></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">Callable&lt;T&gt; <span class="title">callable</span><span class="params">(Runnable task, T result)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (task == <span class="keyword">null</span>)</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> RunnableAdapter&lt;T&gt;(task, result);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Runnable 到 Callable 的适配器。</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">RunnableAdapter</span>&lt;<span class="title">T</span>&gt; <span class="keyword">implements</span> <span class="title">Callable</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">      <span class="keyword">private</span> <span class="keyword">final</span> Runnable task;</span><br><span class="line">      <span class="keyword">private</span> <span class="keyword">final</span> T result;</span><br><span class="line">      RunnableAdapter(Runnable task, T result) &#123;</span><br><span class="line">          <span class="keyword">this</span>.task = task;</span><br><span class="line">          <span class="keyword">this</span>.result = result;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="function"><span class="keyword">public</span> T <span class="title">call</span><span class="params">()</span> </span>&#123;</span><br><span class="line">          task.run();</span><br><span class="line">          <span class="keyword">return</span> result;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">          <span class="keyword">return</span> <span class="keyword">super</span>.toString() + <span class="string">"[Wrapped task = "</span> + task + <span class="string">"]"</span>;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="状态转化"><a href="#状态转化" class="headerlink" title="状态转化"></a>状态转化</h2><p><img src="/images/FutureTask状态转化.svg" alt="FutureTask状态转化"></p>
<h2 id="调用函数的执行流程"><a href="#调用函数的执行流程" class="headerlink" title="调用函数的执行流程"></a>调用函数的执行流程</h2><p><img src="/images/FutureTask执行流程.svg" alt="FutureTask执行流程"></p>
<ul>
<li>内部使用链表组成的栈，来记录调用 <code>get()</code> 的线程。</li>
<li>使用 CAS 出栈和入栈。</li>
<li>使用 <code>LockSupport.park()/unpark()</code> 来阻塞和唤醒调用 <code>get()</code> 的线程。</li>
<li><code>finishCompletion()</code> 最后会执行 <code>done()</code> 钩子函数。</li>
</ul>
<h1 id="AbstractExecutorService"><a href="#AbstractExecutorService" class="headerlink" title="AbstractExecutorService"></a>AbstractExecutorService</h1><h2 id="submit"><a href="#submit" class="headerlink" title="submit()"></a>submit()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ThreadPoolExecutor 的父类。</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractExecutorService</span> <span class="keyword">implements</span> <span class="title">ExecutorService</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 静态工厂方法，用 Runnable 实例、提前设好的返回结果，创建 FutureTask 实例</span></span><br><span class="line">    <span class="keyword">protected</span> &lt;T&gt; <span class="function">RunnableFuture&lt;T&gt; <span class="title">newTaskFor</span><span class="params">(Runnable runnable, T value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> FutureTask&lt;T&gt;(runnable, value);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态工厂方法，用 Callable 实例，创建 FutureTask 实例。</span></span><br><span class="line">    <span class="keyword">protected</span> &lt;T&gt; <span class="function">RunnableFuture&lt;T&gt; <span class="title">newTaskFor</span><span class="params">(Callable&lt;T&gt; callable)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> FutureTask&lt;T&gt;(callable);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 提交Runnable类型任务，get()的返回值为null。</span></span><br><span class="line">    <span class="keyword">public</span> Future&lt;?&gt; submit(Runnable task) &#123;</span><br><span class="line">        <span class="keyword">if</span> (task == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">        <span class="comment">// 适配任务为FutureTask实例，注意最终计算结果已经提前设置为null</span></span><br><span class="line">        RunnableFuture&lt;Void&gt; ftask = newTaskFor(task, <span class="keyword">null</span>);</span><br><span class="line">        <span class="comment">// 提交到线程池</span></span><br><span class="line">        execute(ftask);</span><br><span class="line">        <span class="keyword">return</span> ftask;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 提交Runnable类型任务，get()的返回值为传入的result。</span></span><br><span class="line">    <span class="keyword">public</span> &lt;T&gt; <span class="function">Future&lt;T&gt; <span class="title">submit</span><span class="params">(Runnable task, T result)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (task == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">        <span class="comment">// 适配任务为FutureTask实例</span></span><br><span class="line">        RunnableFuture&lt;T&gt; ftask = newTaskFor(task, result);</span><br><span class="line">        <span class="comment">// 提交到线程池</span></span><br><span class="line">        execute(ftask);</span><br><span class="line">        <span class="keyword">return</span> ftask;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 提交Callable类型任务，get()的返回值为计算的结果。</span></span><br><span class="line">    <span class="keyword">public</span> &lt;T&gt; <span class="function">Future&lt;T&gt; <span class="title">submit</span><span class="params">(Callable&lt;T&gt; task)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (task == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">         <span class="comment">// 适配任务为FutureTask实例</span></span><br><span class="line">        RunnableFuture&lt;T&gt; ftask = newTaskFor(task);</span><br><span class="line">        <span class="comment">// 提交到线程池</span></span><br><span class="line">        execute(ftask);</span><br><span class="line">        <span class="keyword">return</span> ftask;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>submit(Callable task)</code> 中，</p>
<ol>
<li>任务提交到 <code>execute(futureTask)</code> 后，</li>
<li>工作线程领到这个任务，开始执行 <code>futureTask.run()</code>。</li>
<li><code>futureTask.run()</code> 中会执行 <code>futureTask.callable.call()</code>。</li>
</ol>
<p>这样工作线程就执行到了传入的 <code>Callable</code> 的 <code>task</code>。</p>
<h2 id="cancelAll"><a href="#cancelAll" class="headerlink" title="cancelAll()"></a>cancelAll()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取消所有的Future实例</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">cancelAll</span><span class="params">(ArrayList&lt;Future&lt;T&gt;&gt; futures)</span> </span>&#123;</span><br><span class="line">  cancelAll(futures, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 遍历所有的Future实例调用其cancel方法，因为参数为true，所以会响应中断</span></span><br><span class="line"><span class="comment">// j参数是决定遍历的起点，0表示整个列表遍历</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">cancelAll</span><span class="params">(ArrayList&lt;Future&lt;T&gt;&gt; futures, <span class="keyword">int</span> j)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> size = futures.size(); j &lt; size; j++)</span><br><span class="line">    futures.get(j).cancel(<span class="keyword">true</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>循环遍历传入的 FutureTask 的实例列表，调用每一个实例的 <code>cancel(true)</code> 方法。</p>
<h2 id="invokeAny"><a href="#invokeAny" class="headerlink" title="invokeAny()"></a>invokeAny()</h2><h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><p><code>&lt;T&gt; T invokeAny(Collection&lt;? extends Callable&lt;T&gt;&gt; tasks)</code> 函数会执行列表中的任意一个任务。可能会执行多个任务，但是只会返回第一个执行完的结果。</p>
<h3 id="基本原理"><a href="#基本原理" class="headerlink" title="基本原理"></a>基本原理</h3><ul>
<li>使用一个 <code>BlockingQueue</code>，在任务执行完后，会放入队列。</li>
<li>遍历 <code>tasks</code>，如果非阻塞出队失败（即还没有任务执行完），就 <code>submit()</code> 当前遍历到的 <code>task</code>。</li>
<li>如果都遍历完了，就阻塞出队。</li>
</ul>
<h3 id="实际的实现"><a href="#实际的实现" class="headerlink" title="实际的实现"></a>实际的实现</h3><ul>
<li><p>使用一个内部类 <code>ExecutorCompletionService</code>，它内置了一个 <code>LinkedBlockingQueue completionQueue</code>。</p>
</li>
<li><p>提交的任务，会使用 <code>FutureTask</code> 的子类 <code>QueueingFuture</code> 包装。</p>
<ul>
<li>使用钩子函数 <code>done()</code>，在任务执行完成后，把它放入 <code>completionQueue</code> 中。</li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ExecutorCompletionService</span>&lt;<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">CompletionService</span>&lt;<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Executor executor;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> AbstractExecutorService aes;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> BlockingQueue&lt;Future&lt;V&gt;&gt; completionQueue;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ExecutorCompletionService</span><span class="params">(Executor executor)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (executor == <span class="keyword">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">        <span class="keyword">this</span>.executor = executor;</span><br><span class="line">        <span class="keyword">this</span>.aes = (executor <span class="keyword">instanceof</span> AbstractExecutorService) ?</span><br><span class="line">            (AbstractExecutorService) executor : <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">this</span>.completionQueue = <span class="keyword">new</span> LinkedBlockingQueue&lt;Future&lt;V&gt;&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">QueueingFuture</span> <span class="keyword">extends</span> <span class="title">FutureTask</span>&lt;<span class="title">Void</span>&gt; </span>&#123;</span><br><span class="line">      QueueingFuture(RunnableFuture&lt;V&gt; task) &#123;</span><br><span class="line">        <span class="keyword">super</span>(task, <span class="keyword">null</span>);</span><br><span class="line">        <span class="keyword">this</span>.task = task;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">done</span><span class="params">()</span> </span>&#123; completionQueue.add(task); &#125;</span><br><span class="line">      <span class="keyword">private</span> <span class="keyword">final</span> Future&lt;V&gt; task;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> Future&lt;V&gt; <span class="title">submit</span><span class="params">(Callable&lt;V&gt; task)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (task == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">      RunnableFuture&lt;V&gt; f = newTaskFor(task);</span><br><span class="line">      executor.execute(<span class="keyword">new</span> QueueingFuture(f));</span><br><span class="line">      <span class="keyword">return</span> f;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">  	<span class="function"><span class="keyword">public</span> Future&lt;V&gt; <span class="title">take</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> completionQueue.take();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Future&lt;V&gt; <span class="title">poll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> completionQueue.poll();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Future&lt;V&gt; <span class="title">poll</span><span class="params">(<span class="keyword">long</span> timeout, TimeUnit unit)</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> completionQueue.poll(timeout, unit);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="invokeAll"><a href="#invokeAll" class="headerlink" title="invokeAll()"></a>invokeAll()</h2><p><code>&lt;T&gt; List&lt;Future&lt;T&gt;&gt; invokeAll(Collection&lt;? extends Callable&lt;T&gt;&gt; tasks)</code> 函数会执行列表中的所有任务。</p>
<ul>
<li>遍历任务列表，<code>submit()</code> 每个任务得到 <code>futureTask</code> 列表。</li>
<li>遍历  <code>futureTask</code> 列表，执行每个 <code>futureTask</code> 的 <code>get(0)</code> 方法。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>12. ThreadLocalRandom</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/12ThreadLocalRandom/</url>
    <content><![CDATA[<h1 id="Random"><a href="#Random" class="headerlink" title="Random"></a>Random</h1><p><code>Random</code> 类在生成随机数时，所有线程 CAS 竞争同一个 <code>seed</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> AtomicLong seed;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">int</span> <span class="title">next</span><span class="params">(<span class="keyword">int</span> bits)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">long</span> oldseed, nextseed;</span><br><span class="line">    AtomicLong seed = <span class="keyword">this</span>.seed;</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        oldseed = seed.get();</span><br><span class="line">        nextseed = (oldseed * multiplier + addend) &amp; mask;</span><br><span class="line">    &#125; <span class="keyword">while</span> (!seed.compareAndSet(oldseed, nextseed));</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">int</span>)(nextseed &gt;&gt;&gt; (<span class="number">48</span> - bits));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="ThreadLocalRandom"><a href="#ThreadLocalRandom" class="headerlink" title="ThreadLocalRandom"></a>ThreadLocalRandom</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> java.util.concurrent;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadLocalRandom</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> ThreadLocalRandom <span class="title">current</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (UNSAFE.getInt(Thread.currentThread(), PROBE) == <span class="number">0</span>)</span><br><span class="line">            localInit();</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">localInit</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> p = probeGenerator.addAndGet(PROBE_INCREMENT);</span><br><span class="line">        <span class="keyword">int</span> probe = (p == <span class="number">0</span>) ? <span class="number">1</span> : p; <span class="comment">// skip 0</span></span><br><span class="line">        <span class="keyword">long</span> seed = mix64(seeder.getAndAdd(SEEDER_INCREMENT));</span><br><span class="line">        Thread t = Thread.currentThread();</span><br><span class="line">        UNSAFE.putLong(t, SEED, seed);</span><br><span class="line">        UNSAFE.putInt(t, PROBE, probe);</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">  	<span class="comment">// 使用 probe 作为 hash 值，出现竞争时，调整 probe。</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">advanceProbe</span><span class="params">(<span class="keyword">int</span> probe)</span> </span>&#123;</span><br><span class="line">        probe ^= probe &lt;&lt; <span class="number">13</span>;   <span class="comment">// xorshift</span></span><br><span class="line">        probe ^= probe &gt;&gt;&gt; <span class="number">17</span>;</span><br><span class="line">        probe ^= probe &lt;&lt; <span class="number">5</span>;</span><br><span class="line">        UNSAFE.putInt(Thread.currentThread(), PROBE, probe);</span><br><span class="line">        <span class="keyword">return</span> probe;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="comment">// Unsafe mechanics</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> sun.misc.Unsafe UNSAFE;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> SEED;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> PROBE;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> SECONDARY;</span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            UNSAFE = sun.misc.Unsafe.getUnsafe();</span><br><span class="line">            Class&lt;?&gt; tk = Thread.class;</span><br><span class="line">            SEED = UNSAFE.objectFieldOffset</span><br><span class="line">                (tk.getDeclaredField(<span class="string">"threadLocalRandomSeed"</span>));</span><br><span class="line">            PROBE = UNSAFE.objectFieldOffset</span><br><span class="line">                (tk.getDeclaredField(<span class="string">"threadLocalRandomProbe"</span>));</span><br><span class="line">            SECONDARY = UNSAFE.objectFieldOffset</span><br><span class="line">                (tk.getDeclaredField(<span class="string">"threadLocalRandomSecondarySeed"</span>));</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Error(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> java.util;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">    <span class="comment">/** The current seed for a ThreadLocalRandom */</span></span><br><span class="line">    <span class="meta">@sun</span>.misc.Contended(<span class="string">"tlr"</span>)</span><br><span class="line">    <span class="keyword">long</span> threadLocalRandomSeed;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** Probe hash value; nonzero if threadLocalRandomSeed initialized */</span></span><br><span class="line">    <span class="meta">@sun</span>.misc.Contended(<span class="string">"tlr"</span>)</span><br><span class="line">    <span class="keyword">int</span> threadLocalRandomProbe;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** Secondary seed isolated from public ThreadLocalRandom sequence */</span></span><br><span class="line">    <span class="meta">@sun</span>.misc.Contended(<span class="string">"tlr"</span>)</span><br><span class="line">    <span class="keyword">int</span> threadLocalRandomSecondarySeed;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>每个线程第一次调用 <code>current()</code> 会分配 <code>seed</code> 和 <code>probe</code> 给 <code>Thread</code>。</p>
<ul>
<li>注意，<code>Thread</code> 中的 <code>seed</code> 和 <code>probe</code> 是包访问权限的，而 <code>ThreadLocalRandom</code> 与其不在一个包，所以需要使用 unsafe 方法更新他们。</li>
</ul>
</li>
<li><p><code>probe</code> 的作用。</p>
<ul>
<li>初始化标识，来表示是否初始化了线程 <code>ThreadLocalRandom</code> 相关的域。</li>
<li>作为线程的内部 hash 值，在 ConcurrentHashMap 等类中需要把 Thread 映射为下标的时候使用。避免碰撞。因为 <code>Thread.hashCode()</code> 返回的是 <code>Thread</code> 对象的地址，没有 <code>probe</code> 的值均匀。</li>
<li>使用 probe 作为 hash 值，出现竞争时，使用 <code>advanceProbe()</code>，调整 probe。</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>11. ThreadPoolExecutor 源码分析</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/11ThreadPoolExecutor%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="11-ThreadPoolExecutor-源码分析"><a href="#11-ThreadPoolExecutor-源码分析" class="headerlink" title="11. ThreadPoolExecutor 源码分析"></a>11. ThreadPoolExecutor 源码分析</h1><h2 id="0-引用"><a href="#0-引用" class="headerlink" title="0. 引用"></a>0. 引用</h2><ul>
<li><a href="https://www.cnblogs.com/binarylei/p/10952055.html">JUC源码分析-线程池篇（一）：ThreadPoolExecutor</a></li>
</ul>
<h2 id="1-数据结构"><a href="#1-数据结构" class="headerlink" title="1. 数据结构"></a>1. 数据结构</h2><h3 id="线程池表示"><a href="#线程池表示" class="headerlink" title="线程池表示"></a>线程池表示</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ctl 高 3 位：线程池状态，低 29 位：工作线程数</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> AtomicInteger ctl = <span class="keyword">new</span> AtomicInteger(ctlOf(RUNNING, <span class="number">0</span>));</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> COUNT_BITS = Integer.SIZE - <span class="number">3</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CAPACITY   = (<span class="number">1</span> &lt;&lt; COUNT_BITS) - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// runState is stored in the high-order bits</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> RUNNING    = -<span class="number">1</span> &lt;&lt; COUNT_BITS;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> SHUTDOWN   =  <span class="number">0</span> &lt;&lt; COUNT_BITS;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> STOP       =  <span class="number">1</span> &lt;&lt; COUNT_BITS;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TIDYING    =  <span class="number">2</span> &lt;&lt; COUNT_BITS;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TERMINATED =  <span class="number">3</span> &lt;&lt; COUNT_BITS;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 从 ctl 获取线程池状态</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">runStateOf</span><span class="params">(<span class="keyword">int</span> c)</span>     </span>&#123; <span class="keyword">return</span> c &amp; ~CAPACITY; &#125;</span><br><span class="line"><span class="comment">// 从 ctl 获取工作线程数</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">workerCountOf</span><span class="params">(<span class="keyword">int</span> c)</span>  </span>&#123; <span class="keyword">return</span> c &amp; CAPACITY; &#125;</span><br><span class="line"><span class="comment">// 用线程池状态、工作线程数 构造ctl</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ctlOf</span><span class="params">(<span class="keyword">int</span> rs, <span class="keyword">int</span> wc)</span> </span>&#123; <span class="keyword">return</span> rs | wc; &#125;</span><br></pre></td></tr></table></figure>
<h2 id="2-构造函数"><a href="#2-构造函数" class="headerlink" title="2. 构造函数"></a>2. 构造函数</h2><h2 id="3-execute"><a href="#3-execute" class="headerlink" title="3. execute()"></a>3. execute()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable command)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (command == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">  	<span class="comment">// 1. 工作线程数目&lt;核心线程数目，创建新的核心线程执行任务。</span></span><br><span class="line">    <span class="keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;</span><br><span class="line">        <span class="keyword">if</span> (addWorker(command, <span class="keyword">true</span>))</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        c = ctl.get();</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// 2. 工作线程数目&gt;=核心线程数目、任务队列未满，将任务提交到任务队列中。</span></span><br><span class="line">    <span class="keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;</span><br><span class="line">        <span class="keyword">int</span> recheck = ctl.get();</span><br><span class="line">      	<span class="comment">// 2.1 如果这时线程池关闭了、且任务还没有开始执行（开始执行会从队列中取出，所以删除会失败），</span></span><br><span class="line">      	<span class="comment">//		把任务从队列删除、执行拒绝策略。</span></span><br><span class="line">        <span class="keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))</span><br><span class="line">            reject(command);</span><br><span class="line">      	<span class="comment">// 2.2 没有工作线程（可能corePoolSize为0），</span></span><br><span class="line">      	<span class="comment">//		创建新的非核心线程。这个线程会去队列里拿任务执行。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (workerCountOf(recheck) == <span class="number">0</span>)</span><br><span class="line">            addWorker(<span class="keyword">null</span>, <span class="keyword">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// 3. 工作线程数目&gt;=核心线程数目、任务队列已满，创建新的非核心线程执行任务。</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!addWorker(command, <span class="keyword">false</span>))</span><br><span class="line">      	<span class="comment">// 如果创建失败（线程数目达到最大值、或线程池关闭了），执行拒绝策略。</span></span><br><span class="line">        reject(command);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">reject</span><span class="params">(Runnable command)</span> </span>&#123;</span><br><span class="line">    handler.rejectedExecution(command, <span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调用线程池的域 <code>RejectedExecutionHandler</code> 实例的回调任务函数。</p>
<h2 id="4-addWorker"><a href="#4-addWorker" class="headerlink" title="4. addWorker()"></a>4. addWorker()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">addWorker</span><span class="params">(Runnable firstTask, <span class="keyword">boolean</span> core)</span> </span>&#123;</span><br><span class="line">    retry:</span><br><span class="line">  	<span class="comment">// 1. 检查是否可以创建新的线程，多个线程使用 workerCount 串行化。</span></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">        <span class="keyword">int</span> rs = runStateOf(c);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1.1. 检查线程池状态。</span></span><br><span class="line">      	<span class="comment">// 如果a. 线程池为SHUTDOWN状态，并且传入的任务不为空（execute传过来 要执行新任务的）、或任务队列为空；</span></span><br><span class="line">      	<span class="comment">//		b. 线程池过了SHUTDOWN状态了。</span></span><br><span class="line">      	<span class="comment">// 则不能再创建新的线程。</span></span><br><span class="line">        <span class="keyword">if</span> (rs &gt;= SHUTDOWN &amp;&amp;</span><br><span class="line">            ! (rs == SHUTDOWN &amp;&amp;</span><br><span class="line">               firstTask == <span class="keyword">null</span> &amp;&amp;</span><br><span class="line">               ! workQueue.isEmpty()))</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">int</span> wc = workerCountOf(c);</span><br><span class="line">          	<span class="comment">// 1.2. 检查线程池容量。</span></span><br><span class="line">          	<span class="comment">// 如果a. 要创建核心线程、且工作线程数目超过核心线程数目了</span></span><br><span class="line">          	<span class="comment">//		b. 要创建非核心线程、且工作线程数目超过最大线程数目了</span></span><br><span class="line">          	<span class="comment">// 不能再创建新的线程。</span></span><br><span class="line">            <span class="keyword">if</span> (wc &gt;= CAPACITY ||</span><br><span class="line">                wc &gt;= (core ? corePoolSize : maximumPoolSize))</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">          	<span class="comment">// 1.3. 把工作线程数目 CAS 加1。</span></span><br><span class="line">            <span class="keyword">if</span> (compareAndIncrementWorkerCount(c))</span><br><span class="line">                <span class="keyword">break</span> retry;</span><br><span class="line">            c = ctl.get();  <span class="comment">// Re-read ctl</span></span><br><span class="line">          	<span class="comment">// 1.4. 如果 1.3步骤 中 CAS 失败了。</span></span><br><span class="line">          	<span class="comment">// 		a. 线程池状态发生变化了，返回1.1步骤。</span></span><br><span class="line">          	<span class="comment">//		b. 有其他线程改变了工作线程数目，返回1.2步骤。</span></span><br><span class="line">            <span class="keyword">if</span> (runStateOf(c) != rs)</span><br><span class="line">                <span class="keyword">continue</span> retry;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 2. 开始创建工作线程、并运行。</span></span><br><span class="line">    <span class="keyword">boolean</span> workerStarted = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">boolean</span> workerAdded = <span class="keyword">false</span>;</span><br><span class="line">    Worker w = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 2.1. 创建工作线程。</span></span><br><span class="line">        w = <span class="keyword">new</span> Worker(firstTask);</span><br><span class="line">        <span class="keyword">final</span> Thread t = w.thread;</span><br><span class="line">        <span class="keyword">if</span> (t != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">          	<span class="comment">// 2.2 加全局锁。</span></span><br><span class="line">          	<span class="comment">//		因为要操作HashMap、线程内、全局计数、线程池可能正在terminate。</span></span><br><span class="line">            mainLock.lock();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// Recheck while holding lock.</span></span><br><span class="line">                <span class="comment">// Back out on ThreadFactory failure or if</span></span><br><span class="line">                <span class="comment">// shut down before lock acquired.</span></span><br><span class="line">                <span class="keyword">int</span> rs = runStateOf(ctl.get());</span><br><span class="line"></span><br><span class="line">              	<span class="comment">// 2.3 检查线程池状态。</span></span><br><span class="line">              	<span class="comment">//	如果 a. 线程池是SHUTDOWN状态、且传入任务不为空</span></span><br><span class="line">              	<span class="comment">//			b. 线程池度过SHUTDOWN状态了。</span></span><br><span class="line">              	<span class="comment">//	则不能使用这个线程，添加线程失败。</span></span><br><span class="line">                <span class="keyword">if</span> (rs &lt; SHUTDOWN ||</span><br><span class="line">                    (rs == SHUTDOWN &amp;&amp; firstTask == <span class="keyword">null</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (t.isAlive()) <span class="comment">// precheck that t is startable</span></span><br><span class="line">                        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalThreadStateException();</span><br><span class="line">                    <span class="comment">// 2.4 把新的工作线程加入 HashSet 中。</span></span><br><span class="line">                  	workers.add(w);</span><br><span class="line">                    <span class="keyword">int</span> s = workers.size();</span><br><span class="line">                  	<span class="comment">// 2.5 更新线程池最大线程数目。</span></span><br><span class="line">                    <span class="keyword">if</span> (s &gt; largestPoolSize)</span><br><span class="line">                        largestPoolSize = s;</span><br><span class="line">                    workerAdded = <span class="keyword">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">              	<span class="comment">// 2.6 解锁。</span></span><br><span class="line">                mainLock.unlock();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (workerAdded) &#123;</span><br><span class="line">              	<span class="comment">// 2.7 启动新的工作线程。</span></span><br><span class="line">                t.start();</span><br><span class="line">                workerStarted = <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 2.8 如果添加或启动线程失败了，进行收尾处理。</span></span><br><span class="line">        <span class="keyword">if</span> (! workerStarted)</span><br><span class="line">            addWorkerFailed(w);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> workerStarted;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">addWorkerFailed</span><span class="params">(Worker w)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">  	<span class="comment">// 1. 加全局锁</span></span><br><span class="line">    mainLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 2. 从 HashMap 中删除这个工作线程。</span></span><br><span class="line">        <span class="keyword">if</span> (w != <span class="keyword">null</span>)</span><br><span class="line">            workers.remove(w);</span><br><span class="line">      	<span class="comment">// 3. 工作线程数目 CAS 减1</span></span><br><span class="line">      	<span class="comment">//	（因为这是在addWorker()中CAS加1成功后、创建线程失败时，调用的此函数）</span></span><br><span class="line">        decrementWorkerCount();</span><br><span class="line">      	<span class="comment">// 4. 因为减少了工作线程数目，尝试终结。</span></span><br><span class="line">        tryTerminate();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        mainLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="5-tryTerminate"><a href="#5-tryTerminate" class="headerlink" title="5. tryTerminate()"></a>5. tryTerminate()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在可能会使变为 TERMINATED 状态可能的地方，都会调用这个函数。</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">tryTerminate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">      	<span class="comment">// 1. 检查线程池状态。</span></span><br><span class="line">      	<span class="comment">// 如果 a. 线程池是RUNNING状态。</span></span><br><span class="line">      	<span class="comment">//		 b. 线程池是TIDYING或TERMINATED状态，说明已经终结过了。</span></span><br><span class="line">      	<span class="comment">//		 c. 线程池是SHUTDOWN状态、且任务队列非空。</span></span><br><span class="line">      	<span class="comment">// 不满足终结的条件，返回。</span></span><br><span class="line">        <span class="keyword">if</span> (isRunning(c) ||</span><br><span class="line">            runStateAtLeast(c, TIDYING) ||</span><br><span class="line">            (runStateOf(c) == SHUTDOWN &amp;&amp; ! workQueue.isEmpty()))</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">      	<span class="comment">// 2. 如果工作线程数目还存在，中断第一个空闲线程。</span></span><br><span class="line">      	<span class="comment">//		可能调用shutDown()后，有些核心线程在工作，随后会陷入无限等待</span></span><br><span class="line">      	<span class="comment">//		所以每个工作线程退出时，会中断一个空闲线程。</span></span><br><span class="line">        <span class="keyword">if</span> (workerCountOf(c) != <span class="number">0</span>) &#123; <span class="comment">// Eligible to terminate</span></span><br><span class="line">          	<span class="comment">// 因为要遍历 HashMap，所以要加全局锁。</span></span><br><span class="line">            interruptIdleWorkers(ONLY_ONE);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">      	<span class="comment">// 3. 加全局锁。</span></span><br><span class="line">        mainLock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          	<span class="comment">// 4. CAS 设置状态为 TIDYING。</span></span><br><span class="line">            <span class="keyword">if</span> (ctl.compareAndSet(c, ctlOf(TIDYING, <span class="number">0</span>))) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                  	<span class="comment">// 5. 调用钩子方法。</span></span><br><span class="line">                    terminated();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                  	<span class="comment">// 6. 状态变为TERMINATED。</span></span><br><span class="line">                    ctl.set(ctlOf(TERMINATED, <span class="number">0</span>));</span><br><span class="line">                  	<span class="comment">// 7. 唤醒阻塞在termination条件的所有线程，</span></span><br><span class="line">                  	<span class="comment">//		这个变量的await()方法在awaitTermination()中调用</span></span><br><span class="line">                    termination.signalAll();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">          	<span class="comment">// 8. 解锁。</span></span><br><span class="line">            mainLock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// else retry on failed CAS</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="6-Worker"><a href="#6-Worker" class="headerlink" title="6. Worker"></a>6. Worker</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Worker</span> <span class="keyword">extends</span> <span class="title">AbstractQueuedSynchronizer</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Thread thread;</span><br><span class="line">    Runnable firstTask;</span><br><span class="line">  	<span class="comment">// 统计本工作线程完成的任务数目</span></span><br><span class="line">    <span class="keyword">volatile</span> <span class="keyword">long</span> completedTasks;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// firstTask 可以为 null。</span></span><br><span class="line">    Worker(Runnable firstTask) &#123;</span><br><span class="line">      	<span class="comment">// 禁止中断，直到 runWorker() 调用。</span></span><br><span class="line">        setState(-<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">this</span>.firstTask = firstTask;</span><br><span class="line">      	<span class="comment">// 使用线程池的 ThreadFactory 创建线程。</span></span><br><span class="line">        <span class="keyword">this</span>.thread = getThreadFactory().newThread(<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 代理到线程池的 runWorker() 函数。</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        runWorker(<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line">  </span><br><span class="line">		<span class="comment">// 1持有锁、说明线程在执行任务；0不持有锁、说明线程没有在执行任务。</span></span><br><span class="line">  	<span class="comment">// 通过 tryAcquire() 是否成功，判断线程是否处于空闲状态。</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">isHeldExclusively</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> getState() != <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> unused)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, <span class="number">1</span>)) &#123;</span><br><span class="line">            setExclusiveOwnerThread(Thread.currentThread());</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryRelease</span><span class="params">(<span class="keyword">int</span> unused)</span> </span>&#123;</span><br><span class="line">        setExclusiveOwnerThread(<span class="keyword">null</span>);</span><br><span class="line">        setState(<span class="number">0</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span>        </span>&#123; acquire(<span class="number">1</span>); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">()</span>  </span>&#123; <span class="keyword">return</span> tryAcquire(<span class="number">1</span>); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span>      </span>&#123; release(<span class="number">1</span>); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isLocked</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> isHeldExclusively(); &#125;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 只有调用过runWorker()、线程没有被中断，才会去中断。</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">interruptIfStarted</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Thread t;</span><br><span class="line">        <span class="keyword">if</span> (getState() &gt;= <span class="number">0</span> &amp;&amp; (t = thread) != <span class="keyword">null</span> &amp;&amp; !t.isInterrupted()) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                t.interrupt();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (SecurityException ignore) &#123;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="7-runWorker"><a href="#7-runWorker" class="headerlink" title="7. runWorker()"></a>7. runWorker()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">runWorker</span><span class="params">(Worker w)</span> </span>&#123;</span><br><span class="line">    Thread wt = Thread.currentThread();</span><br><span class="line">    Runnable task = w.firstTask;</span><br><span class="line">    w.firstTask = <span class="keyword">null</span>;</span><br><span class="line">  	<span class="comment">// 1. 使得线程可以被中断。</span></span><br><span class="line">    w.unlock(); </span><br><span class="line">    <span class="keyword">boolean</span> completedAbruptly = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 2. 循环从任务队列取任务。</span></span><br><span class="line">        <span class="keyword">while</span> (task != <span class="keyword">null</span> || (task = getTask()) != <span class="keyword">null</span>) &#123;</span><br><span class="line">          	<span class="comment">// 2.1 工作线程加锁，说明正在执行任务。</span></span><br><span class="line">            w.lock();</span><br><span class="line">            <span class="comment">// 2.2 如果线程池正在进入STOP状态，检查本工作线程的中断状态，</span></span><br><span class="line">          	<span class="comment">//		没有中断的话，就去中断它。</span></span><br><span class="line">            <span class="keyword">if</span> ((runStateAtLeast(ctl.get(), STOP) ||</span><br><span class="line">                 (Thread.interrupted() &amp;&amp;</span><br><span class="line">                  runStateAtLeast(ctl.get(), STOP))) &amp;&amp;</span><br><span class="line">                !wt.isInterrupted())</span><br><span class="line">                wt.interrupt();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">              	<span class="comment">// 2.3 钩子函数。</span></span><br><span class="line">                beforeExecute(wt, task);</span><br><span class="line">                Throwable thrown = <span class="keyword">null</span>;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                  	<span class="comment">// 2.4 执行任务。</span></span><br><span class="line">                    task.run();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (RuntimeException x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Error x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> x;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Throwable x) &#123;</span><br><span class="line">                    thrown = x; <span class="keyword">throw</span> <span class="keyword">new</span> Error(x);</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                  	<span class="comment">// 2.5 执行钩子函数。</span></span><br><span class="line">                  	<span class="comment">// 如果发生了异常，会在2.8后直接跳到2.9。</span></span><br><span class="line">                    afterExecute(task, thrown);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">              	<span class="comment">// 2.6 清空task，准备下次从队列取task。</span></span><br><span class="line">                task = <span class="keyword">null</span>;</span><br><span class="line">              	<span class="comment">// 2.7 统计数目+1。</span></span><br><span class="line">                w.completedTasks++;</span><br><span class="line">              	<span class="comment">// 2.8 工作线程解锁。</span></span><br><span class="line">                w.unlock();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        completedAbruptly = <span class="keyword">false</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 2.9 为要终结的工作线程进行清理和统计。</span></span><br><span class="line">        processWorkerExit(w, completedAbruptly);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="8-getTask"><a href="#8-getTask" class="headerlink" title="8.  getTask()"></a>8.  getTask()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> Runnable <span class="title">getTask</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> timedOut = <span class="keyword">false</span>; <span class="comment">// Did the last poll() time out?</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">        <span class="keyword">int</span> rs = runStateOf(c);</span><br><span class="line"></span><br><span class="line">      	<span class="comment">// 1. 检查线程池状态。</span></span><br><span class="line">      	<span class="comment">// 如果 a. 状态为SHUTDOWN、并且任务队列为空</span></span><br><span class="line">      	<span class="comment">//		b. 状态度过了SHUTDOWN</span></span><br><span class="line">      	<span class="comment">// 则把工作线程数目减1，返回null。</span></span><br><span class="line">        <span class="keyword">if</span> (rs &gt;= SHUTDOWN &amp;&amp; (rs &gt;= STOP || workQueue.isEmpty())) &#123;</span><br><span class="line">            decrementWorkerCount();</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> wc = workerCountOf(c);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 检查是是使用核心线程还是临时线程模式。</span></span><br><span class="line">      	<span class="comment">// 如果是临时线程模式，在阻塞出队poll时，会使用超时等待。</span></span><br><span class="line">        <span class="keyword">boolean</span> timed = allowCoreThreadTimeOut || wc &gt; corePoolSize;</span><br><span class="line"></span><br><span class="line">      	<span class="comment">// 3. 检查数目状态。</span></span><br><span class="line">      	<span class="comment">// 		如果</span></span><br><span class="line">      	<span class="comment">//			a. 当前工作线程数目超过了maximumPoolSize（说明用setMaximumPoolSize()减少了）</span></span><br><span class="line">      	<span class="comment">// 			b. 出队超时了</span></span><br><span class="line">      	<span class="comment">//			并且工作线程数目大于1（线程池至少有一个线程）、或任务队列是空的（没有还在等待执行的任务）</span></span><br><span class="line">      	<span class="comment">//		则返回null，关闭这个工作线程。</span></span><br><span class="line">        <span class="keyword">if</span> ((wc &gt; maximumPoolSize || (timed &amp;&amp; timedOut))</span><br><span class="line">            &amp;&amp; (wc &gt; <span class="number">1</span> || workQueue.isEmpty())) &#123;</span><br><span class="line">            <span class="keyword">if</span> (compareAndDecrementWorkerCount(c))</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">      	<span class="comment">// 4. 从任务队列出队任务。</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Runnable r = timed ?</span><br><span class="line">                workQueue.poll(keepAliveTime, TimeUnit.NANOSECONDS) :</span><br><span class="line">                workQueue.take();</span><br><span class="line">            <span class="keyword">if</span> (r != <span class="keyword">null</span>)</span><br><span class="line">                <span class="keyword">return</span> r;</span><br><span class="line">            timedOut = <span class="keyword">true</span>;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException retry) &#123;</span><br><span class="line">            timedOut = <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="9-processWorkerExit"><a href="#9-processWorkerExit" class="headerlink" title="9. processWorkerExit()"></a>9. processWorkerExit()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">processWorkerExit</span><span class="params">(Worker w, <span class="keyword">boolean</span> completedAbruptly)</span> </span>&#123;</span><br><span class="line">  	<span class="comment">// 1. 检查是否把工作线程数目减1</span></span><br><span class="line">  	<span class="comment">// 		a. 如果是getTask()为null而退出的工作线程，在getTask()中已经减1了。</span></span><br><span class="line">  	<span class="comment">//		b. 如果是用户任务抛出异常，需要在这里减1。</span></span><br><span class="line">    <span class="keyword">if</span> (completedAbruptly) </span><br><span class="line">        decrementWorkerCount();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">  	<span class="comment">// 2. 加全局锁。</span></span><br><span class="line">    mainLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 3. 把退出的线程线程完成的任务数统计到全局的计数上。</span></span><br><span class="line">        completedTaskCount += w.completedTasks;</span><br><span class="line">      	<span class="comment">// 4. 从 HashMap 中移除。</span></span><br><span class="line">        workers.remove(w);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 5. 解锁。</span></span><br><span class="line">        mainLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// 6. 因为减少了工作线程数目，尝试终结。</span></span><br><span class="line">    tryTerminate();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">  	<span class="comment">// 7. 看是否要创建一个非核心线程。</span></span><br><span class="line">    <span class="keyword">if</span> (runStateLessThan(c, STOP)) &#123;</span><br><span class="line">      	<span class="comment">// 7.1 不是用户任务抛出异常、且工作线程数目少于核心线程数目个（至少为1）。</span></span><br><span class="line">      	<span class="comment">// 7.2 用户任务抛出异常。</span></span><br><span class="line">        <span class="keyword">if</span> (!completedAbruptly) &#123;</span><br><span class="line">            <span class="keyword">int</span> min = allowCoreThreadTimeOut ? <span class="number">0</span> : corePoolSize;</span><br><span class="line">            <span class="keyword">if</span> (min == <span class="number">0</span> &amp;&amp; ! workQueue.isEmpty())</span><br><span class="line">                min = <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">if</span> (workerCountOf(c) &gt;= min)</span><br><span class="line">                <span class="keyword">return</span>; <span class="comment">// replacement not needed</span></span><br><span class="line">        &#125;</span><br><span class="line">        addWorker(<span class="keyword">null</span>, <span class="keyword">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>退出时的清理工作</p>
<ol>
<li><p>把退出的线程线程完成的任务数统计到全局的计数上。</p>
</li>
<li><p>从HashMap移除。</p>
</li>
<li><p>1、2 需要加锁。</p>
</li>
<li><p>确保 workerCount 减1。</p>
</li>
<li>如果 SHUTDOWN 了，尝试中断、唤醒一个空闲工作线程。<ul>
<li>可能 SHUTDOWN 后，多个工作线程 poll 阻塞在任务队列，没有足够的任务，这些线程会永远阻塞在队里上。所以要一个线程退出了，去尝试唤醒一个空闲工作线程。</li>
</ul>
</li>
<li>如果是因为用户任务抛出异常、或工作线程数目少于核心线程数目个，创建一个工作线程。</li>
</ol>
<h2 id="10-shutdown"><a href="#10-shutdown" class="headerlink" title="10. shutdown()"></a>10. shutdown()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">shutdown</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">  	<span class="comment">// 1. 加全局锁。</span></span><br><span class="line">    mainLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        checkShutdownAccess();</span><br><span class="line">      	<span class="comment">// 2. 把RUNNING状态改为SHUTDOWN，其他状态不变。</span></span><br><span class="line">        advanceRunState(SHUTDOWN);</span><br><span class="line">      	<span class="comment">// 3. 中断所有空闲的工作线程。</span></span><br><span class="line">        interruptIdleWorkers();</span><br><span class="line">      	<span class="comment">// 4. 钩子函数。</span></span><br><span class="line">        onShutdown(); </span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 5. 解锁。</span></span><br><span class="line">        mainLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// 6. 中断了线程，尝试终结线程池。</span></span><br><span class="line">    tryTerminate();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">advanceRunState</span><span class="params">(<span class="keyword">int</span> targetState)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">        <span class="keyword">if</span> (runStateAtLeast(c, targetState) ||</span><br><span class="line">            ctl.compareAndSet(c, ctlOf(targetState, workerCountOf(c))))</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> List&lt;Runnable&gt; <span class="title">shutdownNow</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    List&lt;Runnable&gt; tasks;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">    mainLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        checkShutdownAccess();</span><br><span class="line">        <span class="comment">// 1. 设置为STOP状态</span></span><br><span class="line">        advanceRunState(STOP);</span><br><span class="line">        <span class="comment">// 2. 中断所有的工作线程（不管是否空闲）。</span></span><br><span class="line">        interruptWorkers();</span><br><span class="line">        <span class="comment">// 3. 把任务队列中的任务都转移到 ArrayList 中。</span></span><br><span class="line">        tasks = drainQueue();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 4. 解锁。</span></span><br><span class="line">        mainLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 5. 中断了线程，尝试终结线程池。</span></span><br><span class="line">    tryTerminate();</span><br><span class="line">    <span class="keyword">return</span> tasks;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="11-awaitTermination"><a href="#11-awaitTermination" class="headerlink" title="11. awaitTermination"></a>11. awaitTermination</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">awaitTermination</span><span class="params">(<span class="keyword">long</span> timeout, TimeUnit unit)</span></span></span><br><span class="line"><span class="function">    <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    <span class="keyword">long</span> nanos = unit.toNanos(timeout);</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">this</span>.mainLock;</span><br><span class="line">    mainLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 循环等待直到线程池状态更变为TERMINATED，每轮循环等待 timeout 时间</span></span><br><span class="line">        <span class="keyword">while</span> (runStateLessThan(ctl.get(), TERMINATED)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (nanos &lt;= <span class="number">0L</span>)</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            nanos = termination.awaitNanos(nanos);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        mainLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="12-总结"><a href="#12-总结" class="headerlink" title="12. 总结"></a>12. 总结</h2><h3 id="执行任务"><a href="#执行任务" class="headerlink" title="执行任务"></a>执行任务</h3><p><img src="/images/线程池execute.png" alt="线程池execute"></p>
<p>在新建线程的时候需要加锁，代价较大。</p>
<h3 id="工作线程的生命历程"><a href="#工作线程的生命历程" class="headerlink" title="工作线程的生命历程"></a>工作线程的生命历程</h3><ol>
<li><p>工作线程循环从任务队列取任务(Runnable 实例）去执行。</p>
<ul>
<li><p>如果线程池状态为STOP、或SHUTDONW且任务队列空了，就退出。</p>
</li>
<li><p>如果当前工作线程数目多于核心线程数目，如果 KeepAlive 时间内没有领到任务，就退出。</p>
</li>
<li><p>如果用户任务抛出了异常，就退出。</p>
</li>
</ul>
</li>
<li><p>在退出的时候，工作线程数目少于核心线程数目、或是因为用户任务抛出异常，再创建一个工作线程。</p>
</li>
</ol>
<p>通过这种做法，多于核心线程数目的工作线程，只会存活 KeepAlive 的空闲时间。</p>
<h3 id="关闭线程池"><a href="#关闭线程池" class="headerlink" title="关闭线程池"></a>关闭线程池</h3><h4 id="shutdown"><a href="#shutdown" class="headerlink" title="shutdown()"></a>shutdown()</h4><ol>
<li><p>调用 <code>shutdown()</code> 后，线程池状态变为 SHUTDOWN。</p>
</li>
<li><p>拒绝新的 <code>execute()</code> 任务。</p>
</li>
<li><p>此时工作线程和任务队列有两种情况。</p>
<ul>
<li>任务队列是空的，说明有空闲的工作线程在阻塞。<ul>
<li>这些空闲线程被中断、唤醒，都进行退出工作。</li>
</ul>
</li>
<li>任务队列非空的，说明所有线程都在工作。<ul>
<li>他们会不断的认领任务去执行，任务队列空了之后，都进行退出工作。</li>
<li>在任务队列非空之前，都会</li>
</ul>
</li>
</ul>
</li>
<li><p>所有工作线程退出后，进入 TIDYING 状态。</p>
</li>
<li>执行 <code>onShutdown()</code> 钩子函数后，进入 TERMINATED 状态。唤醒所有调用 <code>awaitTermination()</code> 的线程。</li>
</ol>
<h4 id="shutdownNow"><a href="#shutdownNow" class="headerlink" title="shutdownNow()"></a>shutdownNow()</h4><ol>
<li>调用 <code>shutdownNow()</code> 后，线程池状态变为 STOP。</li>
<li>所有工作线程被中断、唤醒，不再领任务，都进行退出工作。</li>
<li>所有工作线程退出后，进入 TIDYING 状态。</li>
<li>执行 <code>onShutdown()</code> 钩子函数后，进入 TERMINATED 状态。唤醒所有调用 <code>awaitTermination()</code> 的线程。</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>10. CyclicBarrier</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/10CyclicBarrier/</url>
    <content><![CDATA[<h1 id="10-CyclicBarrier"><a href="#10-CyclicBarrier" class="headerlink" title="10. CyclicBarrier"></a>10. CyclicBarrier</h1><h2 id="1-用法"><a href="#1-用法" class="headerlink" title="1. 用法"></a>1. 用法</h2><ol>
<li><p>构造函数传入线程数目 <code>threads</code>。</p>
</li>
<li><p>线程调用 <code>barrier.await()</code> 后，会阻塞线程。</p>
</li>
<li><p>线程调用 <code>barrier.await(long timeout, TimeUnit unit)</code> 后，如果超时，抛出<code>InterruptedException</code>。</p>
</li>
<li><p>有线程被中断后，抛出 <code>InterruptedException</code>。</p>
</li>
<li><p>正常结束一代后</p>
<ul>
<li>有 <code>threads</code> 个线程调用 <code>await()</code>。</li>
<li><p>返回是第一个调用的 <code>await()</code>。</p>
</li>
<li><p><code>barrier</code> 进入下一代，可以循环使用。</p>
</li>
</ul>
</li>
<li><p>非正常结束一代时，所有线程</p>
<ul>
<li>调用了<code>reset()</code>、某个线程被中断、某个线程超时了。</li>
<li>线程抛出 <code>BrokenBarrierException</code>。</li>
<li><code>reset()</code> 进入下一代。</li>
<li>其他两种情况，barrier 坏掉了，再调用 <code>BrokenBarrierException</code> 会抛出异常。</li>
</ul>
</li>
</ol>
<h2 id="2-原理"><a href="#2-原理" class="headerlink" title="2. 原理"></a>2. 原理</h2><h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Generation</span> </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> broken = <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Condition trip = lock.newCondition();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> Generation generation = <span class="keyword">new</span> Generation();</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> parties;</span><br></pre></td></tr></table></figure>
<ul>
<li>使用可重入锁，调用 <code>await()</code> 的线程等待在 <code>Condition</code> 上。</li>
<li>使用 <code>generation</code> 区分不同的调用批次。每次 所有线程苏醒/<code>reset()</code> 后进入下一代。</li>
<li><code>count</code> 记录还要调用多少次 <code>await()</code>。</li>
</ul>
<h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">CyclicBarrier</span><span class="params">(<span class="keyword">int</span> parties)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(parties, <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">CyclicBarrier</span><span class="params">(<span class="keyword">int</span> parties, Runnable barrierAction)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (parties &lt;= <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">    <span class="keyword">this</span>.parties = parties;</span><br><span class="line">    <span class="keyword">this</span>.count = parties;</span><br><span class="line">    <span class="keyword">this</span>.barrierCommand = barrierAction;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="await"><a href="#await" class="headerlink" title="await()"></a>await()</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">await</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException, BrokenBarrierException </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> dowait(<span class="keyword">false</span>, <span class="number">0L</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (TimeoutException toe) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> Error(toe); <span class="comment">// cannot happen</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">await</span><span class="params">(<span class="keyword">long</span> timeout, TimeUnit unit)</span></span></span><br><span class="line"><span class="function">    <span class="keyword">throws</span> InterruptedException,</span></span><br><span class="line"><span class="function">           BrokenBarrierException,</span></span><br><span class="line"><span class="function">           TimeoutException </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> dowait(<span class="keyword">true</span>, unit.toNanos(timeout));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">dowait</span><span class="params">(<span class="keyword">boolean</span> timed, <span class="keyword">long</span> nanos)</span></span></span><br><span class="line"><span class="function">    <span class="keyword">throws</span> InterruptedException, BrokenBarrierException,</span></span><br><span class="line"><span class="function">           TimeoutException </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">this</span>.lock;</span><br><span class="line">    <span class="comment">// 1. 加锁。</span></span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> Generation g = generation;</span><br><span class="line"></span><br><span class="line">      	<span class="comment">// 2.1 如果已经坏了，抛出坏掉异常。</span></span><br><span class="line">        <span class="keyword">if</span> (g.broken)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> BrokenBarrierException();</span><br><span class="line"></span><br><span class="line">      	<span class="comment">// 2.2 如果线程被中断，唤醒所有线程，让他们都抛出异常。</span></span><br><span class="line">        <span class="keyword">if</span> (Thread.interrupted()) &#123;</span><br><span class="line">            breakBarrier();</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> InterruptedException();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> index = --count;</span><br><span class="line">      	<span class="comment">// 2.3.1 如果凑够了线程，唤醒所有线程。</span></span><br><span class="line">        <span class="keyword">if</span> (index == <span class="number">0</span>) &#123;  <span class="comment">// tripped</span></span><br><span class="line">            <span class="keyword">boolean</span> ranAction = <span class="keyword">false</span>;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">final</span> Runnable command = barrierCommand;</span><br><span class="line">                <span class="keyword">if</span> (command != <span class="keyword">null</span>)</span><br><span class="line">                    command.run();</span><br><span class="line">                ranAction = <span class="keyword">true</span>;</span><br><span class="line">                nextGeneration();</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (!ranAction)</span><br><span class="line">                    breakBarrier();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// loop until tripped, broken, interrupted, or timed out</span></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">          	<span class="comment">// 2.3.2 还没有凑够线程，等待在 Condition上。</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (!timed)</span><br><span class="line">                    trip.await();</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span> (nanos &gt; <span class="number">0L</span>)</span><br><span class="line">                    nanos = trip.awaitNanos(nanos);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException ie) &#123;</span><br><span class="line">              	<span class="comment">// 2.3.3 线程等待中被中断了，抛出中断异常。唤醒所有线程。</span></span><br><span class="line">                <span class="keyword">if</span> (g == generation &amp;&amp; ! g.broken) &#123;</span><br><span class="line">                    breakBarrier();</span><br><span class="line">                    <span class="keyword">throw</span> ie;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// We're about to finish waiting even if we had not</span></span><br><span class="line">                    <span class="comment">// been interrupted, so this interrupt is deemed to</span></span><br><span class="line">                    <span class="comment">// "belong" to subsequent execution.</span></span><br><span class="line">                    Thread.currentThread().interrupt();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">          	<span class="comment">// 2.3.4 非正常被唤醒的，抛出坏掉异常。</span></span><br><span class="line">          	<span class="comment">// 		reset、某一个线程被中断了、某一个线程超时了。</span></span><br><span class="line">            <span class="keyword">if</span> (g.broken)</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> BrokenBarrierException();</span><br><span class="line"></span><br><span class="line">          	<span class="comment">// 2.3.5 凑够数目后被唤醒的。正常返回。</span></span><br><span class="line">            <span class="keyword">if</span> (g != generation)</span><br><span class="line">                <span class="keyword">return</span> index;</span><br><span class="line"></span><br><span class="line">          	<span class="comment">// 2.3.6 超时了，抛出超时异常。</span></span><br><span class="line">            <span class="keyword">if</span> (timed &amp;&amp; nanos &lt;= <span class="number">0L</span>) &#123;</span><br><span class="line">                breakBarrier();</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> TimeoutException();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 2.4 释放锁</span></span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">reset</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">this</span>.lock;</span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 破坏这一代的 barrier，唤醒这些线程。</span></span><br><span class="line">        breakBarrier();   <span class="comment">// break the current generation</span></span><br><span class="line">        <span class="comment">// 进入下一代。</span></span><br><span class="line">      	nextGeneration(); <span class="comment">// start a new generation</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">breakBarrier</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    generation.broken = <span class="keyword">true</span>;</span><br><span class="line">    count = parties;</span><br><span class="line">    trip.signalAll();</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">nextGeneration</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// signal completion of last generation</span></span><br><span class="line">    trip.signalAll();</span><br><span class="line">    <span class="comment">// set up next generation</span></span><br><span class="line">    count = parties;</span><br><span class="line">    generation = <span class="keyword">new</span> Generation();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>9. CountDownLatch</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/9CountDownLatch/</url>
    <content><![CDATA[<h1 id="9-CountDownLatch"><a href="#9-CountDownLatch" class="headerlink" title="9. CountDownLatch"></a>9. CountDownLatch</h1><h2 id="1-使用"><a href="#1-使用" class="headerlink" title="1. 使用"></a>1. 使用</h2><h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">CountDownLatch counter = <span class="keyword">new</span> CountDownLatch(<span class="number">10</span>);</span><br></pre></td></tr></table></figure>
<p>传入一个要计数的数量。</p>
<h3 id="await"><a href="#await" class="headerlink" title="await()"></a>await()</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">counter.await()</span><br></pre></td></tr></table></figure>
<p>调用 <code>await()</code> 的线程会阻塞住，直到 <code>counter</code> 内部的计数器为 0。</p>
<h3 id="countDown"><a href="#countDown" class="headerlink" title="countDown()"></a>countDown()</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">counter.countDown()</span><br></pre></td></tr></table></figure>
<p>一个线程调用 <code>countDown()</code> 后，会使其内部的计数器减 1。</p>
<h2 id="2-原理"><a href="#2-原理" class="headerlink" title="2. 原理"></a>2. 原理</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CountDownLatch</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Sync</span> <span class="keyword">extends</span> <span class="title">AbstractQueuedSynchronizer</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">4982264981922014374L</span>;</span><br><span class="line"></span><br><span class="line">        Sync(<span class="keyword">int</span> count) &#123;</span><br><span class="line">            setState(count);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">int</span> <span class="title">getCount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> getState();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">int</span> <span class="title">tryAcquireShared</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> (getState() == <span class="number">0</span>) ? <span class="number">1</span> : -<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryReleaseShared</span><span class="params">(<span class="keyword">int</span> releases)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// Decrement count; signal when transition to zero</span></span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="keyword">int</span> c = getState();</span><br><span class="line">                <span class="keyword">if</span> (c == <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">int</span> nextc = c-<span class="number">1</span>;</span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(c, nextc))</span><br><span class="line">                    <span class="keyword">return</span> nextc == <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CountDownLatch</span><span class="params">(<span class="keyword">int</span> count)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (count &lt; <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"count &lt; 0"</span>);</span><br><span class="line">        <span class="keyword">this</span>.sync = <span class="keyword">new</span> Sync(count);</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">await</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        sync.acquireSharedInterruptibly(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">countDown</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        sync.releaseShared(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>内部使用 AQS 实现，相当于一个共享锁。</p>
<ul>
<li><p>构造函数初始化 <code>state</code>。</p>
</li>
<li><p>每次调用 <code>countDown()</code> 方法，会把 <code>state</code> 减 1。</p>
</li>
<li>调用 <code>await()</code> 方法，只有 <code>state</code> 为 0 时，才可以获取锁成功。<ul>
<li>而且获取锁只会检查 <code>state</code>，不会更新它，所以可以有无数个线程调用 <code>await</code>，都会被唤醒。</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>8.4 PriorityBlockingQueue 源码分析</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/8.4PriorityBlockingQueue%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="8-4-PriorityBlockingQueue-源码分析"><a href="#8-4-PriorityBlockingQueue-源码分析" class="headerlink" title="8.4 PriorityBlockingQueue 源码分析"></a>8.4 PriorityBlockingQueue 源码分析</h1><h2 id="0-引用"><a href="#0-引用" class="headerlink" title="0. 引用"></a>0. 引用</h2><ul>
<li><a href="https://www.cnblogs.com/binarylei/p/10926062.html">JUC源码分析-集合篇（七）PriorityBlockingQueue</a></li>
</ul>
<h2 id="1-介绍"><a href="#1-介绍" class="headerlink" title="1. 介绍"></a>1. 介绍</h2><h3 id="基于数组的最小堆"><a href="#基于数组的最小堆" class="headerlink" title="基于数组的最小堆"></a>基于数组的最小堆</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> Object[] queue;</span><br><span class="line"><span class="comment">// 存储队列中元素的数目</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">int</span> size;</span><br></pre></td></tr></table></figure>
<h3 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ReentrantLock lock;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Condition notEmpty;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> <span class="keyword">int</span> allocationSpinLock;</span><br></pre></td></tr></table></figure>
<ul>
<li>使用 <code>ReentrantLock lock</code> 非公平锁，在所有出队、入队操作前后都要使用锁。</li>
<li>使用 <code>Condition notEmpty</code> 作为 <code>take()</code> 操作时队列为空的等待队列。<ul>
<li>在入队操作后，唤醒 <code>notEmpty</code> 上的一个线程。</li>
<li>因为队列无界，不需要 <code>notFull</code>，总是能填进去，除非到达最大值 <code>MAX_ARRAY_SIZE</code>。</li>
</ul>
</li>
<li>扩容的如下操作，避免扩容时，不能出队。<ol>
<li>先释放锁，</li>
<li>使用 <code>allocationSpinLock</code> 自旋锁创建新的数组。</li>
<li>再获取锁，拷贝数据。</li>
</ol>
</li>
</ul>
<h2 id="2-构造函数"><a href="#2-构造函数" class="headerlink" title="2. 构造函数"></a>2. 构造函数</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_INITIAL_CAPACITY = <span class="number">11</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">PriorityBlockingQueue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(DEFAULT_INITIAL_CAPACITY, <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">PriorityBlockingQueue</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(initialCapacity, <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">PriorityBlockingQueue</span><span class="params">(<span class="keyword">int</span> initialCapacity,</span></span></span><br><span class="line"><span class="function"><span class="params">                             Comparator&lt;? <span class="keyword">super</span> E&gt; comparator)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (initialCapacity &lt; <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">    <span class="keyword">this</span>.lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">    <span class="keyword">this</span>.notEmpty = lock.newCondition();</span><br><span class="line">    <span class="keyword">this</span>.comparator = comparator;</span><br><span class="line">    <span class="keyword">this</span>.queue = <span class="keyword">new</span> Object[initialCapacity];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>默认初始容量为 11。</p>
<h2 id="2-入队-offer"><a href="#2-入队-offer" class="headerlink" title="2. 入队 offer()"></a>2. 入队 offer()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 阻塞入队 put(e) 也是直接调用的 offer(e)。因为无界，所以无法阻塞。</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">offer</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (e == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">this</span>.lock;</span><br><span class="line">  	<span class="comment">// 1. 获取锁。</span></span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">int</span> n, cap;</span><br><span class="line">    Object[] array;</span><br><span class="line">  	<span class="comment">// 2. 如果数组满了，先扩容。</span></span><br><span class="line">    <span class="keyword">while</span> ((n = size) &gt;= (cap = (array = queue).length))</span><br><span class="line">        tryGrow(array, cap);</span><br><span class="line">    <span class="comment">// 3. 入队。</span></span><br><span class="line">  	<span class="keyword">try</span> &#123;</span><br><span class="line">        Comparator&lt;? <span class="keyword">super</span> E&gt; cmp = comparator;</span><br><span class="line">        <span class="keyword">if</span> (cmp == <span class="keyword">null</span>)</span><br><span class="line">            siftUpComparable(n, e, array);</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            siftUpUsingComparator(n, e, array, cmp);</span><br><span class="line">        size = n + <span class="number">1</span>;</span><br><span class="line">        notEmpty.signal();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 4. 解锁。</span></span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">tryGrow</span><span class="params">(Object[] array, <span class="keyword">int</span> oldCap)</span> </span>&#123;</span><br><span class="line">  	<span class="comment">// 1. 释放锁。使其他线程可以入队、出队。</span></span><br><span class="line">    lock.unlock(); <span class="comment">// must release and then re-acquire main lock</span></span><br><span class="line">    Object[] newArray = <span class="keyword">null</span>;</span><br><span class="line">  	<span class="comment">// 2. 获取扩容自旋锁。</span></span><br><span class="line">    <span class="keyword">if</span> (allocationSpinLock == <span class="number">0</span> &amp;&amp;</span><br><span class="line">        UNSAFE.compareAndSwapInt(<span class="keyword">this</span>, allocationSpinLockOffset,</span><br><span class="line">                                 <span class="number">0</span>, <span class="number">1</span>)) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          	<span class="comment">// 3. 计算扩容后的数组代销。</span></span><br><span class="line">          	<span class="comment">// 		oldGap&lt;64则扩容新增oldcap+2,否者扩容50%，并且最大为MAX_ARRAY_SIZE。</span></span><br><span class="line">            <span class="keyword">int</span> newCap = oldCap + ((oldCap &lt; <span class="number">64</span>) ?</span><br><span class="line">                                   (oldCap + <span class="number">2</span>) : <span class="comment">// grow faster if small</span></span><br><span class="line">                                   (oldCap &gt;&gt; <span class="number">1</span>));</span><br><span class="line">            <span class="keyword">if</span> (newCap - MAX_ARRAY_SIZE &gt; <span class="number">0</span>) &#123;    <span class="comment">// possible overflow</span></span><br><span class="line">                <span class="keyword">int</span> minCap = oldCap + <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">if</span> (minCap &lt; <span class="number">0</span> || minCap &gt; MAX_ARRAY_SIZE)</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> OutOfMemoryError();</span><br><span class="line">                newCap = MAX_ARRAY_SIZE;</span><br><span class="line">            &#125;</span><br><span class="line">          	<span class="comment">// 4. 创建扩容后的数组。</span></span><br><span class="line">            <span class="keyword">if</span> (newCap &gt; oldCap &amp;&amp; queue == array)</span><br><span class="line">                newArray = <span class="keyword">new</span> Object[newCap];</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            allocationSpinLock = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// 4. 获取扩容自旋锁失败的线程，让出 CPU。</span></span><br><span class="line">    <span class="keyword">if</span> (newArray == <span class="keyword">null</span>) <span class="comment">// back off if another thread is allocating</span></span><br><span class="line">        Thread.yield();</span><br><span class="line">    <span class="comment">// 5. 获取锁。因为接下来，要把数组指向刚刚创建的数组、拷贝数据。</span></span><br><span class="line">  	lock.lock();</span><br><span class="line">    <span class="keyword">if</span> (newArray != <span class="keyword">null</span> &amp;&amp; queue == array) &#123;</span><br><span class="line">      	<span class="comment">// 5.1 把数组指向刚刚创建的数组。</span></span><br><span class="line">        queue = newArray;</span><br><span class="line">      	<span class="comment">// 5.2 拷贝数据。</span></span><br><span class="line">        System.arraycopy(array, <span class="number">0</span>, newArray, <span class="number">0</span>, oldCap);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAX_ARRAY_SIZE = Integer.MAX_VALUE - <span class="number">8</span>;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>8.5 DelayQueue 源码分析</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/8.5DelayQueue%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="8-5-DelayQueue-源码分析"><a href="#8-5-DelayQueue-源码分析" class="headerlink" title="8.5 DelayQueue 源码分析"></a>8.5 DelayQueue 源码分析</h1><h2 id="1-数据结构"><a href="#1-数据结构" class="headerlink" title="1. 数据结构"></a>1. 数据结构</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DelayQueue</span>&lt;<span class="title">E</span> <span class="keyword">extends</span> <span class="title">Delayed</span>&gt; <span class="keyword">extends</span> <span class="title">AbstractQueue</span>&lt;<span class="title">E</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="keyword">implements</span> <span class="title">BlockingQueue</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> PriorityQueue&lt;E&gt; q = <span class="keyword">new</span> PriorityQueue&lt;E&gt;();</span><br><span class="line">  </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">transient</span> ReentrantLock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">  	<span class="comment">// 出队线程阻塞在这里。</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Condition available = lock.newCondition();</span><br><span class="line">  </span><br><span class="line">    <span class="keyword">private</span> Thread leader = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Condition signalled when a newer element becomes available</span></span><br><span class="line"><span class="comment">     * at the head of the queue or a new thread may need to</span></span><br><span class="line"><span class="comment">     * become leader.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">DelayQueue</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Delayed</span> <span class="keyword">extends</span> <span class="title">Comparable</span>&lt;<span class="title">Delayed</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 返回还要延迟多久，</span></span><br><span class="line">  	<span class="comment">// 小于等于 0 时，说明时间到了。</span></span><br><span class="line">    <span class="function"><span class="keyword">long</span> <span class="title">getDelay</span><span class="params">(TimeUnit unit)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>使用 <code>PriorityQueue</code> + <code>ReentrantLock</code> 实现。</li>
<li>内部元素必须实现 <code>Delayed</code> 接口，等待队列的元素是按照节点的  <code>compareTo()</code> 从小到大出队的。</li>
<li>使用 <code>leader</code> 每次头结点变化时，唤醒一个元素，使其有超时等待，其他线程无限等待。</li>
</ol>
<h2 id="2-入队-offer-e"><a href="#2-入队-offer-e" class="headerlink" title="2. 入队 offer(e)"></a>2. 入队 offer(e)</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 无界，所以 put(e) 也是直接调用 offer(e)。</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">offer</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">this</span>.lock;</span><br><span class="line">  	<span class="comment">// 1. 加锁。</span></span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 2. 入优先队列。</span></span><br><span class="line">        q.offer(e);</span><br><span class="line">      	<span class="comment">// 3. 如果这次入队的元素最小，有了赋予一次 leader 的机会，唤醒等待队列上的一个线程。</span></span><br><span class="line">        <span class="keyword">if</span> (q.peek() == e) &#123;</span><br><span class="line">            leader = <span class="keyword">null</span>;</span><br><span class="line">            available.signal();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 4. 解锁。</span></span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="3-阻塞出队-take"><a href="#3-阻塞出队-take" class="headerlink" title="3. 阻塞出队 take()"></a>3. 阻塞出队 take()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">take</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">  	<span class="comment">// 1. 加锁。</span></span><br><span class="line">    <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">this</span>.lock;</span><br><span class="line">    lock.lockInterruptibly();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            E first = q.peek();</span><br><span class="line">          	<span class="comment">// 2. 如果队列为空，阻塞。</span></span><br><span class="line">            <span class="keyword">if</span> (first == <span class="keyword">null</span>)</span><br><span class="line">                available.await();</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">long</span> delay = first.getDelay(NANOSECONDS);</span><br><span class="line">              	<span class="comment">// 3. 如果首节点过期，返回。</span></span><br><span class="line">                <span class="keyword">if</span> (delay &lt;= <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">return</span> q.poll();</span><br><span class="line">                first = <span class="keyword">null</span>; <span class="comment">// don't retain ref while waiting</span></span><br><span class="line">                <span class="comment">// 4.1 如果 leader 不为 null，说明其他线程抢到了这次赋予 leader 的机会。</span></span><br><span class="line">              	<span class="comment">//		例如本线程是等待队列里被唤醒的，但是赋值 leader 前，</span></span><br><span class="line">              	<span class="comment">//		有了一个新的 take() 抢先完成了赋值 leader 的机会。</span></span><br><span class="line">              	<span class="keyword">if</span> (leader != <span class="keyword">null</span>)</span><br><span class="line">                    available.await();</span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                  	<span class="comment">// 4.2 得到了赋值 leader 的机会，不忙等、而是等待 delay 时间。</span></span><br><span class="line">                    Thread thisThread = Thread.currentThread();</span><br><span class="line">                    leader = thisThread;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        available.awaitNanos(delay);</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                      	<span class="comment">// 5. 清空 leader。</span></span><br><span class="line">                        <span class="keyword">if</span> (leader == thisThread)</span><br><span class="line">                            leader = <span class="keyword">null</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 6. 如果本线程取出一个元素后，队列里还有元素，</span></span><br><span class="line">      	<span class="comment">//			则又有赋予 leader 的权限了。唤醒一个线程。</span></span><br><span class="line">        <span class="keyword">if</span> (leader == <span class="keyword">null</span> &amp;&amp; q.peek() != <span class="keyword">null</span>)</span><br><span class="line">            available.signal();</span><br><span class="line">      	<span class="comment">// 7. 释放锁。</span></span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>在以下两个情况，会有一次赋值 <code>leader</code> 的机会，唤醒一个等待队列上的出队线程。<ul>
<li>在入队操作后，如果本次入队的是最小元素。</li>
<li>出队操作后，如果队列还有元素。</li>
</ul>
</li>
<li>争夺到 <code>leader</code> 机会的出队线程，进行有超时的等待。</li>
<li>队列没有元素时，所有出队线程都是无限等待。</li>
</ul>
<h2 id="4-非阻塞出队-poll"><a href="#4-非阻塞出队-poll" class="headerlink" title="4. 非阻塞出队 poll()"></a>4. 非阻塞出队 poll()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">poll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock lock = <span class="keyword">this</span>.lock;</span><br><span class="line">  	<span class="comment">// 1. 加锁。</span></span><br><span class="line">    lock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        E first = q.peek();</span><br><span class="line">      	<span class="comment">// 2.1 如果队列为空，返回 null。</span></span><br><span class="line">        <span class="keyword">if</span> (first == <span class="keyword">null</span> || first.getDelay(NANOSECONDS) &gt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">      	<span class="comment">// 2.2 否则，出队首元素。</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> q.poll();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 3. 解锁。</span></span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>8.1 LinkedBlockingQueue 源码分析</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/8.1LinkedBlockingQueue%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="8-1-LinkedBlockingQueue-源码分析"><a href="#8-1-LinkedBlockingQueue-源码分析" class="headerlink" title="8.1 LinkedBlockingQueue 源码分析"></a>8.1 LinkedBlockingQueue 源码分析</h1><h2 id="0-引用"><a href="#0-引用" class="headerlink" title="0. 引用"></a>0. 引用</h2><ul>
<li><a href="https://blog.csdn.net/levena/article/details/78322573">阻塞队列LinkedBlockingQueue源码分析</a></li>
<li><a href="https://www.cnblogs.com/binarylei/p/10925048.html">JUC源码分析-集合篇（六）LinkedBlockingQueue</a></li>
</ul>
<h2 id="1-准备"><a href="#1-准备" class="headerlink" title="1. 准备"></a>1. 准备</h2><h3 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">    E item;</span><br><span class="line">    Node&lt;E&gt; next;</span><br><span class="line"></span><br><span class="line">    Node(E x) &#123; item = x; &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">transient</span> Node&lt;E&gt; head;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> Node&lt;E&gt; last;</span><br></pre></td></tr></table></figure>
<p><code>Node</code> 的成员域、<code>head</code>、<code>last</code> 不用设为 <code>volatile</code>。</p>
<h3 id="ReentrantLock"><a href="#ReentrantLock" class="headerlink" title="ReentrantLock"></a>ReentrantLock</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** Lock held by take, poll, etc */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ReentrantLock takeLock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"><span class="comment">// 集合已空则调用notEmpty.await，等集合添加元素后调用notEmpty.singal</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Condition notEmpty = takeLock.newCondition();</span><br><span class="line"></span><br><span class="line"><span class="comment">/** Lock held by put, offer, etc */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ReentrantLock putLock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"><span class="comment">// 集合已满则调用notFull.await，等集合取出元素后调用notFull.singal</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Condition notFull = putLock.newCondition();</span><br></pre></td></tr></table></figure>
<ul>
<li>分离入队锁、出队锁。</li>
<li>非公平锁。</li>
</ul>
<h3 id="计数"><a href="#计数" class="headerlink" title="计数"></a>计数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> AtomicInteger count = <span class="keyword">new</span> AtomicInteger();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  	<span class="keyword">return</span> count.get();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用 <code>AtomicInteger</code> 来计数。</p>
<h3 id="如何保证可见性"><a href="#如何保证可见性" class="headerlink" title="如何保证可见性"></a>如何保证可见性</h3><ol>
<li>都是入队线程因为放入元素前对 <code>putLock</code> 加锁，放入元素后对 <code>putLock</code> 解锁，保证了可见性。</li>
<li>都是出队线程因为放入元素前对 <code>takeLock</code> 加锁，放入元素后对 <code>takeLock</code> 解锁，保证了可见性。</li>
<li>入队和出队线程之间的可见性，使用 <code>AtomicInteger</code> 的 count 保证，内部是 <code>volatile</code> 修饰的 <code>int</code>。<ul>
<li>节点入队后，会写 <code>volatile</code>。</li>
<li>节点出队前，会读 <code>volatile</code>。</li>
</ul>
</li>
</ol>
<h2 id="2-构造函数"><a href="#2-构造函数" class="headerlink" title="2. 构造函数"></a>2. 构造函数</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">LinkedBlockingQueue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>(Integer.MAX_VALUE);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">LinkedBlockingQueue</span><span class="params">(<span class="keyword">int</span> capacity)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (capacity &lt;= <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">    <span class="keyword">this</span>.capacity = capacity;</span><br><span class="line">    last = head = <span class="keyword">new</span> Node&lt;E&gt;(<span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>有容量上限的。</p>
</li>
<li><p>默认容量为 <code>Integer.MAX_VALUE</code>。</p>
</li>
<li><p>首尾节点初始化为虚拟节点。</p>
</li>
</ul>
<h2 id="3-非阻塞入队-offer"><a href="#3-非阻塞入队-offer" class="headerlink" title="3. 非阻塞入队 offer()"></a>3. 非阻塞入队 offer()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">offer</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">  	<span class="comment">// 元素不能为空</span></span><br><span class="line">    <span class="keyword">if</span> (e == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">final</span> AtomicInteger count = <span class="keyword">this</span>.count;</span><br><span class="line">  	<span class="comment">// 队列满了，返回 false。</span></span><br><span class="line">    <span class="keyword">if</span> (count.get() == capacity)</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">int</span> c = -<span class="number">1</span>;</span><br><span class="line">    Node&lt;E&gt; node = <span class="keyword">new</span> Node&lt;E&gt;(e);</span><br><span class="line">  	<span class="comment">// 1. 获取入队锁。</span></span><br><span class="line">    <span class="keyword">final</span> ReentrantLock putLock = <span class="keyword">this</span>.putLock;</span><br><span class="line">    putLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (count.get() &lt; capacity) &#123;</span><br><span class="line">          	<span class="comment">// 2. 入队。</span></span><br><span class="line">            enqueue(node);</span><br><span class="line">          	<span class="comment">// 3. 写 count。</span></span><br><span class="line">            c = count.getAndIncrement();</span><br><span class="line">          	<span class="comment">// a. 如果操作后队列还没有满，唤醒一个入队线程。</span></span><br><span class="line">            <span class="keyword">if</span> (c + <span class="number">1</span> &lt; capacity)</span><br><span class="line">                notFull.signal();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 4. 释放入队锁</span></span><br><span class="line">        putLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// b. 如果操作前队列本来为空，唤醒一个出队线程。</span></span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>)</span><br><span class="line">        signalNotEmpty();</span><br><span class="line">    <span class="keyword">return</span> c &gt;= <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这个函数的调用者需要自己保证：持有入队锁。</span></span><br><span class="line"><span class="comment">// 1. 插入到 node 后。</span></span><br><span class="line"><span class="comment">// 2. 更新 last。</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">enqueue</span><span class="params">(Node&lt;E&gt; node)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// assert putLock.isHeldByCurrentThread();</span></span><br><span class="line">    <span class="comment">// assert last.next == null;</span></span><br><span class="line">  	last = last.next = node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">signalNotEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock takeLock = <span class="keyword">this</span>.takeLock;</span><br><span class="line">    takeLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	notEmpty.signal();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	takeLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="4-阻塞入队-put"><a href="#4-阻塞入队-put" class="headerlink" title="4. 阻塞入队 put()"></a>4. 阻塞入队 put()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(E e)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (e == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">int</span> c = -<span class="number">1</span>;</span><br><span class="line">    Node&lt;E&gt; node = <span class="keyword">new</span> Node&lt;E&gt;(e);</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock putLock = <span class="keyword">this</span>.putLock;</span><br><span class="line">    <span class="keyword">final</span> AtomicInteger count = <span class="keyword">this</span>.count;</span><br><span class="line">  	<span class="comment">// 1. 可中断获取入队锁。</span></span><br><span class="line">    putLock.lockInterruptibly();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 2. 队列满了，阻塞等待。</span></span><br><span class="line">      	<span class="comment">// 需要每次 count 变化时（both 入队、出队），都检测进行唤醒。</span></span><br><span class="line">        <span class="keyword">while</span> (count.get() == capacity) &#123;</span><br><span class="line">            notFull.await();</span><br><span class="line">        &#125;</span><br><span class="line">      	<span class="comment">// 3. 入队。</span></span><br><span class="line">        enqueue(node);</span><br><span class="line">      	<span class="comment">// 4. 写 count。</span></span><br><span class="line">        c = count.getAndIncrement();</span><br><span class="line">      	<span class="comment">// a. 如果操作后队列还没有满，唤醒一个入队线程。</span></span><br><span class="line">        <span class="keyword">if</span> (c + <span class="number">1</span> &lt; capacity)</span><br><span class="line">            notFull.signal();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        putLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// b. 如果操作前队列本来为空，唤醒一个出队线程。</span></span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>)</span><br><span class="line">        signalNotEmpty();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="5-非阻塞出队-poll"><a href="#5-非阻塞出队-poll" class="headerlink" title="5. 非阻塞出队 poll()"></a>5. 非阻塞出队 poll()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">poll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> AtomicInteger count = <span class="keyword">this</span>.count;</span><br><span class="line">  	<span class="comment">// 读 count。</span></span><br><span class="line">  	<span class="comment">// 队列空，返回 null。</span></span><br><span class="line">    <span class="keyword">if</span> (count.get() == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    E x = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">int</span> c = -<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock takeLock = <span class="keyword">this</span>.takeLock;</span><br><span class="line">  	<span class="comment">// 1. 加出队锁。</span></span><br><span class="line">    takeLock.lock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (count.get() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">          	<span class="comment">// 2. 出队。</span></span><br><span class="line">            x = dequeue();</span><br><span class="line">          	<span class="comment">// 3. 写 count。</span></span><br><span class="line">            c = count.getAndDecrement();</span><br><span class="line">          	<span class="comment">// a. 如果操作后队列非空，唤醒一个出队线程。</span></span><br><span class="line">            <span class="keyword">if</span> (c &gt; <span class="number">1</span>)</span><br><span class="line">                notEmpty.signal();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 4. 释放出对锁。</span></span><br><span class="line">        takeLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// b. 如果操作前队列本来是满的，唤醒一个入队线程。</span></span><br><span class="line">    <span class="keyword">if</span> (c == capacity)</span><br><span class="line">        signalNotFull();</span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这个函数的调用者需要自己保证：持有出队锁、队列非空。</span></span><br><span class="line"><span class="comment">// 因为 head 为虚拟头结点，head.next 才是真正的头结点。</span></span><br><span class="line"><span class="comment">// 1. 更新真正的头结点。更新 head.next 为 first.next。</span></span><br><span class="line"><span class="comment">// 2. 头结点 first.next 指向自己，帮助 GC。</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> E <span class="title">dequeue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// assert takeLock.isHeldByCurrentThread();</span></span><br><span class="line">    <span class="comment">// assert head.item == null;</span></span><br><span class="line">    Node&lt;E&gt; h = head;</span><br><span class="line">    Node&lt;E&gt; first = h.next;</span><br><span class="line">    h.next = h; <span class="comment">// help GC</span></span><br><span class="line">    head = first;</span><br><span class="line">    E x = first.item;</span><br><span class="line">    first.item = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="6-阻塞出队-take"><a href="#6-阻塞出队-take" class="headerlink" title="6. 阻塞出队 take()"></a>6. 阻塞出队 take()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">take</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    E x;</span><br><span class="line">    <span class="keyword">int</span> c = -<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">final</span> AtomicInteger count = <span class="keyword">this</span>.count;</span><br><span class="line">    <span class="keyword">final</span> ReentrantLock takeLock = <span class="keyword">this</span>.takeLock;</span><br><span class="line">  	<span class="comment">// 1. 可中断获取出队锁。</span></span><br><span class="line">    takeLock.lockInterruptibly();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// 2. 队列空了，等待阻塞。</span></span><br><span class="line">        <span class="keyword">while</span> (count.get() == <span class="number">0</span>) &#123;</span><br><span class="line">            notEmpty.await();</span><br><span class="line">        &#125;</span><br><span class="line">      	<span class="comment">// 3. 出队。</span></span><br><span class="line">        x = dequeue();</span><br><span class="line">      	<span class="comment">// 4. 写 count。</span></span><br><span class="line">        c = count.getAndDecrement();</span><br><span class="line">      	<span class="comment">// a. 如果操作后队列非空，唤醒一个出队线程。</span></span><br><span class="line">        <span class="keyword">if</span> (c &gt; <span class="number">1</span>)</span><br><span class="line">            notEmpty.signal();</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 5. 解出队锁。</span></span><br><span class="line">        takeLock.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// b. 如果操作前队列满的，唤醒一个入队线程。</span></span><br><span class="line">    <span class="keyword">if</span> (c == capacity)</span><br><span class="line">        signalNotFull();</span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="7-删除指定元素-remove-e"><a href="#7-删除指定元素-remove-e" class="headerlink" title="7. 删除指定元素 remove(e)"></a>7. 删除指定元素 remove(e)</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">remove</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (o == <span class="keyword">null</span>) <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">  	<span class="comment">// 1. 同时加入队锁、出队锁。</span></span><br><span class="line">    fullyLock();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      	<span class="comment">// p 为当前遍历的节点。</span></span><br><span class="line">      	<span class="comment">// trail 为 p 的前驱结点。</span></span><br><span class="line">        <span class="keyword">for</span> (Node&lt;E&gt; trail = head, p = trail.next;</span><br><span class="line">             p != <span class="keyword">null</span>;</span><br><span class="line">             trail = p, p = p.next) &#123;</span><br><span class="line">          	<span class="comment">// 2. 找到了指定元素，把元素从队列删除。</span></span><br><span class="line">            <span class="keyword">if</span> (o.equals(p.item)) &#123;</span><br><span class="line">                unlink(p, trail);</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      	<span class="comment">// 3. 解入队锁、出队锁。</span></span><br><span class="line">        fullyUnlock();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fullyLock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    putLock.lock();</span><br><span class="line">    takeLock.lock();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fullyUnlock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    takeLock.unlock();</span><br><span class="line">    putLock.unlock();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unlink</span><span class="params">(Node&lt;E&gt; p, Node&lt;E&gt; trail)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// assert isFullyLocked();</span></span><br><span class="line">    <span class="comment">// p.next is not changed, to allow iterators that are</span></span><br><span class="line">    <span class="comment">// traversing p to maintain their weak-consistency guarantee.</span></span><br><span class="line">    p.item = <span class="keyword">null</span>;</span><br><span class="line">  	<span class="comment">// 删除节点的前驱节点指向删除节点的后继节点。</span></span><br><span class="line">    trail.next = p.next;</span><br><span class="line">  	<span class="comment">// 如果删除节点是 last，更新 last 为删除节点的前驱结点。</span></span><br><span class="line">    <span class="keyword">if</span> (last == p)</span><br><span class="line">        last = trail;</span><br><span class="line">  	<span class="comment">// 如果操作前队列是满的，唤醒一个入队线程。</span></span><br><span class="line">    <span class="keyword">if</span> (count.getAndDecrement() == capacity)</span><br><span class="line">        notFull.signal();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="8-总结"><a href="#8-总结" class="headerlink" title="8. 总结"></a>8. 总结</h2><ul>
<li>使用 <code>volatile</code> 的 count，保证了入队线程和出队线程之间的可见性。</li>
<li>使用入队锁和出队锁，分离开入队和出队的操作。</li>
<li>阻塞入队时，如果队列满了，阻塞在入队锁的 <code>Condition</code> 上。</li>
<li>阻塞出队时，如果队列为空，阻塞在出队锁的 <code>Condition</code> 上。</li>
<li>每次操作 count 后，都会检测，是否要唤醒入队/出队锁上的等待线程。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>8. BlockingQueue 简介</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/8BlockingQueue%E7%AE%80%E4%BB%8B/</url>
    <content><![CDATA[<h1 id="8-BlockingQueue-简介"><a href="#8-BlockingQueue-简介" class="headerlink" title="8. BlockingQueue 简介"></a>8. BlockingQueue 简介</h1><h2 id="0-引用"><a href="#0-引用" class="headerlink" title="0. 引用"></a>0. 引用</h2><ul>
<li><a href="https://www.jianshu.com/p/af6f83c78506">SynchronousQueue原理解析</a></li>
</ul>
<h2 id="1-介绍"><a href="#1-介绍" class="headerlink" title="1. 介绍"></a>1. 介绍</h2><p>阻塞队列 <code>BlockingQueue</code> 继承自 <code>Queue</code>，添加了阻塞入队、阻塞出队的功能。</p>
<ul>
<li><code>put(e)</code>：入队时，如果队列已满，则阻塞直到队列不满。</li>
<li><code>take()</code>：出队时，如果队列为空，则阻塞直到队列不空。</li>
</ul>
<p><code>BlockingQueue</code> 提供的方法：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>操作</th>
<th>返回特殊值</th>
<th>超时退出</th>
<th>抛出异常</th>
<th>阻塞</th>
</tr>
</thead>
<tbody>
<tr>
<td>插入</td>
<td>offer(e)</td>
<td>offer(e, time, unit)</td>
<td>add(e)</td>
<td>put(e)</td>
</tr>
<tr>
<td>移除</td>
<td>poll()</td>
<td>poll(e, time, unit)</td>
<td>remove()</td>
<td>take()</td>
</tr>
<tr>
<td>获取队首</td>
<td>peek()</td>
<td>pe(e, time, unit)</td>
<td>element()</td>
<td>无</td>
</tr>
</tbody>
</table>
</div>
<h2 id="2-7-个阻塞队列"><a href="#2-7-个阻塞队列" class="headerlink" title="2. 7 个阻塞队列"></a>2. 7 个阻塞队列</h2><ol>
<li>ArrayBlockingQueue：数组组成、有界、一把公平/非公平锁。</li>
<li>LinkedBlockingQueue：链表组成、有界、两把非公平锁。</li>
<li>LinkedBlockingDeque：链表组成、有界、一把非公平锁、双向。</li>
<li>PriorityBlockingQueue：基于数组的最小堆、无界、一把非公平锁 + 扩容自旋锁、优先级排序。</li>
<li>DelayQueue：基于 <code>PriorityQueue</code>、无界、一把非公平锁、优先级排序。</li>
<li>SynchronousQueue：链表组成的队列（公平）/栈（非公平）、无界、CAS 更新、每一个出队和入队操作必须匹配，否则阻塞。</li>
<li>LinkedTransferQueue：一个由链表结构组成的无界阻塞队列。TODO</li>
</ol>
<p>这些阻塞队列一般都是由 <code>ReentrantLock</code> 锁 + <code>Condition</code> 实现的。</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>类名</th>
<th>底层结构</th>
<th>是否有界</th>
<th>锁的形式</th>
<th>备注</th>
</tr>
</thead>
<tbody>
<tr>
<td>ArrayBlockingQueue</td>
<td>数组</td>
<td>有界</td>
<td>一把公平/非公平锁</td>
<td></td>
</tr>
<tr>
<td>LinkedBlockingQueue</td>
<td>链表</td>
<td>有界</td>
<td>两把非公平锁</td>
<td></td>
</tr>
<tr>
<td>LinkedBlockingDeque</td>
<td>链表</td>
<td>有界</td>
<td>一把非公平锁</td>
<td>双向队列</td>
</tr>
<tr>
<td>PriorityBlockingQueue</td>
<td>基于数组的最小堆</td>
<td>无界</td>
<td>一把非公平锁 + 扩容自旋锁</td>
<td>优先级排序</td>
</tr>
<tr>
<td>DelayQueue</td>
<td><code>PriorityQueue</code>(基于数组的最小堆)</td>
<td>无界</td>
<td>一把非公平锁</td>
<td>优先级排序</td>
</tr>
<tr>
<td>SynchronousQueue TODO</td>
<td>链表组成的队列（公平）、<br />或栈（非公平）</td>
<td>无界</td>
<td>CAS 更新，LockSupport 等待和唤醒</td>
<td>put 入队操作需要等待出队操作；<br />offer 入队操作需要有出队阻塞存在才可以，否则返回false。</td>
</tr>
<tr>
<td>LinkedTransferQueue TODO</td>
<td>松弛型双重队列</td>
<td>无界</td>
<td>CAS 更新，LockSupport 等待和唤醒</td>
<td>transfer 入队操作必须等待出队操作。</td>
</tr>
</tbody>
</table>
</div>
<h3 id="1-ArrayBlockingQueue"><a href="#1-ArrayBlockingQueue" class="headerlink" title="1. ArrayBlockingQueue"></a>1. ArrayBlockingQueue</h3><ul>
<li>由固定长度数组组成。构造函数传入容量。</li>
<li>使用 <code>ReentrantLock lock</code> 一把锁控制入队、出队操作，并发度低于 <code>LinkedBlockingQueue</code>。构造函数可以传入是否使用公平锁。<ul>
<li>入队/出队前，都需要持有锁。</li>
<li>因为只用一把锁，<code>count</code> 使用 <code>int</code> 就可以。</li>
</ul>
</li>
<li>使用 <code>lock</code> 的两个 <code>Condition</code> 等待队列：<code>notEmpty</code>、<code>notFull</code>。<ul>
<li>入队后，唤醒一个 <code>notEmpty</code> 上的线程。</li>
<li>出队后，唤醒一个 <code>notFull</code> 上的线程。</li>
</ul>
</li>
<li>入队/出队移动数组元素来实现。</li>
</ul>
<h3 id="3-LinkedBlockingDeque"><a href="#3-LinkedBlockingDeque" class="headerlink" title="3. LinkedBlockingDeque"></a>3. LinkedBlockingDeque</h3><ul>
<li>由链表组成，构造函数传入容量，默认 <code>Integer.MAX_VALUE</code>。</li>
<li>和 <code>ArrayBlockingQueue</code> 类似，使用一把<code>ReentrantLock lock</code> 一把锁控制入队、出队操作。</li>
<li>使用 <code>lock</code> 的两个 <code>Condition</code> 等待队列。</li>
</ul>
<h3 id="7-LinkedTransferQueue"><a href="#7-LinkedTransferQueue" class="headerlink" title="7. LinkedTransferQueue"></a>7. LinkedTransferQueue</h3><p>与 <code>SynchronousQueue</code> 的异同点：</p>
<div class="table-container">
<table>
<thead>
<tr>
<th>类名</th>
<th><code>offer(e)</code></th>
<th><code>poll()</code></th>
<th><code>put(e)</code></th>
<th><code>take()</code></th>
</tr>
</thead>
<tbody>
<tr>
<td>LinkedTransferQueue</td>
<td>总是可以入队</td>
<td>队列为空，返回false</td>
<td>没有阻塞的take()线程，则阻塞。</td>
<td>需要阻塞等待一个put(e)。</td>
</tr>
<tr>
<td>SynchronousQueue</td>
<td>如果没有需要阻塞等待的take()，返回false。</td>
<td>如果没有需要阻塞等待的put(e)，返回false。</td>
<td>需要阻塞等待一个take()。</td>
<td>需要阻塞等待一个put(e)。</td>
</tr>
</tbody>
</table>
</div>
<p><code>LinkedTransferQueue</code> 的</p>
<ul>
<li><p><code>offer(e)</code> 、 <code>poll()</code> 与 <code>LinkedBlockingQueue</code> 功能相同。</p>
</li>
<li><p><code>put(e)</code>、<code>take()</code> 与 <code>SynchronousQueue</code> 的功能相同。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>7. ConcuurentLinkedQueue 源码分析</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/7ConcurrentLinkedQueue%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="7-ConcuurentLinkedQueue-源码分析"><a href="#7-ConcuurentLinkedQueue-源码分析" class="headerlink" title="7. ConcuurentLinkedQueue 源码分析"></a>7. ConcuurentLinkedQueue 源码分析</h1><h2 id="0-引用"><a href="#0-引用" class="headerlink" title="0. 引用"></a>0. 引用</h2><ul>
<li><a href="https://www.cnblogs.com/jmcui/p/11433016.html"><a href="https://www.cnblogs.com/jmcui/p/11433016.html">ConcurrentLinkedQueue 源码解读</a></a></li>
<li><a href="https://www.cnblogs.com/binarylei/p/10923374.html">JUC源码分析-集合篇（三）ConcurrentLinkedQueue</a></li>
</ul>
<h2 id="1-准备"><a href="#1-准备" class="headerlink" title="1. 准备"></a>1. 准备</h2><h3 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">volatile</span> E item;</span><br><span class="line">    <span class="keyword">volatile</span> Node&lt;E&gt; next;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node&lt;E&gt; head;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node&lt;E&gt; tail;</span><br></pre></td></tr></table></figure>
<p><code>Node</code> 中的 <code>item</code> 和 <code>next</code> 使用 <code>volatile</code> 来保证可见性、避免重排序。</p>
<h3 id="首尾节点"><a href="#首尾节点" class="headerlink" title="首尾节点"></a>首尾节点</h3><p><code>head</code> 和 <code>tail</code> 并不是每次操作都会进行更新，而是在操作时发现首节点/尾节点发生了变化（与<code>head</code>和<code>tail</code> 不同时）才会更新。减少了 CAS 竞争的概率，提高了并发。</p>
<p>因此，<code>head</code> 和 <code>tail</code> 中存储的可能并不是当前的首节点和尾节点。</p>
<h3 id="CAS-更新"><a href="#CAS-更新" class="headerlink" title="CAS 更新"></a>CAS 更新</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">casItem</span><span class="params">(E cmp, E val)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> UNSAFE.compareAndSwapObject(<span class="keyword">this</span>, itemOffset, cmp, val);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">lazySetNext</span><span class="params">(Node&lt;E&gt; val)</span> </span>&#123;</span><br><span class="line">      UNSAFE.putOrderedObject(<span class="keyword">this</span>, nextOffset, val);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">casNext</span><span class="params">(Node&lt;E&gt; cmp, Node&lt;E&gt; val)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> UNSAFE.compareAndSwapObject(<span class="keyword">this</span>, nextOffset, cmp, val);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">casTail</span><span class="params">(Node&lt;E&gt; cmp, Node&lt;E&gt; val)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> UNSAFE.compareAndSwapObject(<span class="keyword">this</span>, tailOffset, cmp, val);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">casHead</span><span class="params">(Node&lt;E&gt; cmp, Node&lt;E&gt; val)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> UNSAFE.compareAndSwapObject(<span class="keyword">this</span>, headOffset, cmp, val);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>直接使用 <code>unsafe</code> 的底层的 CAS 方法，不使用 <code>AtomicXXX</code> 类，避免性能损耗。</p>
<ul>
<li><code>AtomicXXX</code> 和 这里用的方法是一样的，都是 <code>UNSAFE.compareAndSwapObject</code> + <code>offset</code>。</li>
<li>避免再套一层对象。</li>
</ul>
<h2 id="2-构造函数"><a href="#2-构造函数" class="headerlink" title="2. 构造函数"></a>2. 构造函数</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ConcurrentLinkedQueue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    head = tail = <span class="keyword">new</span> Node&lt;E&gt;(<span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>首尾节点都设为空的虚拟 Node 节点。</p>
<h2 id="3-入队列-offer"><a href="#3-入队列-offer" class="headerlink" title="3. 入队列 offer()"></a>3. 入队列 offer()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">offer</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">  	<span class="comment">// 节点不能为 null</span></span><br><span class="line">    checkNotNull(e);</span><br><span class="line">    <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> Node&lt;E&gt;(e);</span><br><span class="line"></span><br><span class="line">  	<span class="comment">// t 是 tail 的快照</span></span><br><span class="line">  	<span class="comment">// 等会要把节点插入到 p 后边</span></span><br><span class="line">    <span class="keyword">for</span> (Node&lt;E&gt; t = tail, p = t;;) &#123;</span><br><span class="line">        Node&lt;E&gt; q = p.next;</span><br><span class="line">      	<span class="comment">// 1. 说明 p 是最后一个节点。</span></span><br><span class="line">        <span class="keyword">if</span> (q == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 1.1 CAS 把节点插入到 p 后边</span></span><br><span class="line">            <span class="keyword">if</span> (p.casNext(<span class="keyword">null</span>, newNode)) &#123;</span><br><span class="line">                <span class="comment">// 1.1.1 p 是最后一个节点，却不等于 tail。说明 tail 不是最新的尾节点了。</span></span><br><span class="line">              	<span class="comment">// 		所以更新 tail 为最新的尾节点</span></span><br><span class="line">                <span class="keyword">if</span> (p != t) <span class="comment">// hop two nodes at a time</span></span><br><span class="line">                    casTail(t, newNode);  <span class="comment">// 失败了也没有关系，可能是之后加入的节点更新了 tail</span></span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Lost CAS race to another thread; re-read next</span></span><br><span class="line">        &#125;</span><br><span class="line">      	<span class="comment">// 2. p 的 next 指向了自己（在 poll() 中做的），说明 p 节点被删除了。</span></span><br><span class="line">      	<span class="comment">// 		因为 p 是最后一个节点，所以重新读取 tail 的快照。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (p == q)</span><br><span class="line">            <span class="comment">// 2.1 重新取 tail 的快照，从新的 tail 开始遍历。</span></span><br><span class="line">          	<span class="comment">// 2.2 如果没有变化，说明 tail 也被删除了，只能从 head 开始遍历。</span></span><br><span class="line">            p = (t != (t = tail)) ? t : head;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="comment">// 3.1 如果 p 与 t 相等，说明 p 不是最后一个节点（因为 p 有后继节点），</span></span><br><span class="line">          	<span class="comment">//		继续向 p 的后继节点遍历。</span></span><br><span class="line">          	<span class="comment">// 3.2 如果 p 与 t 不相等，说明尾节点发生变化了，</span></span><br><span class="line">          	<span class="comment">//		重新读取 tail 的快照，从新的 tail 开始遍历。</span></span><br><span class="line">            p = (p != t &amp;&amp; t != (t = tail)) ? t : q;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>从 tail 开始遍历查找到最新的尾节点 p ，找到后 CAS 把新加的节点插入 p 后边。</p>
</li>
<li><p>只有查找中，发现 tail 不是最新的尾节点，才会 CAS 更新 tail。</p>
</li>
</ul>
<h2 id="4-出队列-poll"><a href="#4-出队列-poll" class="headerlink" title="4. 出队列 poll()"></a>4. 出队列 poll()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">poll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    restartFromHead:</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">      	<span class="comment">// h 是 head 的快照。</span></span><br><span class="line">      	<span class="comment">// p 是等会要删除的节点，从 head 开始遍历。</span></span><br><span class="line">        <span class="keyword">for</span> (Node&lt;E&gt; h = head, p = h, q;;) &#123;</span><br><span class="line">            E item = p.item;</span><br><span class="line"></span><br><span class="line">          	<span class="comment">// 1 把 p.item CAS 设为 null，如果成功，说明节点被删除。</span></span><br><span class="line">          	<span class="comment">//		注意，还需后续操作：把 p.next 指向自己、更新 head。</span></span><br><span class="line">            <span class="keyword">if</span> (item != <span class="keyword">null</span> &amp;&amp; p.casItem(item, <span class="keyword">null</span>)) &#123;</span><br><span class="line">                <span class="comment">// 如果 p 不等于 h，说明 p 经过遍历才删除了节点，即 head 现在不是最新的首节点。</span></span><br><span class="line">              	<span class="comment">// 		所以，更新首节点到 head、把 p.next 指向自己。</span></span><br><span class="line">                <span class="keyword">if</span> (p != h)</span><br><span class="line">                  	<span class="comment">// a. 如果 p（被删除了） 有下一个节点，则设首节点为 p 的下一个节点。</span></span><br><span class="line">                  	<span class="comment">// b. 否则，只能把首节点设为 p 了，此时首节点是一个虚拟节点。</span></span><br><span class="line">                    updateHead(h, ((q = p.next) != <span class="keyword">null</span>) ? q : p);</span><br><span class="line">                <span class="keyword">return</span> item;</span><br><span class="line">            &#125;</span><br><span class="line">          	<span class="comment">// 2. 和上边类似。如果 p 没有下一个节点了，只能把 p 设为首节点了。</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((q = p.next) == <span class="keyword">null</span>) &#123;</span><br><span class="line">                updateHead(h, p);</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">          	<span class="comment">// 3. 如果 p.next 指向自己，说明 p 被删除了。需要从新的 head 开始。</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (p == q)</span><br><span class="line">                <span class="keyword">continue</span> restartFromHead;</span><br><span class="line">          	<span class="comment">// 4. 否则，遍历下一个节点。</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                p = q;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">updateHead</span><span class="params">(Node&lt;E&gt; h, Node&lt;E&gt; p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (h != p &amp;&amp; casHead(h, p))</span><br><span class="line">      h.lazySetNext(h); <span class="comment">// 不用 CAS</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>从 head 开始遍历查找最新的首节点 p，CAS 更新 p.item 为 null。</li>
<li>只有查找中，发现 head 不是最新的首节点，才会 CAS 更新 head、把旧的 head 指向自己。</li>
</ul>
<h2 id="5-删除节点-remove"><a href="#5-删除节点-remove" class="headerlink" title="5. 删除节点 remove()"></a>5. 删除节点 remove()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">remove</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (o != <span class="keyword">null</span>) &#123;</span><br><span class="line">        Node&lt;E&gt; next, pred = <span class="keyword">null</span>;</span><br><span class="line">      	<span class="comment">// 从首节点开始遍历</span></span><br><span class="line">        <span class="keyword">for</span> (Node&lt;E&gt; p = first(); p != <span class="keyword">null</span>; pred = p, p = next) &#123;</span><br><span class="line">            <span class="keyword">boolean</span> removed = <span class="keyword">false</span>;</span><br><span class="line">            E item = p.item;</span><br><span class="line">            <span class="keyword">if</span> (item != <span class="keyword">null</span>) &#123;</span><br><span class="line">              	<span class="comment">// 没有找到</span></span><br><span class="line">                <span class="keyword">if</span> (!o.equals(item)) &#123;</span><br><span class="line">                    next = succ(p);</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                &#125;</span><br><span class="line">              	<span class="comment">// 找到了，尝试 CAS 把 p.item 设为 null。抢夺删除权。</span></span><br><span class="line">                removed = p.casItem(item, <span class="keyword">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            next = succ(p);</span><br><span class="line">          	<span class="comment">// 把 p 前后节点连起来，注意可以和得到删除权的线程不同。</span></span><br><span class="line">            <span class="keyword">if</span> (pred != <span class="keyword">null</span> &amp;&amp; next != <span class="keyword">null</span>) <span class="comment">// unlink</span></span><br><span class="line">                pred.casNext(p, next);</span><br><span class="line">            <span class="keyword">if</span> (removed)</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Node&lt;E&gt; <span class="title">first</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    restartFromHead:</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">      <span class="keyword">for</span> (Node&lt;E&gt; h = head, p = h, q;;) &#123;</span><br><span class="line">        <span class="keyword">boolean</span> hasItem = (p.item != <span class="keyword">null</span>);</span><br><span class="line">        <span class="comment">// 找到了 p.item != null 的节点。</span></span><br><span class="line">        <span class="comment">// 或遍历到尾部了。</span></span><br><span class="line">        <span class="keyword">if</span> (hasItem || (q = p.next) == <span class="keyword">null</span>) &#123;</span><br><span class="line">          updateHead(h, p);</span><br><span class="line">          <span class="keyword">return</span> hasItem ? p : <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 节点指向自己，从头开始遍历。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (p == q)</span><br><span class="line">          <span class="keyword">continue</span> restartFromHead;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">          p = q;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> Node&lt;E&gt; <span class="title">succ</span><span class="params">(Node&lt;E&gt; p)</span> </span>&#123;</span><br><span class="line">    Node&lt;E&gt; next = p.next;</span><br><span class="line">  	<span class="comment">// 如果 p.next 指向自己，说明 p 已经被从 poll() 中删除了。</span></span><br><span class="line">    <span class="keyword">return</span> (p == next) ? head : next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>遍历链表，找到要删除的节点 p 后，使用 CAS 成功把 p.item 设为 null的，获得本次返回 true 的权利。其他线程要继续遍历查找。</p>
</li>
<li><p>找到节点后，要把 p 前后节点 CAS 连起来，可以和获得删除权的节点不同。</p>
</li>
<li><code>first()</code> 找首节点的函数，也会更新 head 为最新的首节点。</li>
</ul>
<h2 id="6-获取节点数目-size"><a href="#6-获取节点数目-size" class="headerlink" title="6. 获取节点数目 size()"></a>6. 获取节点数目 size()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (Node&lt;E&gt; p = first(); p != <span class="keyword">null</span>; p = succ(p))</span><br><span class="line">        <span class="keyword">if</span> (p.item != <span class="keyword">null</span>)</span><br><span class="line">            <span class="comment">// Collection.size() spec says to max out</span></span><br><span class="line">            <span class="keyword">if</span> (++count == Integer.MAX_VALUE)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>遍历整个链表，找到 item 不为 null 的节点。</p>
<p><code>size()</code> 是 O(n) 的时间复杂度。</p>
<h2 id="7-元素是否为空-isEmpty"><a href="#7-元素是否为空-isEmpty" class="headerlink" title="7. 元素是否为空 isEmpty()"></a>7. 元素是否为空 isEmpty()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> first() == <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>6. ConcurrentHashMap 源码分析</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/6ConcurrentHashMap%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.cnblogs.com/binarylei/p/10921214.html">JUC源码分析-集合篇（一）ConcurrentHashMap</a></li>
<li><a href="https://bugs.openjdk.java.net/browse/JDK-8215409">probable bug in logic of ConcurrentHashMap.tryPresize()</a></li>
<li><a href="https://bugs.openjdk.java.net/browse/JDK-8202422">value of ‘sizeCtl’ in ConcurrentHashMap varies with the constructor called</a></li>
</ul>
<h1 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h1><p>JDK 1.7 中，<code>ConcurrentHashMap</code> 使用分段锁 <code>ReentrantLock</code> 实现；</p>
<p>而在 JDK 1.8 中，使用 CAS + <code>synchronized</code> 锁每个桶的第一个节点实现。</p>
<h2 id="为什么换了实现方式"><a href="#为什么换了实现方式" class="headerlink" title="为什么换了实现方式"></a>为什么换了实现方式</h2><ol>
<li>JDK 1.8 中的 <code>synchronized</code> 已经足够优化。</li>
<li>锁细化到每个桶一个，并发度更高。</li>
<li>锁细化到每个桶一个，出现锁竞争的概率很小，<code>synchronized</code> 很小概率会膨胀为重量级锁。</li>
<li><code>ReentrantLock</code> 尝试加锁失败就会加入等待队列，<code>synchronized</code> 首次进入重量级锁也会尝试自旋若干次。</li>
</ol>
<h2 id="volatile-CAS"><a href="#volatile-CAS" class="headerlink" title="volatile + CAS"></a>volatile + CAS</h2><h3 id="table"><a href="#table" class="headerlink" title="table"></a>table</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">transient</span> <span class="keyword">volatile</span> Node&lt;K,V&gt;[] table;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node&lt;K,V&gt;[] nextTable;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> &lt;K,V&gt; <span class="function">Node&lt;K,V&gt; <span class="title">tabAt</span><span class="params">(Node&lt;K,V&gt;[] tab, <span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (Node&lt;K,V&gt;)U.getObjectVolatile(tab, ((<span class="keyword">long</span>)i &lt;&lt; ASHIFT) + ABASE);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> &lt;K,V&gt; <span class="function"><span class="keyword">boolean</span> <span class="title">casTabAt</span><span class="params">(Node&lt;K,V&gt;[] tab, <span class="keyword">int</span> i,</span></span></span><br><span class="line"><span class="function"><span class="params">                                    Node&lt;K,V&gt; c, Node&lt;K,V&gt; v)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> U.compareAndSwapObject(tab, ((<span class="keyword">long</span>)i &lt;&lt; ASHIFT) + ABASE, c, v);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> &lt;K,V&gt; <span class="function"><span class="keyword">void</span> <span class="title">setTabAt</span><span class="params">(Node&lt;K,V&gt;[] tab, <span class="keyword">int</span> i, Node&lt;K,V&gt; v)</span> </span>&#123;</span><br><span class="line">    U.putObjectVolatile(tab, ((<span class="keyword">long</span>)i &lt;&lt; ASHIFT) + ABASE, v);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>虽然 <code>table</code> 声明为 <code>volatile</code> 的数组，但是只是这个数组的引用为 <code>volatile</code> 的。</p>
<ul>
<li>即只有 <code>table = XXX</code> 时生效。</li>
<li>而为数组的元素赋值并不保证 <code>volatile</code> 的效果。</li>
<li>所以使用了<code>Unsafe</code> 的 <code>volatile</code> 的相关函数来从 <code>table</code> 赋值和取值。</li>
</ul>
<h3 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> hash;</span><br><span class="line">    <span class="keyword">final</span> K key;</span><br><span class="line">    <span class="keyword">volatile</span> V val;</span><br><span class="line">    <span class="keyword">volatile</span> Node&lt;K,V&gt; next;</span><br><span class="line">  </span><br><span class="line">    Node(<span class="keyword">int</span> hash, K key, V val, Node&lt;K,V&gt; next) &#123;</span><br><span class="line">      <span class="keyword">this</span>.hash = hash;</span><br><span class="line">      <span class="keyword">this</span>.key = key;</span><br><span class="line">      <span class="keyword">this</span>.val = val;</span><br><span class="line">      <span class="keyword">this</span>.next = next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p><code>key</code> 和 <code>hash</code> 是不会改变的，使用 <code>final</code> 就可以保证可见性、避免重排序。</p>
</li>
<li><p><code>val</code> 和 <code>next</code> 是会改变的，所以需要使用 <code>volatile</code> 保证 可见性、避免重排序。</p>
</li>
</ul>
<h2 id="key-和-value-不能是-null"><a href="#key-和-value-不能是-null" class="headerlink" title="key 和 value 不能是 null"></a>key 和 value 不能是 null</h2><blockquote>
<ul>
<li><a href="https://stackoverflow.com/questions/698638/why-does-concurrenthashmap-prevent-null-keys-and-values">Why does ConcurrentHashMap prevent null keys and values?</a></li>
</ul>
</blockquote>
<p>如果 <code>value</code> 可以是 null，在 <code>map.get(key)</code> 返回 null 的时候，不知道 map 中是否包含这个元素。</p>
<p>在 <code>HashMap</code> 中可以用 <code>map.containsKey(key)</code> 看这个 key 是否存在。</p>
<p>但是在多线程版本中，用 <code>map.containsKey(key)</code> 和 <code>map.get(key)</code> 不是原子的。</p>
<h1 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">()</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (initialCapacity &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">    <span class="keyword">int</span> cap = ((initialCapacity &gt;= (MAXIMUM_CAPACITY &gt;&gt;&gt; <span class="number">1</span>)) ?</span><br><span class="line">               MAXIMUM_CAPACITY :</span><br><span class="line">               tableSizeFor(initialCapacity + (initialCapacity &gt;&gt;&gt; <span class="number">1</span>) + <span class="number">1</span>));</span><br><span class="line">    <span class="keyword">this</span>.sizeCtl = cap;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>(initialCapacity, LOAD_FACTOR, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity, <span class="keyword">float</span> loadFactor)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>(initialCapacity, loadFactor, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity,</span></span></span><br><span class="line"><span class="function"><span class="params">                         <span class="keyword">float</span> loadFactor, <span class="keyword">int</span> concurrencyLevel)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!(loadFactor &gt; <span class="number">0.0f</span>) || initialCapacity &lt; <span class="number">0</span> || concurrencyLevel &lt;= <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">  <span class="keyword">if</span> (initialCapacity &lt; concurrencyLevel)   <span class="comment">// Use at least as many bins</span></span><br><span class="line">    initialCapacity = concurrencyLevel;   <span class="comment">// as estimated threads</span></span><br><span class="line">  <span class="keyword">long</span> size = (<span class="keyword">long</span>)(<span class="number">1.0</span> + (<span class="keyword">long</span>)initialCapacity / loadFactor);</span><br><span class="line">  <span class="keyword">int</span> cap = (size &gt;= (<span class="keyword">long</span>)MAXIMUM_CAPACITY) ?</span><br><span class="line">    MAXIMUM_CAPACITY : tableSizeFor((<span class="keyword">int</span>)size);</span><br><span class="line">  <span class="keyword">this</span>.sizeCtl = cap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>sizeCtl</code> 与 HashMap 中的 <code>threshold</code> 含义类似。<ul>
<li>数组未初始化时表示需要初始化数组的大小，</li>
<li>为 -1 时，表示正在初始化。</li>
<li>为 整数时，为扩容的阈值。</li>
<li>为极小的负数时，为正在 transfer 的线程数目。</li>
</ul>
</li>
<li>第 2 个构造函数中，<code>sizeCtl</code> 这里设置为了 <code>initialCapacity * 1.5</code>，应该设置为 <code>initialCapacity / 0.75</code> 才对，是一个 bug。<a href="https://bugs.openjdk.java.net/browse/JDK-8202422。JSR">https://bugs.openjdk.java.net/browse/JDK-8202422。JSR</a> 166 中，更新为第 3 个构造函数的版本。</li>
<li>没有初始化 <code>table</code>。</li>
</ul>
<h1 id="put"><a href="#put" class="headerlink" title="put()"></a>put()</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">  	<span class="keyword">return</span> putVal(key, value, <span class="keyword">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> HASH_BITS = <span class="number">0x7fffffff</span>; <span class="comment">// usable bits of normal node hash</span></span><br><span class="line"><span class="comment">// 与 HashMap 中有些不一样，去除了最高位的1。</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">spread</span><span class="params">(<span class="keyword">int</span> h)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (h ^ (h &gt;&gt;&gt; <span class="number">16</span>)) &amp; HASH_BITS;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(K key, V value, <span class="keyword">boolean</span> onlyIfAbsent)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (key == <span class="keyword">null</span> || value == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">int</span> hash = spread(key.hashCode());</span><br><span class="line">    <span class="keyword">int</span> binCount = <span class="number">0</span>;</span><br><span class="line">  	<span class="comment">// 1. 自旋保证元素一定会添加到 map 中。</span></span><br><span class="line">    <span class="keyword">for</span> (Node&lt;K,V&gt;[] tab = table;;) &#123;</span><br><span class="line">        Node&lt;K,V&gt; f; <span class="keyword">int</span> n, i, fh;</span><br><span class="line">      	<span class="comment">// 1.1 如果没有初始化 table，则初始化。</span></span><br><span class="line">        <span class="keyword">if</span> (tab == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">            tab = initTable();</span><br><span class="line">      	<span class="comment">// 1.2 如果桶空的、且 CAS 添加节点成功，退出循环。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((f = tabAt(tab, i = (n - <span class="number">1</span>) &amp; hash)) == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (casTabAt(tab, i, <span class="keyword">null</span>,</span><br><span class="line">                         <span class="keyword">new</span> Node&lt;K,V&gt;(hash, key, value, <span class="keyword">null</span>)))</span><br><span class="line">                <span class="keyword">break</span>;                   <span class="comment">// no lock when adding to empty bin</span></span><br><span class="line">        &#125;</span><br><span class="line">      	<span class="comment">// 1.3 如果这个桶正在扩容，该线程先帮助扩容、再添加节点。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED)</span><br><span class="line">            tab = helpTransfer(tab, f);</span><br><span class="line">      	<span class="comment">// 1.4 锁住这个桶，进行插入/更新操作，与 HashMap 类似。</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            V oldVal = <span class="keyword">null</span>;</span><br><span class="line">          	<span class="comment">// 为这个桶加锁</span></span><br><span class="line">            <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                <span class="keyword">if</span> (tabAt(tab, i) == f) &#123;</span><br><span class="line">              			<span class="comment">// 1.4.1 链表</span></span><br><span class="line">                    <span class="keyword">if</span> (fh &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        binCount = <span class="number">1</span>;</span><br><span class="line">                        <span class="keyword">for</span> (Node&lt;K,V&gt; e = f;; ++binCount) &#123;</span><br><span class="line">                            K ek;</span><br><span class="line">                          	<span class="comment">// 1.4.1.1找到了对应 key 的节点，更新值。</span></span><br><span class="line">                            <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                                ((ek = e.key) == key ||</span><br><span class="line">                                 (ek != <span class="keyword">null</span> &amp;&amp; key.equals(ek)))) &#123;</span><br><span class="line">                                oldVal = e.val;</span><br><span class="line">                                <span class="keyword">if</span> (!onlyIfAbsent)</span><br><span class="line">                                    e.val = value;</span><br><span class="line">                                <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                            Node&lt;K,V&gt; pred = e;</span><br><span class="line">                          	<span class="comment">// 1.4.1.2 没找到，创建新的节点。</span></span><br><span class="line">                            <span class="keyword">if</span> ((e = e.next) == <span class="keyword">null</span>) &#123; </span><br><span class="line">                                pred.next = <span class="keyword">new</span> Node&lt;K,V&gt;(hash, key,</span><br><span class="line">                                                          value, <span class="keyword">null</span>);</span><br><span class="line">                                <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                  	<span class="comment">// 1.4.2 红黑树</span></span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (f <span class="keyword">instanceof</span> TreeBin) &#123;</span><br><span class="line">                        Node&lt;K,V&gt; p;</span><br><span class="line">                        binCount = <span class="number">2</span>;</span><br><span class="line">                        <span class="keyword">if</span> ((p = ((TreeBin&lt;K,V&gt;)f).putTreeVal(hash, key,</span><br><span class="line">                                                       value)) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                            oldVal = p.val;</span><br><span class="line">                            <span class="keyword">if</span> (!onlyIfAbsent)</span><br><span class="line">                                p.val = value;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">          </span><br><span class="line">          	<span class="comment">// 1.5 判断链表长度</span></span><br><span class="line">            <span class="keyword">if</span> (binCount != <span class="number">0</span>) &#123;</span><br><span class="line">              	<span class="comment">// 如果链表长度大于 8，进行扩容或变为红黑树。</span></span><br><span class="line">              	<span class="comment">// 如果本来就是红黑树，binCount 固定为2，不会进到这个分支。</span></span><br><span class="line">                <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD)</span><br><span class="line">                    treeifyBin(tab, i);</span><br><span class="line">                <span class="keyword">if</span> (oldVal != <span class="keyword">null</span>)</span><br><span class="line">                    <span class="keyword">return</span> oldVal;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line"> 		<span class="comment">// 2. 元素个数加1，并判断是否扩容</span></span><br><span class="line">    addCount(<span class="number">1L</span>, binCount);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>主要的流程：</p>
<ul>
<li>在第一次 put 时，初始化 table。</li>
<li>桶空的时候，CAS 尝试添加节点。</li>
<li>否则说明桶有节点，对桶首节点加锁，进行链表/红黑树的插入/更新节点的操作。</li>
<li>如果桶在进行扩容，要先帮助扩容，再添加节点。</li>
<li>如果是新增节点，元素个数加 1，判断是否扩容。</li>
</ul>
<p>还需要注意以下操作的线程安全性：</p>
<ul>
<li><code>initTable()</code> 初始化 table。</li>
<li><code>helpTransfer()</code> 帮助扩容。</li>
<li><code>addCount()</code> 增加元素。</li>
</ul>
<h2 id="initTable"><a href="#initTable" class="headerlink" title="initTable()"></a>initTable()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Node&lt;K,V&gt;[] initTable() &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; <span class="keyword">int</span> sc;</span><br><span class="line">    <span class="keyword">while</span> ((tab = table) == <span class="keyword">null</span> || tab.length == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((sc = sizeCtl) &lt; <span class="number">0</span>)</span><br><span class="line">            Thread.yield(); <span class="comment">// lost initialization race; just spin</span></span><br><span class="line">        <span class="comment">// 初始化前，CAS 把 sizeCtl 设为 -1，使得只有一个线程可以进入这个分支。</span></span><br><span class="line">      	<span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, -<span class="number">1</span>)) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> ((tab = table) == <span class="keyword">null</span> || tab.length == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">int</span> n = (sc &gt; <span class="number">0</span>) ? sc : DEFAULT_CAPACITY;</span><br><span class="line">                 		<span class="comment">// 数组大小为 sizeCtl</span></span><br><span class="line">                    <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line">                    Node&lt;K,V&gt;[] nt = (Node&lt;K,V&gt;[])<span class="keyword">new</span> Node&lt;?,?&gt;[n]; </span><br><span class="line">                    table = tab = nt;</span><br><span class="line">                    sc = n - (n &gt;&gt;&gt; <span class="number">2</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">              	<span class="comment">// sizeCtl 设为数组大小的 3/4</span></span><br><span class="line">                sizeCtl = sc;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> tab;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>CAS 把 sizeCtl 设为 -1，使得只有一个线程在初始化 table。</li>
<li>初始化后，数组长度为构造函数传入的初始容量、或默认值16.</li>
<li>初始化后，阈值 sizeCtl 为数组长度的 3/4。</li>
</ul>
<h2 id="treeifyBin"><a href="#treeifyBin" class="headerlink" title="treeifyBin()"></a>treeifyBin()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">treeifyBin</span><span class="params">(Node&lt;K,V&gt;[] tab, <span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">    Node&lt;K,V&gt; b; <span class="keyword">int</span> n, sc;</span><br><span class="line">    <span class="keyword">if</span> (tab != <span class="keyword">null</span>) &#123;</span><br><span class="line">      	<span class="comment">// 1 如果数组大小小于 64，进行扩容</span></span><br><span class="line">        <span class="keyword">if</span> ((n = tab.length) &lt; MIN_TREEIFY_CAPACITY)</span><br><span class="line">            tryPresize(n &lt;&lt; <span class="number">1</span>);</span><br><span class="line">      	<span class="comment">// 2 否则锁住桶，把链表转为红黑树。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((b = tabAt(tab, index)) != <span class="keyword">null</span> &amp;&amp; b.hash &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (b) &#123;</span><br><span class="line">                <span class="keyword">if</span> (tabAt(tab, index) == b) &#123;</span><br><span class="line">                    TreeNode&lt;K,V&gt; hd = <span class="keyword">null</span>, tl = <span class="keyword">null</span>;</span><br><span class="line">                    <span class="keyword">for</span> (Node&lt;K,V&gt; e = b; e != <span class="keyword">null</span>; e = e.next) &#123;</span><br><span class="line">                        TreeNode&lt;K,V&gt; p =</span><br><span class="line">                            <span class="keyword">new</span> TreeNode&lt;K,V&gt;(e.hash, e.key, e.val,</span><br><span class="line">                                              <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">                        <span class="keyword">if</span> ((p.prev = tl) == <span class="keyword">null</span>)</span><br><span class="line">                            hd = p;</span><br><span class="line">                        <span class="keyword">else</span></span><br><span class="line">                            tl.next = p;</span><br><span class="line">                        tl = p;</span><br><span class="line">                    &#125;</span><br><span class="line">                  	<span class="comment">// 节点的 hash 值为 -2</span></span><br><span class="line">                    setTabAt(tab, index, <span class="keyword">new</span> TreeBin&lt;K,V&gt;(hd));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>把链表转为红黑树时，要把桶加锁。</li>
<li>节点的 hash 值<ul>
<li>-1 表示正在 MOVE（扩容，拆分链表）</li>
<li>-2 表示是红黑树</li>
<li>大于等于 0，存储的是链表的节点。</li>
</ul>
</li>
</ul>
<h2 id="tryPresize"><a href="#tryPresize" class="headerlink" title="tryPresize()"></a>tryPresize()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// putVal() 调用时，传入的 size 是数组长度的两倍。</span></span><br><span class="line"><span class="comment">// putAll() 调用时，传入的是要放入元素的个数。</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">tryPresize</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">  	<span class="comment">// size 的 1.5 倍，再变为 2 的次幂。</span></span><br><span class="line">    <span class="keyword">int</span> c = (size &gt;= (MAXIMUM_CAPACITY &gt;&gt;&gt; <span class="number">1</span>)) ? MAXIMUM_CAPACITY :</span><br><span class="line">        tableSizeFor(size + (size &gt;&gt;&gt; <span class="number">1</span>) + <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">int</span> sc;</span><br><span class="line">    <span class="keyword">while</span> ((sc = sizeCtl) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        Node&lt;K,V&gt;[] tab = table; <span class="keyword">int</span> n;</span><br><span class="line">      	<span class="comment">// 1. 数组没有初始化先初始化，和数组 initTable 类似</span></span><br><span class="line">        <span class="comment">//    putAll 时可能数组还未初始化</span></span><br><span class="line">        <span class="keyword">if</span> (tab == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>) &#123;</span><br><span class="line">            n = (sc &gt; c) ? sc : c;</span><br><span class="line">            <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, -<span class="number">1</span>)) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="keyword">if</span> (table == tab) &#123;</span><br><span class="line">                        <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line">                        Node&lt;K,V&gt;[] nt = (Node&lt;K,V&gt;[])<span class="keyword">new</span> Node&lt;?,?&gt;[n];</span><br><span class="line">                        table = nt;</span><br><span class="line">                        sc = n - (n &gt;&gt;&gt; <span class="number">2</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    sizeCtl = sc;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      	<span class="comment">// 2. 数组足够大了，不需要扩容，直接退出。</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (c &lt;= sc || n &gt;= MAXIMUM_CAPACITY)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (tab == table) &#123;</span><br><span class="line">            <span class="keyword">int</span> rs = resizeStamp(n);</span><br><span class="line">          	<span class="comment">// 这个分支与上层 while 条件互斥，不可能进入，是 bug。JDK 11 去掉了这个分支。</span></span><br><span class="line">          	<span class="comment">// https://bugs.openjdk.java.net/browse/JDK-8215409</span></span><br><span class="line">            <span class="keyword">if</span> (sc &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                Node&lt;K,V&gt;[] nt;</span><br><span class="line">                <span class="keyword">if</span> ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs || sc == rs + <span class="number">1</span> ||</span><br><span class="line">                    sc == rs + MAX_RESIZERS || (nt = nextTable) == <span class="keyword">null</span> ||</span><br><span class="line">                    transferIndex &lt;= <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, sc + <span class="number">1</span>))</span><br><span class="line">                    transfer(tab, nt);</span><br><span class="line">            &#125;</span><br><span class="line">          	<span class="comment">// 第一次进行 transfer 时 + 2，</span></span><br><span class="line">           	<span class="comment">// 之后进行 transfer 时 + 1，</span></span><br><span class="line">          	<span class="comment">// 完成 transfer 时 - 1。因此，所有线程都完成时，sc 为 (rs &lt;&lt; RESIZE_STAMP_SHIFT) + 1。</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc,</span><br><span class="line">                                         (rs &lt;&lt; RESIZE_STAMP_SHIFT) + <span class="number">2</span>))</span><br><span class="line">                transfer(tab, <span class="keyword">null</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">resizeStamp</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// Integer.numberOfLeadingZeros(n) 表示 n 转二进制后最高位 1 之前的 0 个数，这个数一定小于 32</span></span><br><span class="line">    <span class="comment">// 1 &lt;&lt; (RESIZE_STAMP_BITS - 1) 表示 0x7fff，也就是将低 16 位的第一位改成 1</span></span><br><span class="line">    <span class="comment">// 最终结果为 0000 0000 1000 0000 000x xxxx </span></span><br><span class="line">  	<span class="comment">// 这样在 tryPresize() 中，resizeStamp(n) &lt;&lt; 16，最高位为1，一定是一个负数，并且是极小的负数。</span></span><br><span class="line">    <span class="keyword">return</span> Integer.numberOfLeadingZeros(n) | (<span class="number">1</span> &lt;&lt; (RESIZE_STAMP_BITS - <span class="number">1</span>));</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> RESIZE_STAMP_SHIFT = <span class="number">16</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>如果数组没有初始化先进行初始化。</li>
<li>只有第一个 CAS <code>sizeCtl</code> 为 (rs &lt;&lt; RESIZE_STAMP_SHIFT) + 2  的线程可以进行  transfer 扩容。</li>
<li>帮助扩容的线程是通过 <code>helpTransfer()</code> 进入的（在 <code>putVal()</code> 时，发现对应的桶在扩容触发），每个帮助的线程会把 <code>sizeCtl</code>+ 1。</li>
</ul>
<h2 id="helpTransfer"><a href="#helpTransfer" class="headerlink" title="helpTransfer()"></a>helpTransfer()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// putVal() 时，发现桶在 transfer，调用 helpTransfer() 来帮助扩容。</span></span><br><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt;[] helpTransfer(Node&lt;K,V&gt;[] tab, Node&lt;K,V&gt; f) &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] nextTab; <span class="keyword">int</span> sc;</span><br><span class="line">    <span class="keyword">if</span> (tab != <span class="keyword">null</span> &amp;&amp; (f <span class="keyword">instanceof</span> ForwardingNode) &amp;&amp;</span><br><span class="line">        (nextTab = ((ForwardingNode&lt;K,V&gt;)f).nextTable) != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">int</span> rs = resizeStamp(tab.length);</span><br><span class="line">     </span><br><span class="line">        <span class="keyword">while</span> (nextTab == nextTable &amp;&amp; table == tab &amp;&amp;</span><br><span class="line">               (sc = sizeCtl) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">          	<span class="comment">// 1 如果 sc 的高 16 位不等于标识符(说明sizeCtl已经改变，扩容已经结束)</span></span><br><span class="line">         		<span class="comment">// 2 如果 sc == 标识符 + 1 （扩容结束了，不再有线程进行扩容）</span></span><br><span class="line">            <span class="comment">// 3 如果 sc == 标识符 + 65535（辅助扩容线程数已经达到最大）</span></span><br><span class="line">            <span class="comment">// 4 如果 transferIndex &lt;= 0 (转移状态变化了)</span></span><br><span class="line">            <span class="keyword">if</span> ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs || sc == rs + <span class="number">1</span> ||</span><br><span class="line">                sc == rs + MAX_RESIZERS || transferIndex &lt;= <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">          	<span class="comment">// sizeCtl 加 1，来使该线程参与到扩容中。</span></span><br><span class="line">            <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, sc + <span class="number">1</span>)) &#123;</span><br><span class="line">                transfer(tab, nextTab);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> nextTab;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> table;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="transfer"><a href="#transfer" class="headerlink" title="transfer()"></a>transfer()</h2><p>第一个 <code>transfer()</code> 的线程传入的是 <code>transfer(tab, null)</code>。</p>
<p>之后传入的是 <code>transfer(tab, nextTab)</code>。根据第二个参数判断是不是第一次 <code>transfer()</code>。</p>
<p><code>transferIndex</code> 记录了扩容任务进行到的下标，从右向左处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">transfer</span><span class="params">(Node&lt;K,V&gt;[] tab, Node&lt;K,V&gt;[] nextTab)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n = tab.length, stride;</span><br><span class="line">    <span class="comment">// 1. stride 可以理解为”步长“，有 n 个位置是需要进行迁移的</span></span><br><span class="line">    <span class="comment">//    将这 n 个任务分为多个任务包，每个任务包有 stride 个任务</span></span><br><span class="line">    <span class="comment">//    stride 在单核下直接等于 n，多核模式下为 (n&gt;&gt;&gt;3)/NCPU，最小值是 16</span></span><br><span class="line">    <span class="keyword">if</span> ((stride = (NCPU &gt; <span class="number">1</span>) ? (n &gt;&gt;&gt; <span class="number">3</span>) / NCPU : n) &lt; MIN_TRANSFER_STRIDE)</span><br><span class="line">        stride = MIN_TRANSFER_STRIDE; <span class="comment">// subdivide range</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 如果 nextTab 为 null，是第一次进入。先进行一次初始化。</span></span><br><span class="line">    <span class="keyword">if</span> (nextTab == <span class="keyword">null</span>) &#123;            <span class="comment">// initiating</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">          	<span class="comment">// 数组长度加倍</span></span><br><span class="line">            Node&lt;K,V&gt;[] nt = (Node&lt;K,V&gt;[])<span class="keyword">new</span> Node&lt;?,?&gt;[n &lt;&lt; <span class="number">1</span>];</span><br><span class="line">            nextTab = nt;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Throwable ex) &#123;      <span class="comment">// try to cope with OOME</span></span><br><span class="line">            sizeCtl = Integer.MAX_VALUE;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        nextTable = nextTab;</span><br><span class="line">      	<span class="comment">//     transferIndex初始值为table.length</span></span><br><span class="line">        transferIndex = n;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> nextn = nextTab.length;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. ForwardingNode 是占位用的，标记该节点已经处理过了</span></span><br><span class="line">    <span class="comment">//    这个构造方法会生成一个 Node，hash 为 MOVED (-1)，</span></span><br><span class="line">  	<span class="comment">//			nextTab 缓存如Node。以便之后来帮忙的线程使用。</span></span><br><span class="line">    <span class="comment">//    后面我们会看到，原数组中位置 i 处的节点完成迁移工作后，</span></span><br><span class="line">    <span class="comment">//    就会将位置 i 处设置为这个 ForwardingNode，用来告诉其他线程该位置已经处理过了</span></span><br><span class="line">    ForwardingNode&lt;K,V&gt; fwd = <span class="keyword">new</span> ForwardingNode&lt;K,V&gt;(nextTab);</span><br><span class="line">  	<span class="comment">// 下边的while循环用来找到第一个没有处理的节点，</span></span><br><span class="line">  	<span class="comment">// advance为false时，表示找到了。</span></span><br><span class="line">  	<span class="comment">// advance为true时，说明当前节点已经处理完了，需要处理下一个节点。</span></span><br><span class="line">    <span class="keyword">boolean</span> advance = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">boolean</span> finishing = <span class="keyword">false</span>; <span class="comment">// to ensure sweep before committing nextTab</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// i 每次任务的上边界，bound 是下边界，注意是从后往前</span></span><br><span class="line">    <span class="comment">// --i &lt; bound 时就领取一次的任务，直到任务处理完毕</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>, bound = <span class="number">0</span>;;) &#123;</span><br><span class="line">        Node&lt;K,V&gt; f; <span class="keyword">int</span> fh;</span><br><span class="line">        <span class="comment">// 4. advance 为 true 表示可以进行下一个位置的迁移了</span></span><br><span class="line">        <span class="comment">//    第一次while循环：当前线程领取任务，走第三个else(一直自旋尝试领取任务)</span></span><br><span class="line">        <span class="comment">//          最终的结果为：i 指向了 transferIndex，bound 指向了 transferIndex-stride</span></span><br><span class="line">        <span class="comment">//    之后每处理完一个节点：走第一个if，处理的下一个槽位的节点，</span></span><br><span class="line">      	<span class="comment">//		直到当前线程领取的任务处理完毕， 再次走第三个else，领取步长stride的任务直到transferIndex&lt;=0</span></span><br><span class="line">        <span class="keyword">while</span> (advance) &#123;</span><br><span class="line">            <span class="keyword">int</span> nextIndex, nextBound;</span><br><span class="line">            <span class="comment">// 4.1 --i表示处理一下槽位的节点</span></span><br><span class="line">            <span class="keyword">if</span> (--i &gt;= bound || finishing)</span><br><span class="line">                advance = <span class="keyword">false</span>;</span><br><span class="line">            <span class="comment">// 4.2 当前任务包处理完了，看是否还有节点没处理。</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((nextIndex = transferIndex) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                i = -<span class="number">1</span>;</span><br><span class="line">                advance = <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 4.3 自旋尝试领取步长stride的任务包。</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt</span><br><span class="line">                     (<span class="keyword">this</span>, TRANSFERINDEX, nextIndex,</span><br><span class="line">                      nextBound = (nextIndex &gt; stride ?</span><br><span class="line">                                   nextIndex - stride : <span class="number">0</span>))) &#123;</span><br><span class="line">                <span class="comment">// nextBound 是这次迁移任务的下边界，注意，是从后往前</span></span><br><span class="line">                bound = nextBound;</span><br><span class="line">                <span class="comment">// nextIndex 是这次迁移任务的上边界</span></span><br><span class="line">              	<span class="comment">// 因为找到了一个要处理的节点，会先跳出while处理</span></span><br><span class="line">              	<span class="comment">// 下次要处理的是 i = nextIndex - 1。</span></span><br><span class="line">                i = nextIndex - <span class="number">1</span>;</span><br><span class="line">                advance = <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 5. 如果扩容结束了。</span></span><br><span class="line">        <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= n || i + n &gt;= nextn) &#123;</span><br><span class="line">            <span class="keyword">int</span> sc;</span><br><span class="line">            <span class="comment">// 5.1 完成扩容</span></span><br><span class="line">            <span class="keyword">if</span> (finishing) &#123;                    <span class="comment">// 完成扩容</span></span><br><span class="line">                nextTable = <span class="keyword">null</span>;</span><br><span class="line">                table = nextTab;                <span class="comment">// 更新 table</span></span><br><span class="line">                sizeCtl = (n &lt;&lt; <span class="number">1</span>) - (n &gt;&gt;&gt; <span class="number">1</span>); <span class="comment">// 更新阈值为新数组长度的 3/4</span></span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 5.2 扩容结束，把 sizeCtl - 1。</span></span><br><span class="line">            <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc = sizeCtl, sc - <span class="number">1</span>)) &#123;</span><br><span class="line">                <span class="comment">// 不相等说明有其它线程在辅助扩容，当前线程直接返回。</span></span><br><span class="line">                <span class="keyword">if</span> ((sc - <span class="number">2</span>) != resizeStamp(n) &lt;&lt; RESIZE_STAMP_SHIFT)</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                <span class="comment">// 相等说明没有线程在帮助他们扩容了。也就是说，扩容结束了。</span></span><br><span class="line">                finishing = advance = <span class="keyword">true</span>;</span><br><span class="line">                i = n; <span class="comment">// recheck before commit</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 6. 如果位置 i 处是空的，没有任何节点，那么放入刚刚初始化的 ForwardingNode ”空节点“</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((f = tabAt(tab, i)) == <span class="keyword">null</span>)</span><br><span class="line">            advance = casTabAt(tab, i, <span class="keyword">null</span>, fwd);</span><br><span class="line">        <span class="comment">// 7. 该位置处是一个 ForwardingNode，代表该位置已经迁移过了</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED)</span><br><span class="line">            advance = <span class="keyword">true</span>; <span class="comment">// already processed</span></span><br><span class="line">        <span class="comment">// 8. 数组迁移，原理和 HashMap 一样</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 对数组该位置处的结点加锁，开始处理数组该位置处的迁移工作</span></span><br><span class="line">            <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                ...</span><br><span class="line">                advance = <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="size"><a href="#size" class="headerlink" title="size()"></a>size()</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">long</span> n = sumCount();</span><br><span class="line">    <span class="keyword">return</span> ((n &lt; <span class="number">0L</span>) ? <span class="number">0</span> :</span><br><span class="line">            (n &gt; (<span class="keyword">long</span>)Integer.MAX_VALUE) ? Integer.MAX_VALUE : (<span class="keyword">int</span>)n);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// sumCount 用于统计当前的元素个数</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">long</span> <span class="title">sumCount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    CounterCell[] as = counterCells; CounterCell a;</span><br><span class="line">    <span class="keyword">long</span> sum = baseCount;</span><br><span class="line">    <span class="keyword">if</span> (as != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; as.length; ++i) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((a = as[i]) != <span class="keyword">null</span>)</span><br><span class="line">                sum += a.value;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>计数并没有使用一个 <code>AtomicLong</code>，这样会使每次操作都有竞争，降低并发。</p>
<h2 id="addCount"><a href="#addCount" class="headerlink" title="addCount()"></a>addCount()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//基础值，没有竞争时会使用这个值</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> <span class="keyword">long</span> baseCount;</span><br><span class="line"><span class="comment">//存放Cell的hash表，大小为2的幂</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> CounterCell[] counterCells;</span><br><span class="line"><span class="comment">// 通过cas实现的锁，0无锁，1获得锁</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> <span class="keyword">int</span> cellsBusy;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">addCount</span><span class="params">(<span class="keyword">long</span> x, <span class="keyword">int</span> check)</span> </span>&#123;</span><br><span class="line">    CounterCell[] as; <span class="keyword">long</span> b, s;</span><br><span class="line">    <span class="comment">// 1. 统计元素个数</span></span><br><span class="line">    <span class="comment">// 1.1 baseCount 只有 counterCells 还没有初始化时才使用</span></span><br><span class="line">  	<span class="comment">//		如果baseCount CAS 成功，则统计元素个数结束。</span></span><br><span class="line">    <span class="keyword">if</span> ((as = counterCells) != <span class="keyword">null</span> ||</span><br><span class="line">        !U.compareAndSwapLong(<span class="keyword">this</span>, BASECOUNT, b = baseCount, s = b + x)) &#123;</span><br><span class="line">        CounterCell a; <span class="keyword">long</span> v; <span class="keyword">int</span> m;</span><br><span class="line">        <span class="comment">// 1.1 uncontended表示是否存在锁竞争</span></span><br><span class="line">        <span class="keyword">boolean</span> uncontended = <span class="keyword">true</span>;</span><br><span class="line">        <span class="comment">// 1.2 将线程分流到 CounterCell[] 数组中，使用更小粒度的 cas 操作</span></span><br><span class="line">        <span class="comment">//     (1) CounterCell[]未初始化</span></span><br><span class="line">        <span class="comment">//     (2) CounterCell[]中的节点未初始化</span></span><br><span class="line">        <span class="comment">//     (3) 对 as[random] 进行 cas 操作，失败则存在锁竞争问题uncontended=false，成功直接返回</span></span><br><span class="line">        <span class="keyword">if</span> (as == <span class="keyword">null</span> || (m = as.length - <span class="number">1</span>) &lt; <span class="number">0</span> ||</span><br><span class="line">            (a = as[ThreadLocalRandom.getProbe() &amp; m]) == <span class="keyword">null</span> ||</span><br><span class="line">            !(uncontended = U.compareAndSwapLong(a, CELLVALUE, v = a.value, v + x))) &#123;</span><br><span class="line">                fullAddCount(x, uncontended);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (check &lt;= <span class="number">1</span>)</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        <span class="comment">// 1.3 统计当前元素的个数</span></span><br><span class="line">        s = sumCount();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 2. 扩容操作，与 tryPresize()、helpTransfer() 类似。</span></span><br><span class="line">    <span class="keyword">if</span> (check &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        Node&lt;K,V&gt;[] tab, nt; <span class="keyword">int</span> n, sc;</span><br><span class="line">        <span class="keyword">while</span> (s &gt;= (<span class="keyword">long</span>)(sc = sizeCtl) &amp;&amp; (tab = table) != <span class="keyword">null</span> &amp;&amp;</span><br><span class="line">               (n = tab.length) &lt; MAXIMUM_CAPACITY) &#123;</span><br><span class="line">            <span class="keyword">int</span> rs = resizeStamp(n);</span><br><span class="line">            <span class="comment">// sc&lt;0 表示有其它线程在扩容</span></span><br><span class="line">            <span class="keyword">if</span> (sc &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 扩容已经结束</span></span><br><span class="line">                <span class="keyword">if</span> ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs || sc == rs + <span class="number">1</span> ||</span><br><span class="line">                    sc == rs + MAX_RESIZERS || (nt = nextTable) == <span class="keyword">null</span> ||</span><br><span class="line">                    transferIndex &lt;= <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="comment">// 加入到扩容中，扩容的线程数+1</span></span><br><span class="line">                <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, sc + <span class="number">1</span>))</span><br><span class="line">                    transfer(tab, nt);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 当前线程发起扩容，注意线程数是直接+2</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, (rs &lt;&lt; RESIZE_STAMP_SHIFT) + <span class="number">2</span>))</span><br><span class="line">                transfer(tab, <span class="keyword">null</span>);</span><br><span class="line">            s = sumCount();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用 <code>CounterCell[]</code> 数组来分散 元素数量变化时候的竞争。</p>
<ol>
<li><p>每次计数，根据 <code>Thread.probe</code> 映射到一个  counter  中去处理，使用 CAS 尝试计数。</p>
</li>
<li><p>如果存在竞争，且 counter 数组长度小于 CPU 核数，申请volatile锁，加倍 counter 数组长度。</p>
</li>
<li>如果在 counter CAS 尝试失败、长度小且申请volatile锁失败，对 baseCount 尝试 CAS 计数。</li>
</ol>
<h1 id="扩容总结"><a href="#扩容总结" class="headerlink" title="扩容总结"></a>扩容总结</h1><h2 id="触发时机"><a href="#触发时机" class="headerlink" title="触发时机"></a>触发时机</h2><ul>
<li><p>在添加节点 <code>addCount()</code> 后，如果元素数目超过阈值，会进行 <code>tansfer</code> 扩容。</p>
</li>
<li><p>在一个桶的元素数目超过8个、且table总长度小于64时，会进行 <code>tryPresize</code> 扩容。</p>
</li>
<li>在添加节点时，发现对应的桶在进行扩容，会进行帮助扩容 <code>helpTransfer</code>。</li>
</ul>
<p>其中 <code>tryPresize()</code> 和 <code>helpTransfer</code> 最后都会调用 <code>transfer()</code>。</p>
<h2 id="扩容过程"><a href="#扩容过程" class="headerlink" title="扩容过程"></a>扩容过程</h2><ul>
<li><p>每个线程，每次申请扩容 <code>stride</code> 个桶，从右到左进行扩容。</p>
</li>
<li><p>使用 <code>transferIndex</code> 记录还没有线程认领进行扩容的最右边的位置。</p>
</li>
<li><p>会把处理结束的桶标记为 <code>ForwardingNode</code> 类型的节点，其中存储了 <code>nextTable</code>，即扩容后新的数组。</p>
</li>
<li><p>在第一次进行扩容（调用 <code>transfer()</code> 时），</p>
<ul>
<li>设置<code>transferIndex</code> 为旧数组长度。</li>
<li>创建 <code>nextTable</code>，长度为旧数组长度两倍。</li>
</ul>
</li>
<li>在所有扩容结束后<ul>
<li>设置 <code>sizeCtl</code> 阈值 为新数组长度的 3/4。</li>
</ul>
</li>
</ul>
<h1 id="get"><a href="#get" class="headerlink" title="get()"></a>get()</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; e, p; <span class="keyword">int</span> n, eh; K ek;</span><br><span class="line">    <span class="keyword">int</span> h = spread(key.hashCode());</span><br><span class="line">    <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">        (e = tabAt(tab, (n - <span class="number">1</span>) &amp; h)) != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((eh = e.hash) == h) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((ek = e.key) == key || (ek != <span class="keyword">null</span> &amp;&amp; key.equals(ek)))</span><br><span class="line">                <span class="keyword">return</span> e.val;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (eh &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> (p = e.find(h, key)) != <span class="keyword">null</span> ? p.val : <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (e.hash == h &amp;&amp;</span><br><span class="line">                ((ek = e.key) == key || (ek != <span class="keyword">null</span> &amp;&amp; key.equals(ek))))</span><br><span class="line">                <span class="keyword">return</span> e.val;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>和 HashMap 类似，</p>
<ol>
<li>先看桶的首节点的 key</li>
<li>如果桶中是红黑树，在红黑树里找。</li>
<li>否则在链表中找。</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>5. concurrenct 包中的锁</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/5concurrenct%E5%8C%85%E4%B8%AD%E7%9A%84%E9%94%81/</url>
    <content><![CDATA[<h1 id="Lock-接口"><a href="#Lock-接口" class="headerlink" title="Lock 接口"></a>Lock 接口</h1><h2 id="相比-synchronized-的优势"><a href="#相比-synchronized-的优势" class="headerlink" title="相比 synchronized 的优势"></a>相比 synchronized 的优势</h2><p><code>concurrent</code> 包中的 Lock 接口，比 <code>synchronized</code> 灵活，有如下的优势：</p>
<ul>
<li>可以非阻塞的获取锁</li>
<li>获取锁过程中可以被中断。</li>
<li>超时获取锁。</li>
<li>使用 <code>AbstractQueuedSynchronizer</code> (<code>AQS</code>) 自定义锁。</li>
</ul>
<h2 id="主要的方法"><a href="#主要的方法" class="headerlink" title="主要的方法"></a>主要的方法</h2><ul>
<li><code>void lock()</code> </li>
<li><code>void lockInterruptibly() throws InterruptedException</code> 可以被中断。</li>
<li><code>boolean tryLock()</code> 非阻塞获取锁，能够获得返回 true，否则 false。</li>
<li><code>boolean tryLock(long time, TimeUnit unit) throws InterruptedException</code> 非阻塞获取锁、可以被中断。 </li>
<li><code>void unlock()</code> 解锁。</li>
<li><code>Condition newCondition()</code> 获取等待通知组件，只有获取了锁后，才可以调用 <code>condition.await()</code> 和 <code>condition.signal()</code>。</li>
</ul>
<h2 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line"><span class="comment">// 获取锁不能在 try 里，否则获取失败、抛出异常时，还会去解锁。</span></span><br><span class="line">lock.lock();</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// 更新对象</span></span><br><span class="line">    <span class="comment">//捕获异常</span></span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    lock.unlock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="队列同步器-AbstractQueuedSynchronizer"><a href="#队列同步器-AbstractQueuedSynchronizer" class="headerlink" title="队列同步器 AbstractQueuedSynchronizer"></a>队列同步器 AbstractQueuedSynchronizer</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>AQS 用来构建锁和其他同步组件。<code>ReentrantLock</code>、<code>ReentrantReadWriteLock</code> 和 <code>CountDownLatch</code>。</p>
<p>使用 <code>volatile int state</code> 表示同步的状态。</p>
<p>内置了 同步队列，我们只需要实现如何更新 <code>state</code>，就可以实现自定义的锁。</p>
<h2 id="使用方式-1"><a href="#使用方式-1" class="headerlink" title="使用方式"></a>使用方式</h2><ol>
<li>类 <code>MyLock</code> 实现 <code>Lock</code> 接口。</li>
<li>类 <code>MyLock</code> 的内部类 <code>Sync</code> 继承 <code>AQS</code>，重写响应的方法。</li>
<li>类 <code>MyLock</code> 中的方法调用 <code>Sync</code> 的方法。</li>
</ol>
<h3 id="AQS-可重写的方法"><a href="#AQS-可重写的方法" class="headerlink" title="AQS 可重写的方法"></a>AQS 可重写的方法</h3><ul>
<li><code>protected boolean tryAcquire(int arg)</code></li>
<li><code>protected boolean tryRelease(int arg)</code></li>
<li><code>protected int tryAcquireShared(int arg)</code><ul>
<li>返回值小于0，获取锁失败。</li>
<li>返回值等于0，获取锁失败，下一个线程获取锁应该会失败。</li>
<li>返回值大于0，获取锁失败，下一个线程获取锁应该会成功。</li>
</ul>
</li>
<li><code>protected boolean tryReleaseShared(int arg)</code></li>
<li><code>protected boolean isHeldExclusively()</code> 是否在独占模式下被线程占用。</li>
</ul>
<h3 id="AQS-可以使用的方法"><a href="#AQS-可以使用的方法" class="headerlink" title="AQS 可以使用的方法"></a>AQS 可以使用的方法</h3><ul>
<li>独占模式获取锁<ul>
<li><code>public void acquire(int arg)</code></li>
<li><code>public void acquireInterruptibly(int arg)</code></li>
<li><code>public boolean tryAcquireNanos(int arg, long nanos)</code></li>
</ul>
</li>
<li>共享模式获取锁<ul>
<li><code>public void acquireShared(int arg)</code></li>
<li><code>public void acquireSharedInterruptibly(int arg)</code></li>
<li><code>public boolean tryAcquireSharedNanos(int arg, long nanos)</code></li>
</ul>
</li>
<li>释放锁<ul>
<li><code>public boolean release(int arg)</code></li>
<li><code>public boolean releaseShared(int arg)</code></li>
</ul>
</li>
<li>获取等待在同步队列上的线程集合<ul>
<li><code>public Collection&lt;Thread&gt; getQueuedThreads()</code></li>
</ul>
</li>
</ul>
<h2 id="三个要点"><a href="#三个要点" class="headerlink" title="三个要点"></a>三个要点</h2><ul>
<li>状态<ul>
<li>使用 <code>volatile int state</code> 来描述同步资源的状态</li>
</ul>
</li>
<li>队列<ul>
<li>等待获取锁的线程都进入同步队列，每次只有头结点的后继节点会被唤醒，进行获取锁的尝试。</li>
</ul>
</li>
<li>CAS<ul>
<li>更新 <code>state</code>、头结点、尾节点，都是用的 CAS 完成。</li>
<li>这里没有使用 AtomicXXX 相关类，而是直接用了 unsafe 方法，来提供性能。</li>
</ul>
</li>
</ul>
<h1 id="AQS-独占锁的获取源码分析"><a href="#AQS-独占锁的获取源码分析" class="headerlink" title="AQS 独占锁的获取源码分析"></a>AQS 独占锁的获取源码分析</h1><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://segmentfault.com/a/1190000015739343">逐行分析AQS源码(1)——独占锁的获取</a></li>
</ul>
<h2 id="acquire"><a href="#acquire" class="headerlink" title="acquire()"></a>acquire()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!tryAcquire(arg) &amp;&amp; acquireQueued(addWaiter(Node.EXCLUSIVE), arg))</span><br><span class="line">    selfInterrupt();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">selfInterrupt</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  Thread.currentThread().interrupt();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>使用 <code>tryAcquire(arg)</code> 获取同步状态。</li>
<li>如果失败了，构造独占式节点（Node.EXCLUSIVE），使用 <code>addWaiter()</code> 加入到同步队列的尾部。</li>
<li>使用 <code>acquireQueued()</code> 在被唤醒时，死循环获取同步状态。</li>
<li>因为是不可中断的，整个过程记录中断状态，在获取到同步状态后，如果被中断了，则调用 <code>Thread.currentThread().interrupt();</code> 中断线程。</li>
</ol>
<h2 id="addWaiter"><a href="#addWaiter" class="headerlink" title="addWaiter()"></a>addWaiter()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> Node <span class="title">addWaiter</span><span class="params">(Node mode)</span> </span>&#123;</span><br><span class="line">  Node node = <span class="keyword">new</span> Node(Thread.currentThread(), mode);</span><br><span class="line">  <span class="comment">// Try the fast path of enq; backup to full enq on failure</span></span><br><span class="line">  Node pred = tail;</span><br><span class="line">  <span class="keyword">if</span> (pred != <span class="keyword">null</span>) &#123; <span class="comment">// 队列不为空</span></span><br><span class="line">    node.prev = pred;</span><br><span class="line">    <span class="keyword">if</span> (compareAndSetTail(pred, node)) &#123; <span class="comment">// 添加节点到尾部</span></span><br><span class="line">      pred.next = node;</span><br><span class="line">      <span class="keyword">return</span> node;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  enq(node);</span><br><span class="line">  <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> Node <span class="title">enq</span><span class="params">(<span class="keyword">final</span> Node node)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    Node t = tail;</span><br><span class="line">    <span class="keyword">if</span> (t == <span class="keyword">null</span>) &#123; <span class="comment">// Must initialize 添加一个空的头结点</span></span><br><span class="line">      <span class="keyword">if</span> (compareAndSetHead(<span class="keyword">new</span> Node()))</span><br><span class="line">        tail = head;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      node.prev = t;</span><br><span class="line">      <span class="keyword">if</span> (compareAndSetTail(t, node)) &#123;</span><br><span class="line">        t.next = node;</span><br><span class="line">        <span class="keyword">return</span> t;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>如果队列不为空、且 CAS 添加节点到尾部成功，则直接返回。</li>
<li>否则，调用 <code>enq</code> 来把节点加入到队列尾部。<ol>
<li>如果队列为空，添加一个空的头结点，<strong>continue</strong>。</li>
<li>如果队列不为空、且 CAS 添加节点到尾部成功，则返回。</li>
</ol>
</li>
</ol>
<h2 id="acquireQueued"><a href="#acquireQueued" class="headerlink" title="acquireQueued()"></a>acquireQueued()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">acquireQueued</span><span class="params">(<span class="keyword">final</span> Node node, <span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">      <span class="keyword">final</span> Node p = node.predecessor();</span><br><span class="line">      <span class="keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;	<span class="comment">// 获取到锁了</span></span><br><span class="line">        setHead(node);</span><br><span class="line">        p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">        failed = <span class="keyword">false</span>;</span><br><span class="line">        <span class="keyword">return</span> interrupted;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span><br><span class="line">          parkAndCheckInterrupt())</span><br><span class="line">        interrupted = <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (failed)</span><br><span class="line">      cancelAcquire(node);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>如果前驱节点为头结点、且获取同步状态成功，说明获取到锁了。直接设置节点为头结点，返回。注意不用 CAS，因为独占模式下，只有一个节点能获取到锁。</li>
<li>否则，使用 <code>LockSupport.park(this);</code> 阻塞线程，并记录中断状态。</li>
<li>在被唤醒的时候，从步骤 1 开始重复。</li>
</ol>
<h2 id="shouldParkAfterFailedAcquire"><a href="#shouldParkAfterFailedAcquire" class="headerlink" title="shouldParkAfterFailedAcquire()"></a>shouldParkAfterFailedAcquire()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">shouldParkAfterFailedAcquire</span><span class="params">(Node pred, Node node)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> ws = pred.waitStatus;</span><br><span class="line">  <span class="keyword">if</span> (ws == Node.SIGNAL)</span><br><span class="line">    <span class="comment">// 如果前驱节点的状态已经设为 SIGNAL，自己可以直接睡了。</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">  <span class="keyword">if</span> (ws &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 如果前驱节点状态是 CANCELLED，向前找节点，舍弃 CANCELLED 节点。</span></span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">      node.prev = pred = pred.prev;</span><br><span class="line">    &#125; <span class="keyword">while</span> (pred.waitStatus &gt; <span class="number">0</span>);</span><br><span class="line">    pred.next = node;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 前驱结点状态是 0，说明没有设置过，CAS 更新为 SIGNAL。</span></span><br><span class="line">    compareAndSetWaitStatus(pred, ws, Node.SIGNAL);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 线程所处的等待锁的状态，初始化时，该值为0</span></span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">int</span> waitStatus;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CANCELLED =  <span class="number">1</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> SIGNAL    = -<span class="number">1</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CONDITION = -<span class="number">2</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> PROPAGATE = -<span class="number">3</span>;</span><br></pre></td></tr></table></figure>
<p>节点的 <code>waitStatus</code>，SIGNAL 表示后继节点被挂起了（马上要被挂起了）。</p>
<ul>
<li><p>一个节点在陷入阻塞状态前，需要把前驱结点的状态设为 SIGNAL。以便自己之后可以被唤醒。</p>
</li>
<li><p>在节点释放锁时，如果节点状态是 SIGNAL，需要把后继节点唤醒。</p>
</li>
</ul>
<p>函数 <code>shouldParkAfterFailedAcquire()</code> 就是用来做这件事的。</p>
<ul>
<li><p>如果前驱节点的状态已经设为 SIGNAL，自己可以直接睡了。返回 true。</p>
</li>
<li><p>如果前驱节点状态是 CANCELLED，说明前驱节点被取消了，向前找节点，舍弃 CANCELLED 节点。返回 false。</p>
</li>
<li><p>前驱结点状态是 0，说明没有设置过，CAS 更新为 SIGNAL。返回 false。</p>
</li>
</ul>
<h1 id="AQS-独占锁的释放源码分析"><a href="#AQS-独占锁的释放源码分析" class="headerlink" title="AQS 独占锁的释放源码分析"></a>AQS 独占锁的释放源码分析</h1><h2 id="引用-1"><a href="#引用-1" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://segmentfault.com/a/1190000015752512">逐行分析AQS源码(2)——独占锁的释放</a></li>
</ul>
<h2 id="release"><a href="#release" class="headerlink" title="release()"></a>release()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">release</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">    Node h = head;</span><br><span class="line">    <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">      unparkSuccessor(h);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>释放同步资源状态。</li>
<li>如果同步队列不为空、且头结点的 <code>waitStatus</code> 不为 0（头结点的后继节点在等待被唤醒），则使用 <code>unparkSuccessor()</code> 唤醒头结点的后继节点。</li>
</ol>
<h2 id="unparkSuccessor"><a href="#unparkSuccessor" class="headerlink" title="unparkSuccessor()"></a>unparkSuccessor()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">unparkSuccessor</span><span class="params">(Node node)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> ws = node.waitStatus;</span><br><span class="line">  <span class="comment">// 把节点的 waitStatus 清 0。</span></span><br><span class="line">  <span class="keyword">if</span> (ws &lt; <span class="number">0</span>)</span><br><span class="line">    compareAndSetWaitStatus(node, ws, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  Node s = node.next;</span><br><span class="line">  <span class="comment">// 如果使用 node.next 查看，没有后继节点、或后继节点为 CANCELLED 状态，</span></span><br><span class="line">  <span class="comment">// 从尾节点反向遍历，找到距离 node 最近的没有被 CANCELLED 的节点。</span></span><br><span class="line">  <span class="keyword">if</span> (s == <span class="keyword">null</span> || s.waitStatus &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    s = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">for</span> (Node t = tail; t != <span class="keyword">null</span> &amp;&amp; t != node; t = t.prev)</span><br><span class="line">      <span class="keyword">if</span> (t.waitStatus &lt;= <span class="number">0</span>)</span><br><span class="line">        s = t;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 如果找到后面需要唤醒的节点，唤醒它。</span></span><br><span class="line">  <span class="keyword">if</span> (s != <span class="keyword">null</span>)</span><br><span class="line">    LockSupport.unpark(s.thread);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>把节点的 waitStatus 清 0。</li>
<li>如果使用 node.next 查看，没有后继节点、或后继节点为 CANCELLED 状态。从尾节点反向遍历，找到距离 node 最近的没有被 CANCELLED 的节点。</li>
<li>如果找到后面需要唤醒的节点，唤醒它。</li>
</ol>
<p><strong>之所以从尾部向前找</strong>，是因为节点入队并不是原子操作。在 CAS 入队后，只保证了节点是尾节点、指向前一个尾节点，但是可能这个时候可能还没有设置前一个尾节点的 <code>next</code> 值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> Node <span class="title">enq</span><span class="params">(<span class="keyword">final</span> Node node)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    Node t = tail;</span><br><span class="line">    <span class="keyword">if</span> (t == <span class="keyword">null</span>) &#123; <span class="comment">// Must initialize 添加一个空的头结点</span></span><br><span class="line">      <span class="keyword">if</span> (compareAndSetHead(<span class="keyword">new</span> Node()))</span><br><span class="line">        tail = head;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      node.prev = t;	<span class="comment">// 1							</span></span><br><span class="line">      <span class="keyword">if</span> (compareAndSetTail(t, node)) &#123;	<span class="comment">// 2</span></span><br><span class="line">        t.next = node;	<span class="comment">// 3</span></span><br><span class="line">        <span class="keyword">return</span> t;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="AQS-共享锁的获取源码分析"><a href="#AQS-共享锁的获取源码分析" class="headerlink" title="AQS 共享锁的获取源码分析"></a>AQS 共享锁的获取源码分析</h1><h2 id="引用-2"><a href="#引用-2" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://segmentfault.com/a/1190000016447307">逐行分析AQS源码(3)——共享锁的获取与释放</a></li>
</ul>
<h2 id="acquireShared"><a href="#acquireShared" class="headerlink" title="acquireShared()"></a>acquireShared()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquireShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (tryAcquireShared(arg) &lt; <span class="number">0</span>)</span><br><span class="line">        doAcquireShared(arg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>尝试获取同步资源，成功则返回。</li>
<li>失败调用 <code>doAcquireShared(arg);</code>。</li>
</ol>
<h2 id="doAcquireShared"><a href="#doAcquireShared" class="headerlink" title="doAcquireShared()"></a>doAcquireShared()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doAcquireShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> Node node = addWaiter(Node.SHARED);</span><br><span class="line">    <span class="comment">/*boolean failed = true;</span></span><br><span class="line"><span class="comment">    try &#123;</span></span><br><span class="line"><span class="comment">        boolean interrupted = false;</span></span><br><span class="line"><span class="comment">        for (;;) &#123;</span></span><br><span class="line"><span class="comment">            final Node p = node.predecessor();*/</span></span><br><span class="line">            <span class="keyword">if</span> (p == head) &#123;</span><br><span class="line">                <span class="keyword">int</span> r = tryAcquireShared(arg);</span><br><span class="line">                <span class="keyword">if</span> (r &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                    setHeadAndPropagate(node, r);</span><br><span class="line">                    p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                    <span class="keyword">if</span> (interrupted)</span><br><span class="line">                        selfInterrupt();</span><br><span class="line">                    failed = <span class="keyword">false</span>;</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">/*if (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</span></span><br><span class="line"><span class="comment">                parkAndCheckInterrupt())</span></span><br><span class="line"><span class="comment">                interrupted = true;</span></span><br><span class="line"><span class="comment">        &#125;</span></span><br><span class="line"><span class="comment">    &#125; finally &#123;</span></span><br><span class="line"><span class="comment">        if (failed)</span></span><br><span class="line"><span class="comment">            cancelAcquire(node);</span></span><br><span class="line"><span class="comment">    &#125;*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Node#isShard()</span></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">isShared</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> nextWaiter == SHARED;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>相同的部分注释掉了。</p>
<p>可以看到和独占锁的获取只有两处不同：</p>
<ol>
<li>新建的节点类型为 <code>SHARED</code>，用来之后判断同步队列中的节点是否是 共享的。</li>
<li>在获取同步资源成功后，使用 <code>setHeadAndPropagate()</code>，而不是 <code>setHead()</code>。<ul>
<li>除了更新自己为头结点，还要唤醒后继节点。</li>
</ul>
</li>
</ol>
<h2 id="setHeadAndPropagate"><a href="#setHeadAndPropagate" class="headerlink" title="setHeadAndPropagate()"></a>setHeadAndPropagate()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">setHeadAndPropagate</span><span class="params">(Node node, <span class="keyword">int</span> propagate)</span> </span>&#123;</span><br><span class="line">    Node h = head; <span class="comment">// Record old head for check below</span></span><br><span class="line">    setHead(node);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (propagate &gt; <span class="number">0</span> || h == <span class="keyword">null</span> || h.waitStatus &lt; <span class="number">0</span> ||</span><br><span class="line">        (h = head) == <span class="keyword">null</span> || h.waitStatus &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        Node s = node.next;</span><br><span class="line">        <span class="keyword">if</span> (s == <span class="keyword">null</span> || s.isShared())</span><br><span class="line">            doReleaseShared();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>调用 <code>setHead()</code> 更新自己为头结点。<ul>
<li>这里不用 CAS 设置，因为在获取共享锁的时候，也只有自己是头结点的后继节点才能进入这里，也就是说获取锁是“串行”调用 <code>setHead()</code> 的。</li>
</ul>
</li>
<li>如果 <code>tryAcquireShared()</code> 大于0，自己的 <code>waitStatus</code> 不是 0 和 CANCELLED，自己的后继节点存在且为共享模式，则调用 <code>doReleaseShared()</code> 唤醒后继节点。</li>
</ol>
<h1 id="AQS-共享锁的释放源码分析"><a href="#AQS-共享锁的释放源码分析" class="headerlink" title="AQS 共享锁的释放源码分析"></a>AQS 共享锁的释放源码分析</h1><h2 id="引用-3"><a href="#引用-3" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://segmentfault.com/a/1190000016447307">逐行分析AQS源码(3)——共享锁的获取与释放</a></li>
</ul>
<h2 id="releaseShared"><a href="#releaseShared" class="headerlink" title="releaseShared()"></a>releaseShared()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">releaseShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (tryReleaseShared(arg)) &#123;</span><br><span class="line">        doReleaseShared();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>释放同步资源。</li>
<li>调用 <code>doReleaseShared()</code> 来唤醒后继节点。</li>
</ol>
<h2 id="doReleaseShared"><a href="#doReleaseShared" class="headerlink" title="doReleaseShared()"></a>doReleaseShared()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doReleaseShared</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        Node h = head;</span><br><span class="line">        <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h != tail) &#123; <span class="comment">// 队列至少有两个节点</span></span><br><span class="line">            <span class="keyword">int</span> ws = h.waitStatus;</span><br><span class="line">            <span class="keyword">if</span> (ws == Node.SIGNAL) &#123;</span><br><span class="line">                <span class="keyword">if</span> (!compareAndSetWaitStatus(h, Node.SIGNAL, <span class="number">0</span>))</span><br><span class="line">                    <span class="keyword">continue</span>;            <span class="comment">// loop to recheck cases</span></span><br><span class="line">                unparkSuccessor(h);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (ws == <span class="number">0</span> &amp;&amp;</span><br><span class="line">                     !compareAndSetWaitStatus(h, <span class="number">0</span>, Node.PROPAGATE))</span><br><span class="line">                <span class="keyword">continue</span>;                <span class="comment">// loop on failed CAS</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (h == head)                   <span class="comment">// loop if head changed</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果队列至少有两个节点，并且头结点的 <code>waitStatus</code> 为 SIGNAL，则唤醒头结点的后继节点。</p>
<p>注意，因为可能有多个线程在执行这个函数，在唤醒前，要先 CAS 使头结点的状态清0成功，才可以唤醒头结点的后继节点。</p>
<h1 id="ReentrantLock-重入锁"><a href="#ReentrantLock-重入锁" class="headerlink" title="ReentrantLock 重入锁"></a>ReentrantLock 重入锁</h1><h2 id="状态"><a href="#状态" class="headerlink" title="状态"></a>状态</h2><p><code>state</code> 值表示锁被获取的次数。</p>
<h2 id="非公平锁获取"><a href="#非公平锁获取" class="headerlink" title="非公平锁获取"></a>非公平锁获取</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">nonfairTryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">  <span class="keyword">int</span> c = getState();</span><br><span class="line">  <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">      setExclusiveOwnerThread(current);</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">    <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">    <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>) <span class="comment">// overflow</span></span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">    setState(nextc);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>如果 <code>state</code> 为 0、且 CAS 把 <code>state</code> 设为 1 成功，则获取锁成功，把当前线程设为 <code>exclusiveOwnerThread</code>。</li>
<li>如果 <code>state</code> 不为0、但是 <code>exclusiveOwnerThread</code> 为当前线程，则获取锁成功，把 <code>state</code> 加 1。</li>
</ol>
<p>翻译一下：</p>
<ol>
<li>如果没有人持有锁、且当前线程获取锁成功，把当前线程设为 <code>exclusiveOwnerThread</code>。</li>
<li>如果是自己线程持有锁，<code>state</code> 加 1。</li>
</ol>
<h2 id="公平锁获取"><a href="#公平锁获取" class="headerlink" title="公平锁获取"></a>公平锁获取</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// FairSync</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">  <span class="keyword">int</span> c = getState();</span><br><span class="line">  <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp;</span><br><span class="line">        compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">      setExclusiveOwnerThread(current);</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">    <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">    <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>)</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">    setState(nextc);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>和非公平锁的获取相比，需要满足同步队列为空、或头节点是当前线程，才可以去 CAS 尝试获取锁。</p>
<p>这样每个线程都必须先入同步队列（除了队列为空时），才有可能获取锁。</p>
<h3 id="与非公平锁对比"><a href="#与非公平锁对比" class="headerlink" title="与非公平锁对比"></a>与非公平锁对比</h3><ul>
<li><p>对于非公平锁，假设线程 A 持有锁、线程 B 在同步队列中，在 A 释放锁时，线程 C 请求获取锁，可能会比同步队列的头结点 B 更早获取到锁。</p>
</li>
<li><p>对于公平锁，线程 C 无法在队列为空时获取到锁。</p>
</li>
</ul>
<h1 id="ReentrantReadWriteLock-读写锁"><a href="#ReentrantReadWriteLock-读写锁" class="headerlink" title="ReentrantReadWriteLock 读写锁"></a>ReentrantReadWriteLock 读写锁</h1><h2 id="状态-1"><a href="#状态-1" class="headerlink" title="状态"></a>状态</h2><p><code>state</code> 前 16 位表示获取读锁的次数。</p>
<p><code>state</code> 后 16 位表示获取写锁的次数。</p>
<h2 id="写锁的获取"><a href="#写锁的获取" class="headerlink" title="写锁的获取"></a>写锁的获取</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">    Thread current = Thread.currentThread();</span><br><span class="line">    <span class="keyword">int</span> c = getState();</span><br><span class="line">    <span class="keyword">int</span> w = exclusiveCount(c);</span><br><span class="line">    <span class="keyword">if</span> (c != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// (Note: if c != 0 and w == 0 then shared count != 0)</span></span><br><span class="line">        <span class="keyword">if</span> (w == <span class="number">0</span> || current != getExclusiveOwnerThread()) <span class="comment">// 有人持有读锁、或持有写锁线程不为自己</span></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        <span class="keyword">if</span> (w + exclusiveCount(acquires) &gt; MAX_COUNT)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">        <span class="comment">// Reentrant acquire</span></span><br><span class="line">        setState(c + acquires);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  	<span class="comment">// writerShouldBlock() </span></span><br><span class="line">  	<span class="comment">//		非公平锁 返回 false</span></span><br><span class="line">    <span class="comment">//		公平锁，返回 hasQueuedPredecessors()</span></span><br><span class="line">    <span class="keyword">if</span> (writerShouldBlock() ||</span><br><span class="line">        !compareAndSetState(c, c + acquires))</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    setExclusiveOwnerThread(current);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>如果有人持有读锁，返回 false。</li>
<li>如果有人持有写锁，<ol>
<li>不是自己线程持有，返回 false。</li>
<li>是自己持有，<code>state</code> 加 1，返回 true。</li>
</ol>
</li>
<li>否则，没有人持有锁（<code>state == 0</code>）<ol>
<li>如果是非公平锁，尝试 CAS 加锁。</li>
<li>如果是公平锁，同步队列为空、或头结点是自己线程才可以尝试 CAS 加锁。</li>
</ol>
</li>
</ol>
<h2 id="读锁的获取"><a href="#读锁的获取" class="headerlink" title="读锁的获取"></a>读锁的获取</h2><p>可以使用 <code>readHoldCount()</code> 来获取本线程持有读锁的次数，使用 <code>ThreadLocal</code> 来计数。</p>
<h2 id="锁降级"><a href="#锁降级" class="headerlink" title="锁降级"></a>锁降级</h2><p>持有写锁的线程，可以在释放写锁之前获取读锁。</p>
<p>这样使整个读写过程都是本线程持有锁的。</p>
<p>否则先释放写锁再获取读锁的话，在释放后可能其他线程获取了读锁，使得线程读到的值不是本线程写的值。</p>
<h1 id="LockSupport"><a href="#LockSupport" class="headerlink" title="LockSupport"></a>LockSupport</h1><ul>
<li><code>LockSupport.park()</code> 阻塞当前线程。<ul>
<li>可以被中断，中断后 <code>Thread.interrupted()</code> 返回 true。</li>
</ul>
</li>
<li><code>LockSupport.unpark(thread)</code> <ul>
<li>如果 thread 被 <code>park</code> 阻塞了，唤醒它。</li>
<li>如果 thread 没有被 <code>park</code>，下次 <code>park</code> 不会阻塞线程。</li>
</ul>
</li>
</ul>
<h1 id="Condition"><a href="#Condition" class="headerlink" title="Condition"></a>Condition</h1><h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConditionObject</span> <span class="keyword">implements</span> <span class="title">Condition</span>, <span class="title">java</span>.<span class="title">io</span>.<span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="comment">/** First node of condition queue. */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">transient</span> Node firstWaiter;</span><br><span class="line">    <span class="comment">/** Last node of condition queue. */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">transient</span> Node lastWaiter;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>每个 <code>Conditoin</code> 对象都存储了自己的等待队列的链表。</p>
<h2 id="await"><a href="#await" class="headerlink" title="await()"></a>await()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">await</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (Thread.interrupted())</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> InterruptedException();</span><br><span class="line">  Node node = addConditionWaiter(); 	<span class="comment">// 把当前线程加入到等待队列中。</span></span><br><span class="line">  <span class="keyword">int</span> savedState = fullyRelease(node);	<span class="comment">// 释放锁资源，唤醒后继节点。</span></span><br><span class="line">  <span class="keyword">int</span> interruptMode = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (!isOnSyncQueue(node)) &#123;</span><br><span class="line">    LockSupport.park(<span class="keyword">this</span>);</span><br><span class="line">    <span class="keyword">if</span> ((interruptMode = checkInterruptWhileWaiting(node)) != <span class="number">0</span>)</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (acquireQueued(node, savedState) &amp;&amp; interruptMode != THROW_IE)</span><br><span class="line">    interruptMode = REINTERRUPT;</span><br><span class="line">  <span class="keyword">if</span> (node.nextWaiter != <span class="keyword">null</span>) <span class="comment">// clean up if cancelled</span></span><br><span class="line">    unlinkCancelledWaiters();</span><br><span class="line">  <span class="keyword">if</span> (interruptMode != <span class="number">0</span>)</span><br><span class="line">    reportInterruptAfterWait(interruptMode);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>把当前线程加入到等待队列中。</li>
<li>释放锁资源，唤醒后继节点。</li>
<li>使用 <code>LockSupport.park()</code> 陷入阻塞状态。</li>
<li>在被唤醒后，调用 <code>acquireQueued</code> 方法，尝试获取锁资源。</li>
<li>被中断，会抛出 InterruptedException。</li>
</ol>
<h2 id="signal"><a href="#signal" class="headerlink" title="signal()"></a>signal()</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">signal</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!isHeldExclusively())</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalMonitorStateException();</span><br><span class="line">    Node first = firstWaiter;</span><br><span class="line">    <span class="keyword">if</span> (first != <span class="keyword">null</span>)</span><br><span class="line">        doSignal(first);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doSignal</span><span class="params">(Node first)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">do</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> ( (firstWaiter = first.nextWaiter) == <span class="keyword">null</span>)</span><br><span class="line">      lastWaiter = <span class="keyword">null</span>;</span><br><span class="line">    first.nextWaiter = <span class="keyword">null</span>;</span><br><span class="line">  &#125; <span class="keyword">while</span> (!transferForSignal(first) &amp;&amp; (first = firstWaiter) != <span class="keyword">null</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">transferForSignal</span><span class="params">(Node node)</span> </span>&#123;</span><br><span class="line">  <span class="comment">/*</span></span><br><span class="line"><span class="comment">   * If cannot change waitStatus, the node has been cancelled.</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="keyword">if</span> (!compareAndSetWaitStatus(node, Node.CONDITION, <span class="number">0</span>))</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">/*</span></span><br><span class="line"><span class="comment">   * Splice onto queue and try to set waitStatus of predecessor to</span></span><br><span class="line"><span class="comment">   * indicate that thread is (probably) waiting. If cancelled or</span></span><br><span class="line"><span class="comment">   * attempt to set waitStatus fails, wake up to resync (in which</span></span><br><span class="line"><span class="comment">   * case the waitStatus can be transiently and harmlessly wrong).</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  Node p = enq(node);</span><br><span class="line">  <span class="keyword">int</span> ws = p.waitStatus;</span><br><span class="line">  <span class="keyword">if</span> (ws &gt; <span class="number">0</span> || !compareAndSetWaitStatus(p, ws, Node.SIGNAL))</span><br><span class="line">    LockSupport.unpark(node.thread);</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol>
<li>当前线程必须持有锁。</li>
<li>把等待队列的头节点移除、加入到同步队列中。</li>
<li>使用 <code>LockSupport.unpark()</code> 唤醒节点中的线程。</li>
</ol>
<h2 id="signalAll"><a href="#signalAll" class="headerlink" title="signalAll()"></a>signalAll()</h2><ol>
<li>会把等待队列的所有节点移除、加入到同步队列中。</li>
<li>唤醒所有节点中的线程。</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>4. Java 并发编程基础</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/4Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>Java 的线程对应操作系统级别的线程。</p>
<p>执行 <code>main()</code> 方法的是主线程。</p>
<h1 id="线程的状态"><a href="#线程的状态" class="headerlink" title="线程的状态"></a>线程的状态</h1><p>Java 线程的 6 个状态：</p>
<p><img src="/images/Java线程的状态.png" alt="Java线程的状态"></p>
<p>线程状态的迁移：</p>
<p><img src="/images/Java线程状态的迁移.png" alt="Java线程状态的迁移"></p>
<h1 id="Daemon-线程"><a href="#Daemon-线程" class="headerlink" title="Daemon 线程"></a>Daemon 线程</h1><p>当 JVM 中不存在非 Daemon 线程时，JVM 将会推出。</p>
<p>使用 <code>thread.setDaemon(true)</code> 来设置，只能在线程启动前设置。</p>
<h1 id="线程初始化"><a href="#线程初始化" class="headerlink" title="线程初始化"></a>线程初始化</h1><p>线程 A 的父线程（创建线程 A 的线程）来初始化线程 A。</p>
<ul>
<li>子线程继承了 父线程的 <code>daemon</code>、优先级、contextClassLoader、InheritiableThreadLocal</li>
<li>分配 <code>threadID</code> (从 0 开始递增)。</li>
</ul>
<h1 id="启动线程"><a href="#启动线程" class="headerlink" title="启动线程"></a>启动线程</h1><p>调用 <code>thread.start()</code> 方法来启动线程，它会调用 <code>thread.run()</code> 方法。</p>
<p><code>Thread</code> 类 默认的 <code>start</code> 方法是调用 <code>Runnable</code> 类型的 <code>target</code> 的 <code>target.run()</code> 方法。</p>
<p>因此，可以在构造线程时，传入一个 <code>Runnable</code> 对象。</p>
<h1 id="线程的中断"><a href="#线程的中断" class="headerlink" title="线程的中断"></a>线程的中断</h1><h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><p>注意，这里的中断不是操作系统的中断，而是线程的一个标志位。</p>
<p>常用方法：</p>
<ul>
<li><code>threadA.interrupt()</code> 其他线程调用来中断 A 线程。</li>
<li><code>threadA.isInterrupted()</code> 返回 A 线程是否被中断了。如果线程状态是 <code>TERMINATED</code>，即使被中断过，也会返回 false。</li>
<li><code>Thread.interrupted()</code> 静态方法，返回当前线程是否被中断了，并且清除标志位。</li>
</ul>
<p>许多方法声明了 <code>throws InterruptedException</code>，在执行这些方法时，如果线程被中断，则会抛出 <code>InterruptedException</code>、清除标志位。</p>
<h2 id="终止线程"><a href="#终止线程" class="headerlink" title="终止线程"></a>终止线程</h2><h3 id="suspend-、resume-、stop"><a href="#suspend-、resume-、stop" class="headerlink" title="suspend()、resume()、stop()"></a>suspend()、resume()、stop()</h3><p><code>suspend()</code>、<code>resume()</code>、<code>stop()</code> 方法可以暂停、回复、终止线程。</p>
<p>但是他们不给线程资源释放的时间、没办法释放锁，只要调用了，就会进行操作，所以现在是<strong>过期</strong>的方法了。</p>
<h3 id="可以使用中断来安全的终止线程"><a href="#可以使用中断来安全的终止线程" class="headerlink" title="可以使用中断来安全的终止线程"></a>可以使用中断来安全的终止线程</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyRunnable</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">  <span class="meta">@override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (!Thread.interrupted()) &#123;</span><br><span class="line">      <span class="comment">// do somthing ...</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 释放资源</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Thread thread = <span class="keyword">new</span> Thread(<span class="keyword">new</span> MyRunnable());</span><br><span class="line">thread.start();</span><br><span class="line">thread.interrupt(); <span class="comment">// 终止线程</span></span><br></pre></td></tr></table></figure>
<h1 id="等待-通知机制"><a href="#等待-通知机制" class="headerlink" title="等待/通知机制"></a>等待/通知机制</h1><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p><code>Object</code> 类上定义了 <code>wait()</code> 和 <code>notify()</code> 方法。</p>
<ul>
<li>持有锁的线程，调用 <code>wait()</code> ，会释放锁，阻塞在 <code>wait()</code> 方法上。这个线程被移动到这个 <code>Object</code> 的监视器的等待队列上。</li>
<li>持有锁的线程 A，调用 <code>notify()</code>，<ul>
<li>会把一个线程 B 从监视器的等待队列移到同步队列，从而 B 开始竞争锁。</li>
<li>在线程 A 释放锁后（注意，不是调用了 <code>notify()</code> 就释放锁），B 获得锁后，从 <code>wait()</code> 方法返回。</li>
</ul>
</li>
<li><code>notifyAll()</code>，把监视器的等待队列上的所有线程都移动到同步队列上。</li>
</ul>
<h2 id="经典范式"><a href="#经典范式" class="headerlink" title="经典范式"></a>经典范式</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 等待方（消费者）</span></span><br><span class="line"><span class="keyword">synchronized</span>(obj) &#123;</span><br><span class="line">  <span class="keyword">while</span> (条件不满足) &#123;</span><br><span class="line">    obj.wait();</span><br><span class="line">  &#125;</span><br><span class="line">  条件满足了，进行逻辑处理</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 通知方（生产者）</span></span><br><span class="line"><span class="keyword">synchronized</span>(obj) &#123;</span><br><span class="line">  进行逻辑处理，改变条件</span><br><span class="line">  obj.notifyAll();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="thread-join-方法"><a href="#thread-join-方法" class="headerlink" title="thread.join() 方法"></a>thread.join() 方法</h1><h3 id="语法-1"><a href="#语法-1" class="headerlink" title="语法"></a>语法</h3><p>线程 A 执行了 <code>threadB.join()</code> 方法，则线程 A 需要等待线程 B 终止之后才能从 <code>threadB.join()</code> 方法返回。</p>
<h3 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h3><ul>
<li>调用 <code>threadB.join()</code> 方法，其实是线程 A 调用了 <code>threadB.wait(0)</code> 方法。注意需要先 <code>synchronized</code> 得到 <code>threadB</code> 的监视器锁。</li>
<li>在线程 B 终止时，会调用 <code>threadB.notifyAll()</code> 方法。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">join</span><span class="params">(<span class="keyword">long</span> millis)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (isAlive()) &#123;</span><br><span class="line">    wait(<span class="number">0</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="ThreadLocal"><a href="#ThreadLocal" class="headerlink" title="ThreadLocal"></a>ThreadLocal</h1><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://juejin.im/post/5a5efb1b518825732b19dca4">ThreadLocal源码深度剖析</a></li>
<li><a href="https://juejin.im/post/59db31c16fb9a00a4843dc36">源码|ThreadLocal的实现原理</a></li>
<li><a href="https://www.cnblogs.com/hf-cherish/p/4970267.html"><a href="https://www.cnblogs.com/hf-cherish/p/4970267.html">java 静态变量生命周期（类生命周期）</a></a></li>
</ul>
<h2 id="语法-2"><a href="#语法-2" class="headerlink" title="语法"></a>语法</h2><p><code>ThreadLocal</code> 类提供了线程内部的局部变量。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ThreadLocal&lt;String&gt; strLocal = ThreadLocal.withInitial(() -&gt; <span class="keyword">new</span> String(<span class="string">"test"</span>));</span><br><span class="line">strLocal.set(<span class="keyword">new</span> String(<span class="string">"hello"</span>));</span><br><span class="line">System.out.println(strLocal.get());</span><br></pre></td></tr></table></figure>
<h2 id="原理-2"><a href="#原理-2" class="headerlink" title="原理"></a>原理</h2><p><img src="/images/threadlocal.png" alt="threadlocal"></p>
<h3 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h3><p>ThreadLocal 的数据结构如上图所示。</p>
<ul>
<li><p>每个 Thread 内部存储了一个 <code>ThreadLocalMap</code>，是一个内部实现的哈希表。</p>
</li>
<li><p><code>ThreadLocalMap</code> 内部存储了一个 <code>Entry</code> 数组，初始大小是 16。</p>
</li>
<li><code>Entry</code> 是 <code>WeakReference&lt;ThreadLocal&lt;?&gt;&gt;</code> 的子类，存储了 key （这个 <code>ThreadLocal</code> 对象）的弱引用和 value （要存储的值）。</li>
</ul>
<h3 id="弱引用的-key"><a href="#弱引用的-key" class="headerlink" title="弱引用的 key"></a>弱引用的 key</h3><p>通过弱引用，实现了<strong>线程封闭</strong>的线程局部变量。</p>
<ul>
<li>当创建 ThreadLocal 变量的对象 GC 后，如果不使用弱引用，因为 entry 还引用着这个对象作为 key，没有办法 GC 这个 ThreadLocal 对象。</li>
<li>而使用弱引用，如果只有 entry 引用了 key，则key会被回收，调用 key.get() 会得到 null。</li>
</ul>
<p>但是注意，只是 entry 的 key 被 GC 了，value 还没有GC。因此，<code>ThreadLocal</code> 的 <code>get()</code>、<code>remove()</code>、<code>set()</code> 方法中会检查一部分的 entry，看 key 是否为null，如果是的话把 value 设为 null，使得 value 可以被 GC。</p>
<h3 id="hash-函数"><a href="#hash-函数" class="headerlink" title="hash 函数"></a>hash 函数</h3><p>在创建一个 <code>ThreadLocal</code> 对象时，会赋予其一个新的 <code>threadLocalHashCode</code>，是从一个 <code>AtomicInteger</code> 增加固定值来的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> threadLocalHashCode = nextHashCode();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> AtomicInteger nextHashCode = <span class="keyword">new</span> AtomicInteger();</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> HASH_INCREMENT = <span class="number">0x61c88647</span>;</span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">nextHashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> nextHashCode.getAndAdd(HASH_INCREMENT);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="hash-算法"><a href="#hash-算法" class="headerlink" title="hash 算法"></a>hash 算法</h3><p>Entry 数组初始大小是 16，当遇到碰撞时，使用寻址法，找到下一个可用的空位置。</p>
<p>当负载达到 <code>threshold</code>（2/3 * 数组的长度）后，进行 rehash。</p>
<ul>
<li>首先遍历所有 Entry，清除 key 为 null 的元素。</li>
<li>如果 <code>size &gt;= threshold - threshold / 4</code>，把数组长度加倍，重新放元素。</li>
</ul>
<h2 id="内存泄漏"><a href="#内存泄漏" class="headerlink" title="内存泄漏"></a>内存泄漏</h2><ol>
<li><p>如果一个线程的 ThreadLocal 变量定义后，再也没有用过，则没有机会调用 <code>get</code>、<code>set</code> 方法，<code>value</code> 永远也没有办法被回收。</p>
</li>
<li><p>此外，如果是使用 <code>static</code> 声明的 ThreadLocal 对象，一般生命周期为整个应用程序，这个对 key 的引用一直存在，则没有办法回收 key。</p>
</li>
</ol>
<blockquote>
<p><code>static</code> 的生命周期：</p>
<ul>
<li>加载：JVM 在加载类的过程中为静态变量分配内存。</li>
<li>销毁：类被卸载时，静态变量被销毁，并释放内存空间。</li>
</ul>
<p>类的生命周期：</p>
<ul>
<li>该类所有的实例都已经被回收，也就是 Java 堆中不存在该类的任何实例。</li>
<li>加载该类的 ClassLoader 已经被回收。</li>
<li>该类对应的 java.lang.Class 对象没有任何地方被引用，无法在任何地方通过反射访问该类的方法。</li>
</ul>
</blockquote>
<p>所以，在不再使用 ThreadLocal 对象时，主动调用 <code>remove()</code> 方法来删除它。</p>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>3. Java 内存模型</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/3Java%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://zhuanlan.zhihu.com/p/29881777">Java内存模型（JMM）总结</a></li>
</ul>
<h1 id="Java-内存模型"><a href="#Java-内存模型" class="headerlink" title="Java 内存模型"></a>Java 内存模型</h1><h2 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h2><p>Java 内存模型（Java Memory Model（JMM））是虚拟机规范，用于屏蔽各种硬件和操作系统的差异，以使 Java 程序在各个平台达到一致的并发效果。</p>
<h2 id="栈与堆"><a href="#栈与堆" class="headerlink" title="栈与堆"></a>栈与堆</h2><p>线程栈中的变量不能多线程访问。</p>
<p>堆中的变量可以多线程访问。</p>
<h2 id="happens-before-规则"><a href="#happens-before-规则" class="headerlink" title="happens-before 规则"></a>happens-before 规则</h2><p>JSR-133 内存模型，使用 happens-before 规则来阐述操作之间的可见性。</p>
<h3 id="定义-1"><a href="#定义-1" class="headerlink" title="定义"></a>定义</h3><ol>
<li>如果操作 A happens-before 操作 B，那么操作 A 的执行结果对操作 B 可见、且操作 A 的执行顺序排在操作 B 之前。</li>
<li><p>两个操作之间存在 happens-before关系，并不意味着Java平台的具体实现必须要按照 happens-before关系指定的顺序来执行。<br>如果重排序之后的执行结果，与按 happens-before 关系来执行的结果一致，这种就重排序并不非法。JMM允许这种重排序。<br>例如下面程序，A happens before B，A 操作的结果不需要对 B 操作可见，并且重排 A 和 B 的顺序，执行结果不变。JMM 认为这种重排序是合法的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> pi = <span class="number">3.14</span>; 				<span class="comment">// A</span></span><br><span class="line"><span class="keyword">double</span> r = <span class="number">1.0</span>;      			<span class="comment">// B</span></span><br><span class="line"><span class="keyword">double</span> area = pi * r * r;	<span class="comment">// C</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<h3 id="as-if-serial-语义"><a href="#as-if-serial-语义" class="headerlink" title="as-if-serial 语义"></a>as-if-serial 语义</h3><p>不管怎么重排序，<strong>单线程</strong>程序的<strong>执行结果</strong>不能被改变。</p>
<p>happens-before 本质上和 as-if-serial 语义一样。</p>
<h3 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h3><p>具体规则如下：</p>
<ul>
<li>程序顺序规则：一个线程中的每个操作，happens-before 于该线程后续操作。</li>
<li>监视器锁规则：对一个锁的解锁，happens-before 于随后对这个锁的解锁。</li>
<li><code>volatile</code> 变量规则：对一个 <code>volatile</code> 域的写，happens-before 于后续对这个 <code>volatile</code> 域的读。</li>
<li>传递性：如果 A happens-before B、B happens-before C，则 A happens-before C。</li>
<li><code>start()</code> 规则：如果线程 A 执行操作 <code>ThreadB.start()</code> 来启动线程 B，那么 A 线程的 <code>ThreadB.start()</code> 操作 happens-before 于线程 B 中的任意操作。</li>
<li><code>join()</code> 规则：如果线程 A 执行操作 <code>ThreadB.join()</code>并成功返回，那么线程B中的任意操作 happens-before 于线程 A 从 <code>ThreadB.join()</code> 成功返回的操作。</li>
</ul>
<h2 id="JMM-的内存可见性保证"><a href="#JMM-的内存可见性保证" class="headerlink" title="JMM 的内存可见性保证"></a>JMM 的内存可见性保证</h2><ol>
<li>单线程程序。单线程程序不会出现内存可见性问题。保证单线程程序与在顺序一致性模型中的执行结果相同。</li>
<li>正确同步的多线程程序。保证正确同步的多线程程序与在顺序一致性模型中的执行结果相同。</li>
<li>未同步/未正确同步的多线程程序。JMM为它们提供了最小安全性保障：线程执行时读取到的值，要么是之前某个线程写入的值，要么是默认值（0、null、false）。</li>
</ol>
<h1 id="JMM-需要解决的问题"><a href="#JMM-需要解决的问题" class="headerlink" title="JMM 需要解决的问题"></a>JMM 需要解决的问题</h1><p>由于编译器、CPU 对指令进行了重排序，以及 CPU 缓存的存在，使得实现 JMM 的规范，需要采取一定的措施。</p>
<h2 id="重排序"><a href="#重排序" class="headerlink" title="重排序"></a>重排序</h2><ol>
<li>编译器优化的重排序。编译器不改变单线程程序的语义前提下，重排语句顺序。</li>
<li>指令级并行的重排序。CPU 的流水线技术。如果不存在数据依赖性，CPU 可以改变机器指令的执行顺序。</li>
</ol>
<h3 id="CPU-重排序类型"><a href="#CPU-重排序类型" class="headerlink" title="CPU 重排序类型"></a>CPU 重排序类型</h3><p>不同 CPU 允许的重排序类型如下。</p>
<p><img src="/images/CPU重排序规则.png" alt="CPU重排序规则"></p>
<p>注意前 4 个规则中，操作之间没有数据依赖。编译器、CPU 不会改变<strong>单线程</strong>中存在<strong>数据依赖</strong>关系的两个操作的执行顺序。</p>
<h2 id="CPU-缓存"><a href="#CPU-缓存" class="headerlink" title="CPU 缓存"></a>CPU 缓存</h2><p>由于 CPU 的多级缓存的存在，多个线程对同一个变量的读取值可能是不同的。</p>
<p>由于 CPU 缓存的存在，在 32 位机器上，<code>long</code> 和 <code>double</code> 的读取和写入不是原子的。</p>
<p>后文的线程<strong>本地内存</strong>指 CPU 的缓存等技术。</p>
<h1 id="解决办法"><a href="#解决办法" class="headerlink" title="解决办法"></a>解决办法</h1><ul>
<li>对于编译器的重排序，Java 编译器<strong>重排序规则</strong>，来禁止特定类型的重排序。</li>
<li>对于处理器的重排序、缓存不一致，编译器插入<strong>内存屏障</strong>指令，来禁止特定类型的 CPU 的重排序、同时刷新/无效化缓存。</li>
</ul>
<h2 id="内存屏障类型"><a href="#内存屏障类型" class="headerlink" title="内存屏障类型"></a>内存屏障类型</h2><p><img src="/images/%E5%86%85%E5%AD%98%E5%B1%8F%E9%9A%9C%E7%B1%BB%E5%9E%8B.png?lastModify=1582630839" alt="内存屏障类型"></p>
<p>StoreLoad Barriers 同时具备其他三个屏障的效果，因此也称之为<strong>全能屏障</strong>，是目前大多数处理器所支持的；但是相对其他屏障，该屏障的开销相对昂贵。</p>
<h1 id="volatile-的内存语义"><a href="#volatile-的内存语义" class="headerlink" title="volatile 的内存语义"></a>volatile 的内存语义</h1><h2 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h2><ul>
<li>可见性。对 <code>volatile</code> 变量的读，总是能看到（任意线程）对这个 <code>volatile</code> 变量最后的写入。</li>
<li>原子性：对任意单个 <code>volatile</code> 变量的读/写具有原子性，但类似于 <code>volatile++</code> 这种复合操作不具有原子性。</li>
</ul>
<h2 id="内存语义"><a href="#内存语义" class="headerlink" title="内存语义"></a>内存语义</h2><ul>
<li>写一个 <code>volatile</code> 变量时，JMM 把该线程对应的本地内存中的共享变量值刷新到主内存。</li>
<li>读一个 <code>volatile</code> 变量时，JMM会把该线程对应的本地内存置为无效。</li>
</ul>
<h2 id="内存语义的实现"><a href="#内存语义的实现" class="headerlink" title="内存语义的实现"></a>内存语义的实现</h2><h3 id="虚拟机重排序限制"><a href="#虚拟机重排序限制" class="headerlink" title="虚拟机重排序限制"></a>虚拟机重排序限制</h3><p>JMM 限制如下的指令重排序：</p>
<p><img src="/images/volatile编译器重排序规则.png" alt="volatile编译器重排序规则"></p>
<ul>
<li><code>volatile</code> 写操作，不能与它前面的指令重排序。</li>
<li><code>volatile</code> 读操作，不能与它后面的指令重排序。</li>
<li>当第一个操作是 <code>volatile</code> 写，第二个操作是 <code>volatile</code> 读时，不能重排序。</li>
</ul>
<h3 id="CPU-内存屏障"><a href="#CPU-内存屏障" class="headerlink" title="CPU 内存屏障"></a>CPU 内存屏障</h3><h4 id="保守的实现方式"><a href="#保守的实现方式" class="headerlink" title="保守的实现方式"></a>保守的实现方式</h4><p>编译器在生成指令序列时，插入相应的内存屏障。</p>
<ul>
<li>在每个 <code>volatile</code> 写操作的前面插入一个 <code>StoreStore</code> 屏障。<br>禁止前面普通写和 <code>volatile</code> 写重排序。</li>
<li>在每个 <code>volatile</code> 写操作的后面插入一个 <code>StoreLoad</code> 屏障。<br>禁止这个<code>volatile</code> 写与后面可能存在的 <code>volatile</code> 读重排序。（因为读线程数目一般多于写线程数目，所以不是在<code>volatile</code> 读前面加入 <code>StoreLoad</code> 屏障）。</li>
<li>在每个 <code>volatile</code> 读操作的后面插入一个 <code>LoadLoad</code> 屏障。<br>禁止后面的普通读操作与 <code>volatile</code> 读重排序。</li>
<li>在每个 <code>volatile</code> 读操作的后面插入一个 <code>LoadStore</code> 屏障。<br>禁止后面的普通写与 <code>volatile</code> 读重排序。</li>
</ul>
<h4 id="实际实现方式"><a href="#实际实现方式" class="headerlink" title="实际实现方式"></a>实际实现方式</h4><p>以上保守实现方式，在不同的 CPU 上实现不同，因为 CPU 本身机会禁止一些重排序。</p>
<p>例如，X86 只会对写-读操作做重排序，JMM 只需在 <code>volatile</code> 写后面插入 <code>StoreLoad</code>。因此，X86 上，<code>volatile</code> 写开销比 <code>volatile</code> 读开销大很多。</p>
<h1 id="锁的内存语义"><a href="#锁的内存语义" class="headerlink" title="锁的内存语义"></a>锁的内存语义</h1><h2 id="内存语义-1"><a href="#内存语义-1" class="headerlink" title="内存语义"></a>内存语义</h2><ul>
<li><p>当线程获取锁时，JMM 把该线程对应的本地内存置为无效。</p>
</li>
<li><p>当线程释放锁时，JMM 把该线程的本地内存的共享变量刷新到主内存中。</p>
</li>
</ul>
<h2 id="内存语义的实现-1"><a href="#内存语义的实现-1" class="headerlink" title="内存语义的实现"></a>内存语义的实现</h2><h3 id="监视器锁"><a href="#监视器锁" class="headerlink" title="监视器锁"></a>监视器锁</h3><p>在 <code>monitorenter</code>和<code>monitorexit</code> 两个指令的前后插入内存屏障。</p>
<h3 id="Lock-类"><a href="#Lock-类" class="headerlink" title="Lock 类"></a>Lock 类</h3><p><code>AQS</code> 内部使用 <code>volatile</code> 的 <code>state</code> 变量来表示锁的状态，在获取锁和释放锁的时候，要进行 <code>volatile</code> 的读取、写入、CAS。</p>
<p>其中 CAS 会在指令上加 <code>lock</code> 前缀，相当于同时具有volatile读和volatile写的内存语义。</p>
<p>intel的手册对lock前缀的说明如下。</p>
<ol>
<li>确保对内存的读改写操作原子执行。使用总线锁或缓存锁。</li>
<li>禁止该指令，与之前和之后的读和写指令重排序。</li>
<li>把写缓冲区中的所有数据刷新到内存中。</li>
</ol>
<h2 id="线程间通信的方法"><a href="#线程间通信的方法" class="headerlink" title="线程间通信的方法"></a>线程间通信的方法</h2><ol>
<li>A 线程写 <code>volatile</code> 变量，随后 B 线程读这个 <code>volatile</code> 变量。</li>
<li>A 线程写 <code>volatile</code> 变量，随后 B 线程用 CAS 更新这个 <code>volatile</code> 变量。</li>
<li>A 线程用 CAS 更新一个 <code>volatile</code> 变量，随后B线程用 CAS 更新这个 <code>volatile</code> 变量。</li>
<li>A 线程用 CAS 更新一个 <code>volatile</code> 变量，随后B线程读这个 <code>volatile</code> 变量。</li>
</ol>
<h1 id="final-域的内存语义"><a href="#final-域的内存语义" class="headerlink" title="final 域的内存语义"></a>final 域的内存语义</h1><h2 id="final-域的重排序规则"><a href="#final-域的重排序规则" class="headerlink" title="final 域的重排序规则"></a>final 域的重排序规则</h2><ol>
<li><p>在构造函数内对一个 <code>final</code> 域的写入，与随后把这个被构造的对象的引用 赋值给一个引用变量，这两个操作之间不能重排序。</p>
</li>
<li><p>初次读一个包含 <code>final</code> 域的对象的引用，与随后初次读这个对象的 <code>final</code> 域，这两个操作之间不能重排序。</p>
</li>
</ol>
<h2 id="写-final-域的重排序规则"><a href="#写-final-域的重排序规则" class="headerlink" title="写 final 域的重排序规则"></a>写 final 域的重排序规则</h2><ol>
<li>对于编译器。JMM 禁止编译器把 <code>final</code> 域的写重排序到构造函数之外。</li>
<li>对于 CPU。编译器会在 <code>final</code> 域的写之后、构造函数 return 之前，插入一个 StoreStore 屏障。这个屏障禁止处理器把 <code>final</code> 域的写重排序到构造函数之外。</li>
</ol>
<h2 id="读-final-域的重排序规则"><a href="#读-final-域的重排序规则" class="headerlink" title="读 final 域的重排序规则"></a>读 final 域的重排序规则</h2><p>对于CPU。编译器会在读 <code>final</code> 域操作的前面插入一个 LoadLoad 屏障。来实现禁止 初次读对象引用与初次读该对象的的 <code>final</code> 域。</p>
<p>这两个操作之间存在间接依赖关系。</p>
<ul>
<li>编译器遵守间接依赖关系，因此编译器不会重排序这两个操作。</li>
<li>大多数处理器也会遵守间接依赖，只有少数 CPU 需要加 LoadLoad 屏障。</li>
</ul>
<h2 id="final-域为引用类型"><a href="#final-域为引用类型" class="headerlink" title="final 域为引用类型"></a>final 域为引用类型</h2><p>对与编译器、CPU。在构造函数内对一个 <code>final</code> 引用对象的成员域的写入，与随后在构造函数外把这个被构造对象的引用赋值给一个引用变量，这两个操作之间不能重排序。</p>
<p>例如，A 与 C 不能重排序，B 与 C也不能重排序。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class Test &#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span>[] nums;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    nums = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>];  <span class="comment">// A</span></span><br><span class="line">    nums[<span class="number">0</span>] = <span class="number">1</span>;				<span class="comment">// B</span></span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">writeRef</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    nums[<span class="number">1</span>] = <span class="number">2</span>;				<span class="comment">// C</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="构造函数的溢出"><a href="#构造函数的溢出" class="headerlink" title="构造函数的溢出"></a>构造函数的溢出</h2><p>即使有 <code>final</code> 域的内存语义，也不能保证构造函数是安全初始化的，必须保证在构造函数内部，不能让这个被构造对象的引用为其他线程所见。</p>
<p>例如，由于 A 和 B 可以重排，D 处可能在 <code>num = 1</code> 之前就进行了读取。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class Test &#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> Test instance;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> num;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="title">Test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    num = <span class="number">1</span>;  							<span class="comment">// A</span></span><br><span class="line">    instance = <span class="keyword">this</span>;				<span class="comment">// B</span></span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title">write</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">new</span> Test()							<span class="comment">// C</span></span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="title">read</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (instance != <span class="keyword">null</span>) &#123;</span><br><span class="line">      System.out.println(instance.num);	<span class="comment">// D</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="单例模式延迟初始化与双重检查"><a href="#单例模式延迟初始化与双重检查" class="headerlink" title="单例模式延迟初始化与双重检查"></a>单例模式延迟初始化与双重检查</h1><h2 id="双重检查"><a href="#双重检查" class="headerlink" title="双重检查"></a>双重检查</h2><p>为了使单例模式延迟初始化线程安全，并且避免每次都检查锁，而使用了双重检查。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DoubelCheckedSingleton</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> DoubelCheckedSingleton instance;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> DoubelCheckedSingleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="keyword">synchronized</span>(DoubleCheckedSingleton.class) &#123;</span><br><span class="line">        instance = <span class="keyword">new</span> DoubleCheckedSingleton(); 			<span class="comment">// A</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> instance;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="双重检查的-bug"><a href="#双重检查的-bug" class="headerlink" title="双重检查的 bug"></a>双重检查的 bug</h2><p>代码 A 处相当于</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">memory = allocate();  	<span class="comment">// 1. 分配对象的内存空间</span></span><br><span class="line">initInstance(memory);		<span class="comment">// 2. 初始化对象</span></span><br><span class="line">instance = memory;			<span class="comment">// 3. 把 instance 指向 memory 的内存地址</span></span><br></pre></td></tr></table></figure>
<p>2 和 3 可能发生重排，这样线程可能看到一个还没有初始化的对象。</p>
<p>解决方法：</p>
<ol>
<li>不允许 2 和 3 重排序。</li>
<li>允许 2 和 3 重排序，但不允许其他线程“看到”这个重排序。</li>
</ol>
<h3 id="基于-volatile-解决方案"><a href="#基于-volatile-解决方案" class="headerlink" title="基于 volatile 解决方案"></a>基于 volatile 解决方案</h3><p>为了使 2 和 3 不能重排序。把 <code>instance</code> 设为 <code>volatile</code> 即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DoubelCheckedSingleton</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> DoubelCheckedSingleton instance;</span><br><span class="line">  </span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> DoubelCheckedSingleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (instance == <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="keyword">synchronized</span>(DoubleCheckedSingleton.class) &#123;</span><br><span class="line">        instance = <span class="keyword">new</span> DoubleCheckedSingleton(); 			<span class="comment">// A</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> instance;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="基于类初始化的解决方案"><a href="#基于类初始化的解决方案" class="headerlink" title="基于类初始化的解决方案"></a>基于类初始化的解决方案</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">InstanceHolder</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Instance instance = <span class="keyword">new</span> Singleton(); 	</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> InstanceHolder.instance;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在使用一个类的静态域时，这个类会被初始化，而初始化的时候会加锁。所以使得只有一个线程可以进行初始化，且其他线程无法看到整个过程。</p>
<h1 id="编译器和-CPU-对代码的优化处理"><a href="#编译器和-CPU-对代码的优化处理" class="headerlink" title="编译器和 CPU 对代码的优化处理"></a>编译器和 CPU 对代码的优化处理</h1><h2 id="条件分支的猜测执行"><a href="#条件分支的猜测执行" class="headerlink" title="条件分支的猜测执行"></a>条件分支的猜测执行</h2><p>控制依赖，会影响指令序列的并行度。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (flag) &#123;</span><br><span class="line">  <span class="keyword">int</span> y = x * x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 可能会被优化为</span></span><br><span class="line"><span class="keyword">int</span> tmp = x * x;</span><br><span class="line"><span class="keyword">if</span> (flag) &#123;</span><br><span class="line">  <span class="keyword">int</span> y = tmp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>2. Java 并发机制的底层实现原理</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/2Java%E5%B9%B6%E5%8F%91%E6%9C%BA%E5%88%B6%E7%9A%84%E5%BA%95%E5%B1%82%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<h1 id="锁总线与锁缓存"><a href="#锁总线与锁缓存" class="headerlink" title="锁总线与锁缓存"></a>锁总线与锁缓存</h1><p>原子操作和 <code>volatile</code> 基于锁总线或锁缓存实现。</p>
<h2 id="锁总线"><a href="#锁总线" class="headerlink" title="锁总线"></a>锁总线</h2><p>使用 CPU 提供的 LOCK# 信号，当一个 CPU 在总线上处理此信号时，其他 CPU 的操作请求将被阻塞住。因此这个处理器可以独占共享内存。</p>
<h2 id="锁缓存"><a href="#锁缓存" class="headerlink" title="锁缓存"></a>锁缓存</h2><p>如果内存区域被缓存在 CPU 的缓存行中、并在 Lock 操作期间被锁定，那么当它执行锁操作写回到内存时，CPU 不在总线上声言 LOCK# 信号，而是使用缓存一致性机制来保证操作的原子性。</p>
<p><strong>缓存一致性机制</strong>，当一个CPU 对缓存中的数据进行操作后，通知其他 CPU 放弃储存在它们内部的缓存，或者从主内存中重新读取。使用 <a href="https://blog.csdn.net/qq_35642036/article/details/82801708">MESI （修改、独占、共享、无效）协议</a>实现。</p>
<p>如果操作的数据不能被缓存在 CPU 内、或操作的数据跨多个缓存行，则<strong>不会使用锁缓存</strong>，而是锁总线。</p>
<h1 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h1><h2 id="语义"><a href="#语义" class="headerlink" title="语义"></a>语义</h2><p>一个字段被声明为 <code>volatile</code>，Java 线程内存模型确保所有线程看到这个变亮的值是一致的。</p>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><ol>
<li>汇编中添加 lock 指令。使用锁缓存机制、或使用锁总线机制，引起 CPU 缓存写回到内存。</li>
<li>一个 CPU 缓存回内存，根据 MESI 协议，使其他处理器的缓存无效。</li>
</ol>
<h2 id="使用技巧"><a href="#使用技巧" class="headerlink" title="使用技巧"></a>使用技巧</h2><p>可以把使用 <code>volatile</code> 的变量追加到 64 字节。否则其他数据和它共用一个缓存行，在它写数据时，会锁缓存行，导致其他 CPU 的其他数据的缓存也失效。</p>
<p>因此在频繁写 <code>volatile</code> 变量的场景，可以使用追加字节的方式，避免缓存数据的相互锁定。</p>
<h1 id="synchronized"><a href="#synchronized" class="headerlink" title="synchronized"></a>synchronized</h1><h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><ol>
<li>普通同步方法，锁当前的实例对象。</li>
<li>同步方法块，锁当前的类的 Class 对象。</li>
<li>静态同步方法，锁 <code>synchronized</code> 括号中的对象。</li>
</ol>
<h2 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h2><blockquote>
<p>主要参考 <a href="https://github.com/farmerjohngit/myblog/issues/12">死磕Synchronized底层实现</a></p>
</blockquote>
<p>编译器会在同步代的开始位置前添加 <code>monitorenter</code> 指令，在方法结束处、异常处添加 <code>monitorexit</code> 指令。</p>
<p>当线程执行到 <code>monitorenter</code> 指令，会尝试获取对象所对应的 <code>monitor</code> 的所有权，即尝试获得对象锁。</p>
<h2 id="锁的三种形式"><a href="#锁的三种形式" class="headerlink" title="锁的三种形式"></a>锁的三种形式</h2><p>大多数情况，锁不仅不存在多线程竞争，而且总是同以线程多次获得。因此，设计了三级锁：偏向锁、轻量级锁、重量级锁。</p>
<p>对象的锁的信息存储在对象头里。</p>
<p><img src="/images/对象头.jpeg" alt="对象头"></p>
<p>其中，</p>
<ul>
<li><p>在为重量级锁时，指针指向<code>monitor</code> 对象。这个对象存储了关于等待的线程们的各种数据结构。</p>
</li>
<li><p>偏向锁和重量级锁，使用自己线程栈中的 <code>lock record</code> 来记录重入的次数，存储了要锁的对象 <code>obj</code>、加锁前对象头的内容 <code>displaced_markword</code>。</p>
</li>
</ul>
<h2 id="偏向锁"><a href="#偏向锁" class="headerlink" title="偏向锁"></a>偏向锁</h2><p>1.6 以上，JVM 默认开启偏向锁。</p>
<h3 id="加锁过程"><a href="#加锁过程" class="headerlink" title="加锁过程"></a>加锁过程</h3><ol>
<li>如果还没有加过锁，尝试 CAS 加锁，将 <code>markword</code> 更新为自己线程的 id。<ul>
<li>成功，则获得了偏向锁，返回。</li>
<li>失败，将偏向锁撤销，升级为轻量级锁。</li>
</ul>
</li>
<li>如果加过锁，并且 <code>markword</code> 中是自己线程的id，即重入了，在自己线程的栈中添加一条 <code>displaced_markdowrd</code> 为空的 Lock Recoard。因为是自己栈的变量，这里没有 CAS 操作。</li>
<li>如果加过锁，并且不是自己线程加的锁，则将偏向锁撤销，升级为轻量级锁。</li>
</ol>
<h3 id="撤销锁"><a href="#撤销锁" class="headerlink" title="撤销锁"></a>撤销锁</h3><p>在 <code>safepoint</code> 中去查看偏向的线程是否还存活。</p>
<ul>
<li>如果存活、且还在同步块中（看持有锁线程是否有还指向锁对象的 <code>lock record</code>），则将锁升级为轻量级锁，原偏向的线程继续拥有锁，当前线程则走入到锁升级的逻辑里。</li>
<li>如果偏向的线程已经不存活或者不在同步块中，则将对象头的<code>markword</code>改为无锁状态（unlocked），之后再升级为轻量级锁。</li>
</ul>
<h3 id="释放锁过程"><a href="#释放锁过程" class="headerlink" title="释放锁过程"></a>释放锁过程</h3><p>将栈中所有 <code>obj</code> 为要释放的锁对象的 <code>lock record</code> 的 <code>obj</code> 置为 null。（其实，还会查看锁是否是轻量级锁或重量级锁，来进行解锁操作）。</p>
<p>注意，没有修改对象头中的 thread id。</p>
<h2 id="轻量级锁"><a href="#轻量级锁" class="headerlink" title="轻量级锁"></a>轻量级锁</h2><h3 id="加锁过程-1"><a href="#加锁过程-1" class="headerlink" title="加锁过程"></a>加锁过程</h3><ol>
<li>在线程栈中创建一个 <code>lock record</code>，将 <code>obj</code> 指向锁对象。</li>
<li>用 CAS 尝试将 <code>lock record</code> 的地址存储在锁对象头的中。<ul>
<li>成功，说明加锁成功，返回。</li>
<li>失败，看是否是当前线程持有的锁。<ul>
<li>是的，那么这一次是重入的，设置第一步创建的 <code>lock record</code> 的 <code>displaced_markword</code> 为 null，起到重入计数的作用。返回。</li>
<li>不是，说明发生了锁竞争，膨胀为重量级锁（会把锁对象头的 markword 更新为持有锁线程的 monitor。</li>
</ul>
</li>
</ul>
</li>
</ol>
<p>问题：轻量级锁也是发现锁竞争，就膨胀为重量级锁，这样它存在的意义是什么？</p>
<ul>
<li>如果只有两个线程 A 和 B，A 获得偏向锁，B申请锁，锁升级为轻量级锁。之后B获得的锁为轻量级锁，只有第三个线程竞争锁，才会膨胀Wie重量级锁。</li>
</ul>
<h3 id="解锁过程"><a href="#解锁过程" class="headerlink" title="解锁过程"></a>解锁过程</h3><ol>
<li>遍历线程栈，找到所有 <code>obj</code> 为当前锁对象的 <code>lock record</code>。<ul>
<li>如果 <code>lock record</code> 的 <code>displaced_markword</code> 为 null，是重入，将 <code>obj</code> 设为 null，continue。</li>
<li>如果不为 null，用 CAS 将锁对象头恢复为 <code>displaced_makrdword</code>，成功则退出，失败则膨胀重量级锁再进行解锁（可能是其他线程尝试加锁，膨胀为重量级锁，修改了对象头）。</li>
</ul>
</li>
</ol>
<h2 id="重量级锁"><a href="#重量级锁" class="headerlink" title="重量级锁"></a>重量级锁</h2><p>重量级锁使用 <code>Monitor</code> 实现。</p>
<ul>
<li>owner 指向持有锁的线程。</li>
<li>当一个线程尝试获得锁，如果该锁已经被占用，则会将该线程封装成一个 ObjectWaiter 对象插入到 cxq 的队列尾部，然后暂停当前线程。当持有锁的线程释放锁前，会将cxq中的所有元素移动到EntryList中去，并唤醒EntryList的队首线程。</li>
<li>一个线程调用了<code>Object#wait</code>方法，会将该线程的 ObjectWaiter 从 EntryList 移除并加入到 WaitSet 中，然后释放锁。当 wait 的线程被 notify 之后，会将对应的 ObjectWaiter 从 WaitSet 移动到 EntryList 中。</li>
</ul>
<p><img src="/images/重量级锁.png" alt="重量级锁"></p>
<h3 id="获取锁"><a href="#获取锁" class="headerlink" title="获取锁"></a>获取锁</h3><ol>
<li>尝试自旋获得锁。</li>
<li>一定次数没有获得后，将当前线程插入到cxq队列的队首。</li>
<li>park 当前线程。</li>
<li>当被唤醒后再尝试获得锁（只会尝试一次，不会自旋了）。</li>
</ol>
<h3 id="释放锁"><a href="#释放锁" class="headerlink" title="释放锁"></a>释放锁</h3><ol>
<li><p>设置owner为null，即释放锁，这个时刻其他的线程能获取到锁。这里是一个非公平锁的优化。</p>
</li>
<li><p>如果当前没有等待的线程则直接返回就好了，因为不需要唤醒其他线程。或者如果说 succ 不为 null，代表当前已经有个”醒着的”继承人线程，那当前线程不需要唤醒任何线程。</p>
</li>
<li><p>当前线程重新获得锁，因为之后要操作 cxq 和 EntryList 队列以及唤醒线程。</p>
</li>
<li><p>根据QMode的不同，会执行不同的唤醒策略；</p>
<ul>
<li>QMode = 2且cxq非空：取cxq队列队首的ObjectWaiter对象，唤醒ObjectWaiter对象的线程，返回。</li>
<li>QMode = 3且cxq非空：把cxq队列插入到EntryList的尾部；</li>
<li>QMode = 4且cxq非空：把cxq队列插入到EntryList的头部；</li>
<li>QMode = 0：暂时什么都不做，继续往下看。0 为<strong>默认值</strong>。</li>
</ul>
</li>
<li><p>如果 EntryList 为空，就将cxq的所有元素放入到EntryList中。</p>
</li>
<li>取出 EntryList 的首元素，唤醒ObjectWaiter对象的线程。</li>
</ol>
<h1 id="乐观锁与悲观锁"><a href="#乐观锁与悲观锁" class="headerlink" title="乐观锁与悲观锁"></a>乐观锁与悲观锁</h1><blockquote>
<p><a href="https://zhuanlan.zhihu.com/p/40211594">面试必备之乐观锁与悲观锁</a></p>
</blockquote>
<h2 id="悲观锁"><a href="#悲观锁" class="headerlink" title="悲观锁"></a>悲观锁</h2><p>总是假设最坏的情况，每次拿数据时，都认为别人会修改。</p>
<p>所以每次在拿数据的时候都会上锁，这样别人想拿这个数据就会阻塞直到它拿到锁。</p>
<h2 id="乐观锁"><a href="#乐观锁" class="headerlink" title="乐观锁"></a>乐观锁</h2><p>总是假设最好的情况，每次去拿数据时，都认为别人不会修改。</p>
<p>所以不会上锁，但是在更新的时候会判断一下在此期间别人有没有去修改数据。</p>
<h3 id="适应场景"><a href="#适应场景" class="headerlink" title="适应场景"></a>适应场景</h3><ul>
<li>适合读多写少的场景。</li>
<li>如果写操作很多，CAS 不断竞争失败，反而浪费了 CPU 资源。</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
  <entry>
    <title>1. 并发编程的挑战</title>
    <url>/2020/java/Java%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E8%89%BA%E6%9C%AF%E7%AC%94%E8%AE%B0/1%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E7%9A%84%E6%8C%91%E6%88%98/</url>
    <content><![CDATA[<h1 id="上下文切换"><a href="#上下文切换" class="headerlink" title="上下文切换"></a>上下文切换</h1><p>在切换线程时，需要保存上一线程状态，加载下一线程状态，即进行<strong>上下文切换</strong>。</p>
<p>因此线程由创建和上下文切换的开销，多线程并发执行的速度，并不一定比单线程快。</p>
<h2 id="减少上下文切换的方法"><a href="#减少上下文切换的方法" class="headerlink" title="减少上下文切换的方法"></a>减少上下文切换的方法</h2><ul>
<li>无锁并发编程。避免使用锁。例如数据 Hash 分段，不同线程处理不同段的数据。</li>
<li>CAS 算法。</li>
<li>使用最少线程。避免创建不需要的线程。</li>
<li>协程。在单线程里实现多任务的调度。</li>
</ul>
<h1 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h1><p>两个线程持有锁，并且等待对方持有的锁。</p>
<p>例如，thread1 持有 str1，thread2 持有 str2：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  String str1 = <span class="string">"str1"</span>;</span><br><span class="line">  String str2 = <span class="string">"str2"</span>;</span><br><span class="line"></span><br><span class="line">  Thread thread1 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (str1) &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">      &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">synchronized</span> (str2) &#123;</span><br><span class="line">        System.out.println(<span class="string">"thread1"</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, <span class="string">"thread1"</span>);</span><br><span class="line"></span><br><span class="line">  Thread thread2 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (str2) &#123;</span><br><span class="line">      <span class="keyword">synchronized</span> (str1) &#123;</span><br><span class="line">        System.out.println(<span class="string">"thread2"</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, <span class="string">"thread2"</span>);</span><br><span class="line"></span><br><span class="line">  thread1.start();</span><br><span class="line">  thread2.start();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以使用 <code>jstack</code> 命令，查看各个线程的状态、持有的锁、等待的锁、发现死锁：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span> jstack &lt;pid&gt;</span><br><span class="line">Java stack information for the threads listed above:</span><br><span class="line">===================================================</span><br><span class="line">"thread2":</span><br><span class="line">        at Main.lambda$main$1(Main.java:26)</span><br><span class="line">        - waiting to lock &lt;0x000000076ac23580&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x000000076ac235b0&gt; (a java.lang.String)</span><br><span class="line">        at Main$$Lambda$2/764977973.run(Unknown Source)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line">"thread1":</span><br><span class="line">        at Main.lambda$main$0(Main.java:18)</span><br><span class="line">        - waiting to lock &lt;0x000000076ac235b0&gt; (a java.lang.String)</span><br><span class="line">        - locked &lt;0x000000076ac23580&gt; (a java.lang.String)</span><br><span class="line">        at Main$$Lambda$1/931919113.run(Unknown Source)</span><br><span class="line">        at java.lang.Thread.run(Thread.java:748)</span><br><span class="line"></span><br><span class="line">Found 1 deadlock.</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
        <category>concurrency</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>concurrency</tag>
      </tags>
  </entry>
</search>
